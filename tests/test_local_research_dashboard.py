import json
import hashlib
import os
import sys
from dataclasses import replace
from pathlib import Path

import pandas as pd

from quant_replay_system import cli
from quant_replay_system.local_research_dashboard import (
    LocalResearchDashboardResult,
    infer_local_research_next_action,
    infer_local_research_workflow_stage,
    run_local_research_dashboard,
)
from quant_replay_system.historical_replay_input_gate_validator_fixture import (
    build_historical_replay_input_gate_validator_fixture,
)
from quant_replay_system.historical_replay_input_gate_validator import (
    run_historical_replay_input_gate_validator,
)
from quant_replay_system.minimal_replay_input_package_fixture_smoke import (
    MinimalReplayInputPackageFixtureSmokeSettings,
    run_minimal_replay_input_package_fixture_smoke,
)
from quant_replay_system.active_replay_input_promotion import (
    ActiveReplayInputPromotionSettings,
    run_active_replay_input_promotion,
)
from quant_replay_system.active_replay_input_acceptance import (
    ActiveReplayInputAcceptanceSettings,
    run_active_replay_input_acceptance,
)
from quant_replay_system.active_replay_input_active_ready import (
    ActiveReplayInputActiveReadySettings,
    run_active_replay_input_active_ready,
)
from quant_replay_system.active_replay_input_final_review import (
    ActiveReplayInputFinalReviewSettings,
    run_active_replay_input_final_review,
)
from quant_replay_system.active_replay_input_emission import (
    ActiveReplayInputEmissionSettings,
    run_active_replay_input_emission,
)
from quant_replay_system.active_replay_input_ready import (
    ActiveReplayInputReadySettings,
    run_active_replay_input_ready,
)
from quant_replay_system.actual_replay_execute import ACTUAL_REPLAY_EXECUTED, run_actual_replay_execute
from quant_replay_system.forward_return_label import FORWARD_RETURN_LABELS_CREATED, run_forward_return_label
from quant_replay_system.replay_decision_freeze import REPLAY_DECISION_FROZEN, run_replay_decision_freeze
from quant_replay_system.training_evaluation import (
    TRAINING_EVALUATION_DATASET_CREATED,
    run_training_evaluation,
)
from quant_replay_system.metric_evaluation import (
    METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED,
    run_metric_evaluation,
)
from quant_replay_system.pit_evidence_checklist_validator import SUMMARY_COLUMNS, VALIDATION_COLUMNS
from quant_replay_system.replay_substrate_schema_fixture import build_replay_substrate_schema_fixture
from quant_replay_system.factor_definition_schema_fixture import build_factor_definition_schema_fixture
from quant_replay_system.company_exposure_schema_fixture import build_company_exposure_schema_fixture
from quant_replay_system.event_structured_schema_fixture import build_event_structured_schema_fixture
from quant_replay_system.factor_observation_schema_fixture import build_factor_observation_schema_fixture
from quant_replay_system.replay_evidence_bundle_schema_fixture import (
    build_replay_evidence_bundle_schema_fixture,
)
from quant_replay_system.replay_decision_schema_fixture import build_replay_decision_schema_fixture
from quant_replay_system.forward_return_label_schema_fixture import (
    build_forward_return_label_schema_fixture,
)
from quant_replay_system.reviewed_local_csv_replay_prototype_input_contract_fixture import (
    build_reviewed_local_csv_replay_prototype_input_contract_fixture,
)
from quant_replay_system.tiny_pit_admissibility_validator_contract_fixture import (
    build_tiny_pit_admissibility_validator_contract_fixture,
)
from quant_replay_system.tiny_pit_admissibility_validator import build_synthetic_validator_artifacts
from quant_replay_system.tiny_pit_reviewed_package_fixture import (
    build_tiny_pit_reviewed_package_fixture_artifacts,
)
from quant_replay_system.tiny_pit_real_reviewed_package_candidate_contract_fixture import (
    build_real_reviewed_package_candidate_contract_fixture_artifacts,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture import (
    build_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts,
    write_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_real_preflight_prototype import (
    run_manifest_only_preflight_prototype,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following import (
    INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY,
    REQUIRED_FALSE_FLAGS as METADATA_REFERENCE_FOLLOWING_REQUIRED_FALSE_FLAGS,
    run_metadata_reference_following,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_file_touch import (
    REQUIRED_FALSE_FLAGS as CSV_STRUCTURAL_HEADER_ONLY_REQUIRED_FALSE_FLAGS,
    run_csv_structural_file_touch,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_local_file_byte_hash_only import (
    CSV_READ_NONE as LOCAL_FILE_BYTE_HASH_ONLY_CSV_READ_NONE,
    HASH_PREVIEW_HEX_CHARS as LOCAL_FILE_BYTE_HASH_ONLY_PREVIEW_CHARS,
    LOCAL_FILE_BYTE_HASH_ONLY,
    LOCAL_FILE_BYTE_HASH_SHA256_ONLY,
    REQUIRED_FALSE_FLAGS as LOCAL_FILE_BYTE_HASH_ONLY_REQUIRED_FALSE_FLAGS,
    run_local_file_byte_hash_only,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_expected_hash_verification import (
    CSV_READ_NONE as EXPECTED_HASH_VERIFICATION_CSV_READ_NONE,
    EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY,
    HASH_PREVIEW_HEX_CHARS as EXPECTED_HASH_VERIFICATION_PREVIEW_CHARS,
    LOCAL_FILE_HASH_SHA256_METADATA_REFERENCE_ONLY,
    REQUIRED_FALSE_FLAGS as EXPECTED_HASH_VERIFICATION_REQUIRED_FALSE_FLAGS,
    run_expected_hash_verification,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only import (
    CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
    EXPECTED_HASH_VERIFICATION_NONE as CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_EXPECTED_HASH_NONE,
    LOCAL_FILE_HASH_NONE as CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_LOCAL_HASH_NONE,
    PHYSICAL_NON_HEADER_LINE_COUNT,
    REQUIRED_FALSE_FLAGS as CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_REQUIRED_FALSE_FLAGS,
    csv_physical_data_line_count_safety_flags,
    run_csv_physical_data_line_count_only,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time import (
    AVAILABLE_TIME_METADATA_PRESENT_ONLY,
    PIT_ADMISSIBILITY_NONE,
    REQUIRED_FALSE_FLAGS as SOURCE_REVISION_TIME_REQUIRED_FALSE_FLAGS,
    REVISION_ID_METADATA_PRESENT_ONLY,
    SOURCE_HASH_METADATA_PRESENT_ONLY,
    SOURCE_REVISION_TIME_METADATA_PRESENT_ONLY,
    run_source_hash_revision_available_time,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_reviewer_authority_quality_limitation import (
    LIMITATION_METADATA_PRESENT_ONLY,
    PACKAGE_PROMOTION_NONE,
    PERMISSION_CLASS_METADATA_PRESENT_ONLY,
    QUALITY_METADATA_PRESENT_ONLY,
    REQUIRED_FALSE_FLAGS as REVIEWER_QUALITY_LIMITATION_REQUIRED_FALSE_FLAGS,
    REVIEWER_METADATA_PRESENT_ONLY,
    run_reviewer_authority_quality_limitation,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_preflight import (
    ACTIVE_INPUT_NONE as PREFLIGHT_ACTIVE_INPUT_NONE,
    AVAILABLE_TIME_VALIDATION_NONE as PREFLIGHT_AVAILABLE_TIME_VALIDATION_NONE,
    CSV_READ_NONE as PREFLIGHT_CSV_READ_NONE,
    LIMITATION_REVIEW_NONE as PREFLIGHT_LIMITATION_REVIEW_NONE,
    NEGATIVE_FALSE_FIELDS as PREFLIGHT_NEGATIVE_FALSE_FIELDS,
    OPTIONAL_REFERENCE_NAMES as PREFLIGHT_OPTIONAL_REFERENCE_NAMES,
    PACKAGE_CREATION_NONE as PREFLIGHT_PACKAGE_CREATION_NONE,
    PERMISSION_REVIEW_NONE as PREFLIGHT_PERMISSION_REVIEW_NONE,
    PIT_ADMISSIBILITY_NONE as PREFLIGHT_PIT_ADMISSIBILITY_NONE,
    PREFLIGHT_METADATA_REFERENCES_ONLY,
    QUALITY_STATUS_NONE as PREFLIGHT_QUALITY_STATUS_NONE,
    REQUIRED_REFERENCE_NAMES as PREFLIGHT_REQUIRED_REFERENCE_NAMES,
    REPLAY_READINESS_NONE as PREFLIGHT_REPLAY_READINESS_NONE,
    REVIEWER_AUTHORITY_NONE as PREFLIGHT_REVIEWER_AUTHORITY_NONE,
    REVISION_ID_VALIDATION_NONE as PREFLIGHT_REVISION_ID_VALIDATION_NONE,
    SOURCE_HASH_VALIDATION_NONE as PREFLIGHT_SOURCE_HASH_VALIDATION_NONE,
    SOURCE_RELIABILITY_NONE as PREFLIGHT_SOURCE_RELIABILITY_NONE,
    STATUS_BLOCKED_BY_FORBIDDEN_DOWNSTREAM as PREFLIGHT_STATUS_BLOCKED_FORBIDDEN_DOWNSTREAM,
    STATUS_BLOCKED_BY_MISSING_REQUIRED_METADATA as PREFLIGHT_STATUS_BLOCKED_MISSING_REQUIRED,
    STATUS_BLOCKED_BY_UNSUPPORTED_VALIDATION_CLAIM as PREFLIGHT_STATUS_BLOCKED_UNSUPPORTED_VALIDATION,
    STATUS_METADATA_CONTEXT_REPORT_ONLY as PREFLIGHT_STATUS_METADATA_CONTEXT,
    STATUS_NO_INPUT as PREFLIGHT_STATUS_NO_INPUT,
    STATUS_WARN_MISSING_OPTIONAL_EVIDENCE as PREFLIGHT_STATUS_WARN_MISSING_OPTIONAL,
    run_real_reviewed_local_csv_package_candidate_preflight,
)
from quant_replay_system.personal_mvp_daily_advisory_review import (
    REQUIRED_FALSE_SAFETY_FIELDS as PERSONAL_MVP_DAILY_ADVISORY_REVIEW_FALSE_FIELDS,
    run_personal_mvp_daily_advisory_review,
)
from quant_replay_system.personal_mvp_daily_advisory_review_health import (
    check_personal_mvp_daily_advisory_review_health,
)
from quant_replay_system.personal_mvp_daily_advisory_review_index import (
    build_personal_mvp_daily_advisory_review_index,
)
from quant_replay_system.personal_mvp_daily_advisory_review_status import (
    run_personal_mvp_daily_advisory_review_status,
)
from quant_replay_system.tiny_pit_real_reviewed_local_csv_package_candidate_source_artifact_byte_hash import (
    AVAILABLE_TIME_VALIDATION_NONE as SOURCE_ARTIFACT_BYTE_HASH_AVAILABLE_TIME_NONE,
    CSV_READ_NONE as SOURCE_ARTIFACT_BYTE_HASH_CSV_READ_NONE,
    DISCLOSURE_PREVIEW_ONLY_PUBLIC_SURFACES,
    EXPECTED_HASH_VERIFICATION_NONE as SOURCE_ARTIFACT_BYTE_HASH_EXPECTED_HASH_NONE,
    FULL_HASH_RECORDING_LOCAL_METADATA_ONLY,
    HASH_PREVIEW_LENGTH as SOURCE_ARTIFACT_BYTE_HASH_PREVIEW_CHARS,
    LOCAL_FILE_HASH_NONE as SOURCE_ARTIFACT_BYTE_HASH_LOCAL_FILE_HASH_NONE,
    ACTIVE_INPUT_NONE as SOURCE_ARTIFACT_BYTE_HASH_ACTIVE_INPUT_NONE,
    PACKAGE_CREATION_NONE as SOURCE_ARTIFACT_BYTE_HASH_PACKAGE_CREATION_NONE,
    PIT_ADMISSIBILITY_NONE as SOURCE_ARTIFACT_BYTE_HASH_PIT_NONE,
    REQUIRED_FALSE_FLAGS as SOURCE_ARTIFACT_BYTE_HASH_REQUIRED_FALSE_FLAGS,
    REPLAY_READINESS_NONE as SOURCE_ARTIFACT_BYTE_HASH_REPLAY_READINESS_NONE,
    REVISION_ID_VALIDATION_NONE as SOURCE_ARTIFACT_BYTE_HASH_REVISION_ID_NONE,
    REVIEWER_AUTHORITY_NONE as SOURCE_ARTIFACT_BYTE_HASH_REVIEWER_AUTHORITY_NONE,
    SOURCE_ARTIFACT_BYTE_READ_NONE,
    SOURCE_ARTIFACT_BYTE_READ_STREAMING_HASH_ONLY,
    SOURCE_CONTENT_READ_NONE as SOURCE_ARTIFACT_BYTE_HASH_CONTENT_READ_NONE,
    SOURCE_HASH_RECOMPUTE_NONE,
    SOURCE_HASH_RECOMPUTE_SHA256_ONLY,
    SOURCE_HASH_VALIDATION_NONE as SOURCE_ARTIFACT_BYTE_HASH_VALIDATION_NONE,
    SOURCE_RELIABILITY_NONE as SOURCE_ARTIFACT_BYTE_HASH_RELIABILITY_NONE,
    run_source_artifact_byte_hash,
)
from quant_replay_system.historical_replay_pit_evidence_closure_worklist import (
    SAFETY_FALSE_FIELDS as PIT_EVIDENCE_CLOSURE_WORKLIST_SAFETY_FALSE_FIELDS,
    run_historical_replay_pit_evidence_closure_worklist,
)

EXPECTED_METADATA_REFERENCE_FOLLOWING_NEXT_BOUNDARY_DESIGN_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Metadata-Reference-Following "
    "Next Boundary Design Planning Report-Only v0.1"
)
STALE_METADATA_REFERENCE_FOLLOWING_NEXT_ACTION_PHRASES = [
    "CLI or Research-Status Planning",
    "Research-Status and Checkpoint",
    "Artifact Views / Index / Health / Status",
    "checkpoint planning",
]
EXPECTED_CSV_STRUCTURAL_HEADER_ONLY_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate CSV Structural Header-Only "
    "Post-v1.75 Next Boundary Design Planning Report-Only v0.1"
)
STALE_CSV_STRUCTURAL_HEADER_ONLY_NEXT_ACTION_PHRASES = [
    "Artifact Views Report-Only v0.1",
    "CLI Report-Only v0.1",
    "Research-Status Planning Report-Only v0.1",
    "Research-Status and Checkpoint Planning Report-Only v0.1",
]
UNSAFE_CSV_STRUCTURAL_HEADER_ONLY_NEXT_ACTION_PHRASES = [
    "row-count implementation",
    "file-hash implementation",
    "full-content reading",
    "real package candidate",
    "PIT validator",
    "active replay",
    "replay implementation",
    "labels",
    "training",
    "model",
    "stock_profile",
    "paper validation",
    "buy-review",
    "trading",
]
EXPECTED_LOCAL_FILE_BYTE_HASH_ONLY_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Local File Byte-Hash-Only "
    "Checkpoint Planning Report-Only v0.1"
)
STALE_LOCAL_FILE_BYTE_HASH_ONLY_NEXT_ACTION_PHRASES = [
    "Artifact Views Report-Only v0.1",
    "CLI Report-Only v0.1",
    "Research-Status Planning Report-Only v0.1",
]
UNSAFE_LOCAL_FILE_BYTE_HASH_ONLY_WORDING = [
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_HASH_VERIFICATION_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Expected-Hash Verification "
    "Checkpoint Planning Report-Only v0.1"
)
STALE_EXPECTED_HASH_VERIFICATION_NEXT_ACTION_PHRASES = [
    "Artifact Views Report-Only v0.1",
    "CLI Report-Only v0.1",
    "Research-Status Planning Report-Only v0.1",
]
UNSAFE_EXPECTED_HASH_VERIFICATION_WORDING = [
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate CSV Physical Data-Line "
    "Count-Only Checkpoint Planning Report-Only v0.1"
)
STALE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_NEXT_ACTION_PHRASES = [
    "Artifact Views Report-Only v0.1",
    "CLI Report-Only v0.1",
    "Research-Status Planning Report-Only v0.1",
]
UNSAFE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_WORDING = [
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_SOURCE_REVISION_TIME_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Source Hash Revision "
    "Available-Time Checkpoint Planning Report-Only v0.1"
)
STALE_SOURCE_REVISION_TIME_NEXT_ACTION_PHRASES = [
    "Research-Status Planning Report-Only v0.1",
]
UNSAFE_SOURCE_REVISION_TIME_WORDING = [
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_REVIEWER_QUALITY_LIMITATION_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Reviewer Authority Quality "
    "Limitation Checkpoint Planning Report-Only v0.1"
)
STALE_REVIEWER_QUALITY_LIMITATION_NEXT_ACTION_PHRASES = [
    "Research-Status Planning Report-Only v0.1",
]
UNSAFE_REVIEWER_QUALITY_LIMITATION_WORDING = [
    "REVIEWER_APPROVED_PACKAGE",
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_PREFLIGHT_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Preflight Checkpoint Planning "
    "Report-Only v0.1"
)
STALE_PREFLIGHT_NEXT_ACTION_PHRASES = [
    "Research-Status Planning Report-Only v0.1",
    "Artifact Views Report-Only v0.1",
    "CLI Report-Only v0.1",
]
UNSAFE_PREFLIGHT_WORDING = [
    "REAL_PACKAGE_CANDIDATE_CREATED",
    "PACKAGE_APPROVED",
    "PACKAGE_ADMISSIBLE",
    "PIT_ADMISSIBLE_PACKAGE",
    "READY_FOR_REPLAY",
    "REPLAY_INPUT_READY",
    "ACTIVE_REPLAY_INPUT_READY",
    "APPROVED_FOR_ACTIVE_INPUT",
    "TRADING_READY",
    "BUY_REVIEW_READY",
    "PERFORMANCE_VALIDATED",
]
EXPECTED_SOURCE_ARTIFACT_BYTE_HASH_NEXT_TASK = (
    "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Source Artifact Byte-Hash "
    "Research-Status Planning Report-Only v0.1"
)
EXPECTED_PERSONAL_MVP_DAILY_ADVISORY_REVIEW_NEXT_TASK = (
    "Personal MVP Daily Advisory Review Surface Research-Status Integration Planning Report-Only v0.1"
)
EXPECTED_PIT_EVIDENCE_CLOSURE_WORKLIST_NEXT_TASK = (
    "Historical Replay Official Status Evidence Packet Closure Planning for 2024-04-02 etf_core Report-Only v0.1"
)

from quant_replay_system.raw_document_store_schema_fixture import build_raw_document_store_schema_fixture
from quant_replay_system.source_registry_schema_fixture import build_source_registry_schema_fixture
from quant_replay_system.reviewer_no_hit_source_coverage_acceptance import (
    build_reviewer_no_hit_source_coverage_acceptance,
)
from quant_replay_system.reviewer_no_hit_acceptance_downstream_impact import (
    build_reviewer_no_hit_acceptance_downstream_impact,
)
from quant_replay_system.universe_profile_policy_audit import build_universe_profile_policy_audit
from test_actual_replay_execute import _happy_settings as _actual_replay_happy_settings
from test_forward_return_label import _happy_settings as _forward_return_label_happy_settings
from test_replay_decision_freeze import _happy_settings as _replay_decision_freeze_happy_settings
from test_training_evaluation import _happy_settings as _training_evaluation_happy_settings
from test_metric_evaluation import _happy_settings as _metric_evaluation_happy_settings
from test_training_result_planning import _happy_settings as _training_result_planning_happy_settings
from test_training_result import _happy_settings as _training_result_happy_settings
from test_model_weight_versioning import _happy_settings as _model_weight_versioning_happy_settings
from quant_replay_system.training_result_planning import (
    TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED,
    run_training_result_planning,
)
from quant_replay_system.training_result import TRAINING_RESULT_CREATED, run_training_result
from quant_replay_system.model_weight_versioning import (
    MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED,
    run_model_weight_versioning,
)
from quant_replay_system.active_model import (
    ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED,
    run_active_model,
)
from test_active_model import _happy_settings as _active_model_happy_settings
from quant_replay_system.stock_profile import (
    STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED,
    run_stock_profile,
)
from test_stock_profile import _happy_settings as _stock_profile_happy_settings
from quant_replay_system.paper_workflow_phase1 import (
    PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED,
    run_paper_workflow_phase1,
)
from test_paper_workflow_phase1 import _happy_settings as _paper_workflow_phase1_happy_settings
from quant_replay_system.approved_for_paper_phase1 import (
    APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED,
    run_approved_for_paper_phase1,
)
from test_approved_for_paper_phase1 import _happy_settings as _approved_for_paper_phase1_happy_settings
from quant_replay_system.global_approved_for_paper_approval_review import (
    GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED,
    run_global_approved_for_paper_approval_review,
)
from test_global_approved_for_paper_approval_review import (
    _happy_settings as _global_approved_for_paper_approval_review_happy_settings,
)
from quant_replay_system.operational_global_approved_for_paper import (
    OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED,
    run_operational_global_approved_for_paper,
)
from test_operational_global_approved_for_paper import (
    _happy_settings as _operational_global_approved_for_paper_happy_settings,
)


DECISION_DATE = "2024-05-20"
UNIVERSE = "etf_core"


def test_dashboard_handles_no_artifacts(tmp_path: Path) -> None:
    result = run_local_research_dashboard(root=_reports_root(tmp_path), output_dir=tmp_path / "dashboard")

    assert isinstance(result, LocalResearchDashboardResult)
    assert result.workflow_stage == "NO_DATA"
    assert result.status == "WARN"
    assert result.next_manual_action == "Run data-pipeline."


def test_dashboard_detects_data_preparation_workflow_status_artifact(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _data_preparation_status(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.data_preparation_status == "PASS"
    assert result.workflow_stage == "DATA_PREPARATION_READY"


def test_dashboard_detects_current_candidate_artifacts(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.current_candidate_status == "READY"
    assert result.latest_decision_date == DECISION_DATE
    assert result.workflow_stage == "CURRENT_CANDIDATES_READY"


def test_dashboard_detects_current_candidate_health_artifacts(tmp_path: Path) -> None:
    root = _workflow_to_current_candidate_health(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.current_candidate_health_status == "PASS"
    assert result.workflow_stage == "CURRENT_CANDIDATES_HEALTH_READY"


def test_dashboard_includes_current_candidates_backfill_plan_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(root, plan_id="plan-ready")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_PLAN_STATUS"
    ].iloc[0]

    assert result.latest_current_candidates_backfill_plan_id == "plan-ready"
    assert result.current_candidates_backfill_plan_status == "PASS"
    assert result.current_candidates_backfill_plan_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"
    assert result.current_candidates_backfill_plan_health_status == "PASS"
    assert result.current_candidates_backfill_plan_selected_date_count == 8
    assert result.current_candidates_backfill_plan_first_signal_date == "2024-04-02"
    assert result.current_candidates_backfill_plan_last_signal_date == "2024-05-06"
    assert result.current_candidates_backfill_plan_warmup_trading_days == 60
    assert '"forward_10d_available_count": 8' in result.current_candidates_backfill_plan_forward_horizon_summary
    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"
    assert plan_row["warning_classification"] == ""
    assert "before candidate generation" in result.next_manual_action


def test_dashboard_current_candidates_backfill_plan_ready_is_non_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(root, plan_id="plan-non-blocking")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_PLAN_STATUS"
    ].iloc[0]

    assert result.status != "FAIL"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert result.summary_frame.iloc[0]["actionable_warning_count"] == 0
    assert plan_row["warning_classification"] == ""


def test_dashboard_failed_current_candidates_backfill_plan_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(
        root,
        plan_id="plan-fail",
        status="FAIL",
        workflow_stage="CURRENT_CANDIDATES_BACKFILL_PLAN_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_PLAN_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_FAILED"
    assert result.current_candidates_backfill_plan_health_status == "FAIL"
    assert plan_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair current-candidates backfill plan artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_current_candidates_backfill_plan(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(root, plan_id="plan-context", status="WARN")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_PLAN_STATUS"
    ].iloc[0]

    assert result.latest_current_candidates_backfill_plan_id == "plan-context"
    assert result.current_candidates_backfill_plan_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert plan_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exposes_legacy_backfill_plan_context_without_active_override(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(
        root,
        plan_id="plan-active",
        legacy_plan_count=1,
        stale_plan_warning_count=1,
        legacy_missing_warmup_count=1,
        latest_plan_is_warmup_aware=True,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_current_candidates_backfill_plan_id == "plan-active"
    assert result.current_candidates_backfill_plan_status == "PASS"
    assert result.current_candidates_backfill_plan_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"
    assert result.current_candidates_backfill_plan_health_status == "PASS"
    assert result.current_candidates_backfill_plan_legacy_plan_count == 1
    assert result.current_candidates_backfill_plan_stale_plan_warning_count == 1
    assert result.current_candidates_backfill_plan_active_plan_issue_count == 0
    assert result.current_candidates_backfill_plan_active_plan_error_count == 0
    assert result.current_candidates_backfill_plan_legacy_missing_warmup_count == 1
    assert result.current_candidates_backfill_plan_latest_plan_is_warmup_aware is True
    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"


def test_dashboard_exports_current_candidates_backfill_plan_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(root, plan_id="plan-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_current_candidates_backfill_plan_id"] == "plan-export"
    assert row["current_candidates_backfill_plan_status"] == "PASS"
    assert row["current_candidates_backfill_plan_stage"] == "CURRENT_CANDIDATES_BACKFILL_PLAN_READY"
    assert row["current_candidates_backfill_plan_health_status"] == "PASS"
    assert row["current_candidates_backfill_plan_selected_date_count"] == "8"
    assert row["current_candidates_backfill_plan_warmup_trading_days"] == "60"
    assert '"forward_5d_available_count": 8' in row["current_candidates_backfill_plan_forward_horizon_summary"]
    assert row["current_candidates_backfill_plan_latest_plan_is_warmup_aware"] == "True"
    assert metadata["latest_current_candidates_backfill_plan_id"] == "plan-export"
    assert metadata["current_candidates_backfill_plan_health_status"] == "PASS"
    assert metadata["current_candidates_backfill_plan_selected_date_count"] == 8
    assert metadata["current_candidates_backfill_plan_latest_plan_is_warmup_aware"] is True
    assert metadata["component_statuses"]["latest_current_candidates_backfill_plan_id"] == "plan-export"


def test_cli_research_status_prints_current_candidates_backfill_plan_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_plan_status(root, plan_id="plan-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_current_candidates_backfill_plan_id: plan-cli" in output.out
    assert "current_candidates_backfill_plan_status: PASS" in output.out
    assert "current_candidates_backfill_plan_stage: CURRENT_CANDIDATES_BACKFILL_PLAN_READY" in output.out
    assert "current_candidates_backfill_plan_health_status: PASS" in output.out
    assert "current_candidates_backfill_plan_selected_date_count: 8" in output.out
    assert "current_candidates_backfill_plan_latest_plan_is_warmup_aware: True" in output.out


def test_dashboard_includes_current_candidates_backfill_execution_manifest_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(root, execution_manifest_id="manifest-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    manifest_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_STATUS"
    ].iloc[0]

    assert result.latest_current_candidates_backfill_execution_manifest_id == "manifest-blocked"
    assert result.current_candidates_backfill_execution_manifest_status == "WARN"
    assert (
        result.current_candidates_backfill_execution_manifest_stage
        == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_BLOCKED"
    )
    assert result.current_candidates_backfill_execution_manifest_health_status == "PASS"
    assert result.current_candidates_backfill_execution_manifest_plan_id == "plan-a"
    assert result.current_candidates_backfill_execution_manifest_row_count == 8
    assert result.current_candidates_backfill_execution_manifest_ready_count == 0
    assert result.current_candidates_backfill_execution_manifest_blocked_count == 8
    assert result.current_candidates_backfill_execution_manifest_blocked_universe_as_of_count == 8
    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_BLOCKED"
    assert manifest_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "no current-candidates were run" in result.next_manual_action


def test_dashboard_backfill_execution_manifest_ready_for_review_does_not_imply_execution(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(
        root,
        execution_manifest_id="manifest-ready",
        status="PASS",
        workflow_stage="CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_READY_FOR_REVIEW",
        ready_count=8,
        blocked_count=0,
        blocked_universe_as_of_count=0,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    manifest_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_READY_FOR_REVIEW"
    assert result.current_candidates_backfill_execution_manifest_ready_count == 8
    assert result.current_candidates_backfill_execution_manifest_blocked_count == 0
    assert manifest_row["warning_classification"] == ""
    assert "separate candidate generation" in result.next_manual_action
    assert result.audit_metadata["local_research_dashboard_only"] is True


def test_dashboard_failed_current_candidates_backfill_execution_manifest_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(
        root,
        execution_manifest_id="manifest-fail",
        status="FAIL",
        workflow_stage="CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    manifest_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_FAILED"
    assert manifest_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair execution manifest artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_backfill_execution_manifest(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(root, execution_manifest_id="manifest-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    manifest_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_STATUS"
    ].iloc[0]

    assert result.latest_current_candidates_backfill_execution_manifest_id == "manifest-context"
    assert result.current_candidates_backfill_execution_manifest_blocked_universe_as_of_count == 8
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert manifest_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_current_candidates_backfill_execution_manifest_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(root, execution_manifest_id="manifest-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_current_candidates_backfill_execution_manifest_id"] == "manifest-export"
    assert row["current_candidates_backfill_execution_manifest_status"] == "WARN"
    assert row["current_candidates_backfill_execution_manifest_health_status"] == "PASS"
    assert row["current_candidates_backfill_execution_manifest_plan_id"] == "plan-a"
    assert row["current_candidates_backfill_execution_manifest_row_count"] == "8"
    assert row["current_candidates_backfill_execution_manifest_blocked_universe_as_of_count"] == "8"
    assert metadata["latest_current_candidates_backfill_execution_manifest_id"] == "manifest-export"
    assert metadata["current_candidates_backfill_execution_manifest_plan_id"] == "plan-a"
    assert metadata["current_candidates_backfill_execution_manifest_blocked_universe_as_of_count"] == 8
    assert (
        metadata["component_statuses"]["latest_current_candidates_backfill_execution_manifest_id"]
        == "manifest-export"
    )


def test_cli_research_status_prints_current_candidates_backfill_execution_manifest_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _current_candidates_backfill_execution_manifest_status(root, execution_manifest_id="manifest-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_current_candidates_backfill_execution_manifest_id: manifest-cli" in output.out
    assert "current_candidates_backfill_execution_manifest_status: WARN" in output.out
    assert "current_candidates_backfill_execution_manifest_plan_id: plan-a" in output.out
    assert "current_candidates_backfill_execution_manifest_blocked_universe_as_of_count: 8" in output.out


def test_dashboard_includes_pit_universe_overlay_plan_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(root, overlay_plan_id="overlay-needs-review")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    overlay_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_PLAN_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_overlay_plan_id == "overlay-needs-review"
    assert result.pit_universe_overlay_plan_status == "WARN"
    assert result.pit_universe_overlay_plan_stage == "PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW"
    assert result.pit_universe_overlay_plan_health_status == "PASS"
    assert result.pit_universe_overlay_plan_row_count == 72
    assert result.pit_universe_overlay_plan_signal_date_count == 8
    assert result.pit_universe_overlay_plan_symbol_count == 9
    assert result.pit_universe_overlay_plan_needs_manual_review_count == 72
    assert result.pit_universe_overlay_plan_valid_for_signal_date_count == 0
    assert result.pit_universe_overlay_plan_survivorship_bias_warning_count == 72
    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW"
    assert overlay_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "manual review" in result.next_manual_action
    assert "not valid" in result.next_manual_action


def test_dashboard_pit_universe_overlay_needs_review_does_not_imply_candidate_generation(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(root, overlay_plan_id="overlay-context")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    overlay_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_PLAN_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW"
    assert result.pit_universe_overlay_plan_valid_for_signal_date_count == 0
    assert overlay_row["actionable_warning_count"] == 0
    assert overlay_row["blocking_error_count"] == 0
    assert result.summary_frame.iloc[0]["expected_reviewable_warning_count"] == 1
    assert result.audit_metadata["local_research_dashboard_only"] is True
    assert result.audit_metadata["live_trading_enabled"] is False
    assert result.audit_metadata["broker_api_invoked"] is False


def test_dashboard_failed_pit_universe_overlay_plan_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(
        root,
        overlay_plan_id="overlay-fail",
        status="FAIL",
        workflow_stage="PIT_UNIVERSE_OVERLAY_PLAN_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    overlay_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_PLAN_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_PLAN_FAILED"
    assert overlay_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair PIT universe overlay plan artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_overlay_plan(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(root, overlay_plan_id="overlay-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    overlay_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_PLAN_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_overlay_plan_id == "overlay-paper-context"
    assert result.pit_universe_overlay_plan_needs_manual_review_count == 72
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert overlay_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_overlay_plan_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(root, overlay_plan_id="overlay-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_overlay_plan_id"] == "overlay-export"
    assert row["pit_universe_overlay_plan_status"] == "WARN"
    assert row["pit_universe_overlay_plan_stage"] == "PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW"
    assert row["pit_universe_overlay_plan_health_status"] == "PASS"
    assert row["pit_universe_overlay_plan_row_count"] == "72"
    assert row["pit_universe_overlay_plan_needs_manual_review_count"] == "72"
    assert row["pit_universe_overlay_plan_valid_for_signal_date_count"] == "0"
    assert row["pit_universe_overlay_plan_survivorship_bias_warning_count"] == "72"
    assert metadata["latest_pit_universe_overlay_plan_id"] == "overlay-export"
    assert metadata["pit_universe_overlay_plan_needs_manual_review_count"] == 72
    assert metadata["pit_universe_overlay_plan_valid_for_signal_date_count"] == 0
    assert metadata["component_statuses"]["latest_pit_universe_overlay_plan_id"] == "overlay-export"


def test_cli_research_status_prints_pit_universe_overlay_plan_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_plan_status(root, overlay_plan_id="overlay-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_overlay_plan_id: overlay-cli" in output.out
    assert "pit_universe_overlay_plan_status: WARN" in output.out
    assert "pit_universe_overlay_plan_stage: PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW" in output.out
    assert "pit_universe_overlay_plan_needs_manual_review_count: 72" in output.out
    assert "pit_universe_overlay_plan_survivorship_bias_warning_count: 72" in output.out


def test_dashboard_includes_pit_universe_overlay_review_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(root, review_id="review-needs-evidence")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    review_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_REVIEW_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_overlay_review_id == "review-needs-evidence"
    assert result.pit_universe_overlay_review_status == "WARN"
    assert result.pit_universe_overlay_review_stage == "PIT_UNIVERSE_OVERLAY_REVIEW_NEEDS_MORE_EVIDENCE"
    assert result.pit_universe_overlay_review_health_status == "PASS"
    assert result.pit_universe_overlay_review_approved_count == 1
    assert result.pit_universe_overlay_review_valid_for_signal_date_count == 1
    assert result.pit_universe_overlay_review_needs_more_evidence_count == 1
    assert result.pit_universe_overlay_review_unresolved_survivorship_warning_count == 1
    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_REVIEW_NEEDS_MORE_EVIDENCE"
    assert review_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "more evidence" in result.next_manual_action


def test_dashboard_pit_universe_overlay_review_approved_rows_do_not_imply_candidate_generation(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(
        root,
        review_id="review-approved",
        status="PASS",
        workflow_stage="PIT_UNIVERSE_OVERLAY_REVIEW_HAS_APPROVED_ROWS",
        approved_count=1,
        valid_for_signal_date_count=1,
        needs_more_evidence_count=0,
        unresolved_survivorship_warning_count=0,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    review_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_REVIEW_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_REVIEW_HAS_APPROVED_ROWS"
    assert result.pit_universe_overlay_review_approved_count == 1
    assert result.pit_universe_overlay_review_valid_for_signal_date_count == 1
    assert review_row["blocking_error_count"] == 0
    assert result.audit_metadata["local_research_dashboard_only"] is True
    assert result.audit_metadata["live_trading_enabled"] is False
    assert result.audit_metadata["broker_api_invoked"] is False


def test_dashboard_failed_pit_universe_overlay_review_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(
        root,
        review_id="review-fail",
        status="FAIL",
        workflow_stage="PIT_UNIVERSE_OVERLAY_REVIEW_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    review_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_REVIEW_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "PIT_UNIVERSE_OVERLAY_REVIEW_FAILED"
    assert review_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair PIT universe overlay review artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_overlay_review(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(root, review_id="review-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    review_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_OVERLAY_REVIEW_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_overlay_review_id == "review-paper-context"
    assert result.pit_universe_overlay_review_needs_more_evidence_count == 1
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert review_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_overlay_review_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(root, review_id="review-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_overlay_review_id"] == "review-export"
    assert row["pit_universe_overlay_review_status"] == "WARN"
    assert row["pit_universe_overlay_review_stage"] == "PIT_UNIVERSE_OVERLAY_REVIEW_NEEDS_MORE_EVIDENCE"
    assert row["pit_universe_overlay_review_health_status"] == "PASS"
    assert row["pit_universe_overlay_review_approved_count"] == "1"
    assert row["pit_universe_overlay_review_valid_for_signal_date_count"] == "1"
    assert row["pit_universe_overlay_review_needs_more_evidence_count"] == "1"
    assert metadata["latest_pit_universe_overlay_review_id"] == "review-export"
    assert metadata["pit_universe_overlay_review_approved_count"] == 1
    assert metadata["component_statuses"]["latest_pit_universe_overlay_review_id"] == "review-export"


def test_cli_research_status_prints_pit_universe_overlay_review_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_review_status(root, review_id="review-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_overlay_review_id: review-cli" in output.out
    assert "pit_universe_overlay_review_status: WARN" in output.out
    assert "pit_universe_overlay_review_stage: PIT_UNIVERSE_OVERLAY_REVIEW_NEEDS_MORE_EVIDENCE" in output.out
    assert "pit_universe_overlay_review_approved_count: 1" in output.out
    assert "pit_universe_overlay_review_unresolved_survivorship_warning_count: 1" in output.out


def test_dashboard_includes_pit_universe_export_readiness_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_export_readiness_status(root, export_readiness_id="export-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    readiness_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EXPORT_READINESS_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_export_readiness_id == "export-blocked"
    assert result.pit_universe_export_readiness_status == "WARN"
    assert result.pit_universe_export_readiness_stage == "PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS"
    assert result.pit_universe_export_readiness_health_status == "PASS"
    assert result.pit_universe_export_readiness_review_id == "review-a"
    assert result.pit_universe_export_readiness_approved_count == 0
    assert result.pit_universe_export_readiness_export_ready_count == 0
    assert result.pit_universe_export_readiness_blocked_count == 72
    assert result.pit_universe_export_readiness_no_approved_rows is True
    assert result.workflow_stage == "PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS"
    assert readiness_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Approve PIT universe rows" in result.next_manual_action


def test_dashboard_failed_pit_universe_export_readiness_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_export_readiness_status(
        root,
        export_readiness_id="export-fail",
        status="FAIL",
        workflow_stage="PIT_UNIVERSE_EXPORT_READINESS_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    readiness_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EXPORT_READINESS_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "PIT_UNIVERSE_EXPORT_READINESS_FAILED"
    assert readiness_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair PIT universe export-readiness artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_export_readiness(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_export_readiness_status(root, export_readiness_id="export-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    readiness_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EXPORT_READINESS_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_export_readiness_id == "export-paper-context"
    assert result.pit_universe_export_readiness_blocked_count == 72
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert readiness_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_export_readiness_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_export_readiness_status(root, export_readiness_id="export-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_export_readiness_id"] == "export-summary"
    assert row["pit_universe_export_readiness_status"] == "WARN"
    assert row["pit_universe_export_readiness_stage"] == "PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS"
    assert row["pit_universe_export_readiness_health_status"] == "PASS"
    assert row["pit_universe_export_readiness_approved_count"] == "0"
    assert row["pit_universe_export_readiness_blocked_count"] == "72"
    assert metadata["latest_pit_universe_export_readiness_id"] == "export-summary"
    assert metadata["pit_universe_export_readiness_blocked_count"] == 72
    assert metadata["component_statuses"]["latest_pit_universe_export_readiness_id"] == "export-summary"


def test_cli_research_status_prints_pit_universe_export_readiness_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_overlay_export_readiness_status(root, export_readiness_id="export-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_export_readiness_id: export-cli" in output.out
    assert "pit_universe_export_readiness_status: WARN" in output.out
    assert "pit_universe_export_readiness_stage: PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS" in output.out
    assert "pit_universe_export_readiness_approved_count: 0" in output.out
    assert "pit_universe_export_readiness_blocked_count: 72" in output.out


def test_dashboard_includes_pit_universe_export_staging_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_export_staging_status(root, staging_id="stage-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    staging_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EXPORT_STAGING_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_export_staging_id == "stage-blocked"
    assert result.pit_universe_export_staging_status == "WARN"
    assert result.pit_universe_export_staging_stage == "PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_NO_READY_ROWS"
    assert result.pit_universe_export_staging_health_status == "PASS"
    assert result.pit_universe_export_staging_export_readiness_id == "export-a"
    assert result.pit_universe_export_staging_export_ready_input_count == 0
    assert result.pit_universe_export_staging_staged_row_count == 0
    assert result.pit_universe_export_staging_blocked_count == 72
    assert result.pit_universe_export_staging_no_ready_rows is True
    assert result.workflow_stage == "PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_NO_READY_ROWS"
    assert staging_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Complete PIT universe review evidence" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_export_staging(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_export_staging_status(root, staging_id="stage-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    staging_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EXPORT_STAGING_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_export_staging_id == "stage-paper-context"
    assert result.pit_universe_export_staging_blocked_count == 72
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert staging_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_export_staging_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_export_staging_status(root, staging_id="stage-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_export_staging_id"] == "stage-summary"
    assert row["pit_universe_export_staging_status"] == "WARN"
    assert row["pit_universe_export_staging_stage"] == "PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_NO_READY_ROWS"
    assert row["pit_universe_export_staging_health_status"] == "PASS"
    assert row["pit_universe_export_staging_staged_row_count"] == "0"
    assert row["pit_universe_export_staging_blocked_count"] == "72"
    assert metadata["latest_pit_universe_export_staging_id"] == "stage-summary"
    assert metadata["pit_universe_export_staging_blocked_count"] == 72
    assert metadata["component_statuses"]["latest_pit_universe_export_staging_id"] == "stage-summary"


def test_cli_research_status_prints_pit_universe_export_staging_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_export_staging_status(root, staging_id="stage-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_export_staging_id: stage-cli" in output.out
    assert "pit_universe_export_staging_status: WARN" in output.out
    assert "pit_universe_export_staging_stage: PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_NO_READY_ROWS" in output.out
    assert "pit_universe_export_staging_staged_row_count: 0" in output.out
    assert "pit_universe_export_staging_blocked_count: 72" in output.out


def test_dashboard_includes_pit_universe_evidence_helper_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_completion_helper_status(root, helper_id="helper-needs-review")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    helper_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_helper_id == "helper-needs-review"
    assert result.pit_universe_evidence_helper_status == "WARN"
    assert result.pit_universe_evidence_helper_stage == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_NEEDS_REVIEW"
    assert result.pit_universe_evidence_helper_health_status == "PASS"
    assert result.pit_universe_evidence_helper_review_id == "review-a"
    assert result.pit_universe_evidence_helper_row_count == 72
    assert result.pit_universe_evidence_helper_needs_evidence_count == 72
    assert result.pit_universe_evidence_helper_rows_with_base_hints_count == 72
    assert result.pit_universe_evidence_helper_future_dated_hint_count == 72
    assert result.pit_universe_evidence_helper_authoritative_hint_count == 0
    assert result.workflow_stage == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_NEEDS_REVIEW"
    assert helper_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Complete PIT universe evidence" in result.next_manual_action


def test_dashboard_failed_pit_universe_evidence_helper_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_completion_helper_status(
        root,
        helper_id="helper-fail",
        status="FAIL",
        workflow_stage="PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    helper_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_FAILED"
    assert helper_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair PIT universe evidence completion helper artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_evidence_helper(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_completion_helper_status(root, helper_id="helper-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    helper_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_helper_id == "helper-paper-context"
    assert result.pit_universe_evidence_helper_needs_evidence_count == 72
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert helper_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_evidence_helper_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_completion_helper_status(root, helper_id="helper-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_evidence_helper_id"] == "helper-summary"
    assert row["pit_universe_evidence_helper_status"] == "WARN"
    assert row["pit_universe_evidence_helper_stage"] == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_NEEDS_REVIEW"
    assert row["pit_universe_evidence_helper_health_status"] == "PASS"
    assert row["pit_universe_evidence_helper_row_count"] == "72"
    assert row["pit_universe_evidence_helper_needs_evidence_count"] == "72"
    assert row["pit_universe_evidence_helper_authoritative_hint_count"] == "0"
    assert metadata["latest_pit_universe_evidence_helper_id"] == "helper-summary"
    assert metadata["pit_universe_evidence_helper_needs_evidence_count"] == 72
    assert metadata["pit_universe_evidence_helper_authoritative_hint_count"] == 0
    assert metadata["component_statuses"]["latest_pit_universe_evidence_helper_id"] == "helper-summary"


def test_cli_research_status_prints_pit_universe_evidence_helper_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_completion_helper_status(root, helper_id="helper-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_evidence_helper_id: helper-cli" in output.out
    assert "pit_universe_evidence_helper_status: WARN" in output.out
    assert "pit_universe_evidence_helper_stage: PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_NEEDS_REVIEW" in output.out
    assert "pit_universe_evidence_helper_needs_evidence_count: 72" in output.out
    assert "pit_universe_evidence_helper_authoritative_hint_count: 0" in output.out


def test_dashboard_includes_pit_universe_evidence_worklist_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_review_worklist_status(root, worklist_id="worklist-needs-review")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    worklist_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_worklist_id == "worklist-needs-review"
    assert result.pit_universe_evidence_worklist_status == "WARN"
    assert result.pit_universe_evidence_worklist_stage == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_NEEDS_REVIEW"
    assert result.pit_universe_evidence_worklist_health_status == "PASS"
    assert result.pit_universe_evidence_worklist_review_id == "review-a"
    assert result.pit_universe_evidence_worklist_helper_id == "helper-a"
    assert result.pit_universe_evidence_worklist_row_count == 72
    assert result.pit_universe_evidence_worklist_symbol_count == 9
    assert result.pit_universe_evidence_worklist_signal_date_count == 8
    assert result.pit_universe_evidence_worklist_needs_evidence_count == 72
    assert result.pit_universe_evidence_worklist_future_dated_hint_count == 72
    assert result.workflow_stage == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_NEEDS_REVIEW"
    assert worklist_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Complete PIT universe evidence" in result.next_manual_action


def test_dashboard_failed_pit_universe_evidence_worklist_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_review_worklist_status(
        root,
        worklist_id="worklist-fail",
        status="FAIL",
        workflow_stage="PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    worklist_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_FAILED"
    assert worklist_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair PIT universe evidence review worklist artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_pit_universe_evidence_worklist(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_review_worklist_status(root, worklist_id="worklist-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    worklist_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_worklist_id == "worklist-paper-context"
    assert result.pit_universe_evidence_worklist_needs_evidence_count == 72
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert worklist_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_universe_evidence_worklist_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_review_worklist_status(root, worklist_id="worklist-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_evidence_worklist_id"] == "worklist-summary"
    assert row["pit_universe_evidence_worklist_status"] == "WARN"
    assert row["pit_universe_evidence_worklist_stage"] == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_NEEDS_REVIEW"
    assert row["pit_universe_evidence_worklist_health_status"] == "PASS"
    assert row["pit_universe_evidence_worklist_row_count"] == "72"
    assert row["pit_universe_evidence_worklist_needs_evidence_count"] == "72"
    assert row["pit_universe_evidence_worklist_future_dated_hint_count"] == "72"
    assert metadata["latest_pit_universe_evidence_worklist_id"] == "worklist-summary"
    assert metadata["pit_universe_evidence_worklist_needs_evidence_count"] == 72
    assert metadata["pit_universe_evidence_worklist_future_dated_hint_count"] == 72
    assert metadata["component_statuses"]["latest_pit_universe_evidence_worklist_id"] == "worklist-summary"


def test_cli_research_status_prints_pit_universe_evidence_worklist_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_review_worklist_status(root, worklist_id="worklist-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_evidence_worklist_id: worklist-cli" in output.out
    assert "pit_universe_evidence_worklist_status: WARN" in output.out
    assert "pit_universe_evidence_worklist_stage: PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_NEEDS_REVIEW" in output.out
    assert "pit_universe_evidence_worklist_needs_evidence_count: 72" in output.out
    assert "pit_universe_evidence_worklist_future_dated_hint_count: 72" in output.out


def test_dashboard_includes_pit_universe_evidence_update_ingestion_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_update_ingestion_status(root, ingestion_id="ingest-no-ready")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_update_ingestion_id == "ingest-no-ready"
    assert result.pit_universe_evidence_update_ingestion_status == "WARN"
    assert (
        result.pit_universe_evidence_update_ingestion_stage
        == "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_NO_READY_UPDATES"
    )
    assert result.pit_universe_evidence_update_ingestion_health_status == "PASS"
    assert result.pit_universe_evidence_update_ingestion_row_count == 72
    assert result.pit_universe_evidence_update_ingestion_ready_for_review_update_count == 0
    assert result.pit_universe_evidence_update_ingestion_blocked_count == 72
    assert result.workflow_stage == "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_NO_READY_UPDATES"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_pit_universe_evidence_update_ingestion(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_update_ingestion_status(root, ingestion_id="ingest-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_STATUS"
    ].iloc[0]

    assert result.latest_pit_universe_evidence_update_ingestion_id == "ingest-paper-context"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_dashboard_exports_pit_universe_evidence_update_ingestion_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_update_ingestion_status(root, ingestion_id="ingest-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_universe_evidence_update_ingestion_id"] == "ingest-summary"
    assert row["pit_universe_evidence_update_ingestion_status"] == "WARN"
    assert (
        row["pit_universe_evidence_update_ingestion_stage"]
        == "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_NO_READY_UPDATES"
    )
    assert row["pit_universe_evidence_update_ingestion_ready_for_review_update_count"] == "0"
    assert row["pit_universe_evidence_update_ingestion_blocked_count"] == "72"
    assert metadata["latest_pit_universe_evidence_update_ingestion_id"] == "ingest-summary"
    assert metadata["pit_universe_evidence_update_ingestion_blocked_count"] == 72


def test_cli_research_status_prints_pit_universe_evidence_update_ingestion_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_universe_evidence_update_ingestion_status(root, ingestion_id="ingest-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_universe_evidence_update_ingestion_id: ingest-cli" in output.out
    assert "pit_universe_evidence_update_ingestion_status: WARN" in output.out
    assert (
        "pit_universe_evidence_update_ingestion_stage: "
        "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_NO_READY_UPDATES"
    ) in output.out
    assert "pit_universe_evidence_update_ingestion_ready_for_review_update_count: 0" in output.out
    assert "pit_universe_evidence_update_ingestion_blocked_count: 72" in output.out


def test_dashboard_includes_pit_evidence_checklist_validator_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_checklist_validator_artifact(root, validator_id="validator-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_EVIDENCE_CHECKLIST_VALIDATOR_STATUS"
    ].iloc[0]

    assert result.latest_pit_evidence_checklist_validator_id == "validator-blocked"
    assert result.pit_evidence_checklist_validator_status == "WARN"
    assert result.pit_evidence_checklist_validator_stage == "PIT_EVIDENCE_CHECKLIST_VALIDATION_BLOCKED"
    assert result.pit_evidence_checklist_validator_health_status == "PASS"
    assert result.pit_evidence_checklist_validator_row_count == 16
    assert result.pit_evidence_checklist_validator_checklist_pass_count == 0
    assert result.pit_evidence_checklist_validator_blocked_count == 16
    assert result.pit_evidence_checklist_validator_stock_core_blocked_count == 8
    assert result.pit_evidence_checklist_validator_etf_core_blocked_count == 8
    assert result.workflow_stage == "PIT_EVIDENCE_CHECKLIST_VALIDATION_BLOCKED"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_pit_evidence_checklist_validator(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_checklist_validator_artifact(root, validator_id="validator-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_EVIDENCE_CHECKLIST_VALIDATOR_STATUS"
    ].iloc[0]

    assert result.latest_pit_evidence_checklist_validator_id == "validator-paper-context"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_pit_evidence_checklist_validator_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_checklist_validator_artifact(root, validator_id="validator-summary")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_pit_evidence_checklist_validator_id"] == "validator-summary"
    assert row["pit_evidence_checklist_validator_status"] == "WARN"
    assert row["pit_evidence_checklist_validator_stage"] == "PIT_EVIDENCE_CHECKLIST_VALIDATION_BLOCKED"
    assert row["pit_evidence_checklist_validator_health_status"] == "PASS"
    assert row["pit_evidence_checklist_validator_row_count"] == "16"
    assert row["pit_evidence_checklist_validator_checklist_pass_count"] == "0"
    assert row["pit_evidence_checklist_validator_blocked_count"] == "16"
    assert row["pit_evidence_checklist_validator_stock_core_blocked_count"] == "8"
    assert row["pit_evidence_checklist_validator_etf_core_blocked_count"] == "8"
    assert metadata["latest_pit_evidence_checklist_validator_id"] == "validator-summary"
    assert metadata["pit_evidence_checklist_validator_blocked_count"] == 16
    assert metadata["component_statuses"]["latest_pit_evidence_checklist_validator_id"] == "validator-summary"


def test_cli_research_status_prints_pit_evidence_checklist_validator_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_checklist_validator_artifact(root, validator_id="validator-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_evidence_checklist_validator_id: validator-cli" in output.out
    assert "pit_evidence_checklist_validator_status: WARN" in output.out
    assert "pit_evidence_checklist_validator_stage: PIT_EVIDENCE_CHECKLIST_VALIDATION_BLOCKED" in output.out
    assert "pit_evidence_checklist_validator_health_status: PASS" in output.out
    assert "pit_evidence_checklist_validator_blocked_count: 16" in output.out


def test_dashboard_includes_pit_evidence_policy_profile_comparison_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_policy_profile_comparison_artifact(root, comparison_id="comparison-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_EVIDENCE_POLICY_PROFILE_COMPARISON_STATUS"
    ].iloc[0]

    assert result.latest_pit_evidence_policy_profile_comparison_id == "comparison-blocked"
    assert result.pit_evidence_policy_profile_comparison_status == "WARN"
    assert (
        result.pit_evidence_policy_profile_comparison_stage
        == "PIT_EVIDENCE_POLICY_PROFILE_COMPARISON_ALL_BLOCKED"
    )
    assert result.pit_evidence_policy_profile_comparison_health_status == "PASS"
    assert result.pit_evidence_policy_profile_comparison_profile_name == "EOD_POST_CLOSE_LOW_BUDGET_PIT"
    assert result.pit_evidence_policy_profile_comparison_row_count == 16
    assert result.pit_evidence_policy_profile_comparison_strict_pass_count == 0
    assert result.pit_evidence_policy_profile_comparison_eod_low_budget_pass_count == 0
    assert result.pit_evidence_policy_profile_comparison_reviewed_no_hit_support_pass_count == 0
    assert result.pit_evidence_policy_profile_comparison_no_hit_context_supported_count == 0
    assert result.pit_evidence_policy_profile_comparison_reviewer_acceptance_required_count == 0
    assert result.pit_evidence_policy_profile_comparison_relaxed_blocker_count == 16
    assert result.pit_evidence_policy_profile_comparison_remaining_blocked_count == 16
    assert result.workflow_stage == "PIT_EVIDENCE_POLICY_PROFILE_COMPARISON_ALL_BLOCKED"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_pit_evidence_policy_profile_comparison(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_policy_profile_comparison_artifact(root, comparison_id="comparison-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_EVIDENCE_POLICY_PROFILE_COMPARISON_STATUS"
    ].iloc[0]

    assert result.latest_pit_evidence_policy_profile_comparison_id == "comparison-paper-context"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_cli_research_status_prints_pit_evidence_policy_profile_comparison_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_evidence_policy_profile_comparison_artifact(root, comparison_id="comparison-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_evidence_policy_profile_comparison_id: comparison-cli" in output.out
    assert "pit_evidence_policy_profile_comparison_status: WARN" in output.out
    assert (
        "pit_evidence_policy_profile_comparison_stage: "
        "PIT_EVIDENCE_POLICY_PROFILE_COMPARISON_ALL_BLOCKED"
    ) in output.out
    assert "pit_evidence_policy_profile_comparison_eod_low_budget_pass_count: 0" in output.out
    assert "pit_evidence_policy_profile_comparison_reviewed_no_hit_support_pass_count: 0" in output.out


def test_dashboard_includes_pit_official_status_evidence_packet_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_artifact(root, packet_id="packet-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_STATUS"
    ].iloc[0]

    assert result.latest_pit_official_status_evidence_packet_id == "packet-blocked"
    assert result.pit_official_status_evidence_packet_status == "WARN"
    assert result.pit_official_status_evidence_packet_stage == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_BLOCKED"
    assert result.pit_official_status_evidence_packet_health_status == "PASS"
    assert result.pit_official_status_evidence_packet_row_count == 16
    assert result.pit_official_status_evidence_packet_blocked_count == 16
    assert result.pit_official_status_evidence_packet_supporting_local_eod_cache_count == 16
    assert result.workflow_stage == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_BLOCKED"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_pit_official_status_evidence_packet(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_artifact(root, packet_id="packet-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_STATUS"
    ].iloc[0]

    assert result.latest_pit_official_status_evidence_packet_id == "packet-paper-context"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_cli_research_status_prints_pit_official_status_evidence_packet_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_artifact(root, packet_id="packet-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_official_status_evidence_packet_id: packet-cli" in output.out
    assert "pit_official_status_evidence_packet_status: WARN" in output.out
    assert "pit_official_status_evidence_packet_stage: PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_BLOCKED" in output.out
    assert "pit_official_status_evidence_packet_blocked_count: 16" in output.out


def test_dashboard_includes_pit_official_status_evidence_packet_enrichment_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_enrichment_artifact(root, enrichment_id="enrich-blocked")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_ENRICHMENT_STATUS"
    ].iloc[0]

    assert result.latest_pit_official_status_evidence_packet_enrichment_id == "enrich-blocked"
    assert result.pit_official_status_evidence_packet_enrichment_status == "WARN"
    assert (
        result.pit_official_status_evidence_packet_enrichment_stage
        == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_ENRICHMENT_BLOCKED"
    )
    assert result.pit_official_status_evidence_packet_enrichment_health_status == "PASS"
    assert result.pit_official_status_evidence_packet_enrichment_source_packet_id == "packet-a"
    assert result.pit_official_status_evidence_packet_enrichment_policy_comparison_id == "comparison-a"
    assert result.pit_official_status_evidence_packet_enrichment_row_count == 16
    assert result.pit_official_status_evidence_packet_enrichment_strong_official_date_specific_quotation_count == 16
    assert result.pit_official_status_evidence_packet_enrichment_reviewed_no_hit_context_supported_count == 16
    assert result.pit_official_status_evidence_packet_enrichment_reviewer_acceptance_required_count == 16
    assert result.pit_official_status_evidence_packet_enrichment_checklist_pass_count == 0
    assert result.pit_official_status_evidence_packet_enrichment_remaining_blocked_count == 16
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_pit_official_status_evidence_packet_enrichment(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_enrichment_artifact(root, enrichment_id="enrich-paper-context")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_ENRICHMENT_STATUS"
    ].iloc[0]

    assert result.latest_pit_official_status_evidence_packet_enrichment_id == "enrich-paper-context"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_cli_research_status_prints_pit_official_status_evidence_packet_enrichment_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _pit_official_status_evidence_packet_enrichment_artifact(root, enrichment_id="enrich-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_pit_official_status_evidence_packet_enrichment_id: enrich-cli" in output.out
    assert "pit_official_status_evidence_packet_enrichment_status: WARN" in output.out
    assert (
        "pit_official_status_evidence_packet_enrichment_stage: "
        "PIT_OFFICIAL_STATUS_EVIDENCE_PACKET_ENRICHMENT_BLOCKED"
    ) in output.out
    assert "pit_official_status_evidence_packet_enrichment_remaining_blocked_count: 16" in output.out


def test_dashboard_includes_reviewer_no_hit_downstream_impact_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    impact_dir = _reviewer_no_hit_downstream_impact_artifact(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REVIEWER_NO_HIT_ACCEPTANCE_DOWNSTREAM_IMPACT_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.latest_reviewer_no_hit_acceptance_downstream_impact_id == impact_dir.name
    assert result.reviewer_no_hit_downstream_impact_status == "WARN"
    assert (
        result.reviewer_no_hit_downstream_impact_stage
        == "REVIEWER_NO_HIT_ACCEPTANCE_DOWNSTREAM_IMPACT_NO_ACCEPTED_CONTEXT"
    )
    assert result.reviewer_no_hit_downstream_impact_health_status == "PASS"
    assert result.reviewer_no_hit_downstream_impact_accepted_no_hit_context_count == 0
    assert result.reviewer_no_hit_downstream_impact_checklist_pass_count == 0
    assert result.reviewer_no_hit_downstream_impact_remaining_blocked_count == 2
    assert not result.reviewer_no_hit_downstream_impact_approval_applied
    assert result.workflow_stage == "REVIEWER_NO_HIT_ACCEPTANCE_DOWNSTREAM_IMPACT_NO_ACCEPTED_CONTEXT"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert summary.loc[0, "latest_reviewer_no_hit_acceptance_downstream_impact_id"] == impact_dir.name
    assert metadata["reviewer_no_hit_downstream_impact_checklist_pass_count"] == 0


def test_dashboard_preserves_later_paper_priority_over_reviewer_no_hit_downstream_impact(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _reviewer_no_hit_downstream_impact_artifact(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REVIEWER_NO_HIT_ACCEPTANCE_DOWNSTREAM_IMPACT_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_reviewer_no_hit_acceptance_downstream_impact_id
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_cli_research_status_prints_reviewer_no_hit_downstream_impact_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    impact_dir = _reviewer_no_hit_downstream_impact_artifact(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert (
        "latest_reviewer_no_hit_acceptance_downstream_impact_id: "
        f"{impact_dir.name}"
    ) in output.out
    assert "reviewer_no_hit_downstream_impact_status: WARN" in output.out
    assert (
        "reviewer_no_hit_downstream_impact_stage: "
        "REVIEWER_NO_HIT_ACCEPTANCE_DOWNSTREAM_IMPACT_NO_ACCEPTED_CONTEXT"
    ) in output.out
    assert "reviewer_no_hit_downstream_impact_approval_applied: False" in output.out


def test_research_status_includes_replay_substrate_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_substrate_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_substrate_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.latest_replay_substrate_schema_fixture_id == fixture.fixture_id
    assert result.replay_substrate_schema_fixture_status == "PASS"
    assert result.replay_substrate_schema_fixture_stage == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_READY"
    assert result.replay_substrate_schema_fixture_health_status == "PASS"
    assert result.replay_substrate_schema_fixture_entity_count == 14
    assert result.replay_substrate_schema_fixture_validation_issue_count == 0
    assert result.replay_substrate_schema_fixture_overclaim_guard_status == "PASS"
    assert result.replay_substrate_schema_fixture_overclaim_guard_pass_count == 8
    assert result.replay_substrate_schema_fixture_overclaim_guard_total_count == 8
    assert result.replay_substrate_schema_fixture_active_replay_input is False
    assert result.replay_substrate_schema_fixture_forward_labels_exist is False
    assert result.replay_substrate_schema_fixture_weights_trained is False
    assert result.replay_substrate_schema_fixture_active_stock_profile_exists is False
    assert result.replay_substrate_schema_fixture_real_buy_review_eligible is False
    assert result.replay_substrate_schema_fixture_report_only is True
    assert result.replay_substrate_schema_fixture_diagnostic_only is True
    assert result.replay_substrate_schema_fixture_no_live_trading is True
    assert result.replay_substrate_schema_fixture_no_broker_api is True
    assert result.replay_substrate_schema_fixture_no_order_placement is True
    assert result.workflow_stage == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_READY"
    assert row["status"] == "PASS"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_replay_substrate_schema_fixture_id"] == fixture.fixture_id
    assert summary.loc[0, "replay_substrate_schema_fixture_overclaim_guard_pass_count"] == "8"
    assert summary.loc[0, "replay_substrate_schema_fixture_overclaim_guard_total_count"] == "8"
    assert summary.loc[0, "replay_substrate_schema_fixture_active_replay_input"] == "False"
    assert metadata["replay_substrate_schema_fixture_status"] == "PASS"
    assert metadata["replay_substrate_schema_fixture_overclaim_guard_pass_count"] == 8
    assert metadata["replay_substrate_schema_fixture_overclaim_guard_total_count"] == 8
    assert metadata["replay_substrate_schema_fixture_active_replay_input"] is False
    assert metadata["replay_substrate_schema_fixture_forward_labels_exist"] is False
    assert metadata["replay_substrate_schema_fixture_weights_trained"] is False
    assert metadata["replay_substrate_schema_fixture_active_stock_profile_exists"] is False
    assert metadata["replay_substrate_schema_fixture_real_buy_review_eligible"] is False
    assert metadata["replay_substrate_schema_fixture_report_only"] is True
    assert metadata["replay_substrate_schema_fixture_diagnostic_only"] is True
    assert metadata["replay_substrate_schema_fixture_no_live_trading"] is True
    assert metadata["replay_substrate_schema_fixture_no_broker_api"] is True
    assert metadata["replay_substrate_schema_fixture_no_order_placement"] is True


def test_research_status_preserves_paper_priority_over_replay_substrate_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_replay_substrate_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_substrate_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.replay_substrate_schema_fixture_status == "PASS"
    assert row["status"] == "PASS"


def test_research_status_includes_source_registry_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_source_registry_schema_fixture(
        output_dir=root / "manual_diagnostics" / "source_registry_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SOURCE_REGISTRY_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.source_registry_schema_fixture_workflow_implemented is True
    assert result.source_registry_schema_fixture_views_implemented is True
    assert result.latest_source_registry_schema_fixture_id == fixture.source_registry_schema_fixture_id
    assert result.latest_source_registry_schema_fixture_status == "PASS"
    assert result.latest_source_registry_schema_fixture_health_status == "PASS"
    assert result.latest_source_registry_schema_fixture_workflow_stage == "SOURCE_REGISTRY_SCHEMA_FIXTURE_CREATED"
    assert result.source_registry_schema_fixture_context_visible is True
    assert result.source_registry_schema_fixture_created is True
    assert result.source_registry_schema_fixture_source_count == 5
    assert result.source_registry_schema_fixture_validation_issue_count == 0
    assert result.source_registry_schema_fixture_report_only is True
    assert result.source_registry_schema_fixture_diagnostic_only is True
    assert result.source_registry_schema_fixture_live_trading_enabled is False
    assert result.source_registry_schema_fixture_broker_api_called is False
    assert result.source_registry_schema_fixture_external_api_called is False
    assert result.source_registry_schema_fixture_llm_api_called is False
    assert result.source_registry_schema_fixture_data_raw_written is False
    assert result.source_registry_schema_fixture_data_processed_written is False
    assert result.source_registry_schema_fixture_data_cache_written is False
    assert result.source_registry_schema_fixture_current_candidates_run is False
    assert result.source_registry_schema_fixture_snapshot_built is False
    assert result.source_registry_schema_fixture_signal_semantics_changed is False
    assert result.source_registry_schema_fixture_active_stock_profile_created is False
    assert result.source_registry_schema_fixture_real_buy_review_eligible is False
    assert result.source_registry_schema_fixture_buy_review_allowed is False
    assert result.source_registry_schema_fixture_strategy_performance_validated is False
    assert result.source_registry_schema_fixture_trading_allowed is False
    assert result.source_registry_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "SOURCE_REGISTRY_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_source_registry_schema_fixture_id"] == (
        fixture.source_registry_schema_fixture_id
    )
    assert summary.loc[0, "source_registry_schema_fixture_source_count"] == "5"
    assert summary.loc[0, "source_registry_schema_fixture_context_visible"] == "True"
    assert metadata["latest_source_registry_schema_fixture_status"] == "PASS"
    assert metadata["source_registry_schema_fixture_context_visible"] is True
    assert metadata["source_registry_schema_fixture_created"] is True
    assert metadata["source_registry_schema_fixture_report_only"] is True
    assert metadata["source_registry_schema_fixture_diagnostic_only"] is True
    assert metadata["source_registry_schema_fixture_buy_review_allowed"] is False
    assert metadata["source_registry_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_source_registry_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_source_registry_schema_fixture(
        output_dir=root / "manual_diagnostics" / "source_registry_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Source Registry Schema Fixture is report-only context, "
            "not real source permission, production readiness, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_source_registry_schema_fixture_status == "PASS"
    assert result.source_registry_schema_fixture_context_visible is True
    assert result.source_registry_schema_fixture_buy_review_allowed is False
    assert result.source_registry_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_source_registry_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_source_registry_schema_fixture(
        output_dir=root / "manual_diagnostics" / "source_registry_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "source_registry_schema_fixture_workflow_implemented: True" in output.out
    assert "source_registry_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_source_registry_schema_fixture_id: {fixture.source_registry_schema_fixture_id}"
        in output.out
    )
    assert "latest_source_registry_schema_fixture_status: PASS" in output.out
    assert "latest_source_registry_schema_fixture_health_status: PASS" in output.out
    assert "latest_source_registry_schema_fixture_workflow_stage: SOURCE_REGISTRY_SCHEMA_FIXTURE_CREATED" in output.out
    assert "source_registry_schema_fixture_context_visible: True" in output.out
    assert "source_registry_schema_fixture_created: True" in output.out
    assert "source_registry_schema_fixture_source_count: 5" in output.out
    assert "source_registry_schema_fixture_validation_issue_count: 0" in output.out
    assert "source_registry_schema_fixture_report_only: True" in output.out
    assert "source_registry_schema_fixture_diagnostic_only: True" in output.out
    assert "source_registry_schema_fixture_current_candidates_run: False" in output.out
    assert "source_registry_schema_fixture_snapshot_built: False" in output.out
    assert "source_registry_schema_fixture_signal_semantics_changed: False" in output.out
    assert "source_registry_schema_fixture_active_stock_profile_created: False" in output.out
    assert "source_registry_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "source_registry_schema_fixture_buy_review_allowed: False" in output.out
    assert "source_registry_schema_fixture_strategy_performance_validated: False" in output.out
    assert "source_registry_schema_fixture_trading_allowed: False" in output.out
    assert "source_registry_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_raw_document_store_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_raw_document_store_schema_fixture(
        output_dir=root / "manual_diagnostics" / "raw_document_store_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.raw_document_store_schema_fixture_workflow_implemented is True
    assert result.raw_document_store_schema_fixture_views_implemented is True
    assert result.latest_raw_document_store_schema_fixture_id == fixture.raw_document_store_schema_fixture_id
    assert result.latest_raw_document_store_schema_fixture_status == "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED"
    assert result.latest_raw_document_store_schema_fixture_health_status == "PASS"
    assert result.latest_raw_document_store_schema_fixture_workflow_stage == (
        "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED"
    )
    assert result.raw_document_store_schema_fixture_context_visible is True
    assert result.raw_document_store_schema_fixture_created is True
    assert result.raw_document_store_schema_fixture_document_count == 7
    assert result.raw_document_store_schema_fixture_validation_issue_count == 0
    assert result.raw_document_store_schema_fixture_report_only is True
    assert result.raw_document_store_schema_fixture_diagnostic_only is True
    assert result.raw_document_store_schema_fixture_production_raw_document_store_created is False
    assert result.raw_document_store_schema_fixture_real_source_permission_created is False
    assert result.raw_document_store_schema_fixture_real_data_fetched is False
    assert result.raw_document_store_schema_fixture_raw_document_ingestion_created is False
    assert result.raw_document_store_schema_fixture_factor_observations_created is False
    assert result.raw_document_store_schema_fixture_event_ingestion_created is False
    assert result.raw_document_store_schema_fixture_company_exposure_created is False
    assert result.raw_document_store_schema_fixture_replay_evidence_bundle_created is False
    assert result.raw_document_store_schema_fixture_live_trading_enabled is False
    assert result.raw_document_store_schema_fixture_broker_api_called is False
    assert result.raw_document_store_schema_fixture_external_api_called is False
    assert result.raw_document_store_schema_fixture_llm_api_called is False
    assert result.raw_document_store_schema_fixture_data_raw_written is False
    assert result.raw_document_store_schema_fixture_data_processed_written is False
    assert result.raw_document_store_schema_fixture_data_cache_written is False
    assert result.raw_document_store_schema_fixture_current_candidates_run is False
    assert result.raw_document_store_schema_fixture_snapshot_built is False
    assert result.raw_document_store_schema_fixture_signal_semantics_changed is False
    assert result.raw_document_store_schema_fixture_active_stock_profile_created is False
    assert result.raw_document_store_schema_fixture_real_buy_review_eligible is False
    assert result.raw_document_store_schema_fixture_buy_review_allowed is False
    assert result.raw_document_store_schema_fixture_strategy_performance_validated is False
    assert result.raw_document_store_schema_fixture_trading_allowed is False
    assert result.raw_document_store_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_raw_document_store_schema_fixture_id"] == (
        fixture.raw_document_store_schema_fixture_id
    )
    assert summary.loc[0, "raw_document_store_schema_fixture_document_count"] == "7"
    assert summary.loc[0, "raw_document_store_schema_fixture_context_visible"] == "True"
    assert metadata["latest_raw_document_store_schema_fixture_status"] == "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED"
    assert metadata["raw_document_store_schema_fixture_context_visible"] is True
    assert metadata["raw_document_store_schema_fixture_created"] is True
    assert metadata["raw_document_store_schema_fixture_report_only"] is True
    assert metadata["raw_document_store_schema_fixture_diagnostic_only"] is True
    assert metadata["raw_document_store_schema_fixture_buy_review_allowed"] is False
    assert metadata["raw_document_store_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_raw_document_store_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_raw_document_store_schema_fixture(
        output_dir=root / "manual_diagnostics" / "raw_document_store_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Raw Document Store Schema Fixture is report-only context, "
            "not production raw document store, real source permission, raw ingestion, buy-review, "
            "performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_raw_document_store_schema_fixture_status == "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED"
    assert result.raw_document_store_schema_fixture_context_visible is True
    assert result.raw_document_store_schema_fixture_raw_document_ingestion_created is False
    assert result.raw_document_store_schema_fixture_buy_review_allowed is False
    assert result.raw_document_store_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_raw_document_store_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_raw_document_store_schema_fixture(
        output_dir=root / "manual_diagnostics" / "raw_document_store_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "raw_document_store_schema_fixture_workflow_implemented: True" in output.out
    assert "raw_document_store_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_raw_document_store_schema_fixture_id: {fixture.raw_document_store_schema_fixture_id}"
        in output.out
    )
    assert "latest_raw_document_store_schema_fixture_status: RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED" in output.out
    assert "latest_raw_document_store_schema_fixture_health_status: PASS" in output.out
    assert "latest_raw_document_store_schema_fixture_workflow_stage: RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED" in output.out
    assert "raw_document_store_schema_fixture_context_visible: True" in output.out
    assert "raw_document_store_schema_fixture_created: True" in output.out
    assert "raw_document_store_schema_fixture_document_count: 7" in output.out
    assert "raw_document_store_schema_fixture_validation_issue_count: 0" in output.out
    assert "raw_document_store_schema_fixture_report_only: True" in output.out
    assert "raw_document_store_schema_fixture_diagnostic_only: True" in output.out
    assert "raw_document_store_schema_fixture_production_raw_document_store_created: False" in output.out
    assert "raw_document_store_schema_fixture_real_source_permission_created: False" in output.out
    assert "raw_document_store_schema_fixture_real_data_fetched: False" in output.out
    assert "raw_document_store_schema_fixture_raw_document_ingestion_created: False" in output.out
    assert "raw_document_store_schema_fixture_factor_observations_created: False" in output.out
    assert "raw_document_store_schema_fixture_event_ingestion_created: False" in output.out
    assert "raw_document_store_schema_fixture_company_exposure_created: False" in output.out
    assert "raw_document_store_schema_fixture_replay_evidence_bundle_created: False" in output.out
    assert "raw_document_store_schema_fixture_current_candidates_run: False" in output.out
    assert "raw_document_store_schema_fixture_snapshot_built: False" in output.out
    assert "raw_document_store_schema_fixture_signal_semantics_changed: False" in output.out
    assert "raw_document_store_schema_fixture_active_stock_profile_created: False" in output.out
    assert "raw_document_store_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "raw_document_store_schema_fixture_buy_review_allowed: False" in output.out
    assert "raw_document_store_schema_fixture_strategy_performance_validated: False" in output.out
    assert "raw_document_store_schema_fixture_trading_allowed: False" in output.out
    assert "raw_document_store_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_factor_definition_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_factor_definition_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_definition_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "FACTOR_DEFINITION_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.factor_definition_schema_fixture_workflow_implemented is True
    assert result.factor_definition_schema_fixture_views_implemented is True
    assert result.latest_factor_definition_schema_fixture_id == fixture.factor_definition_schema_fixture_id
    assert result.latest_factor_definition_schema_fixture_status == "PASS"
    assert result.latest_factor_definition_schema_fixture_health_status == "PASS"
    assert result.latest_factor_definition_schema_fixture_workflow_stage == (
        "FACTOR_DEFINITION_SCHEMA_FIXTURE_CREATED"
    )
    assert result.factor_definition_schema_fixture_context_visible is True
    assert result.factor_definition_schema_fixture_created is True
    assert result.factor_definition_schema_fixture_factor_count == 8
    assert result.factor_definition_schema_fixture_taxonomy_layer_count == 8
    assert result.factor_definition_schema_fixture_validation_issue_count == 0
    assert result.factor_definition_schema_fixture_report_only is True
    assert result.factor_definition_schema_fixture_diagnostic_only is True
    assert result.factor_definition_schema_fixture_factor_definition_rows_created is True
    assert result.factor_definition_schema_fixture_taxonomy_primary_classification is True
    assert result.factor_definition_schema_fixture_legacy_12_factor_tags_checklist_only is True
    assert result.factor_definition_schema_fixture_signal_score_formula_active is False
    assert result.factor_definition_schema_fixture_signal_score_implemented is False
    assert result.factor_definition_schema_fixture_live_signals_created is False
    assert result.factor_definition_schema_fixture_signal_semantics_changed is False
    assert result.factor_definition_schema_fixture_factor_observations_created is False
    assert result.factor_definition_schema_fixture_event_ingestion_created is False
    assert result.factor_definition_schema_fixture_company_exposure_created is False
    assert result.factor_definition_schema_fixture_replay_evidence_bundle_created is False
    assert result.factor_definition_schema_fixture_model_training_performed is False
    assert result.factor_definition_schema_fixture_active_weights_created is False
    assert result.factor_definition_schema_fixture_active_thresholds_created is False
    assert result.factor_definition_schema_fixture_stock_profile_validation_created is False
    assert result.factor_definition_schema_fixture_paper_validation_created is False
    assert result.factor_definition_schema_fixture_real_buy_review_eligible is False
    assert result.factor_definition_schema_fixture_buy_review_allowed is False
    assert result.factor_definition_schema_fixture_strategy_performance_validated is False
    assert result.factor_definition_schema_fixture_trading_allowed is False
    assert result.factor_definition_schema_fixture_live_trading_enabled is False
    assert result.factor_definition_schema_fixture_broker_api_called is False
    assert result.factor_definition_schema_fixture_external_api_called is False
    assert result.factor_definition_schema_fixture_llm_api_called is False
    assert result.factor_definition_schema_fixture_data_raw_written is False
    assert result.factor_definition_schema_fixture_data_processed_written is False
    assert result.factor_definition_schema_fixture_data_cache_written is False
    assert result.factor_definition_schema_fixture_current_candidates_run is False
    assert result.factor_definition_schema_fixture_snapshot_built is False
    assert result.factor_definition_schema_fixture_active_stock_profile_created is False
    assert result.factor_definition_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "FACTOR_DEFINITION_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_factor_definition_schema_fixture_id"] == (
        fixture.factor_definition_schema_fixture_id
    )
    assert summary.loc[0, "factor_definition_schema_fixture_factor_count"] == "8"
    assert summary.loc[0, "factor_definition_schema_fixture_context_visible"] == "True"
    assert metadata["latest_factor_definition_schema_fixture_status"] == "PASS"
    assert metadata["factor_definition_schema_fixture_context_visible"] is True
    assert metadata["factor_definition_schema_fixture_created"] is True
    assert metadata["factor_definition_schema_fixture_report_only"] is True
    assert metadata["factor_definition_schema_fixture_diagnostic_only"] is True
    assert metadata["factor_definition_schema_fixture_buy_review_allowed"] is False
    assert metadata["factor_definition_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_factor_definition_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_factor_definition_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_definition_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Factor Definition Schema Fixture is report-only context, "
            "not an active factor library, signal_score, training input, buy-review, performance validation, "
            "or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_factor_definition_schema_fixture_status == "PASS"
    assert result.factor_definition_schema_fixture_context_visible is True
    assert result.factor_definition_schema_fixture_factor_observations_created is False
    assert result.factor_definition_schema_fixture_signal_score_implemented is False
    assert result.factor_definition_schema_fixture_model_training_performed is False
    assert result.factor_definition_schema_fixture_buy_review_allowed is False
    assert result.factor_definition_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_factor_definition_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_factor_definition_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_definition_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "factor_definition_schema_fixture_workflow_implemented: True" in output.out
    assert "factor_definition_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_factor_definition_schema_fixture_id: {fixture.factor_definition_schema_fixture_id}"
        in output.out
    )
    assert "latest_factor_definition_schema_fixture_status: PASS" in output.out
    assert "latest_factor_definition_schema_fixture_health_status: PASS" in output.out
    assert "latest_factor_definition_schema_fixture_workflow_stage: FACTOR_DEFINITION_SCHEMA_FIXTURE_CREATED" in output.out
    assert "factor_definition_schema_fixture_context_visible: True" in output.out
    assert "factor_definition_schema_fixture_created: True" in output.out
    assert "factor_definition_schema_fixture_factor_count: 8" in output.out
    assert "factor_definition_schema_fixture_taxonomy_layer_count: 8" in output.out
    assert "factor_definition_schema_fixture_validation_issue_count: 0" in output.out
    assert "factor_definition_schema_fixture_report_only: True" in output.out
    assert "factor_definition_schema_fixture_diagnostic_only: True" in output.out
    assert "factor_definition_schema_fixture_factor_definition_rows_created: True" in output.out
    assert "factor_definition_schema_fixture_taxonomy_primary_classification: True" in output.out
    assert "factor_definition_schema_fixture_legacy_12_factor_tags_checklist_only: True" in output.out
    assert "factor_definition_schema_fixture_signal_score_formula_active: False" in output.out
    assert "factor_definition_schema_fixture_signal_score_implemented: False" in output.out
    assert "factor_definition_schema_fixture_factor_observations_created: False" in output.out
    assert "factor_definition_schema_fixture_event_ingestion_created: False" in output.out
    assert "factor_definition_schema_fixture_company_exposure_created: False" in output.out
    assert "factor_definition_schema_fixture_replay_evidence_bundle_created: False" in output.out
    assert "factor_definition_schema_fixture_model_training_performed: False" in output.out
    assert "factor_definition_schema_fixture_active_weights_created: False" in output.out
    assert "factor_definition_schema_fixture_active_thresholds_created: False" in output.out
    assert "factor_definition_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "factor_definition_schema_fixture_paper_validation_created: False" in output.out
    assert "factor_definition_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "factor_definition_schema_fixture_buy_review_allowed: False" in output.out
    assert "factor_definition_schema_fixture_strategy_performance_validated: False" in output.out
    assert "factor_definition_schema_fixture_trading_allowed: False" in output.out
    assert "factor_definition_schema_fixture_current_candidates_run: False" in output.out
    assert "factor_definition_schema_fixture_snapshot_built: False" in output.out
    assert "factor_definition_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_company_exposure_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_company_exposure_schema_fixture(
        output_dir=root / "manual_diagnostics" / "company_exposure_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "COMPANY_EXPOSURE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.company_exposure_schema_fixture_workflow_implemented is True
    assert result.company_exposure_schema_fixture_views_implemented is True
    assert result.latest_company_exposure_schema_fixture_id == fixture.company_exposure_schema_fixture_id
    assert result.latest_company_exposure_schema_fixture_status == "PASS"
    assert result.latest_company_exposure_schema_fixture_health_status == "PASS"
    assert result.latest_company_exposure_schema_fixture_workflow_stage == (
        "COMPANY_EXPOSURE_SCHEMA_FIXTURE_CREATED"
    )
    assert result.company_exposure_schema_fixture_context_visible is True
    assert result.company_exposure_schema_fixture_created is True
    assert result.company_exposure_schema_fixture_exposure_count == 10
    assert result.company_exposure_schema_fixture_validation_issue_count == 0
    assert result.company_exposure_schema_fixture_report_only is True
    assert result.company_exposure_schema_fixture_diagnostic_only is True
    assert result.company_exposure_schema_fixture_rows_created is True
    assert result.company_exposure_schema_fixture_production_company_exposure_created is False
    assert result.company_exposure_schema_fixture_active_company_exposure_mapping_created is False
    assert result.company_exposure_schema_fixture_company_knowledge_graph_created is False
    assert result.company_exposure_schema_fixture_real_holdings_ingested is False
    assert result.company_exposure_schema_fixture_supplier_customer_graph_created is False
    assert result.company_exposure_schema_fixture_factor_observations_created is False
    assert result.company_exposure_schema_fixture_event_ingestion_created is False
    assert result.company_exposure_schema_fixture_replay_evidence_bundle_created is False
    assert result.company_exposure_schema_fixture_signal_score_implemented is False
    assert result.company_exposure_schema_fixture_model_training_performed is False
    assert result.company_exposure_schema_fixture_active_weights_created is False
    assert result.company_exposure_schema_fixture_active_thresholds_created is False
    assert result.company_exposure_schema_fixture_stock_profile_validation_created is False
    assert result.company_exposure_schema_fixture_paper_validation_created is False
    assert result.company_exposure_schema_fixture_real_buy_review_eligible is False
    assert result.company_exposure_schema_fixture_buy_review_allowed is False
    assert result.company_exposure_schema_fixture_strategy_performance_validated is False
    assert result.company_exposure_schema_fixture_trading_allowed is False
    assert result.company_exposure_schema_fixture_live_trading_enabled is False
    assert result.company_exposure_schema_fixture_broker_api_called is False
    assert result.company_exposure_schema_fixture_external_api_called is False
    assert result.company_exposure_schema_fixture_llm_api_called is False
    assert result.company_exposure_schema_fixture_data_raw_written is False
    assert result.company_exposure_schema_fixture_data_processed_written is False
    assert result.company_exposure_schema_fixture_data_cache_written is False
    assert result.company_exposure_schema_fixture_current_candidates_run is False
    assert result.company_exposure_schema_fixture_snapshot_built is False
    assert result.company_exposure_schema_fixture_signal_semantics_changed is False
    assert result.company_exposure_schema_fixture_active_stock_profile_created is False
    assert result.company_exposure_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "COMPANY_EXPOSURE_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_company_exposure_schema_fixture_id"] == (
        fixture.company_exposure_schema_fixture_id
    )
    assert summary.loc[0, "company_exposure_schema_fixture_exposure_count"] == "10"
    assert summary.loc[0, "company_exposure_schema_fixture_context_visible"] == "True"
    assert metadata["latest_company_exposure_schema_fixture_status"] == "PASS"
    assert metadata["company_exposure_schema_fixture_context_visible"] is True
    assert metadata["company_exposure_schema_fixture_created"] is True
    assert metadata["company_exposure_schema_fixture_report_only"] is True
    assert metadata["company_exposure_schema_fixture_diagnostic_only"] is True
    assert metadata["company_exposure_schema_fixture_buy_review_allowed"] is False
    assert metadata["company_exposure_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_company_exposure_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_company_exposure_schema_fixture(
        output_dir=root / "manual_diagnostics" / "company_exposure_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Company Exposure Schema Fixture is report-only context, "
            "not production exposure mapping, signal_score, training input, stock_profile validation, "
            "buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_company_exposure_schema_fixture_status == "PASS"
    assert result.company_exposure_schema_fixture_context_visible is True
    assert result.company_exposure_schema_fixture_production_company_exposure_created is False
    assert result.company_exposure_schema_fixture_factor_observations_created is False
    assert result.company_exposure_schema_fixture_signal_score_implemented is False
    assert result.company_exposure_schema_fixture_model_training_performed is False
    assert result.company_exposure_schema_fixture_buy_review_allowed is False
    assert result.company_exposure_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_company_exposure_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_company_exposure_schema_fixture(
        output_dir=root / "manual_diagnostics" / "company_exposure_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "company_exposure_schema_fixture_workflow_implemented: True" in output.out
    assert "company_exposure_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_company_exposure_schema_fixture_id: {fixture.company_exposure_schema_fixture_id}"
        in output.out
    )
    assert "latest_company_exposure_schema_fixture_status: PASS" in output.out
    assert "latest_company_exposure_schema_fixture_health_status: PASS" in output.out
    assert "latest_company_exposure_schema_fixture_workflow_stage: COMPANY_EXPOSURE_SCHEMA_FIXTURE_CREATED" in output.out
    assert "company_exposure_schema_fixture_context_visible: True" in output.out
    assert "company_exposure_schema_fixture_created: True" in output.out
    assert "company_exposure_schema_fixture_exposure_count: 10" in output.out
    assert "company_exposure_schema_fixture_validation_issue_count: 0" in output.out
    assert "company_exposure_schema_fixture_report_only: True" in output.out
    assert "company_exposure_schema_fixture_diagnostic_only: True" in output.out
    assert "company_exposure_schema_fixture_rows_created: True" in output.out
    assert "company_exposure_schema_fixture_production_company_exposure_created: False" in output.out
    assert "company_exposure_schema_fixture_active_company_exposure_mapping_created: False" in output.out
    assert "company_exposure_schema_fixture_company_knowledge_graph_created: False" in output.out
    assert "company_exposure_schema_fixture_real_holdings_ingested: False" in output.out
    assert "company_exposure_schema_fixture_supplier_customer_graph_created: False" in output.out
    assert "company_exposure_schema_fixture_factor_observations_created: False" in output.out
    assert "company_exposure_schema_fixture_event_ingestion_created: False" in output.out
    assert "company_exposure_schema_fixture_replay_evidence_bundle_created: False" in output.out
    assert "company_exposure_schema_fixture_signal_score_implemented: False" in output.out
    assert "company_exposure_schema_fixture_model_training_performed: False" in output.out
    assert "company_exposure_schema_fixture_active_weights_created: False" in output.out
    assert "company_exposure_schema_fixture_active_thresholds_created: False" in output.out
    assert "company_exposure_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "company_exposure_schema_fixture_paper_validation_created: False" in output.out
    assert "company_exposure_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "company_exposure_schema_fixture_buy_review_allowed: False" in output.out
    assert "company_exposure_schema_fixture_strategy_performance_validated: False" in output.out
    assert "company_exposure_schema_fixture_trading_allowed: False" in output.out
    assert "company_exposure_schema_fixture_current_candidates_run: False" in output.out
    assert "company_exposure_schema_fixture_snapshot_built: False" in output.out
    assert "company_exposure_schema_fixture_signal_semantics_changed: False" in output.out
    assert "company_exposure_schema_fixture_active_stock_profile_created: False" in output.out
    assert "company_exposure_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_event_structured_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_event_structured_schema_fixture(
        output_dir=root / "manual_diagnostics" / "event_structured_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "EVENT_STRUCTURED_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.event_structured_schema_fixture_workflow_implemented is True
    assert result.event_structured_schema_fixture_views_implemented is True
    assert result.latest_event_structured_schema_fixture_id == fixture.event_structured_schema_fixture_id
    assert result.latest_event_structured_schema_fixture_status == "PASS"
    assert result.latest_event_structured_schema_fixture_health_status == "PASS"
    assert result.latest_event_structured_schema_fixture_workflow_stage == (
        "EVENT_STRUCTURED_SCHEMA_FIXTURE_CREATED"
    )
    assert result.event_structured_schema_fixture_artifact_path.endswith(
        fixture.event_structured_schema_fixture_id
    )
    assert result.event_structured_schema_fixture_context_visible is True
    assert result.event_structured_schema_fixture_created is True
    assert result.event_structured_schema_fixture_event_count == 10
    assert result.event_structured_schema_fixture_validation_issue_count == 0
    assert result.event_structured_schema_fixture_report_only is True
    assert result.event_structured_schema_fixture_diagnostic_only is True
    assert result.event_structured_schema_fixture_rows_created is True
    assert result.event_structured_schema_fixture_production_event_ingestion_created is False
    assert result.event_structured_schema_fixture_active_event_library_created is False
    assert result.event_structured_schema_fixture_real_raw_document_ingestion_created is False
    assert result.event_structured_schema_fixture_factor_observations_created is False
    assert result.event_structured_schema_fixture_company_exposure_production_mapping_created is False
    assert result.event_structured_schema_fixture_replay_evidence_bundle_created is False
    assert result.event_structured_schema_fixture_signal_score_implemented is False
    assert result.event_structured_schema_fixture_model_training_performed is False
    assert result.event_structured_schema_fixture_active_weights_created is False
    assert result.event_structured_schema_fixture_active_thresholds_created is False
    assert result.event_structured_schema_fixture_stock_profile_validation_created is False
    assert result.event_structured_schema_fixture_paper_validation_created is False
    assert result.event_structured_schema_fixture_real_buy_review_eligible is False
    assert result.event_structured_schema_fixture_buy_review_allowed is False
    assert result.event_structured_schema_fixture_strategy_performance_validated is False
    assert result.event_structured_schema_fixture_trading_allowed is False
    assert result.event_structured_schema_fixture_live_trading_enabled is False
    assert result.event_structured_schema_fixture_broker_api_called is False
    assert result.event_structured_schema_fixture_external_api_called is False
    assert result.event_structured_schema_fixture_llm_api_called is False
    assert result.event_structured_schema_fixture_data_raw_written is False
    assert result.event_structured_schema_fixture_data_processed_written is False
    assert result.event_structured_schema_fixture_data_cache_written is False
    assert result.event_structured_schema_fixture_current_candidates_run is False
    assert result.event_structured_schema_fixture_snapshot_built is False
    assert result.event_structured_schema_fixture_signal_semantics_changed is False
    assert result.event_structured_schema_fixture_active_stock_profile_created is False
    assert result.event_structured_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "EVENT_STRUCTURED_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_event_structured_schema_fixture_id"] == (
        fixture.event_structured_schema_fixture_id
    )
    assert summary.loc[0, "event_structured_schema_fixture_event_count"] == "10"
    assert summary.loc[0, "event_structured_schema_fixture_context_visible"] == "True"
    assert metadata["latest_event_structured_schema_fixture_status"] == "PASS"
    assert metadata["event_structured_schema_fixture_context_visible"] is True
    assert metadata["event_structured_schema_fixture_created"] is True
    assert metadata["event_structured_schema_fixture_report_only"] is True
    assert metadata["event_structured_schema_fixture_diagnostic_only"] is True
    assert metadata["event_structured_schema_fixture_buy_review_allowed"] is False
    assert metadata["event_structured_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_event_structured_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_event_structured_schema_fixture(
        output_dir=root / "manual_diagnostics" / "event_structured_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Event Structured Schema Fixture is report-only context, "
            "not production event ingestion, active event library, factor observations, signal_score, "
            "training input, stock_profile validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_event_structured_schema_fixture_status == "PASS"
    assert result.event_structured_schema_fixture_context_visible is True
    assert result.event_structured_schema_fixture_production_event_ingestion_created is False
    assert result.event_structured_schema_fixture_active_event_library_created is False
    assert result.event_structured_schema_fixture_factor_observations_created is False
    assert result.event_structured_schema_fixture_signal_score_implemented is False
    assert result.event_structured_schema_fixture_model_training_performed is False
    assert result.event_structured_schema_fixture_buy_review_allowed is False
    assert result.event_structured_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_event_structured_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_event_structured_schema_fixture(
        output_dir=root / "manual_diagnostics" / "event_structured_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "event_structured_schema_fixture_workflow_implemented: True" in output.out
    assert "event_structured_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_event_structured_schema_fixture_id: {fixture.event_structured_schema_fixture_id}"
        in output.out
    )
    assert "latest_event_structured_schema_fixture_status: PASS" in output.out
    assert "latest_event_structured_schema_fixture_health_status: PASS" in output.out
    assert "latest_event_structured_schema_fixture_workflow_stage: EVENT_STRUCTURED_SCHEMA_FIXTURE_CREATED" in output.out
    assert "event_structured_schema_fixture_context_visible: True" in output.out
    assert "event_structured_schema_fixture_created: True" in output.out
    assert "event_structured_schema_fixture_event_count: 10" in output.out
    assert "event_structured_schema_fixture_validation_issue_count: 0" in output.out
    assert "event_structured_schema_fixture_report_only: True" in output.out
    assert "event_structured_schema_fixture_diagnostic_only: True" in output.out
    assert "event_structured_schema_fixture_rows_created: True" in output.out
    assert "event_structured_schema_fixture_production_event_ingestion_created: False" in output.out
    assert "event_structured_schema_fixture_active_event_library_created: False" in output.out
    assert "event_structured_schema_fixture_real_raw_document_ingestion_created: False" in output.out
    assert "event_structured_schema_fixture_factor_observations_created: False" in output.out
    assert "event_structured_schema_fixture_company_exposure_production_mapping_created: False" in output.out
    assert "event_structured_schema_fixture_replay_evidence_bundle_created: False" in output.out
    assert "event_structured_schema_fixture_signal_score_implemented: False" in output.out
    assert "event_structured_schema_fixture_model_training_performed: False" in output.out
    assert "event_structured_schema_fixture_active_weights_created: False" in output.out
    assert "event_structured_schema_fixture_active_thresholds_created: False" in output.out
    assert "event_structured_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "event_structured_schema_fixture_paper_validation_created: False" in output.out
    assert "event_structured_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "event_structured_schema_fixture_buy_review_allowed: False" in output.out
    assert "event_structured_schema_fixture_strategy_performance_validated: False" in output.out
    assert "event_structured_schema_fixture_trading_allowed: False" in output.out
    assert "event_structured_schema_fixture_current_candidates_run: False" in output.out
    assert "event_structured_schema_fixture_snapshot_built: False" in output.out
    assert "event_structured_schema_fixture_signal_semantics_changed: False" in output.out
    assert "event_structured_schema_fixture_active_stock_profile_created: False" in output.out
    assert "event_structured_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_factor_observation_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_factor_observation_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_observation_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "FACTOR_OBSERVATION_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.factor_observation_schema_fixture_workflow_implemented is True
    assert result.factor_observation_schema_fixture_views_implemented is True
    assert result.latest_factor_observation_schema_fixture_id == fixture.factor_observation_schema_fixture_id
    assert result.latest_factor_observation_schema_fixture_status == "PASS"
    assert result.latest_factor_observation_schema_fixture_health_status == "PASS"
    assert result.latest_factor_observation_schema_fixture_workflow_stage == (
        "FACTOR_OBSERVATION_SCHEMA_FIXTURE_CREATED"
    )
    assert result.factor_observation_schema_fixture_artifact_path.endswith(
        fixture.factor_observation_schema_fixture_id
    )
    assert result.factor_observation_schema_fixture_context_visible is True
    assert result.factor_observation_schema_fixture_created is True
    assert result.factor_observation_schema_fixture_observation_count == 10
    assert result.factor_observation_schema_fixture_validation_issue_count == 0
    assert result.factor_observation_schema_fixture_report_only is True
    assert result.factor_observation_schema_fixture_diagnostic_only is True
    assert result.factor_observation_schema_fixture_rows_created is True
    assert result.factor_observation_schema_fixture_production_factor_observations_created is False
    assert result.factor_observation_schema_fixture_real_factor_observations_created is False
    assert result.factor_observation_schema_fixture_production_factor_registry_created is False
    assert result.factor_observation_schema_fixture_active_factor_library_created is False
    assert result.factor_observation_schema_fixture_production_event_ingestion_created is False
    assert result.factor_observation_schema_fixture_active_event_library_created is False
    assert result.factor_observation_schema_fixture_production_company_exposure_mapping_created is False
    assert result.factor_observation_schema_fixture_real_raw_document_ingestion_created is False
    assert result.factor_observation_schema_fixture_replay_evidence_bundle_created is False
    assert result.factor_observation_schema_fixture_replay_decisions_created is False
    assert result.factor_observation_schema_fixture_forward_labels_created is False
    assert result.factor_observation_schema_fixture_normalization_created is False
    assert result.factor_observation_schema_fixture_winsorization_created is False
    assert result.factor_observation_schema_fixture_direction_adjusted_values_created is False
    assert result.factor_observation_schema_fixture_signal_score_implemented is False
    assert result.factor_observation_schema_fixture_signal_score_input_authorized is False
    assert result.factor_observation_schema_fixture_model_training_performed is False
    assert result.factor_observation_schema_fixture_active_weights_created is False
    assert result.factor_observation_schema_fixture_active_thresholds_created is False
    assert result.factor_observation_schema_fixture_stock_profile_validation_created is False
    assert result.factor_observation_schema_fixture_paper_validation_created is False
    assert result.factor_observation_schema_fixture_real_buy_review_eligible is False
    assert result.factor_observation_schema_fixture_buy_review_allowed is False
    assert result.factor_observation_schema_fixture_strategy_performance_validated is False
    assert result.factor_observation_schema_fixture_trading_allowed is False
    assert result.factor_observation_schema_fixture_live_trading_enabled is False
    assert result.factor_observation_schema_fixture_broker_api_called is False
    assert result.factor_observation_schema_fixture_external_api_called is False
    assert result.factor_observation_schema_fixture_llm_api_called is False
    assert result.factor_observation_schema_fixture_data_raw_written is False
    assert result.factor_observation_schema_fixture_data_processed_written is False
    assert result.factor_observation_schema_fixture_data_cache_written is False
    assert result.factor_observation_schema_fixture_current_candidates_run is False
    assert result.factor_observation_schema_fixture_snapshot_built is False
    assert result.factor_observation_schema_fixture_signal_semantics_changed is False
    assert result.factor_observation_schema_fixture_active_stock_profile_created is False
    assert result.factor_observation_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "FACTOR_OBSERVATION_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_factor_observation_schema_fixture_id"] == (
        fixture.factor_observation_schema_fixture_id
    )
    assert summary.loc[0, "factor_observation_schema_fixture_observation_count"] == "10"
    assert summary.loc[0, "factor_observation_schema_fixture_context_visible"] == "True"
    assert metadata["latest_factor_observation_schema_fixture_status"] == "PASS"
    assert metadata["factor_observation_schema_fixture_context_visible"] is True
    assert metadata["factor_observation_schema_fixture_created"] is True
    assert metadata["factor_observation_schema_fixture_report_only"] is True
    assert metadata["factor_observation_schema_fixture_diagnostic_only"] is True
    assert metadata["factor_observation_schema_fixture_buy_review_allowed"] is False
    assert metadata["factor_observation_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_factor_observation_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_factor_observation_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_observation_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Factor Observation Schema Fixture is report-only context, "
            "not real factor observations, active factor library, signal_score, model training input, "
            "stock_profile validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_factor_observation_schema_fixture_status == "PASS"
    assert result.factor_observation_schema_fixture_context_visible is True
    assert result.factor_observation_schema_fixture_production_factor_observations_created is False
    assert result.factor_observation_schema_fixture_active_factor_library_created is False
    assert result.factor_observation_schema_fixture_signal_score_implemented is False
    assert result.factor_observation_schema_fixture_model_training_performed is False
    assert result.factor_observation_schema_fixture_buy_review_allowed is False
    assert result.factor_observation_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_factor_observation_schema_fixture_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    fixture = build_factor_observation_schema_fixture(
        output_dir=root / "manual_diagnostics" / "factor_observation_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "factor_observation_schema_fixture_workflow_implemented: True" in output.out
    assert "factor_observation_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_factor_observation_schema_fixture_id: {fixture.factor_observation_schema_fixture_id}"
        in output.out
    )
    assert "latest_factor_observation_schema_fixture_status: PASS" in output.out
    assert "latest_factor_observation_schema_fixture_health_status: PASS" in output.out
    assert "latest_factor_observation_schema_fixture_workflow_stage: FACTOR_OBSERVATION_SCHEMA_FIXTURE_CREATED" in output.out
    assert "factor_observation_schema_fixture_context_visible: True" in output.out
    assert "factor_observation_schema_fixture_created: True" in output.out
    assert "factor_observation_schema_fixture_observation_count: 10" in output.out
    assert "factor_observation_schema_fixture_validation_issue_count: 0" in output.out
    assert "factor_observation_schema_fixture_report_only: True" in output.out
    assert "factor_observation_schema_fixture_diagnostic_only: True" in output.out
    assert "factor_observation_schema_fixture_rows_created: True" in output.out
    assert "factor_observation_schema_fixture_production_factor_observations_created: False" in output.out
    assert "factor_observation_schema_fixture_real_factor_observations_created: False" in output.out
    assert "factor_observation_schema_fixture_production_factor_registry_created: False" in output.out
    assert "factor_observation_schema_fixture_active_factor_library_created: False" in output.out
    assert "factor_observation_schema_fixture_production_event_ingestion_created: False" in output.out
    assert "factor_observation_schema_fixture_active_event_library_created: False" in output.out
    assert "factor_observation_schema_fixture_production_company_exposure_mapping_created: False" in output.out
    assert "factor_observation_schema_fixture_real_raw_document_ingestion_created: False" in output.out
    assert "factor_observation_schema_fixture_replay_evidence_bundle_created: False" in output.out
    assert "factor_observation_schema_fixture_replay_decisions_created: False" in output.out
    assert "factor_observation_schema_fixture_forward_labels_created: False" in output.out
    assert "factor_observation_schema_fixture_normalization_created: False" in output.out
    assert "factor_observation_schema_fixture_winsorization_created: False" in output.out
    assert "factor_observation_schema_fixture_direction_adjusted_values_created: False" in output.out
    assert "factor_observation_schema_fixture_signal_score_implemented: False" in output.out
    assert "factor_observation_schema_fixture_signal_score_input_authorized: False" in output.out
    assert "factor_observation_schema_fixture_model_training_performed: False" in output.out
    assert "factor_observation_schema_fixture_active_weights_created: False" in output.out
    assert "factor_observation_schema_fixture_active_thresholds_created: False" in output.out
    assert "factor_observation_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "factor_observation_schema_fixture_paper_validation_created: False" in output.out
    assert "factor_observation_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "factor_observation_schema_fixture_buy_review_allowed: False" in output.out
    assert "factor_observation_schema_fixture_strategy_performance_validated: False" in output.out
    assert "factor_observation_schema_fixture_trading_allowed: False" in output.out
    assert "factor_observation_schema_fixture_current_candidates_run: False" in output.out
    assert "factor_observation_schema_fixture_snapshot_built: False" in output.out
    assert "factor_observation_schema_fixture_signal_semantics_changed: False" in output.out
    assert "factor_observation_schema_fixture_active_stock_profile_created: False" in output.out
    assert "factor_observation_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_replay_evidence_bundle_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_evidence_bundle_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_evidence_bundle_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_EVIDENCE_BUNDLE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.replay_evidence_bundle_schema_fixture_workflow_implemented is True
    assert result.replay_evidence_bundle_schema_fixture_views_implemented is True
    assert (
        result.latest_replay_evidence_bundle_schema_fixture_id
        == fixture.replay_evidence_bundle_schema_fixture_id
    )
    assert result.latest_replay_evidence_bundle_schema_fixture_status == "PASS"
    assert result.latest_replay_evidence_bundle_schema_fixture_health_status == "PASS"
    assert result.latest_replay_evidence_bundle_schema_fixture_workflow_stage == (
        "REPLAY_EVIDENCE_BUNDLE_SCHEMA_FIXTURE_CREATED"
    )
    assert result.replay_evidence_bundle_schema_fixture_artifact_path.endswith(
        fixture.replay_evidence_bundle_schema_fixture_id
    )
    assert result.replay_evidence_bundle_schema_fixture_context_visible is True
    assert result.replay_evidence_bundle_schema_fixture_created is True
    assert result.replay_evidence_bundle_schema_fixture_bundle_count == 10
    assert result.replay_evidence_bundle_schema_fixture_validation_issue_count == 0
    assert result.replay_evidence_bundle_schema_fixture_report_only is True
    assert result.replay_evidence_bundle_schema_fixture_diagnostic_only is True
    assert result.replay_evidence_bundle_schema_fixture_rows_created is True
    assert result.replay_evidence_bundle_schema_fixture_real_replay_evidence_bundles_created is False
    assert result.replay_evidence_bundle_schema_fixture_replay_decisions_created is False
    assert result.replay_evidence_bundle_schema_fixture_forward_labels_created is False
    assert result.replay_evidence_bundle_schema_fixture_future_labels_joined is False
    assert result.replay_evidence_bundle_schema_fixture_production_factor_observations_created is False
    assert result.replay_evidence_bundle_schema_fixture_real_factor_observations_created is False
    assert result.replay_evidence_bundle_schema_fixture_production_factor_registry_created is False
    assert result.replay_evidence_bundle_schema_fixture_active_factor_library_created is False
    assert result.replay_evidence_bundle_schema_fixture_production_event_ingestion_created is False
    assert result.replay_evidence_bundle_schema_fixture_active_event_library_created is False
    assert result.replay_evidence_bundle_schema_fixture_production_company_exposure_mapping_created is False
    assert result.replay_evidence_bundle_schema_fixture_real_raw_document_ingestion_created is False
    assert result.replay_evidence_bundle_schema_fixture_normalization_created is False
    assert result.replay_evidence_bundle_schema_fixture_winsorization_created is False
    assert result.replay_evidence_bundle_schema_fixture_direction_adjusted_values_created is False
    assert result.replay_evidence_bundle_schema_fixture_signal_score_implemented is False
    assert result.replay_evidence_bundle_schema_fixture_signal_score_input_authorized is False
    assert result.replay_evidence_bundle_schema_fixture_model_training_performed is False
    assert result.replay_evidence_bundle_schema_fixture_active_weights_created is False
    assert result.replay_evidence_bundle_schema_fixture_active_thresholds_created is False
    assert result.replay_evidence_bundle_schema_fixture_stock_profile_validation_created is False
    assert result.replay_evidence_bundle_schema_fixture_paper_validation_created is False
    assert result.replay_evidence_bundle_schema_fixture_real_buy_review_eligible is False
    assert result.replay_evidence_bundle_schema_fixture_buy_review_allowed is False
    assert result.replay_evidence_bundle_schema_fixture_strategy_performance_validated is False
    assert result.replay_evidence_bundle_schema_fixture_trading_allowed is False
    assert result.replay_evidence_bundle_schema_fixture_live_trading_enabled is False
    assert result.replay_evidence_bundle_schema_fixture_broker_api_called is False
    assert result.replay_evidence_bundle_schema_fixture_external_api_called is False
    assert result.replay_evidence_bundle_schema_fixture_llm_api_called is False
    assert result.replay_evidence_bundle_schema_fixture_data_raw_written is False
    assert result.replay_evidence_bundle_schema_fixture_data_processed_written is False
    assert result.replay_evidence_bundle_schema_fixture_data_cache_written is False
    assert result.replay_evidence_bundle_schema_fixture_current_candidates_run is False
    assert result.replay_evidence_bundle_schema_fixture_snapshot_built is False
    assert result.replay_evidence_bundle_schema_fixture_signal_semantics_changed is False
    assert result.replay_evidence_bundle_schema_fixture_active_stock_profile_created is False
    assert result.replay_evidence_bundle_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "REPLAY_EVIDENCE_BUNDLE_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_replay_evidence_bundle_schema_fixture_id"] == (
        fixture.replay_evidence_bundle_schema_fixture_id
    )
    assert summary.loc[0, "replay_evidence_bundle_schema_fixture_bundle_count"] == "10"
    assert summary.loc[0, "replay_evidence_bundle_schema_fixture_context_visible"] == "True"
    assert metadata["latest_replay_evidence_bundle_schema_fixture_status"] == "PASS"
    assert metadata["replay_evidence_bundle_schema_fixture_context_visible"] is True
    assert metadata["replay_evidence_bundle_schema_fixture_created"] is True
    assert metadata["replay_evidence_bundle_schema_fixture_report_only"] is True
    assert metadata["replay_evidence_bundle_schema_fixture_diagnostic_only"] is True
    assert metadata["replay_evidence_bundle_schema_fixture_buy_review_allowed"] is False
    assert metadata["replay_evidence_bundle_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_replay_evidence_bundle_schema_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_replay_evidence_bundle_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_evidence_bundle_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Replay Evidence Bundle Schema Fixture is report-only context, "
            "not real replay evidence bundles, replay decisions, forward labels, signal_score inputs, "
            "model training input, stock_profile validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_replay_evidence_bundle_schema_fixture_status == "PASS"
    assert result.replay_evidence_bundle_schema_fixture_context_visible is True
    assert result.replay_evidence_bundle_schema_fixture_real_replay_evidence_bundles_created is False
    assert result.replay_evidence_bundle_schema_fixture_replay_decisions_created is False
    assert result.replay_evidence_bundle_schema_fixture_forward_labels_created is False
    assert result.replay_evidence_bundle_schema_fixture_signal_score_input_authorized is False
    assert result.replay_evidence_bundle_schema_fixture_model_training_performed is False
    assert result.replay_evidence_bundle_schema_fixture_buy_review_allowed is False
    assert result.replay_evidence_bundle_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_replay_evidence_bundle_schema_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_evidence_bundle_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_evidence_bundle_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "replay_evidence_bundle_schema_fixture_workflow_implemented: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_replay_evidence_bundle_schema_fixture_id: {fixture.replay_evidence_bundle_schema_fixture_id}"
        in output.out
    )
    assert "latest_replay_evidence_bundle_schema_fixture_status: PASS" in output.out
    assert "latest_replay_evidence_bundle_schema_fixture_health_status: PASS" in output.out
    assert (
        "latest_replay_evidence_bundle_schema_fixture_workflow_stage: "
        "REPLAY_EVIDENCE_BUNDLE_SCHEMA_FIXTURE_CREATED"
    ) in output.out
    assert "replay_evidence_bundle_schema_fixture_context_visible: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_created: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_bundle_count: 10" in output.out
    assert "replay_evidence_bundle_schema_fixture_validation_issue_count: 0" in output.out
    assert "replay_evidence_bundle_schema_fixture_report_only: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_diagnostic_only: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_rows_created: True" in output.out
    assert "replay_evidence_bundle_schema_fixture_real_replay_evidence_bundles_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_replay_decisions_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_forward_labels_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_future_labels_joined: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_production_factor_observations_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_real_factor_observations_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_production_factor_registry_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_active_factor_library_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_production_event_ingestion_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_active_event_library_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_production_company_exposure_mapping_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_real_raw_document_ingestion_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_signal_score_implemented: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_signal_score_input_authorized: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_model_training_performed: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_active_weights_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_active_thresholds_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_paper_validation_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_buy_review_allowed: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_strategy_performance_validated: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_trading_allowed: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_current_candidates_run: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_snapshot_built: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_signal_semantics_changed: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_active_stock_profile_created: False" in output.out
    assert "replay_evidence_bundle_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_replay_decision_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_decision_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_decision_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "REPLAY_DECISION_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.replay_decision_schema_fixture_workflow_implemented is True
    assert result.replay_decision_schema_fixture_views_implemented is True
    assert result.latest_replay_decision_schema_fixture_id == fixture.replay_decision_schema_fixture_id
    assert result.latest_replay_decision_schema_fixture_status == "PASS"
    assert result.latest_replay_decision_schema_fixture_health_status == "PASS"
    assert result.latest_replay_decision_schema_fixture_workflow_stage == "REPLAY_DECISION_SCHEMA_FIXTURE_CREATED"
    assert result.replay_decision_schema_fixture_artifact_path.endswith(fixture.replay_decision_schema_fixture_id)
    assert result.replay_decision_schema_fixture_context_visible is True
    assert result.replay_decision_schema_fixture_created is True
    assert result.replay_decision_schema_fixture_decision_count == 10
    assert result.replay_decision_schema_fixture_validation_issue_count == 0
    assert result.replay_decision_schema_fixture_report_only is True
    assert result.replay_decision_schema_fixture_diagnostic_only is True
    assert result.replay_decision_schema_fixture_rows_created is True
    assert result.replay_decision_schema_fixture_real_replay_decisions_created is False
    assert result.replay_decision_schema_fixture_replay_evidence_bundle_schema_fixture_used is True
    assert result.replay_decision_schema_fixture_real_replay_evidence_bundle_used is False
    assert result.replay_decision_schema_fixture_forward_labels_created is False
    assert result.replay_decision_schema_fixture_future_labels_joined is False
    assert result.replay_decision_schema_fixture_signal_score_implemented is False
    assert result.replay_decision_schema_fixture_signal_score_input_authorized is False
    assert result.replay_decision_schema_fixture_model_training_performed is False
    assert result.replay_decision_schema_fixture_active_weights_created is False
    assert result.replay_decision_schema_fixture_active_thresholds_created is False
    assert result.replay_decision_schema_fixture_stock_profile_validation_created is False
    assert result.replay_decision_schema_fixture_paper_validation_created is False
    assert result.replay_decision_schema_fixture_real_buy_review_eligible is False
    assert result.replay_decision_schema_fixture_buy_review_allowed is False
    assert result.replay_decision_schema_fixture_strategy_performance_validated is False
    assert result.replay_decision_schema_fixture_trading_allowed is False
    assert result.replay_decision_schema_fixture_live_trading_enabled is False
    assert result.replay_decision_schema_fixture_broker_api_called is False
    assert result.replay_decision_schema_fixture_external_api_called is False
    assert result.replay_decision_schema_fixture_llm_api_called is False
    assert result.replay_decision_schema_fixture_data_raw_written is False
    assert result.replay_decision_schema_fixture_data_processed_written is False
    assert result.replay_decision_schema_fixture_data_cache_written is False
    assert result.replay_decision_schema_fixture_current_candidates_run is False
    assert result.replay_decision_schema_fixture_snapshot_built is False
    assert result.replay_decision_schema_fixture_signal_semantics_changed is False
    assert result.replay_decision_schema_fixture_active_stock_profile_created is False
    assert result.replay_decision_schema_fixture_operational_global_approved_for_paper_granted is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "REPLAY_DECISION_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_replay_decision_schema_fixture_id"] == fixture.replay_decision_schema_fixture_id
    assert summary.loc[0, "replay_decision_schema_fixture_decision_count"] == "10"
    assert summary.loc[0, "replay_decision_schema_fixture_context_visible"] == "True"
    assert metadata["latest_replay_decision_schema_fixture_status"] == "PASS"
    assert metadata["replay_decision_schema_fixture_context_visible"] is True
    assert metadata["replay_decision_schema_fixture_created"] is True
    assert metadata["replay_decision_schema_fixture_report_only"] is True
    assert metadata["replay_decision_schema_fixture_diagnostic_only"] is True
    assert metadata["replay_decision_schema_fixture_replay_evidence_bundle_schema_fixture_used"] is True
    assert metadata["replay_decision_schema_fixture_real_replay_evidence_bundle_used"] is False
    assert metadata["replay_decision_schema_fixture_buy_review_allowed"] is False
    assert metadata["replay_decision_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_replay_decision_schema_fixture(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    build_replay_decision_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_decision_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Replay Decision Schema Fixture is report-only context, "
            "not real replay decisions, real replay evidence bundle consumption, forward labels, "
            "signal_score inputs, model training input, stock_profile validation, buy-review, "
            "performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_replay_decision_schema_fixture_status == "PASS"
    assert result.replay_decision_schema_fixture_context_visible is True
    assert result.replay_decision_schema_fixture_real_replay_decisions_created is False
    assert result.replay_decision_schema_fixture_real_replay_evidence_bundle_used is False
    assert result.replay_decision_schema_fixture_forward_labels_created is False
    assert result.replay_decision_schema_fixture_future_labels_joined is False
    assert result.replay_decision_schema_fixture_signal_score_input_authorized is False
    assert result.replay_decision_schema_fixture_model_training_performed is False
    assert result.replay_decision_schema_fixture_stock_profile_validation_created is False
    assert result.replay_decision_schema_fixture_paper_validation_created is False
    assert result.replay_decision_schema_fixture_buy_review_allowed is False
    assert result.replay_decision_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_replay_decision_schema_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_decision_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_decision_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "replay_decision_schema_fixture_workflow_implemented: True" in output.out
    assert "replay_decision_schema_fixture_views_implemented: True" in output.out
    assert f"latest_replay_decision_schema_fixture_id: {fixture.replay_decision_schema_fixture_id}" in output.out
    assert "latest_replay_decision_schema_fixture_status: PASS" in output.out
    assert "latest_replay_decision_schema_fixture_health_status: PASS" in output.out
    assert "latest_replay_decision_schema_fixture_workflow_stage: REPLAY_DECISION_SCHEMA_FIXTURE_CREATED" in output.out
    assert "replay_decision_schema_fixture_context_visible: True" in output.out
    assert "replay_decision_schema_fixture_created: True" in output.out
    assert "replay_decision_schema_fixture_decision_count: 10" in output.out
    assert "replay_decision_schema_fixture_validation_issue_count: 0" in output.out
    assert "replay_decision_schema_fixture_report_only: True" in output.out
    assert "replay_decision_schema_fixture_diagnostic_only: True" in output.out
    assert "replay_decision_schema_fixture_rows_created: True" in output.out
    assert "replay_decision_schema_fixture_real_replay_decisions_created: False" in output.out
    assert "replay_decision_schema_fixture_replay_evidence_bundle_schema_fixture_used: True" in output.out
    assert "replay_decision_schema_fixture_real_replay_evidence_bundle_used: False" in output.out
    assert "replay_decision_schema_fixture_forward_labels_created: False" in output.out
    assert "replay_decision_schema_fixture_future_labels_joined: False" in output.out
    assert "replay_decision_schema_fixture_signal_score_implemented: False" in output.out
    assert "replay_decision_schema_fixture_signal_score_input_authorized: False" in output.out
    assert "replay_decision_schema_fixture_model_training_performed: False" in output.out
    assert "replay_decision_schema_fixture_active_weights_created: False" in output.out
    assert "replay_decision_schema_fixture_active_thresholds_created: False" in output.out
    assert "replay_decision_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "replay_decision_schema_fixture_paper_validation_created: False" in output.out
    assert "replay_decision_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "replay_decision_schema_fixture_buy_review_allowed: False" in output.out
    assert "replay_decision_schema_fixture_strategy_performance_validated: False" in output.out
    assert "replay_decision_schema_fixture_trading_allowed: False" in output.out
    assert "replay_decision_schema_fixture_current_candidates_run: False" in output.out
    assert "replay_decision_schema_fixture_snapshot_built: False" in output.out
    assert "replay_decision_schema_fixture_signal_semantics_changed: False" in output.out
    assert "replay_decision_schema_fixture_active_stock_profile_created: False" in output.out
    assert "replay_decision_schema_fixture_operational_global_approved_for_paper_granted: False" in output.out


def test_research_status_includes_forward_return_label_schema_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_forward_return_label_schema_fixture(
        output_dir=root / "manual_diagnostics" / "forward_return_label_schema_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "FORWARD_RETURN_LABEL_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.forward_return_label_schema_fixture_workflow_implemented is True
    assert result.forward_return_label_schema_fixture_views_implemented is True
    assert result.latest_forward_return_label_schema_fixture_id == fixture.forward_return_label_schema_fixture_id
    assert result.latest_forward_return_label_schema_fixture_status == "PASS"
    assert result.latest_forward_return_label_schema_fixture_health_status == "PASS"
    assert result.latest_forward_return_label_schema_fixture_workflow_stage == (
        "FORWARD_RETURN_LABEL_SCHEMA_FIXTURE_CREATED"
    )
    assert result.forward_return_label_schema_fixture_artifact_path.endswith(
        fixture.forward_return_label_schema_fixture_id
    )
    assert result.forward_return_label_schema_fixture_context_visible is True
    assert result.forward_return_label_schema_fixture_created is True
    assert result.forward_return_label_schema_fixture_label_count == 10
    assert result.forward_return_label_schema_fixture_validation_issue_count == 0
    assert result.forward_return_label_schema_fixture_report_only is True
    assert result.forward_return_label_schema_fixture_diagnostic_only is True
    assert result.forward_return_label_schema_fixture_rows_created is True
    assert result.forward_return_label_schema_fixture_real_forward_labels_created is False
    assert result.forward_return_label_schema_fixture_future_labels_joined is False
    assert result.forward_return_label_schema_fixture_future_label_joined_to_decision_input is False
    assert result.forward_return_label_schema_fixture_signal_score_implemented is False
    assert result.forward_return_label_schema_fixture_signal_score_input_authorized is False
    assert result.forward_return_label_schema_fixture_model_training_performed is False
    assert result.forward_return_label_schema_fixture_model_training_input_authorized is False
    assert result.forward_return_label_schema_fixture_active_weights_created is False
    assert result.forward_return_label_schema_fixture_active_thresholds_created is False
    assert result.forward_return_label_schema_fixture_stock_profile_validation_created is False
    assert result.forward_return_label_schema_fixture_paper_validation_created is False
    assert result.forward_return_label_schema_fixture_real_buy_review_eligible is False
    assert result.forward_return_label_schema_fixture_buy_review_allowed is False
    assert result.forward_return_label_schema_fixture_strategy_performance_validated is False
    assert result.forward_return_label_schema_fixture_current_candidates_run is False
    assert result.forward_return_label_schema_fixture_snapshot_built is False
    assert result.forward_return_label_schema_fixture_signal_semantics_changed is False
    assert result.forward_return_label_schema_fixture_trading_allowed is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "FORWARD_RETURN_LABEL_SCHEMA_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_forward_return_label_schema_fixture_id"] == (
        fixture.forward_return_label_schema_fixture_id
    )
    assert summary.loc[0, "forward_return_label_schema_fixture_label_count"] == "10"
    assert summary.loc[0, "forward_return_label_schema_fixture_context_visible"] == "True"
    assert metadata["research_status_final_workflow_stage"] == result.workflow_stage
    assert metadata["latest_forward_return_label_schema_fixture_status"] == "PASS"
    assert metadata["forward_return_label_schema_fixture_context_visible"] is True
    assert metadata["forward_return_label_schema_fixture_created"] is True
    assert metadata["forward_return_label_schema_fixture_report_only"] is True
    assert metadata["forward_return_label_schema_fixture_diagnostic_only"] is True
    assert metadata["forward_return_label_schema_fixture_future_label_joined_to_decision_input"] is False
    assert metadata["forward_return_label_schema_fixture_buy_review_allowed"] is False
    assert metadata["forward_return_label_schema_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_forward_return_label_schema_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_forward_return_label_schema_fixture(
        output_dir=root / "manual_diagnostics" / "forward_return_label_schema_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Forward Return Label Schema Fixture is report-only context, "
            "not real forward labels, future-label joins, signal_score inputs, model training input, "
            "stock_profile validation, paper validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_forward_return_label_schema_fixture_status == "PASS"
    assert result.forward_return_label_schema_fixture_context_visible is True
    assert result.forward_return_label_schema_fixture_real_forward_labels_created is False
    assert result.forward_return_label_schema_fixture_future_labels_joined is False
    assert result.forward_return_label_schema_fixture_future_label_joined_to_decision_input is False
    assert result.forward_return_label_schema_fixture_signal_score_input_authorized is False
    assert result.forward_return_label_schema_fixture_model_training_performed is False
    assert result.forward_return_label_schema_fixture_model_training_input_authorized is False
    assert result.forward_return_label_schema_fixture_stock_profile_validation_created is False
    assert result.forward_return_label_schema_fixture_paper_validation_created is False
    assert result.forward_return_label_schema_fixture_buy_review_allowed is False
    assert result.forward_return_label_schema_fixture_trading_allowed is False


def test_cli_research_status_prints_forward_return_label_schema_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_forward_return_label_schema_fixture(
        output_dir=root / "manual_diagnostics" / "forward_return_label_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "forward_return_label_schema_fixture_workflow_implemented: True" in output.out
    assert "forward_return_label_schema_fixture_views_implemented: True" in output.out
    assert (
        f"latest_forward_return_label_schema_fixture_id: {fixture.forward_return_label_schema_fixture_id}"
        in output.out
    )
    assert "latest_forward_return_label_schema_fixture_status: PASS" in output.out
    assert "latest_forward_return_label_schema_fixture_health_status: PASS" in output.out
    assert (
        "latest_forward_return_label_schema_fixture_workflow_stage: "
        "FORWARD_RETURN_LABEL_SCHEMA_FIXTURE_CREATED"
    ) in output.out
    assert "forward_return_label_schema_fixture_context_visible: True" in output.out
    assert "forward_return_label_schema_fixture_created: True" in output.out
    assert "forward_return_label_schema_fixture_label_count: 10" in output.out
    assert "forward_return_label_schema_fixture_validation_issue_count: 0" in output.out
    assert "forward_return_label_schema_fixture_report_only: True" in output.out
    assert "forward_return_label_schema_fixture_diagnostic_only: True" in output.out
    assert "forward_return_label_schema_fixture_rows_created: True" in output.out
    assert "forward_return_label_schema_fixture_real_forward_labels_created: False" in output.out
    assert "forward_return_label_schema_fixture_future_labels_joined: False" in output.out
    assert "forward_return_label_schema_fixture_future_label_joined_to_decision_input: False" in output.out
    assert "forward_return_label_schema_fixture_signal_score_implemented: False" in output.out
    assert "forward_return_label_schema_fixture_signal_score_input_authorized: False" in output.out
    assert "forward_return_label_schema_fixture_model_training_performed: False" in output.out
    assert "forward_return_label_schema_fixture_model_training_input_authorized: False" in output.out
    assert "forward_return_label_schema_fixture_stock_profile_validation_created: False" in output.out
    assert "forward_return_label_schema_fixture_paper_validation_created: False" in output.out
    assert "forward_return_label_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "forward_return_label_schema_fixture_buy_review_allowed: False" in output.out
    assert "forward_return_label_schema_fixture_strategy_performance_validated: False" in output.out
    assert "forward_return_label_schema_fixture_trading_allowed: False" in output.out
    assert "forward_return_label_schema_fixture_current_candidates_run: False" in output.out
    assert "forward_return_label_schema_fixture_snapshot_built: False" in output.out
    assert "forward_return_label_schema_fixture_signal_semantics_changed: False" in output.out


def test_research_status_includes_tiny_pit_admissibility_validator_contract_fixture_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_tiny_pit_admissibility_validator_contract_fixture(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_contract_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.tiny_pit_admissibility_validator_contract_fixture_context_visible is True
    assert result.latest_tiny_pit_admissibility_validator_contract_fixture_id == (
        fixture.tiny_pit_admissibility_validator_contract_fixture_id
    )
    assert result.latest_tiny_pit_admissibility_validator_contract_fixture_status == "PASS"
    assert result.latest_tiny_pit_admissibility_validator_contract_fixture_health_status == "PASS"
    assert result.latest_tiny_pit_admissibility_validator_contract_fixture_workflow_stage == (
        "TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE_CREATED"
    )
    assert result.tiny_pit_admissibility_validator_contract_fixture_created is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_case_count == 12
    assert result.tiny_pit_admissibility_validator_contract_fixture_package_section_count == 12
    assert result.tiny_pit_admissibility_validator_contract_fixture_gate_group_count == 24
    assert result.tiny_pit_admissibility_validator_contract_fixture_timing_rule_count == 10
    assert result.tiny_pit_admissibility_validator_contract_fixture_validation_issue_count == 0
    assert result.tiny_pit_admissibility_validator_contract_fixture_report_only is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_diagnostic_only is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_cases_created is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_package_sections_created is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_gate_groups_created is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_timing_rules_created is True
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_reviewed_csv_package_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_active_reviewed_input_candidate_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_pit_admissibility_validator_implemented is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_replay_input_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_replay_evidence_bundle_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_replay_decision_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_replay_decision_frozen is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_forward_labels_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_future_labels_joined is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_future_labels_joined_to_decision_inputs is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_future_labels_joined_to_training_dataset is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_training_dataset_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_metric_computation_performed is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_signal_score_implemented is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_signal_score_input_authorized is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_model_training_performed is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_stock_profile_validation_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_paper_validation_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_buy_review_eligible is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_buy_review_allowed is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_strategy_performance_validated is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_trading_allowed is False
    assert result.workflow_stage != "TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE_CREATED"
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_tiny_pit_admissibility_validator_contract_fixture_id"] == (
        fixture.tiny_pit_admissibility_validator_contract_fixture_id
    )
    assert summary.loc[0, "tiny_pit_admissibility_validator_contract_fixture_case_count"] == "12"
    assert summary.loc[0, "tiny_pit_admissibility_validator_contract_fixture_context_visible"] == "True"
    assert metadata["research_status_final_workflow_stage"] == result.workflow_stage
    assert metadata["latest_tiny_pit_admissibility_validator_contract_fixture_status"] == "PASS"
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_context_visible"] is True
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_created"] is True
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_report_only"] is True
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_diagnostic_only"] is True
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_buy_review_allowed"] is False
    assert metadata["tiny_pit_admissibility_validator_contract_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_tiny_pit_contract_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_tiny_pit_admissibility_validator_contract_fixture(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_contract_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Tiny PIT contract fixture is report-only context, "
            "not a real PIT validator, reviewed package, replay input, labels, training, stock_profile, "
            "paper validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.tiny_pit_admissibility_validator_contract_fixture_context_visible is True
    assert result.latest_tiny_pit_admissibility_validator_contract_fixture_status == "PASS"
    assert result.tiny_pit_admissibility_validator_contract_fixture_pit_admissibility_validator_implemented is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_reviewed_csv_package_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_replay_input_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_real_forward_labels_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_training_dataset_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_signal_score_input_authorized is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_model_training_performed is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_stock_profile_validation_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_paper_validation_created is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_buy_review_allowed is False
    assert result.tiny_pit_admissibility_validator_contract_fixture_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_contract_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_tiny_pit_admissibility_validator_contract_fixture(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_contract_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "tiny_pit_admissibility_validator_contract_fixture_context_visible: True" in output.out
    assert (
        f"latest_tiny_pit_admissibility_validator_contract_fixture_id: "
        f"{fixture.tiny_pit_admissibility_validator_contract_fixture_id}"
    ) in output.out
    assert "latest_tiny_pit_admissibility_validator_contract_fixture_status: PASS" in output.out
    assert "latest_tiny_pit_admissibility_validator_contract_fixture_health_status: PASS" in output.out
    assert (
        "latest_tiny_pit_admissibility_validator_contract_fixture_workflow_stage: "
        "TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE_CREATED"
    ) in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_created: True" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_case_count: 12" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_package_section_count: 12" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_gate_group_count: 24" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_timing_rule_count: 10" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_validation_issue_count: 0" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_cases_created: True" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_package_sections_created: True" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_gate_groups_created: True" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_timing_rules_created: True" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_real_reviewed_csv_package_created: False" in output.out
    assert (
        "tiny_pit_admissibility_validator_contract_fixture_pit_admissibility_validator_implemented: False"
        in output.out
    )
    assert "tiny_pit_admissibility_validator_contract_fixture_real_replay_input_created: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_real_forward_labels_created: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_future_labels_joined: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_training_dataset_created: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_metric_computation_performed: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_signal_score_input_authorized: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_model_training_performed: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_stock_profile_validation_created: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_paper_validation_created: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_buy_review_allowed: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_strategy_performance_validated: False" in output.out
    assert "tiny_pit_admissibility_validator_contract_fixture_trading_allowed: False" in output.out
    assert "\nworkflow_stage: TINY_PIT_ADMISSIBILITY_VALIDATOR_CONTRACT_FIXTURE_CREATED\n" not in output.out


def test_tiny_pit_checkpoint_docs_exist_and_do_not_overclaim() -> None:
    docs = [
        Path("docs/tiny_pit_admissibility_validator_contract_fixture.md"),
        Path("docs/release_checkpoint_v1.68.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_68_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "Tiny PIT Admissibility Validator Contract Fixture" in text
        assert "not a real PIT validator" in text or "No real PIT admissibility validator" in text
        assert "not trading" in text or "No trading" in text
        assert "Post-Checkpoint Governance Audit Report-Only v0.1" in text
    assert not Path("docs/project_sources").exists()


def test_research_status_reports_failed_replay_substrate_fixture_as_context_blocker(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_substrate_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_substrate_schema_fixture_v0_1"
    )
    fixture.artifact_paths["report"].unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_STATUS"
    ].iloc[0]

    assert result.replay_substrate_schema_fixture_status == "FAIL"
    assert result.replay_substrate_schema_fixture_stage == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_FAILED"
    assert result.workflow_stage == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_FAILED"
    assert row["blocking_error_count"] >= 1
    assert result.replay_substrate_schema_fixture_active_replay_input is False
    assert result.replay_substrate_schema_fixture_forward_labels_exist is False
    assert result.replay_substrate_schema_fixture_weights_trained is False
    assert result.replay_substrate_schema_fixture_active_stock_profile_exists is False
    assert result.replay_substrate_schema_fixture_real_buy_review_eligible is False


def test_cli_research_status_prints_replay_substrate_schema_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_replay_substrate_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_substrate_schema_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_replay_substrate_schema_fixture_id: {fixture.fixture_id}" in output.out
    assert "replay_substrate_schema_fixture_status: PASS" in output.out
    assert "replay_substrate_schema_fixture_stage: REPLAY_SUBSTRATE_SCHEMA_FIXTURE_READY" in output.out
    assert "replay_substrate_schema_fixture_overclaim_guard_pass_count: 8" in output.out
    assert "replay_substrate_schema_fixture_overclaim_guard_total_count: 8" in output.out
    assert "replay_substrate_schema_fixture_active_replay_input: False" in output.out
    assert "replay_substrate_schema_fixture_forward_labels_exist: False" in output.out
    assert "replay_substrate_schema_fixture_weights_trained: False" in output.out
    assert "replay_substrate_schema_fixture_active_stock_profile_exists: False" in output.out
    assert "replay_substrate_schema_fixture_real_buy_review_eligible: False" in output.out
    assert "replay_substrate_schema_fixture_report_only: True" in output.out
    assert "replay_substrate_schema_fixture_diagnostic_only: True" in output.out
    assert "replay_substrate_schema_fixture_no_live_trading: True" in output.out
    assert "replay_substrate_schema_fixture_no_broker_api: True" in output.out
    assert "replay_substrate_schema_fixture_no_order_placement: True" in output.out


def test_research_status_includes_input_gate_validator_fixture_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    fixture = build_historical_replay_input_gate_validator_fixture(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "INPUT_GATE_VALIDATOR_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.latest_input_gate_validator_fixture_run_id == fixture.fixture_run_id
    assert result.input_gate_validator_fixture_status == "PASS"
    assert result.input_gate_validator_fixture_stage == "INPUT_GATE_VALIDATOR_FIXTURE_READY"
    assert result.input_gate_validator_fixture_health_status == "PASS"
    assert result.input_gate_validator_fixture_case_count == 68
    assert result.input_gate_validator_fixture_blocked_case_count == 67
    assert result.input_gate_validator_fixture_pass_candidate_case_count == 1
    assert result.input_gate_validator_fixture_active_ready_case_count == 0
    assert result.input_gate_validator_fixture_validation_issue_count == 0
    assert result.input_gate_validator_fixture_overclaim_guard_pass_count == 14
    assert result.input_gate_validator_fixture_overclaim_guard_total_count == 14
    assert result.input_gate_validator_fixture_active_replay_input is False
    assert result.input_gate_validator_fixture_forward_labels_exist is False
    assert result.input_gate_validator_fixture_weights_trained is False
    assert result.input_gate_validator_fixture_active_stock_profile_exists is False
    assert result.input_gate_validator_fixture_real_buy_review_eligible is False
    assert result.input_gate_validator_fixture_validator_implemented is False
    assert result.input_gate_validator_fixture_report_only is True
    assert result.input_gate_validator_fixture_diagnostic_only is True
    assert result.input_gate_validator_fixture_no_live_trading is True
    assert result.input_gate_validator_fixture_no_broker_api is True
    assert result.input_gate_validator_fixture_no_order_placement is True
    assert result.input_gate_validator_fixture_no_message_sent is True
    assert result.input_gate_validator_fixture_llm_api_called is False
    assert result.input_gate_validator_fixture_external_api_called is False
    assert result.input_gate_validator_fixture_cache_mutated is False
    assert result.input_gate_validator_fixture_current_candidates_run is False
    assert result.input_gate_validator_fixture_snapshot_built is False
    assert result.input_gate_validator_fixture_signal_semantics_changed is False
    assert result.workflow_stage == "INPUT_GATE_VALIDATOR_FIXTURE_READY"
    assert result.workflow_stage not in {
        "ACTIVE_REPLAY_INPUT_READY",
        "REAL_REPLAY_READY",
        "FORWARD_LABEL_READY",
        "TRAINING_READY",
        "STOCK_PROFILE_READY",
        "REAL_BUY_REVIEW_READY",
    }
    assert row["status"] == "PASS"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_input_gate_validator_fixture_run_id"] == fixture.fixture_run_id
    assert summary.loc[0, "input_gate_validator_fixture_case_count"] == "68"
    assert summary.loc[0, "input_gate_validator_fixture_active_replay_input"] == "False"
    assert metadata["input_gate_validator_fixture_status"] == "PASS"
    assert metadata["input_gate_validator_fixture_overclaim_guard_pass_count"] == 14
    assert metadata["input_gate_validator_fixture_active_replay_input"] is False
    assert metadata["input_gate_validator_fixture_forward_labels_exist"] is False
    assert metadata["input_gate_validator_fixture_weights_trained"] is False
    assert metadata["input_gate_validator_fixture_active_stock_profile_exists"] is False
    assert metadata["input_gate_validator_fixture_real_buy_review_eligible"] is False
    assert metadata["input_gate_validator_fixture_validator_implemented"] is False
    assert metadata["input_gate_validator_fixture_report_only"] is True
    assert metadata["input_gate_validator_fixture_diagnostic_only"] is True
    assert metadata["input_gate_validator_fixture_no_live_trading"] is True
    assert metadata["input_gate_validator_fixture_no_broker_api"] is True
    assert metadata["input_gate_validator_fixture_no_order_placement"] is True
    assert metadata["input_gate_validator_fixture_no_message_sent"] is True
    assert metadata["input_gate_validator_fixture_llm_api_called"] is False
    assert metadata["input_gate_validator_fixture_external_api_called"] is False
    assert metadata["input_gate_validator_fixture_cache_mutated"] is False
    assert metadata["input_gate_validator_fixture_current_candidates_run"] is False
    assert metadata["input_gate_validator_fixture_snapshot_built"] is False
    assert metadata["input_gate_validator_fixture_signal_semantics_changed"] is False


def test_research_status_preserves_paper_priority_over_input_gate_validator_fixture(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    build_historical_replay_input_gate_validator_fixture(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "INPUT_GATE_VALIDATOR_FIXTURE_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.input_gate_validator_fixture_status == "PASS"
    assert result.input_gate_validator_fixture_active_replay_input is False
    assert result.input_gate_validator_fixture_validator_implemented is False
    assert row["status"] == "PASS"


def test_research_status_preserves_replay_substrate_priority_over_input_gate_validator_fixture(
    tmp_path: Path,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    build_replay_substrate_schema_fixture(
        output_dir=root / "manual_diagnostics" / "replay_substrate_schema_fixture_v0_1"
    )
    build_historical_replay_input_gate_validator_fixture(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "REPLAY_SUBSTRATE_SCHEMA_FIXTURE_READY"
    assert result.input_gate_validator_fixture_status == "PASS"
    assert result.input_gate_validator_fixture_active_replay_input is False
    assert result.replay_substrate_schema_fixture_status == "PASS"


def test_cli_research_status_prints_input_gate_validator_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    fixture = build_historical_replay_input_gate_validator_fixture(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_fixture_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_input_gate_validator_fixture_run_id: {fixture.fixture_run_id}" in output.out
    assert "input_gate_validator_fixture_status: PASS" in output.out
    assert "input_gate_validator_fixture_stage: INPUT_GATE_VALIDATOR_FIXTURE_READY" in output.out
    assert "input_gate_validator_fixture_case_count: 68" in output.out
    assert "input_gate_validator_fixture_blocked_case_count: 67" in output.out
    assert "input_gate_validator_fixture_pass_candidate_case_count: 1" in output.out
    assert "input_gate_validator_fixture_active_ready_case_count: 0" in output.out
    assert "input_gate_validator_fixture_validation_issue_count: 0" in output.out
    assert "input_gate_validator_fixture_overclaim_guard_pass_count: 14" in output.out
    assert "input_gate_validator_fixture_overclaim_guard_total_count: 14" in output.out
    assert "input_gate_validator_fixture_active_replay_input: False" in output.out
    assert "input_gate_validator_fixture_forward_labels_exist: False" in output.out
    assert "input_gate_validator_fixture_weights_trained: False" in output.out
    assert "input_gate_validator_fixture_active_stock_profile_exists: False" in output.out
    assert "input_gate_validator_fixture_real_buy_review_eligible: False" in output.out
    assert "input_gate_validator_fixture_validator_implemented: False" in output.out
    assert "input_gate_validator_fixture_report_only: True" in output.out
    assert "input_gate_validator_fixture_diagnostic_only: True" in output.out
    assert "input_gate_validator_fixture_no_live_trading: True" in output.out
    assert "input_gate_validator_fixture_no_broker_api: True" in output.out
    assert "input_gate_validator_fixture_no_order_placement: True" in output.out
    assert "input_gate_validator_fixture_no_message_sent: True" in output.out
    assert "input_gate_validator_fixture_llm_api_called: False" in output.out
    assert "input_gate_validator_fixture_external_api_called: False" in output.out
    assert "input_gate_validator_fixture_cache_mutated: False" in output.out
    assert "input_gate_validator_fixture_current_candidates_run: False" in output.out
    assert "input_gate_validator_fixture_snapshot_built: False" in output.out
    assert "input_gate_validator_fixture_signal_semantics_changed: False" in output.out


def test_research_status_includes_historical_replay_input_gate_validator_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    validator = run_historical_replay_input_gate_validator(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_REPLAY_INPUT_GATE_VALIDATOR_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.latest_historical_replay_input_gate_validator_run_id == validator.validator_run_id
    assert result.historical_replay_input_gate_validator_status == "NO_INPUT"
    assert result.historical_replay_input_gate_validator_stage == "INPUT_GATE_VALIDATOR_NO_INPUT"
    assert result.historical_replay_input_gate_validator_health_status == "PASS"
    assert result.historical_replay_input_gate_validator_pass_candidate is False
    assert result.historical_replay_input_gate_validator_active_replay_input_ready is False
    assert result.historical_replay_input_gate_validator_active_replay_input is False
    assert result.historical_replay_input_gate_validator_forward_labels_exist is False
    assert result.historical_replay_input_gate_validator_weights_trained is False
    assert result.historical_replay_input_gate_validator_active_stock_profile_exists is False
    assert result.historical_replay_input_gate_validator_real_buy_review_eligible is False
    assert result.historical_replay_input_gate_validator_report_only is True
    assert result.historical_replay_input_gate_validator_diagnostic_only is True
    assert result.historical_replay_input_gate_validator_no_live_trading is True
    assert result.historical_replay_input_gate_validator_no_broker_api is True
    assert result.historical_replay_input_gate_validator_no_order_placement is True
    assert result.historical_replay_input_gate_validator_no_message_sent is True
    assert result.historical_replay_input_gate_validator_llm_api_called is False
    assert result.historical_replay_input_gate_validator_external_api_called is False
    assert result.historical_replay_input_gate_validator_cache_mutated is False
    assert result.historical_replay_input_gate_validator_current_candidates_run is False
    assert result.historical_replay_input_gate_validator_snapshot_built is False
    assert result.historical_replay_input_gate_validator_signal_semantics_changed is False
    assert result.workflow_stage == "INPUT_GATE_VALIDATOR_NO_INPUT"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "NO_INPUT"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_historical_replay_input_gate_validator_run_id"] == validator.validator_run_id
    assert summary.loc[0, "historical_replay_input_gate_validator_active_replay_input"] == "False"
    assert metadata["historical_replay_input_gate_validator_status"] == "NO_INPUT"
    assert metadata["historical_replay_input_gate_validator_active_replay_input_ready"] is False
    assert metadata["historical_replay_input_gate_validator_active_replay_input"] is False
    assert metadata["historical_replay_input_gate_validator_forward_labels_exist"] is False
    assert metadata["historical_replay_input_gate_validator_weights_trained"] is False
    assert metadata["historical_replay_input_gate_validator_active_stock_profile_exists"] is False
    assert metadata["historical_replay_input_gate_validator_real_buy_review_eligible"] is False


def test_research_status_preserves_paper_priority_over_historical_replay_input_gate_validator(
    tmp_path: Path,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    run_historical_replay_input_gate_validator(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_REPLAY_INPUT_GATE_VALIDATOR_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.historical_replay_input_gate_validator_status == "NO_INPUT"
    assert result.historical_replay_input_gate_validator_active_replay_input_ready is False
    assert result.historical_replay_input_gate_validator_active_replay_input is False
    assert row["status"] == "NO_INPUT"


def test_cli_research_status_prints_historical_replay_input_gate_validator_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    validator = run_historical_replay_input_gate_validator(
        output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_v0_1"
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert (
        f"latest_historical_replay_input_gate_validator_run_id: {validator.validator_run_id}"
        in output.out
    )
    assert "historical_replay_input_gate_validator_status: NO_INPUT" in output.out
    assert "historical_replay_input_gate_validator_stage: INPUT_GATE_VALIDATOR_NO_INPUT" in output.out
    assert "historical_replay_input_gate_validator_health_status: PASS" in output.out
    assert "historical_replay_input_gate_validator_pass_candidate: False" in output.out
    assert "historical_replay_input_gate_validator_active_replay_input_ready: False" in output.out
    assert "historical_replay_input_gate_validator_active_replay_input: False" in output.out
    assert "historical_replay_input_gate_validator_forward_labels_exist: False" in output.out
    assert "historical_replay_input_gate_validator_weights_trained: False" in output.out
    assert "historical_replay_input_gate_validator_active_stock_profile_exists: False" in output.out
    assert "historical_replay_input_gate_validator_real_buy_review_eligible: False" in output.out
    assert "historical_replay_input_gate_validator_report_only: True" in output.out
    assert "historical_replay_input_gate_validator_diagnostic_only: True" in output.out
    assert "historical_replay_input_gate_validator_no_live_trading: True" in output.out
    assert "historical_replay_input_gate_validator_no_broker_api: True" in output.out
    assert "historical_replay_input_gate_validator_no_order_placement: True" in output.out
    assert "historical_replay_input_gate_validator_no_message_sent: True" in output.out
    assert "historical_replay_input_gate_validator_llm_api_called: False" in output.out
    assert "historical_replay_input_gate_validator_external_api_called: False" in output.out
    assert "historical_replay_input_gate_validator_cache_mutated: False" in output.out
    assert "historical_replay_input_gate_validator_current_candidates_run: False" in output.out
    assert "historical_replay_input_gate_validator_snapshot_built: False" in output.out
    assert "historical_replay_input_gate_validator_signal_semantics_changed: False" in output.out


def test_research_status_includes_minimal_replay_input_package_fixture_smoke_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    smoke = run_minimal_replay_input_package_fixture_smoke(
        MinimalReplayInputPackageFixtureSmokeSettings(
            output_dir=root / "manual_diagnostics" / "minimal_replay_input_package_fixture_smoke_v0_1",
            validator_output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_v0_1",
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MINIMAL_REPLAY_INPUT_PACKAGE_FIXTURE_SMOKE_STATUS"
    ].iloc[0]

    assert result.minimal_replay_input_package_fixture_smoke_implemented is True
    assert result.minimal_replay_input_package_fixture_smoke_views_implemented is True
    assert result.latest_smoke_run_id == smoke.smoke_run_id
    assert result.latest_smoke_status == "REPLAY_INPUT_GATE_PASS_CANDIDATE"
    assert result.latest_smoke_health_status == "PASS"
    assert result.latest_smoke_workflow_stage == "SMOKE_PASS_CANDIDATE_READY"
    assert result.latest_smoke_validator_run_id == smoke.validator_run_id
    assert result.latest_smoke_validator_status == "REPLAY_INPUT_GATE_PASS_CANDIDATE"
    assert result.smoke_pass_candidate is True
    assert result.smoke_active_replay_input_ready is False
    assert result.smoke_active_replay_input is False
    assert result.smoke_forward_labels_exist is False
    assert result.smoke_weights_trained is False
    assert result.smoke_active_stock_profile_exists is False
    assert result.smoke_real_buy_review_eligible is False
    assert result.smoke_approval_applied is False
    assert result.smoke_order_placed is False
    assert result.smoke_llm_api_called is False
    assert result.smoke_external_api_called is False
    assert result.smoke_cache_mutated is False
    assert result.smoke_current_candidates_run is False
    assert result.smoke_snapshot_built is False
    assert result.smoke_signal_semantics_changed is False
    assert result.smoke_report_only is True
    assert result.smoke_diagnostic_only is True
    assert result.smoke_no_live_trading is True
    assert result.smoke_no_broker_api is True
    assert result.smoke_no_order_placement is True
    assert result.smoke_no_message_sent is True
    assert result.workflow_stage == "SMOKE_PASS_CANDIDATE_READY"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "REPLAY_INPUT_GATE_PASS_CANDIDATE"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_smoke_run_id"] == smoke.smoke_run_id
    assert summary.loc[0, "smoke_active_replay_input_ready"] == "False"
    assert metadata["latest_smoke_workflow_stage"] == "SMOKE_PASS_CANDIDATE_READY"
    assert metadata["smoke_active_replay_input_ready"] is False
    assert metadata["smoke_active_replay_input"] is False


def test_research_status_preserves_paper_priority_over_minimal_replay_smoke(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    run_minimal_replay_input_package_fixture_smoke(
        MinimalReplayInputPackageFixtureSmokeSettings(
            output_dir=root / "manual_diagnostics" / "minimal_replay_input_package_fixture_smoke_v0_1",
            validator_output_dir=root / "manual_diagnostics" / "historical_replay_input_gate_validator_v0_1",
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_smoke_workflow_stage == "SMOKE_PASS_CANDIDATE_READY"
    assert result.smoke_pass_candidate is True
    assert result.smoke_active_replay_input_ready is False
    assert result.smoke_active_replay_input is False


def test_research_status_includes_active_replay_input_promotion_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    promotion = _active_replay_input_promotion_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_PROMOTION_STATUS"
    ].iloc[0]

    assert result.active_replay_input_promotion_implemented is True
    assert result.active_replay_input_promotion_views_implemented is True
    assert result.latest_active_replay_input_promotion_run_id == promotion.promotion_run_id
    assert result.latest_active_replay_input_promotion_status == "PROMOTION_READY_FOR_HUMAN_REVIEW"
    assert result.latest_active_replay_input_promotion_health_status == "PASS"
    assert result.latest_active_replay_input_promotion_workflow_stage == "PROMOTION_READY_FOR_HUMAN_REVIEW"
    assert result.active_replay_input_promotion_ready_for_human_review is True
    assert result.active_replay_input_promotion_active_replay_input_ready is False
    assert result.active_replay_input_promotion_active_replay_input is False
    assert result.active_replay_input_promotion_active_ready_emitted is False
    assert result.active_replay_input_promotion_forward_labels_exist is False
    assert result.active_replay_input_promotion_weights_trained is False
    assert result.active_replay_input_promotion_active_stock_profile_exists is False
    assert result.active_replay_input_promotion_real_buy_review_eligible is False
    assert result.active_replay_input_promotion_approval_applied is False
    assert result.active_replay_input_promotion_order_placed is False
    assert result.active_replay_input_promotion_llm_api_called is False
    assert result.active_replay_input_promotion_external_api_called is False
    assert result.active_replay_input_promotion_cache_mutated is False
    assert result.active_replay_input_promotion_current_candidates_run is False
    assert result.active_replay_input_promotion_snapshot_built is False
    assert result.active_replay_input_promotion_signal_semantics_changed is False
    assert result.active_replay_input_promotion_report_only is True
    assert result.active_replay_input_promotion_diagnostic_only is True
    assert result.active_replay_input_promotion_no_live_trading is True
    assert result.active_replay_input_promotion_no_broker_api is True
    assert result.active_replay_input_promotion_no_order_placement is True
    assert result.active_replay_input_promotion_no_message_sent is True
    assert result.workflow_stage == "PROMOTION_READY_FOR_HUMAN_REVIEW"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "PROMOTION_READY_FOR_HUMAN_REVIEW"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_promotion_run_id"] == promotion.promotion_run_id
    assert summary.loc[0, "active_replay_input_promotion_active_replay_input_ready"] == "False"
    assert metadata["latest_active_replay_input_promotion_workflow_stage"] == "PROMOTION_READY_FOR_HUMAN_REVIEW"
    assert metadata["active_replay_input_promotion_active_replay_input_ready"] is False
    assert metadata["active_replay_input_promotion_active_replay_input"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_promotion(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_promotion_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_promotion_workflow_stage == "PROMOTION_READY_FOR_HUMAN_REVIEW"
    assert result.active_replay_input_promotion_ready_for_human_review is True
    assert result.active_replay_input_promotion_active_replay_input_ready is False
    assert result.active_replay_input_promotion_active_replay_input is False


def test_cli_research_status_prints_active_replay_input_promotion_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    promotion = _active_replay_input_promotion_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_promotion_run_id: {promotion.promotion_run_id}" in output.out
    assert "latest_active_replay_input_promotion_status: PROMOTION_READY_FOR_HUMAN_REVIEW" in output.out
    assert "latest_active_replay_input_promotion_health_status: PASS" in output.out
    assert "latest_active_replay_input_promotion_workflow_stage: PROMOTION_READY_FOR_HUMAN_REVIEW" in output.out
    assert "active_replay_input_promotion_ready_for_human_review: True" in output.out
    assert "active_replay_input_promotion_active_replay_input_ready: False" in output.out
    assert "active_replay_input_promotion_active_replay_input: False" in output.out
    assert "active_replay_input_promotion_active_ready_emitted: False" in output.out
    assert "active_replay_input_promotion_forward_labels_exist: False" in output.out
    assert "active_replay_input_promotion_weights_trained: False" in output.out
    assert "active_replay_input_promotion_active_stock_profile_exists: False" in output.out
    assert "active_replay_input_promotion_real_buy_review_eligible: False" in output.out
    assert "active_replay_input_promotion_approval_applied: False" in output.out
    assert "active_replay_input_promotion_order_placed: False" in output.out
    assert "active_replay_input_promotion_llm_api_called: False" in output.out
    assert "active_replay_input_promotion_external_api_called: False" in output.out
    assert "active_replay_input_promotion_cache_mutated: False" in output.out
    assert "active_replay_input_promotion_current_candidates_run: False" in output.out
    assert "active_replay_input_promotion_snapshot_built: False" in output.out
    assert "active_replay_input_promotion_signal_semantics_changed: False" in output.out
    assert "active_replay_input_promotion_report_only: True" in output.out
    assert "active_replay_input_promotion_diagnostic_only: True" in output.out
    assert "active_replay_input_promotion_no_live_trading: True" in output.out
    assert "active_replay_input_promotion_no_broker_api: True" in output.out
    assert "active_replay_input_promotion_no_order_placement: True" in output.out
    assert "active_replay_input_promotion_no_message_sent: True" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_active_replay_input_acceptance_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    acceptance = _active_replay_input_acceptance_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_ACCEPTANCE_STATUS"
    ].iloc[0]

    assert result.active_replay_input_acceptance_implemented is True
    assert result.active_replay_input_acceptance_views_implemented is True
    assert result.latest_active_replay_input_acceptance_run_id == acceptance.acceptance_run_id
    assert result.latest_active_replay_input_acceptance_status == "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
    assert result.latest_active_replay_input_acceptance_health_status == "PASS"
    assert result.latest_active_replay_input_acceptance_workflow_stage == "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
    assert result.active_replay_input_acceptance_ready_for_active_ready_review is True
    assert result.active_replay_input_acceptance_active_replay_input_ready is False
    assert result.active_replay_input_acceptance_active_replay_input is False
    assert result.active_replay_input_acceptance_active_ready_emitted is False
    assert result.active_replay_input_acceptance_forward_labels_exist is False
    assert result.active_replay_input_acceptance_weights_trained is False
    assert result.active_replay_input_acceptance_active_stock_profile_exists is False
    assert result.active_replay_input_acceptance_real_buy_review_eligible is False
    assert result.active_replay_input_acceptance_approval_applied is False
    assert result.active_replay_input_acceptance_order_placed is False
    assert result.active_replay_input_acceptance_llm_api_called is False
    assert result.active_replay_input_acceptance_external_api_called is False
    assert result.active_replay_input_acceptance_cache_mutated is False
    assert result.active_replay_input_acceptance_current_candidates_run is False
    assert result.active_replay_input_acceptance_snapshot_built is False
    assert result.active_replay_input_acceptance_signal_semantics_changed is False
    assert result.active_replay_input_acceptance_report_only is True
    assert result.active_replay_input_acceptance_diagnostic_only is True
    assert result.active_replay_input_acceptance_no_live_trading is True
    assert result.active_replay_input_acceptance_no_broker_api is True
    assert result.active_replay_input_acceptance_no_order_placement is True
    assert result.active_replay_input_acceptance_no_message_sent is True
    assert result.workflow_stage == "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_acceptance_run_id"] == acceptance.acceptance_run_id
    assert summary.loc[0, "active_replay_input_acceptance_active_replay_input_ready"] == "False"
    assert metadata["latest_active_replay_input_acceptance_workflow_stage"] == (
        "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
    )
    assert metadata["active_replay_input_acceptance_active_replay_input_ready"] is False
    assert metadata["active_replay_input_acceptance_active_replay_input"] is False
    assert metadata["active_replay_input_acceptance_active_ready_emitted"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_acceptance(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_acceptance_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_acceptance_workflow_stage == (
        "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
    )
    assert result.active_replay_input_acceptance_ready_for_active_ready_review is True
    assert result.active_replay_input_acceptance_active_replay_input_ready is False
    assert result.active_replay_input_acceptance_active_replay_input is False
    assert result.active_replay_input_acceptance_active_ready_emitted is False


def test_cli_research_status_prints_active_replay_input_acceptance_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    acceptance = _active_replay_input_acceptance_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_acceptance_run_id: {acceptance.acceptance_run_id}" in output.out
    assert "latest_active_replay_input_acceptance_status: ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW" in output.out
    assert "latest_active_replay_input_acceptance_health_status: PASS" in output.out
    assert (
        "latest_active_replay_input_acceptance_workflow_stage: ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW"
        in output.out
    )
    assert "active_replay_input_acceptance_ready_for_active_ready_review: True" in output.out
    assert "active_replay_input_acceptance_active_replay_input_ready: False" in output.out
    assert "active_replay_input_acceptance_active_replay_input: False" in output.out
    assert "active_replay_input_acceptance_active_ready_emitted: False" in output.out
    assert "active_replay_input_acceptance_forward_labels_exist: False" in output.out
    assert "active_replay_input_acceptance_weights_trained: False" in output.out
    assert "active_replay_input_acceptance_active_stock_profile_exists: False" in output.out
    assert "active_replay_input_acceptance_real_buy_review_eligible: False" in output.out
    assert "active_replay_input_acceptance_approval_applied: False" in output.out
    assert "active_replay_input_acceptance_order_placed: False" in output.out
    assert "active_replay_input_acceptance_llm_api_called: False" in output.out
    assert "active_replay_input_acceptance_external_api_called: False" in output.out
    assert "active_replay_input_acceptance_cache_mutated: False" in output.out
    assert "active_replay_input_acceptance_current_candidates_run: False" in output.out
    assert "active_replay_input_acceptance_snapshot_built: False" in output.out
    assert "active_replay_input_acceptance_signal_semantics_changed: False" in output.out
    assert "active_replay_input_acceptance_report_only: True" in output.out
    assert "active_replay_input_acceptance_diagnostic_only: True" in output.out
    assert "active_replay_input_acceptance_no_live_trading: True" in output.out
    assert "active_replay_input_acceptance_no_broker_api: True" in output.out
    assert "active_replay_input_acceptance_no_order_placement: True" in output.out
    assert "active_replay_input_acceptance_no_message_sent: True" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_active_replay_input_acceptance_checkpoint_docs_and_project_source_policy() -> None:
    doc = Path("docs/active_replay_input_acceptance.md")
    checkpoint = Path("docs/release_checkpoint_v1.32.0.md")
    source_note = Path("SOURCE_UPDATE_NOTES_v1_32_0.md")

    for path in [doc, checkpoint, source_note]:
        assert path.exists()
        text = path.read_text(encoding="utf-8")
        assert "active-replay-input-acceptance" in text
        assert "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW" in text
        assert "ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not create active replay input" in text
        assert "does not run replay" in text
        assert "does not compute forward labels" in text
        assert "does not train weights" in text
        assert "does not create active stock profiles" in text
        assert "does not create real buy-review eligibility" in text

    assert "PAPER_WORKFLOW_READY" in checkpoint.read_text(encoding="utf-8")
    source_text = source_note.read_text(encoding="utf-8")
    assert "docs/project_sources" in source_text
    assert "intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_active_replay_input_active_ready_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    active_ready = _active_replay_input_active_ready_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_ACTIVE_READY_STATUS"
    ].iloc[0]

    assert result.active_replay_input_active_ready_implemented is True
    assert result.active_replay_input_active_ready_views_implemented is True
    assert result.latest_active_replay_input_active_ready_run_id == active_ready.active_ready_run_id
    assert result.latest_active_replay_input_active_ready_status == "ACTIVE_READY_READY_FOR_FINAL_REVIEW"
    assert result.latest_active_replay_input_active_ready_health_status == "PASS"
    assert result.latest_active_replay_input_active_ready_workflow_stage == "ACTIVE_READY_READY_FOR_FINAL_REVIEW"
    assert result.ready_for_final_review is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False
    assert result.forward_labels_exist is False
    assert result.weights_trained is False
    assert result.active_stock_profile_exists is False
    assert result.real_buy_review_eligible is False
    assert result.approval_applied is False
    assert result.order_placed is False
    assert result.message_sent is False
    assert result.llm_api_called is False
    assert result.external_api_called is False
    assert result.cache_mutated is False
    assert result.data_raw_written is False
    assert result.data_processed_written is False
    assert result.data_cache_written is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.report_only is True
    assert result.diagnostic_only is True
    assert result.no_live_trading is True
    assert result.no_broker_api is True
    assert result.no_order_placement is True
    assert result.no_message_sent is True
    assert result.workflow_stage == "ACTIVE_READY_READY_FOR_FINAL_REVIEW"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "ACTIVE_READY_READY_FOR_FINAL_REVIEW"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_active_ready_run_id"] == active_ready.active_ready_run_id
    assert summary.loc[0, "active_replay_input_ready"] == "False"
    assert metadata["latest_active_replay_input_active_ready_workflow_stage"] == (
        "ACTIVE_READY_READY_FOR_FINAL_REVIEW"
    )
    assert metadata["active_replay_input_ready"] is False
    assert metadata["active_replay_input"] is False
    assert metadata["active_ready_emitted"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_active_ready(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_active_ready_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_active_ready_workflow_stage == "ACTIVE_READY_READY_FOR_FINAL_REVIEW"
    assert result.ready_for_final_review is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False


def test_cli_research_status_prints_active_replay_input_active_ready_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    active_ready = _active_replay_input_active_ready_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_active_ready_run_id: {active_ready.active_ready_run_id}" in output.out
    assert "latest_active_replay_input_active_ready_status: ACTIVE_READY_READY_FOR_FINAL_REVIEW" in output.out
    assert "latest_active_replay_input_active_ready_health_status: PASS" in output.out
    assert (
        "latest_active_replay_input_active_ready_workflow_stage: ACTIVE_READY_READY_FOR_FINAL_REVIEW"
        in output.out
    )
    assert "ready_for_final_review: True" in output.out
    assert "active_replay_input_ready: False" in output.out
    assert "active_replay_input: False" in output.out
    assert "active_ready_emitted: False" in output.out
    assert "forward_labels_exist: False" in output.out
    assert "weights_trained: False" in output.out
    assert "active_stock_profile_exists: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "approval_applied: False" in output.out
    assert "order_placed: False" in output.out
    assert "message_sent: False" in output.out
    assert "llm_api_called: False" in output.out
    assert "external_api_called: False" in output.out
    assert "cache_mutated: False" in output.out
    assert "data_raw_written: False" in output.out
    assert "data_processed_written: False" in output.out
    assert "data_cache_written: False" in output.out
    assert "current_candidates_run: False" in output.out
    assert "snapshot_built: False" in output.out
    assert "signal_semantics_changed: False" in output.out


def test_active_replay_input_active_ready_checkpoint_docs_and_project_source_policy() -> None:
    doc = Path("docs/active_replay_input_active_ready.md")
    checkpoint = Path("docs/release_checkpoint_v1.33.0.md")
    source_note = Path("SOURCE_UPDATE_NOTES_v1_33_0.md")

    for path in [doc, checkpoint, source_note]:
        assert path.exists()
        text = path.read_text(encoding="utf-8")
        assert "active-replay-input-active-ready" in text
        assert "ACTIVE_READY_READY_FOR_FINAL_REVIEW" in text
        assert "ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not create active replay input" in text
        assert "does not run replay" in text
        assert "does not compute forward labels" in text
        assert "does not train weights" in text
        assert "does not create active stock profiles" in text
        assert "does not create real buy-review eligibility" in text
        assert "does not authorize trading" in text

    assert "PAPER_WORKFLOW_READY" in checkpoint.read_text(encoding="utf-8")
    source_text = source_note.read_text(encoding="utf-8")
    assert "docs/project_sources" in source_text
    assert "intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_active_replay_input_final_review_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    final_review = _active_replay_input_final_review_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_FINAL_REVIEW_STATUS"
    ].iloc[0]

    assert result.active_replay_input_final_review_implemented is True
    assert result.active_replay_input_final_review_views_implemented is True
    assert result.latest_active_replay_input_final_review_run_id == final_review.final_review_run_id
    assert result.latest_active_replay_input_final_review_status == "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
    assert result.latest_active_replay_input_final_review_health_status == "PASS"
    assert result.latest_active_replay_input_final_review_workflow_stage == (
        "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
    )
    assert result.ready_for_emission_review is True
    assert result.active_replay_input_final_review_active_replay_input_ready is False
    assert result.active_replay_input_final_review_active_replay_input is False
    assert result.active_replay_input_final_review_active_ready_emitted is False
    assert result.active_replay_input_final_review_forward_labels_exist is False
    assert result.active_replay_input_final_review_weights_trained is False
    assert result.active_replay_input_final_review_active_stock_profile_exists is False
    assert result.active_replay_input_final_review_real_buy_review_eligible is False
    assert result.active_replay_input_final_review_approval_applied is False
    assert result.active_replay_input_final_review_order_placed is False
    assert result.active_replay_input_final_review_message_sent is False
    assert result.active_replay_input_final_review_llm_api_called is False
    assert result.active_replay_input_final_review_external_api_called is False
    assert result.active_replay_input_final_review_cache_mutated is False
    assert result.active_replay_input_final_review_data_raw_written is False
    assert result.active_replay_input_final_review_data_processed_written is False
    assert result.active_replay_input_final_review_data_cache_written is False
    assert result.active_replay_input_final_review_current_candidates_run is False
    assert result.active_replay_input_final_review_snapshot_built is False
    assert result.active_replay_input_final_review_signal_semantics_changed is False
    assert result.active_replay_input_final_review_report_only is True
    assert result.active_replay_input_final_review_diagnostic_only is True
    assert result.active_replay_input_final_review_no_live_trading is True
    assert result.active_replay_input_final_review_no_broker_api is True
    assert result.active_replay_input_final_review_no_order_placement is True
    assert result.active_replay_input_final_review_no_message_sent is True
    assert result.workflow_stage == "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_final_review_run_id"] == (
        final_review.final_review_run_id
    )
    assert summary.loc[0, "active_replay_input_final_review_active_replay_input_ready"] == "False"
    assert metadata["latest_active_replay_input_final_review_workflow_stage"] == (
        "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
    )
    assert metadata["ready_for_emission_review"] is True
    assert metadata["active_replay_input_final_review_active_replay_input_ready"] is False
    assert metadata["active_replay_input_final_review_active_replay_input"] is False
    assert metadata["active_replay_input_final_review_active_ready_emitted"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_final_review(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_final_review_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_final_review_workflow_stage == (
        "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
    )
    assert result.ready_for_emission_review is True
    assert result.active_replay_input_final_review_active_replay_input_ready is False
    assert result.active_replay_input_final_review_active_replay_input is False
    assert result.active_replay_input_final_review_active_ready_emitted is False


def test_cli_research_status_prints_active_replay_input_final_review_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    final_review = _active_replay_input_final_review_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_final_review_run_id: {final_review.final_review_run_id}" in output.out
    assert "latest_active_replay_input_final_review_status: FINAL_REVIEW_READY_FOR_EMISSION_REVIEW" in output.out
    assert "latest_active_replay_input_final_review_health_status: PASS" in output.out
    assert (
        "latest_active_replay_input_final_review_workflow_stage: FINAL_REVIEW_READY_FOR_EMISSION_REVIEW"
        in output.out
    )
    assert "ready_for_emission_review: True" in output.out
    assert "active_replay_input_final_review_active_replay_input_ready: False" in output.out
    assert "active_replay_input_final_review_active_replay_input: False" in output.out
    assert "active_replay_input_final_review_active_ready_emitted: False" in output.out
    assert "active_replay_input_final_review_forward_labels_exist: False" in output.out
    assert "active_replay_input_final_review_weights_trained: False" in output.out
    assert "active_replay_input_final_review_active_stock_profile_exists: False" in output.out
    assert "active_replay_input_final_review_real_buy_review_eligible: False" in output.out
    assert "active_replay_input_final_review_approval_applied: False" in output.out
    assert "active_replay_input_final_review_order_placed: False" in output.out
    assert "active_replay_input_final_review_message_sent: False" in output.out
    assert "active_replay_input_final_review_llm_api_called: False" in output.out
    assert "active_replay_input_final_review_external_api_called: False" in output.out
    assert "active_replay_input_final_review_cache_mutated: False" in output.out
    assert "active_replay_input_final_review_data_raw_written: False" in output.out
    assert "active_replay_input_final_review_data_processed_written: False" in output.out
    assert "active_replay_input_final_review_data_cache_written: False" in output.out
    assert "active_replay_input_final_review_current_candidates_run: False" in output.out
    assert "active_replay_input_final_review_snapshot_built: False" in output.out
    assert "active_replay_input_final_review_signal_semantics_changed: False" in output.out


def test_active_replay_input_final_review_checkpoint_docs_and_project_source_policy() -> None:
    doc = Path("docs/active_replay_input_final_review.md")
    checkpoint = Path("docs/release_checkpoint_v1.34.0.md")
    source_note = Path("SOURCE_UPDATE_NOTES_v1_34_0.md")

    for path in [doc, checkpoint, source_note]:
        assert path.exists()
        text = path.read_text(encoding="utf-8")
        assert "active-replay-input-final-review" in text
        assert "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW" in text
        assert "ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not create active replay input" in text
        assert "does not run replay" in text
        assert "does not compute forward labels" in text
        assert "does not train weights" in text
        assert "does not create active stock profiles" in text
        assert "does not create real buy-review eligibility" in text
        assert "does not authorize trading" in text

    assert "PAPER_WORKFLOW_READY" in checkpoint.read_text(encoding="utf-8")
    source_text = source_note.read_text(encoding="utf-8")
    assert "docs/project_sources" in source_text
    assert "intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_active_replay_input_emission_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    emission = _active_replay_input_emission_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_EMISSION_STATUS"
    ].iloc[0]

    assert result.active_replay_input_emission_implemented is True
    assert result.active_replay_input_emission_views_implemented is True
    assert result.latest_active_replay_input_emission_run_id == emission.emission_run_id
    assert result.latest_active_replay_input_emission_status == "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    assert result.latest_active_replay_input_emission_health_status == "PASS"
    assert result.latest_active_replay_input_emission_workflow_stage == (
        "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    )
    assert result.ready_for_active_replay_input_ready_review is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False
    assert result.replay_execution_allowed is False
    assert result.forward_labels_allowed is False
    assert result.training_allowed is False
    assert result.stock_profile_allowed is False
    assert result.buy_review_allowed is False
    assert result.trading_allowed is False
    assert result.forward_labels_exist is False
    assert result.weights_trained is False
    assert result.active_stock_profile_exists is False
    assert result.real_buy_review_eligible is False
    assert result.approval_applied is False
    assert result.order_placed is False
    assert result.message_sent is False
    assert result.llm_api_called is False
    assert result.external_api_called is False
    assert result.cache_mutated is False
    assert result.data_raw_written is False
    assert result.data_processed_written is False
    assert result.data_cache_written is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.report_only is True
    assert result.diagnostic_only is True
    assert result.no_live_trading is True
    assert result.no_broker_api is True
    assert result.no_order_placement is True
    assert result.no_message_sent is True
    assert result.workflow_stage == "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_emission_run_id"] == emission.emission_run_id
    assert summary.loc[0, "active_replay_input_ready"] == "False"
    assert summary.loc[0, "active_replay_input"] == "False"
    assert summary.loc[0, "active_ready_emitted"] == "False"
    assert metadata["latest_active_replay_input_emission_workflow_stage"] == (
        "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    )
    assert metadata["ready_for_active_replay_input_ready_review"] is True
    assert metadata["active_replay_input_ready"] is False
    assert metadata["active_replay_input"] is False
    assert metadata["active_ready_emitted"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_emission(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_emission_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_emission_workflow_stage == (
        "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    )
    assert result.ready_for_active_replay_input_ready_review is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False


def test_cli_research_status_prints_active_replay_input_emission_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    emission = _active_replay_input_emission_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_emission_run_id: {emission.emission_run_id}" in output.out
    assert "latest_active_replay_input_emission_status: EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW" in output.out
    assert "latest_active_replay_input_emission_health_status: PASS" in output.out
    assert (
        "latest_active_replay_input_emission_workflow_stage: "
        "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW"
    ) in output.out
    assert "ready_for_active_replay_input_ready_review: True" in output.out
    assert "active_replay_input_ready: False" in output.out
    assert "active_replay_input: False" in output.out
    assert "active_ready_emitted: False" in output.out
    assert "replay_execution_allowed: False" in output.out
    assert "forward_labels_allowed: False" in output.out
    assert "training_allowed: False" in output.out
    assert "stock_profile_allowed: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "trading_allowed: False" in output.out


def test_active_replay_input_emission_checkpoint_docs_and_project_source_policy() -> None:
    doc = Path("docs/active_replay_input_emission.md")
    checkpoint = Path("docs/release_checkpoint_v1.35.0.md")
    source_note = Path("SOURCE_UPDATE_NOTES_v1_35_0.md")

    for path in [doc, checkpoint, source_note]:
        assert path.exists()
        text = path.read_text(encoding="utf-8")
        assert "active-replay-input-emission" in text
        assert "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW" in text
        assert "ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not emit ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not create active replay input" in text
        assert "does not run replay" in text
        assert "does not compute forward labels" in text
        assert "does not train weights" in text
        assert "does not create active stock profiles" in text
        assert "does not create real buy-review eligibility" in text
        assert "does not authorize trading" in text

    checkpoint_text = checkpoint.read_text(encoding="utf-8")
    assert "PAPER_WORKFLOW_READY" in checkpoint_text
    assert "96fae2783877" in checkpoint_text
    source_text = source_note.read_text(encoding="utf-8")
    assert "docs/project_sources" in source_text
    assert "intentionally absent from Git" in source_text
    assert "after tag v1.35.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_active_replay_input_ready_context(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    active_ready = _active_replay_input_ready_ready(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTIVE_REPLAY_INPUT_READY_STATUS"
    ].iloc[0]

    assert result.active_replay_input_ready_workflow_implemented is True
    assert result.active_replay_input_ready_views_implemented is True
    assert result.latest_active_replay_input_ready_run_id == active_ready.active_ready_run_id
    assert result.latest_active_replay_input_ready_status == "READY_TO_EMIT_ACTIVE_REPLAY_INPUT_READY"
    assert result.latest_active_replay_input_ready_health_status == "PASS"
    assert result.latest_active_replay_input_ready_workflow_stage == (
        "ACTIVE_REPLAY_INPUT_READY_READY_TO_EMIT"
    )
    assert result.ready_to_emit_active_replay_input_ready is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False
    assert result.replay_execution_allowed is False
    assert result.replay_decisions_exist is False
    assert result.forward_labels_allowed is False
    assert result.forward_labels_exist is False
    assert result.training_allowed is False
    assert result.weights_trained is False
    assert result.stock_profile_allowed is False
    assert result.active_stock_profile_exists is False
    assert result.buy_review_allowed is False
    assert result.real_buy_review_eligible is False
    assert result.trading_allowed is False
    assert result.order_placed is False
    assert result.message_sent is False
    assert result.llm_api_called is False
    assert result.external_api_called is False
    assert result.cache_mutated is False
    assert result.data_raw_written is False
    assert result.data_processed_written is False
    assert result.data_cache_written is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.report_only is True
    assert result.diagnostic_only is True
    assert result.no_live_trading is True
    assert result.no_broker_api is True
    assert result.no_order_placement is True
    assert result.no_message_sent is True
    assert result.workflow_stage == "ACTIVE_REPLAY_INPUT_READY_READY_TO_EMIT"
    assert result.workflow_stage != "ACTIVE_REPLAY_INPUT_READY"
    assert row["status"] == "READY_TO_EMIT_ACTIVE_REPLAY_INPUT_READY"

    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))
    assert summary.loc[0, "latest_active_replay_input_ready_run_id"] == (
        active_ready.active_ready_run_id
    )
    assert summary.loc[0, "ready_to_emit_active_replay_input_ready"] == "True"
    assert summary.loc[0, "active_replay_input_ready"] == "False"
    assert summary.loc[0, "active_replay_input"] == "False"
    assert summary.loc[0, "active_ready_emitted"] == "False"
    assert metadata["latest_active_replay_input_ready_workflow_stage"] == (
        "ACTIVE_REPLAY_INPUT_READY_READY_TO_EMIT"
    )
    assert metadata["ready_to_emit_active_replay_input_ready"] is True
    assert metadata["active_replay_input_ready"] is False
    assert metadata["active_replay_input"] is False
    assert metadata["active_ready_emitted"] is False


def test_research_status_preserves_paper_priority_over_active_replay_input_ready(tmp_path: Path) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    _active_replay_input_ready_ready(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_replay_input_ready_workflow_stage == (
        "ACTIVE_REPLAY_INPUT_READY_READY_TO_EMIT"
    )
    assert result.ready_to_emit_active_replay_input_ready is True
    assert result.active_replay_input_ready is False
    assert result.active_replay_input is False
    assert result.active_ready_emitted is False


def test_cli_research_status_prints_active_replay_input_ready_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = tmp_path / "outputs" / "reports"
    root.mkdir(parents=True, exist_ok=True)
    active_ready = _active_replay_input_ready_ready(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert f"latest_active_replay_input_ready_run_id: {active_ready.active_ready_run_id}" in output.out
    assert "latest_active_replay_input_ready_status: READY_TO_EMIT_ACTIVE_REPLAY_INPUT_READY" in output.out
    assert "latest_active_replay_input_ready_health_status: PASS" in output.out
    assert (
        "latest_active_replay_input_ready_workflow_stage: ACTIVE_REPLAY_INPUT_READY_READY_TO_EMIT"
        in output.out
    )
    assert "ready_to_emit_active_replay_input_ready: True" in output.out
    assert "active_replay_input_ready: False" in output.out
    assert "active_replay_input: False" in output.out
    assert "active_ready_emitted: False" in output.out
    assert "replay_execution_allowed: False" in output.out
    assert "replay_decisions_exist: False" in output.out
    assert "forward_labels_allowed: False" in output.out
    assert "forward_labels_exist: False" in output.out
    assert "training_allowed: False" in output.out
    assert "weights_trained: False" in output.out
    assert "stock_profile_allowed: False" in output.out
    assert "active_stock_profile_exists: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "trading_allowed: False" in output.out


def test_active_replay_input_ready_checkpoint_docs_and_project_source_policy() -> None:
    doc = Path("docs/active_replay_input_ready.md")
    checkpoint = Path("docs/release_checkpoint_v1.37.0.md")
    source_note = Path("SOURCE_UPDATE_NOTES_v1_37_0.md")

    for path in [doc, checkpoint, source_note]:
        assert path.exists()
        text = path.read_text(encoding="utf-8")
        assert "active-replay-input-ready" in text
        assert "READY_TO_EMIT_ACTIVE_REPLAY_INPUT_READY" in text
        assert "ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not emit ACTIVE_REPLAY_INPUT_READY" in text
        assert "does not create active replay input" in text
        assert "does not run replay" in text
        assert "does not create replay decisions" in text
        assert "does not compute forward labels" in text
        assert "does not train weights" in text
        assert "does not create active stock profiles" in text
        assert "does not create real buy-review eligibility" in text
        assert "does not authorize trading" in text

    checkpoint_text = checkpoint.read_text(encoding="utf-8")
    assert "PAPER_WORKFLOW_READY" in checkpoint_text
    assert "ac0d55bd52b2" in checkpoint_text
    source_text = source_note.read_text(encoding="utf-8")
    assert "docs/project_sources" in source_text
    assert "intentionally absent from Git" in source_text
    assert "after tag v1.37.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_dashboard_includes_universe_profile_policy_audit_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _universe_profile_policy_audit(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "UNIVERSE_PROFILE_POLICY_AUDIT_STATUS"
    ].iloc[0]

    assert result.latest_universe_profile_policy_audit_id
    assert result.universe_profile_policy_audit_status == "WARN"
    assert result.universe_profile_policy_audit_stage == "UNIVERSE_PROFILE_POLICY_AMBIGUOUS_MIXED_UNIVERSE"
    assert result.universe_profile_policy_audit_health_status == "WARN"
    assert result.universe_profile_policy_row_count == 2
    assert result.universe_profile_policy_stock_row_count == 1
    assert result.universe_profile_policy_etf_row_count == 1
    assert result.universe_profile_policy_ambiguous_policy_count == 2
    assert result.workflow_stage == "UNIVERSE_PROFILE_POLICY_AMBIGUOUS_MIXED_UNIVERSE"
    assert row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"


def test_dashboard_preserves_later_paper_priority_over_universe_profile_policy_audit(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _universe_profile_policy_audit(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "UNIVERSE_PROFILE_POLICY_AUDIT_STATUS"
    ].iloc[0]

    assert result.universe_profile_policy_audit_status == "WARN"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_dashboard_exports_universe_profile_policy_audit_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _universe_profile_policy_audit(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["universe_profile_policy_audit_status"] == "WARN"
    assert row["universe_profile_policy_audit_stage"] == "UNIVERSE_PROFILE_POLICY_AMBIGUOUS_MIXED_UNIVERSE"
    assert row["universe_profile_policy_stock_row_count"] == "1"
    assert row["universe_profile_policy_etf_row_count"] == "1"
    assert row["universe_profile_policy_ambiguous_policy_count"] == "2"
    assert metadata["universe_profile_policy_ambiguous_policy_count"] == 2
    assert metadata["universe_profile_policy_stock_row_count"] == 1


def test_cli_research_status_prints_universe_profile_policy_audit_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _universe_profile_policy_audit(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "universe_profile_policy_audit_status: WARN" in output.out
    assert "universe_profile_policy_audit_stage: UNIVERSE_PROFILE_POLICY_AMBIGUOUS_MIXED_UNIVERSE" in output.out
    assert "universe_profile_policy_stock_row_count: 1" in output.out
    assert "universe_profile_policy_etf_row_count: 1" in output.out
    assert "universe_profile_policy_ambiguous_policy_count: 2" in output.out


def test_dashboard_detects_current_to_paper_handoff_artifacts(tmp_path: Path) -> None:
    root = _workflow_to_paper_handoff(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.current_to_paper_status == "READY"
    assert result.workflow_stage == "PAPER_HANDOFF_READY"


def test_dashboard_detects_current_to_paper_review_artifacts(tmp_path: Path) -> None:
    root = _workflow_to_review_template(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.current_to_paper_review_status == "READY"
    assert result.workflow_stage == "REVIEW_TEMPLATE_READY"


def test_dashboard_detects_paper_workflow_status_artifacts(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.paper_workflow_status == "PASS"
    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"


def test_dashboard_infers_next_action_when_no_data_exists(tmp_path: Path) -> None:
    result = run_local_research_dashboard(root=_reports_root(tmp_path), output_dir=tmp_path / "dashboard")

    assert result.next_manual_action == "Run data-pipeline."


def test_dashboard_infers_next_action_after_data_preparation(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _data_preparation_status(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.next_manual_action == "Run current-candidates."


def test_dashboard_infers_next_action_after_current_candidates(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.next_manual_action == "Run current-candidates-index."
    assert infer_local_research_workflow_stage(result.dashboard_frame) == "CURRENT_CANDIDATES_READY"
    assert infer_local_research_next_action(result.dashboard_frame) == "Run current-candidates-index."


def test_dashboard_infers_next_action_after_paper_handoff(tmp_path: Path) -> None:
    root = _workflow_to_paper_handoff(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.next_manual_action == "Run current-to-paper-review."


def test_dashboard_infers_next_action_when_review_template_needs_editing(tmp_path: Path) -> None:
    root = _workflow_to_review_template(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.next_manual_action == "Manually edit review_updates_template.csv."


def test_dashboard_marks_needs_attention_when_health_or_reconciliation_fails(tmp_path: Path) -> None:
    root = _workflow_to_daily(_reports_root(tmp_path))
    _reconciliation(root, status="FAIL", errors=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.status == "FAIL"
    assert result.next_manual_action == "Review warnings/errors."


def test_dashboard_writes_markdown_report(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.artifact_paths["local_research_dashboard"].exists()
    content = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")
    assert "# Unified Local Research Workflow Dashboard" in content


def test_dashboard_writes_dashboard_csv_readable_by_pandas(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_dashboard_csv"])

    assert "workflow_area" in exported.columns
    assert "CURRENT_CANDIDATES" in set(exported["component"])


def test_dashboard_writes_summary_csv_readable_by_pandas(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"])

    assert exported.iloc[0]["workflow_stage"] == "CURRENT_CANDIDATES_READY"


def test_dashboard_summary_csv_exports_market_update_handoff_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_artifact(
        root,
        handoff_id="handoff-export",
        status="WARN",
        pipeline_id="pipeline-export",
        snapshot_quality_status="PASS",
        current_candidate_run_id="candidate-export",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    row = exported.iloc[0].to_dict()

    assert row["latest_market_update_handoff_id"] == "handoff-export"
    assert row["market_update_handoff_status"] == "WARN"
    assert row["market_update_handoff_stage"] == "CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST"
    assert "current-candidates artifact" in row["market_update_handoff_next_action"]
    assert row["market_update_handoff_pipeline_id"] == "pipeline-export"
    assert row["market_update_handoff_snapshot_quality_status"] == "PASS"
    assert row["market_update_handoff_current_candidate_run_id"] == "candidate-export"


def test_dashboard_writes_metadata_json(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert metadata["dashboard_id"] == result.dashboard_id
    assert metadata["live_trading_enabled"] is False
    assert metadata["broker_api_invoked"] is False
    assert metadata["network_api_calls_used_in_tests"] is False


def test_dashboard_metadata_exports_market_update_handoff_fields_and_preserves_paper_priority(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_artifact(
        root,
        handoff_id="handoff-context",
        status="WARN",
        pipeline_id="pipeline-context",
        snapshot_quality_status="PASS",
        current_candidate_run_id="candidate-context",
    )
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    component_statuses = metadata["component_statuses"]

    assert metadata["workflow_stage"] == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert metadata["next_manual_action"] == "Review completed local research workflow artifacts."
    assert metadata["latest_market_update_handoff_id"] == "handoff-context"
    assert metadata["market_update_handoff_status"] == "WARN"
    assert metadata["market_update_handoff_stage"] == "CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST"
    assert "current-candidates artifact" in metadata["market_update_handoff_next_action"]
    assert metadata["market_update_handoff_pipeline_id"] == "pipeline-context"
    assert metadata["market_update_handoff_snapshot_quality_status"] == "PASS"
    assert metadata["market_update_handoff_current_candidate_run_id"] == "candidate-context"
    assert component_statuses["latest_market_update_handoff_id"] == "handoff-context"
    assert component_statuses["market_update_handoff_pipeline_id"] == "pipeline-context"


def test_research_status_includes_actual_replay_execution_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    executed = run_actual_replay_execute(
        replace(_actual_replay_happy_settings(tmp_path), allow_actual_replay_execution=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTUAL_REPLAY_EXECUTE_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == ACTUAL_REPLAY_EXECUTED
    assert row["workflow_area"] == "ACTUAL_REPLAY_EXECUTE"
    assert result.actual_replay_execution_workflow_implemented is True
    assert result.actual_replay_execution_views_implemented is True
    assert result.latest_actual_replay_execution_run_id == executed.actual_replay_execution_run_id
    assert result.latest_actual_replay_execution_status == ACTUAL_REPLAY_EXECUTED
    assert result.latest_actual_replay_execution_health_status == "PASS"
    assert result.latest_actual_replay_execution_workflow_stage == ACTUAL_REPLAY_EXECUTED
    assert result.actual_replay_execution_artifact_path.endswith(executed.actual_replay_execution_run_id)
    assert result.actual_replay_source_active_input_creation_run_id == "293deb5f459a"
    assert result.actual_replay_source_real_replay_precheck_run_id == "0657ae658ab8"
    assert result.ready_for_actual_replay_execution is True
    assert result.actual_replay_executed is True
    assert result.actual_replay_replay_execution_started is True
    assert result.actual_replay_replay_execution_completed is True
    assert result.actual_replay_replay_decisions_created is False
    assert result.actual_replay_replay_decisions_exist is False
    assert result.actual_replay_replay_decision_artifact_path == ""
    assert result.actual_replay_forward_labels_allowed is False
    assert result.actual_replay_forward_labels_exist is False
    assert result.actual_replay_training_allowed is False
    assert result.actual_replay_weights_trained is False
    assert result.actual_replay_stock_profile_allowed is False
    assert result.actual_replay_active_stock_profile_exists is False
    assert result.actual_replay_buy_review_allowed is False
    assert result.actual_replay_real_buy_review_eligible is False
    assert result.actual_replay_trading_allowed is False
    assert result.actual_replay_order_placed is False
    assert result.actual_replay_broker_api_called is False
    assert result.actual_replay_message_sent is False
    assert result.actual_replay_llm_api_called is False
    assert result.actual_replay_external_api_called is False
    assert result.actual_replay_cache_mutated is False
    assert result.actual_replay_data_raw_written is False
    assert result.actual_replay_data_processed_written is False
    assert result.actual_replay_data_cache_written is False
    assert result.actual_replay_current_candidates_run is False
    assert result.actual_replay_snapshot_built is False
    assert result.actual_replay_signal_semantics_changed is False
    assert result.actual_replay_report_only is True
    assert result.actual_replay_diagnostic_only is True
    assert result.actual_replay_no_live_trading is True
    assert result.actual_replay_no_broker_api is True
    assert result.actual_replay_no_order_placement is True
    assert result.actual_replay_no_message_sent is True
    assert str(summary["actual_replay_replay_decisions_created"]) == "False"
    assert metadata["actual_replay_replay_decisions_created"] is False
    assert metadata["actual_replay_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_actual_replay_execution(
    tmp_path: Path,
) -> None:
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    root.mkdir(parents=True, exist_ok=True)
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        stale_warning_count=0,
        actionable_warning_count=0,
        blocking_error_count=0,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )
    run_actual_replay_execute(
        replace(
            _actual_replay_happy_settings(tmp_path),
            output_dir=root / "manual_diagnostics" / "actual_replay_execute_v0_1",
            allow_actual_replay_execution=True,
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    actual_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ACTUAL_REPLAY_EXECUTE_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert actual_row["warning_classification"] == ""
    assert result.latest_actual_replay_execution_status == ACTUAL_REPLAY_EXECUTED
    assert result.actual_replay_replay_decisions_created is False
    assert result.actual_replay_forward_labels_allowed is False
    assert result.actual_replay_training_allowed is False
    assert result.actual_replay_stock_profile_allowed is False
    assert result.actual_replay_buy_review_allowed is False
    assert result.actual_replay_trading_allowed is False


def test_cli_research_status_prints_actual_replay_execution_fields(tmp_path: Path, capsys) -> None:
    run_actual_replay_execute(
        replace(_actual_replay_happy_settings(tmp_path), allow_actual_replay_execution=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "actual_replay_execution_workflow_implemented: True" in output.out
    assert f"latest_actual_replay_execution_status: {ACTUAL_REPLAY_EXECUTED}" in output.out
    assert "actual_replay_executed: True" in output.out
    assert "actual_replay_replay_decisions_created: False" in output.out
    assert "actual_replay_forward_labels_allowed: False" in output.out
    assert "actual_replay_training_allowed: False" in output.out
    assert "actual_replay_stock_profile_allowed: False" in output.out
    assert "actual_replay_buy_review_allowed: False" in output.out
    assert "actual_replay_trading_allowed: False" in output.out


def test_research_status_includes_replay_decision_freeze_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    frozen = run_replay_decision_freeze(
        replace(_replay_decision_freeze_happy_settings(tmp_path), allow_replay_decision_freeze=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_DECISION_FREEZE_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == REPLAY_DECISION_FROZEN
    assert row["workflow_area"] == "REPLAY_DECISION_FREEZE"
    assert result.replay_decision_freeze_workflow_implemented is True
    assert result.replay_decision_freeze_views_implemented is True
    assert result.latest_replay_decision_freeze_run_id == frozen.replay_decision_freeze_run_id
    assert result.latest_replay_decision_freeze_status == REPLAY_DECISION_FROZEN
    assert result.latest_replay_decision_freeze_health_status == "PASS"
    assert result.latest_replay_decision_freeze_workflow_stage == REPLAY_DECISION_FROZEN
    assert result.replay_decision_freeze_artifact_path.endswith(frozen.replay_decision_freeze_run_id)
    assert result.source_actual_replay_execution_run_id == "ad8dfa413ded"
    assert result.replay_decision_freeze_source_active_input_creation_run_id == "293deb5f459a"
    assert result.replay_decision_freeze_source_real_replay_precheck_run_id == "0657ae658ab8"
    assert result.replay_decision_freeze_actual_replay_execution_status == ACTUAL_REPLAY_EXECUTED
    assert result.replay_decision_freeze_actual_replay_execution_health_status == "PASS"
    assert result.replay_decision_freeze_actual_replay_executed is True
    assert result.ready_for_replay_decision_freeze is True
    assert result.replay_decision_freeze_executed is True
    assert result.replay_decision_frozen is True
    assert result.replay_decision_artifacts_created is True
    assert result.replay_decision_freeze_replay_decisions_created is True
    assert result.replay_decision_freeze_replay_decisions_exist is True
    assert result.replay_decision_freeze_replay_decision_artifact_path.endswith("replay_decision_rows.csv")
    assert result.replay_decision_freeze_decision_row_count == 1
    assert result.replay_decision_freeze_decision_label_set == "WATCH"
    assert result.replay_decision_freeze_forward_labels_allowed is False
    assert result.replay_decision_freeze_forward_labels_exist is False
    assert result.replay_decision_freeze_forward_return_labels_created is False
    assert result.replay_decision_freeze_training_allowed is False
    assert result.replay_decision_freeze_weights_trained is False
    assert result.replay_decision_freeze_training_result_created is False
    assert result.replay_decision_freeze_stock_profile_allowed is False
    assert result.replay_decision_freeze_active_stock_profile_exists is False
    assert result.replay_decision_freeze_stock_profile_created is False
    assert result.replay_decision_freeze_buy_review_allowed is False
    assert result.replay_decision_freeze_real_buy_review_eligible is False
    assert result.replay_decision_freeze_approved_for_paper is False
    assert result.replay_decision_freeze_strategy_performance_validated is False
    assert result.replay_decision_freeze_trading_allowed is False
    assert result.replay_decision_freeze_order_placed is False
    assert result.replay_decision_freeze_broker_api_called is False
    assert result.replay_decision_freeze_message_sent is False
    assert result.replay_decision_freeze_llm_api_called is False
    assert result.replay_decision_freeze_external_api_called is False
    assert result.replay_decision_freeze_cache_mutated is False
    assert result.replay_decision_freeze_data_raw_written is False
    assert result.replay_decision_freeze_data_processed_written is False
    assert result.replay_decision_freeze_data_cache_written is False
    assert result.replay_decision_freeze_current_candidates_run is False
    assert result.replay_decision_freeze_snapshot_built is False
    assert result.replay_decision_freeze_signal_semantics_changed is False
    assert result.replay_decision_freeze_report_only is True
    assert result.replay_decision_freeze_diagnostic_only is True
    assert result.replay_decision_freeze_no_live_trading is True
    assert result.replay_decision_freeze_no_broker_api is True
    assert result.replay_decision_freeze_no_order_placement is True
    assert result.replay_decision_freeze_no_message_sent is True
    assert str(summary["replay_decision_freeze_forward_labels_allowed"]) == "False"
    assert metadata["replay_decision_freeze_forward_labels_allowed"] is False
    assert metadata["replay_decision_freeze_strategy_performance_validated"] is False
    assert metadata["replay_decision_freeze_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_replay_decision_freeze(
    tmp_path: Path,
) -> None:
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    root.mkdir(parents=True, exist_ok=True)
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        stale_warning_count=0,
        actionable_warning_count=0,
        blocking_error_count=0,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )
    run_replay_decision_freeze(
        replace(
            _replay_decision_freeze_happy_settings(tmp_path),
            output_dir=root / "manual_diagnostics" / "replay_decision_freeze_v0_1",
            allow_replay_decision_freeze=True,
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    freeze_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REPLAY_DECISION_FREEZE_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert freeze_row["warning_classification"] == ""
    assert result.latest_replay_decision_freeze_status == REPLAY_DECISION_FROZEN
    assert result.replay_decision_freeze_replay_decisions_created is True
    assert result.replay_decision_freeze_forward_labels_allowed is False
    assert result.replay_decision_freeze_forward_labels_exist is False
    assert result.replay_decision_freeze_forward_return_labels_created is False
    assert result.replay_decision_freeze_training_allowed is False
    assert result.replay_decision_freeze_stock_profile_allowed is False
    assert result.replay_decision_freeze_buy_review_allowed is False
    assert result.replay_decision_freeze_approved_for_paper is False
    assert result.replay_decision_freeze_strategy_performance_validated is False
    assert result.replay_decision_freeze_trading_allowed is False


def test_cli_research_status_prints_replay_decision_freeze_fields(tmp_path: Path, capsys) -> None:
    run_replay_decision_freeze(
        replace(_replay_decision_freeze_happy_settings(tmp_path), allow_replay_decision_freeze=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "replay_decision_freeze_workflow_implemented: True" in output.out
    assert f"latest_replay_decision_freeze_status: {REPLAY_DECISION_FROZEN}" in output.out
    assert "replay_decision_frozen: True" in output.out
    assert "replay_decision_freeze_replay_decisions_created: True" in output.out
    assert "replay_decision_freeze_forward_labels_allowed: False" in output.out
    assert "replay_decision_freeze_forward_return_labels_created: False" in output.out
    assert "replay_decision_freeze_training_allowed: False" in output.out
    assert "replay_decision_freeze_stock_profile_allowed: False" in output.out
    assert "replay_decision_freeze_buy_review_allowed: False" in output.out
    assert "replay_decision_freeze_approved_for_paper: False" in output.out
    assert "replay_decision_freeze_strategy_performance_validated: False" in output.out
    assert "replay_decision_freeze_trading_allowed: False" in output.out


def test_research_status_includes_forward_return_label_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    labeled = run_forward_return_label(
        replace(_forward_return_label_happy_settings(tmp_path), allow_forward_return_label=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "FORWARD_RETURN_LABEL_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == FORWARD_RETURN_LABELS_CREATED
    assert row["workflow_area"] == "FORWARD_RETURN_LABEL"
    assert result.forward_return_label_workflow_implemented is True
    assert result.forward_return_label_views_implemented is True
    assert result.latest_forward_return_label_run_id == labeled.forward_return_label_run_id
    assert result.latest_forward_return_label_status == FORWARD_RETURN_LABELS_CREATED
    assert result.latest_forward_return_label_health_status == "PASS"
    assert result.latest_forward_return_label_workflow_stage == FORWARD_RETURN_LABELS_CREATED
    assert result.forward_return_label_artifact_path.endswith(labeled.forward_return_label_run_id)
    assert result.source_replay_decision_freeze_run_id == "freeze_abc123"
    assert result.source_replay_decision_freeze_artifact_path.endswith("forward_return_label_fixture_v0_1")
    assert result.replay_decision_freeze_status == REPLAY_DECISION_FROZEN
    assert result.replay_decision_freeze_health_status == "PASS"
    assert result.forward_return_label_replay_decision_frozen is True
    assert result.forward_return_label_replay_decisions_exist is True
    assert result.ready_for_forward_return_label is True
    assert result.forward_return_label_executed is True
    assert result.forward_return_label_artifacts_created is True
    assert result.forward_return_label_forward_labels_allowed is True
    assert result.forward_return_label_forward_labels_exist is True
    assert result.forward_return_label_forward_return_labels_created is True
    assert result.forward_return_label_label_row_count == 5
    assert "forward_return_5d" in result.forward_return_label_label_name_set
    assert result.forward_return_label_symbol_count == 1
    assert result.forward_return_label_replay_decision_count == 1
    assert result.forward_return_label_training_allowed is False
    assert result.forward_return_label_weights_trained is False
    assert result.forward_return_label_training_result_created is False
    assert result.forward_return_label_stock_profile_allowed is False
    assert result.forward_return_label_active_stock_profile_exists is False
    assert result.forward_return_label_stock_profile_created is False
    assert result.forward_return_label_buy_review_allowed is False
    assert result.forward_return_label_real_buy_review_eligible is False
    assert result.forward_return_label_approved_for_paper is False
    assert result.forward_return_label_strategy_performance_validated is False
    assert result.forward_return_label_trading_allowed is False
    assert result.forward_return_label_order_placed is False
    assert result.forward_return_label_broker_api_called is False
    assert result.forward_return_label_message_sent is False
    assert result.forward_return_label_llm_api_called is False
    assert result.forward_return_label_external_api_called is False
    assert result.forward_return_label_cache_mutated is False
    assert result.forward_return_label_data_raw_written is False
    assert result.forward_return_label_data_processed_written is False
    assert result.forward_return_label_data_cache_written is False
    assert result.forward_return_label_current_candidates_run is False
    assert result.forward_return_label_snapshot_built is False
    assert result.forward_return_label_signal_semantics_changed is False
    assert result.forward_return_label_report_only is True
    assert result.forward_return_label_diagnostic_only is True
    assert result.forward_return_label_no_live_trading is True
    assert result.forward_return_label_no_broker_api is True
    assert result.forward_return_label_no_order_placement is True
    assert result.forward_return_label_no_message_sent is True
    assert str(summary["forward_return_label_label_row_count"]) == "5"
    assert metadata["forward_return_label_label_row_count"] == 5
    assert metadata["forward_return_label_training_allowed"] is False
    assert metadata["forward_return_label_stock_profile_created"] is False
    assert metadata["forward_return_label_real_buy_review_eligible"] is False
    assert metadata["forward_return_label_strategy_performance_validated"] is False
    assert metadata["forward_return_label_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_forward_return_label(tmp_path: Path) -> None:
    run_forward_return_label(
        replace(_forward_return_label_happy_settings(tmp_path), allow_forward_return_label=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority; forward labels are research context only.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_forward_return_label_status == FORWARD_RETURN_LABELS_CREATED
    assert result.forward_return_label_forward_return_labels_created is True
    assert result.forward_return_label_training_allowed is False
    assert result.forward_return_label_stock_profile_allowed is False
    assert result.forward_return_label_buy_review_allowed is False
    assert result.forward_return_label_trading_allowed is False


def test_cli_research_status_prints_forward_return_label_fields(tmp_path: Path, capsys) -> None:
    run_forward_return_label(
        replace(_forward_return_label_happy_settings(tmp_path), allow_forward_return_label=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "forward_return_label_workflow_implemented: True" in output.out
    assert f"latest_forward_return_label_status: {FORWARD_RETURN_LABELS_CREATED}" in output.out
    assert "source_replay_decision_freeze_run_id: freeze_abc123" in output.out
    assert "ready_for_forward_return_label: True" in output.out
    assert "forward_return_label_forward_return_labels_created: True" in output.out
    assert "forward_return_label_label_row_count: 5" in output.out
    assert "forward_return_label_training_allowed: False" in output.out
    assert "forward_return_label_stock_profile_allowed: False" in output.out
    assert "forward_return_label_buy_review_allowed: False" in output.out
    assert "forward_return_label_approved_for_paper: False" in output.out
    assert "forward_return_label_strategy_performance_validated: False" in output.out
    assert "forward_return_label_trading_allowed: False" in output.out


def test_research_status_includes_training_evaluation_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    training = run_training_evaluation(
        replace(_training_evaluation_happy_settings(tmp_path), allow_training_evaluation_dataset=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "TRAINING_EVALUATION_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == TRAINING_EVALUATION_DATASET_CREATED
    assert row["workflow_area"] == "TRAINING_EVALUATION"
    assert result.training_evaluation_workflow_implemented is True
    assert result.training_evaluation_views_implemented is True
    assert result.latest_training_evaluation_run_id == training.training_evaluation_run_id
    assert result.latest_training_evaluation_status == TRAINING_EVALUATION_DATASET_CREATED
    assert result.latest_training_evaluation_health_status == "PASS"
    assert result.latest_training_evaluation_workflow_stage == TRAINING_EVALUATION_DATASET_CREATED
    assert result.training_evaluation_artifact_path.endswith(training.training_evaluation_run_id)
    assert result.source_forward_return_label_run_id == "label_abc123"
    assert result.source_forward_return_label_status == FORWARD_RETURN_LABELS_CREATED
    assert result.source_forward_return_label_health_status == "PASS"
    assert result.source_replay_decision_freeze_run_id == "freeze_abc123"
    assert result.training_evaluation_forward_labels_exist is True
    assert result.training_evaluation_forward_return_labels_created is True
    assert result.training_evaluation_label_row_count == 1
    assert result.training_evaluation_replay_decision_count == 1
    assert result.training_evaluation_symbol_count == 1
    assert "forward_return_5d" in result.training_evaluation_label_name_set
    assert result.ready_for_training_evaluation_dataset is True
    assert result.training_evaluation_executed is True
    assert result.training_evaluation_dataset_artifacts_created is True
    assert result.training_evaluation_bounded_sample_rows_created is True
    assert result.training_evaluation_label_coverage_report_created is True
    assert result.training_evaluation_split_plan_created is True
    assert result.training_evaluation_feature_plan_created is True
    assert result.training_evaluation_label_plan_created is True
    assert result.training_evaluation_dataset_sample_row_count == 1
    assert result.training_evaluation_metrics_computed is False
    assert result.training_evaluation_training_allowed is False
    assert result.training_evaluation_weights_trained is False
    assert result.training_evaluation_training_result_created is False
    assert result.training_evaluation_model_version_created is False
    assert result.training_evaluation_thresholds_optimized is False
    assert result.training_evaluation_predictions_created is False
    assert result.training_evaluation_calibrated_probabilities_created is False
    assert result.training_evaluation_feature_importance_created is False
    assert result.training_evaluation_stock_profile_allowed is False
    assert result.training_evaluation_active_stock_profile_exists is False
    assert result.training_evaluation_stock_profile_created is False
    assert result.training_evaluation_buy_review_allowed is False
    assert result.training_evaluation_real_buy_review_eligible is False
    assert result.training_evaluation_approved_for_paper is False
    assert result.training_evaluation_strategy_performance_validated is False
    assert result.training_evaluation_trading_allowed is False
    assert result.training_evaluation_order_placed is False
    assert result.training_evaluation_broker_api_called is False
    assert result.training_evaluation_message_sent is False
    assert result.training_evaluation_llm_api_called is False
    assert result.training_evaluation_external_api_called is False
    assert result.training_evaluation_cache_mutated is False
    assert result.training_evaluation_data_raw_written is False
    assert result.training_evaluation_data_processed_written is False
    assert result.training_evaluation_data_cache_written is False
    assert result.training_evaluation_current_candidates_run is False
    assert result.training_evaluation_snapshot_built is False
    assert result.training_evaluation_signal_semantics_changed is False
    assert result.training_evaluation_report_only is True
    assert result.training_evaluation_diagnostic_only is True
    assert result.training_evaluation_no_live_trading is True
    assert result.training_evaluation_no_broker_api is True
    assert result.training_evaluation_no_order_placement is True
    assert result.training_evaluation_no_message_sent is True
    assert str(summary["training_evaluation_dataset_sample_row_count"]) == "1"
    assert metadata["training_evaluation_dataset_sample_row_count"] == 1
    assert metadata["training_evaluation_metrics_computed"] is False
    assert metadata["training_evaluation_training_result_created"] is False
    assert metadata["training_evaluation_model_version_created"] is False
    assert metadata["training_evaluation_stock_profile_created"] is False
    assert metadata["training_evaluation_strategy_performance_validated"] is False
    assert metadata["training_evaluation_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_training_evaluation(tmp_path: Path) -> None:
    run_training_evaluation(
        replace(_training_evaluation_happy_settings(tmp_path), allow_training_evaluation_dataset=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; training/evaluation phase 1 "
            "is report-only dataset/planning context."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_training_evaluation_status == TRAINING_EVALUATION_DATASET_CREATED
    assert result.training_evaluation_dataset_artifacts_created is True
    assert result.training_evaluation_metrics_computed is False
    assert result.training_evaluation_training_allowed is False
    assert result.training_evaluation_training_result_created is False
    assert result.training_evaluation_stock_profile_allowed is False
    assert result.training_evaluation_buy_review_allowed is False
    assert result.training_evaluation_trading_allowed is False


def test_cli_research_status_prints_training_evaluation_fields(tmp_path: Path, capsys) -> None:
    run_training_evaluation(
        replace(_training_evaluation_happy_settings(tmp_path), allow_training_evaluation_dataset=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "training_evaluation_workflow_implemented: True" in output.out
    assert f"latest_training_evaluation_status: {TRAINING_EVALUATION_DATASET_CREATED}" in output.out
    assert "source_forward_return_label_run_id: label_abc123" in output.out
    assert "ready_for_training_evaluation_dataset: True" in output.out
    assert "training_evaluation_dataset_artifacts_created: True" in output.out
    assert "training_evaluation_label_row_count: 1" in output.out
    assert "training_evaluation_metrics_computed: False" in output.out
    assert "training_evaluation_training_allowed: False" in output.out
    assert "training_evaluation_training_result_created: False" in output.out
    assert "training_evaluation_model_version_created: False" in output.out
    assert "training_evaluation_stock_profile_allowed: False" in output.out
    assert "training_evaluation_buy_review_allowed: False" in output.out
    assert "training_evaluation_approved_for_paper: False" in output.out
    assert "training_evaluation_strategy_performance_validated: False" in output.out
    assert "training_evaluation_trading_allowed: False" in output.out


def test_research_status_includes_metric_evaluation_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    metric = run_metric_evaluation(
        replace(_metric_evaluation_happy_settings(tmp_path), allow_metric_evaluation_planning_artifacts=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "METRIC_EVALUATION_STATUS"].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED
    assert row["workflow_area"] == "METRIC_EVALUATION"
    assert result.metric_evaluation_workflow_implemented is True
    assert result.metric_evaluation_views_implemented is True
    assert result.latest_metric_evaluation_run_id == metric.metric_evaluation_run_id
    assert result.latest_metric_evaluation_status == METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED
    assert result.latest_metric_evaluation_health_status == "PASS"
    assert result.latest_metric_evaluation_workflow_stage == METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED
    assert result.metric_evaluation_artifact_path.endswith(metric.metric_evaluation_run_id)
    assert result.source_training_evaluation_run_id == "train_eval_abc123"
    assert result.source_training_evaluation_status == TRAINING_EVALUATION_DATASET_CREATED
    assert result.source_training_evaluation_health_status == "PASS"
    assert result.source_forward_return_label_run_id == "label_abc123"
    assert result.source_replay_decision_freeze_run_id == "freeze_abc123"
    assert result.training_evaluation_dataset_artifacts_created is True
    assert result.training_evaluation_sample_row_count == 1
    assert result.training_evaluation_label_row_count == 1
    assert result.metric_evaluation_symbol_count == 1
    assert "forward_return_5d" in result.metric_evaluation_label_name_set
    assert result.ready_for_metric_evaluation_planning_artifacts is True
    assert result.metric_evaluation_executed is True
    assert result.metric_evaluation_planning_artifacts_created is True
    assert result.metric_evaluation_input_index_created is True
    assert result.metric_definitions_created is True
    assert result.sample_scope_created is True
    assert result.denominator_rules_created is True
    assert result.health_status_plan_created is True
    assert result.research_status_plan_created is True
    assert result.metric_definition_count == metric.metric_definition_count
    assert result.sample_scope_row_count == metric.sample_scope_row_count
    assert result.denominator_rule_count == metric.denominator_rule_count
    assert result.metrics_computed is False
    assert result.metric_result_rows_created is False
    assert result.metric_evaluation_results_created is False
    assert result.evaluation_execution_completed is False
    assert result.metric_evaluation_training_allowed is False
    assert result.metric_evaluation_weights_trained is False
    assert result.metric_evaluation_training_result_created is False
    assert result.metric_evaluation_model_version_created is False
    assert result.metric_evaluation_thresholds_optimized is False
    assert result.metric_evaluation_predictions_created is False
    assert result.metric_evaluation_calibrated_probabilities_created is False
    assert result.metric_evaluation_feature_importance_created is False
    assert result.metric_evaluation_stock_profile_allowed is False
    assert result.metric_evaluation_active_stock_profile_exists is False
    assert result.metric_evaluation_stock_profile_created is False
    assert result.metric_evaluation_buy_review_allowed is False
    assert result.metric_evaluation_real_buy_review_eligible is False
    assert result.metric_evaluation_approved_for_paper is False
    assert result.metric_evaluation_strategy_performance_validated is False
    assert result.metric_evaluation_trading_allowed is False
    assert result.metric_evaluation_order_placed is False
    assert result.metric_evaluation_broker_api_called is False
    assert result.metric_evaluation_message_sent is False
    assert result.metric_evaluation_llm_api_called is False
    assert result.metric_evaluation_external_api_called is False
    assert result.metric_evaluation_cache_mutated is False
    assert result.metric_evaluation_data_raw_written is False
    assert result.metric_evaluation_data_processed_written is False
    assert result.metric_evaluation_data_cache_written is False
    assert result.metric_evaluation_current_candidates_run is False
    assert result.metric_evaluation_snapshot_built is False
    assert result.metric_evaluation_signal_semantics_changed is False
    assert result.metric_evaluation_report_only is True
    assert result.metric_evaluation_diagnostic_only is True
    assert result.metric_evaluation_no_live_trading is True
    assert result.metric_evaluation_no_broker_api is True
    assert result.metric_evaluation_no_order_placement is True
    assert result.metric_evaluation_no_message_sent is True
    assert str(summary["metric_definition_count"]) == str(metric.metric_definition_count)
    assert metadata["metrics_computed"] is False
    assert metadata["metric_result_rows_created"] is False
    assert metadata["metric_evaluation_results_created"] is False
    assert metadata["evaluation_execution_completed"] is False
    assert metadata["metric_evaluation_training_result_created"] is False
    assert metadata["metric_evaluation_model_version_created"] is False
    assert metadata["metric_evaluation_stock_profile_created"] is False
    assert metadata["metric_evaluation_strategy_performance_validated"] is False
    assert metadata["metric_evaluation_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_metric_evaluation(tmp_path: Path) -> None:
    run_metric_evaluation(
        replace(_metric_evaluation_happy_settings(tmp_path), allow_metric_evaluation_planning_artifacts=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; metric/evaluation phase 1 "
            "is report-only structural planning context."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_metric_evaluation_status == METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED
    assert result.metric_evaluation_planning_artifacts_created is True
    assert result.metrics_computed is False
    assert result.metric_result_rows_created is False
    assert result.metric_evaluation_results_created is False
    assert result.evaluation_execution_completed is False
    assert result.metric_evaluation_training_allowed is False
    assert result.metric_evaluation_training_result_created is False
    assert result.metric_evaluation_model_version_created is False
    assert result.metric_evaluation_stock_profile_allowed is False
    assert result.metric_evaluation_buy_review_allowed is False
    assert result.metric_evaluation_approved_for_paper is False
    assert result.metric_evaluation_strategy_performance_validated is False
    assert result.metric_evaluation_trading_allowed is False


def test_cli_research_status_prints_metric_evaluation_fields(tmp_path: Path, capsys) -> None:
    run_metric_evaluation(
        replace(_metric_evaluation_happy_settings(tmp_path), allow_metric_evaluation_planning_artifacts=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "metric_evaluation_workflow_implemented: True" in output.out
    assert f"latest_metric_evaluation_status: {METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED}" in output.out
    assert "source_training_evaluation_run_id: train_eval_abc123" in output.out
    assert "ready_for_metric_evaluation_planning_artifacts: True" in output.out
    assert "metric_evaluation_planning_artifacts_created: True" in output.out
    assert "metrics_computed: False" in output.out
    assert "metric_result_rows_created: False" in output.out
    assert "metric_evaluation_results_created: False" in output.out
    assert "evaluation_execution_completed: False" in output.out
    assert "metric_evaluation_training_allowed: False" in output.out
    assert "metric_evaluation_training_result_created: False" in output.out
    assert "metric_evaluation_model_version_created: False" in output.out
    assert "metric_evaluation_stock_profile_allowed: False" in output.out
    assert "metric_evaluation_buy_review_allowed: False" in output.out
    assert "metric_evaluation_approved_for_paper: False" in output.out
    assert "metric_evaluation_strategy_performance_validated: False" in output.out
    assert "metric_evaluation_trading_allowed: False" in output.out


def test_metric_evaluation_docs_checkpoint_and_source_note_are_safety_explicit() -> None:
    required_paths = [
        Path("docs/metric_evaluation.md"),
        Path("docs/release_checkpoint_v1.46.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_46_0.md"),
    ]
    for path in required_paths:
        text = path.read_text(encoding="utf-8").lower()
        for phrase in [
            "structural planning",
            "does not compute metrics",
            "does not create metric/evaluation result rows",
            "does not execute evaluation",
            "does not create training_result",
            "does not train weights",
            "does not create model_version",
            "does not optimize thresholds",
            "does not create predictions",
            "does not create calibrated probabilities",
            "does not create feature importance",
            "does not create active stock profiles",
            "does not create real buy-review eligibility",
            "does not apply paper approval",
            "does not claim strategy performance validation",
            "does not authorize trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_training_result_planning_context_and_safety_fields(
    tmp_path: Path,
) -> None:
    planning = run_training_result_planning(
        replace(_training_result_planning_happy_settings(tmp_path), allow_training_result_planning=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "TRAINING_RESULT_PLANNING_STATUS"].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED
    assert row["workflow_area"] == "TRAINING_RESULT_PLANNING"
    assert result.training_result_planning_workflow_implemented is True
    assert result.training_result_planning_views_implemented is True
    assert result.latest_training_result_planning_run_id == planning.training_result_planning_run_id
    assert result.latest_training_result_planning_status == TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED
    assert result.latest_training_result_planning_health_status == "PASS"
    assert result.latest_training_result_planning_workflow_stage == TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED
    assert result.training_result_planning_artifact_path.endswith(planning.training_result_planning_run_id)
    assert result.source_metric_extension_run_id == "metric_ext_plan"
    assert result.source_metric_extension_status == "METRIC_EXTENSION_REPORT_CREATED"
    assert result.source_metric_extension_health_status == "PASS"
    assert result.source_metric_computation_run_id == "metric_comp_plan"
    assert result.source_metric_computation_status == "METRIC_COMPUTATION_REPORT_CREATED"
    assert result.source_metric_computation_health_status == "PASS"
    assert result.source_metric_evaluation_planning_run_id == "metric_eval_plan"
    assert result.source_metric_evaluation_status == METRIC_EVALUATION_PLANNING_ARTIFACTS_CREATED
    assert result.source_metric_evaluation_health_status == "PASS"
    assert result.source_training_evaluation_run_id == "train_eval_plan"
    assert result.source_training_evaluation_status == TRAINING_EVALUATION_DATASET_CREATED
    assert result.source_training_evaluation_health_status == "PASS"
    assert result.source_forward_return_label_run_id == "label_plan"
    assert result.source_forward_return_label_status == FORWARD_RETURN_LABELS_CREATED
    assert result.source_forward_return_label_health_status == "PASS"
    assert result.source_replay_decision_freeze_run_id == "freeze_plan"
    assert result.source_replay_decision_freeze_status == REPLAY_DECISION_FROZEN
    assert result.source_replay_decision_freeze_health_status == "PASS"
    assert "benchmark_relative_return" in result.metric_evidence_names_present
    assert result.metric_evidence_row_count == planning.metric_evidence_row_count
    assert result.planning_input_row_count == planning.planning_input_row_count
    assert result.eligible_planning_input_count == planning.eligible_planning_input_count
    assert result.quarantined_planning_input_count == planning.quarantined_planning_input_count
    assert result.model_scope_rows_created is True
    assert result.limitations_created is True
    assert result.overfit_warnings_created is True
    assert result.health_plan_created is True
    assert result.status_plan_created is True
    assert result.ready_for_training_result_planning is True
    assert result.training_result_planning_executed is True
    assert result.training_result_planning_artifacts_created is True
    assert result.training_result_created is False
    assert result.weights_trained is False
    assert result.model_version_created is False
    assert result.parameter_version_created is False
    assert result.thresholds_optimized is False
    assert result.predictions_created is False
    assert result.calibrated_probabilities_created is False
    assert result.feature_importance_created is False
    assert result.stock_profile_allowed is False
    assert result.active_stock_profile_exists is False
    assert result.stock_profile_created is False
    assert result.buy_review_allowed is False
    assert result.real_buy_review_eligible is False
    assert result.approved_for_paper is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert result.order_placed is False
    assert result.broker_api_called is False
    assert result.message_sent is False
    assert result.llm_api_called is False
    assert result.external_api_called is False
    assert result.cache_mutated is False
    assert result.data_raw_written is False
    assert result.data_processed_written is False
    assert result.data_cache_written is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.report_only is True
    assert result.diagnostic_only is True
    assert result.no_live_trading is True
    assert result.no_broker_api is True
    assert result.no_order_placement is True
    assert result.no_message_sent is True
    assert str(summary["metric_evidence_row_count"]) == str(planning.metric_evidence_row_count)
    assert metadata["latest_training_result_planning_status"] == TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED
    assert metadata["training_result_created"] is False
    assert metadata["weights_trained"] is False
    assert metadata["model_version_created"] is False
    assert metadata["parameter_version_created"] is False
    assert metadata["thresholds_optimized"] is False
    assert metadata["predictions_created"] is False
    assert metadata["stock_profile_created"] is False
    assert metadata["approved_for_paper"] is False
    assert metadata["strategy_performance_validated"] is False
    assert metadata["trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_training_result_planning(tmp_path: Path) -> None:
    run_training_result_planning(
        replace(_training_result_planning_happy_settings(tmp_path), allow_training_result_planning=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; training result planning phase 1 "
            "is report-only planning context."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_training_result_planning_status == TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED
    assert result.training_result_planning_artifacts_created is True
    assert result.training_result_created is False
    assert result.weights_trained is False
    assert result.model_version_created is False
    assert result.parameter_version_created is False
    assert result.thresholds_optimized is False
    assert result.predictions_created is False
    assert result.stock_profile_allowed is False
    assert result.buy_review_allowed is False
    assert result.approved_for_paper is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False


def test_cli_research_status_prints_training_result_planning_fields(tmp_path: Path, capsys) -> None:
    run_training_result_planning(
        replace(_training_result_planning_happy_settings(tmp_path), allow_training_result_planning=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "training_result_planning_workflow_implemented: True" in output.out
    assert f"latest_training_result_planning_status: {TRAINING_RESULT_PLANNING_ARTIFACTS_CREATED}" in output.out
    assert "source_metric_extension_run_id: metric_ext_plan" in output.out
    assert "ready_for_training_result_planning: True" in output.out
    assert "training_result_planning_artifacts_created: True" in output.out
    assert "training_result_created: False" in output.out
    assert "weights_trained: False" in output.out
    assert "model_version_created: False" in output.out
    assert "parameter_version_created: False" in output.out
    assert "thresholds_optimized: False" in output.out
    assert "predictions_created: False" in output.out
    assert "calibrated_probabilities_created: False" in output.out
    assert "feature_importance_created: False" in output.out
    assert "stock_profile_allowed: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "approved_for_paper: False" in output.out
    assert "strategy_performance_validated: False" in output.out
    assert "trading_allowed: False" in output.out


def test_research_status_includes_actual_training_result_fields(tmp_path: Path) -> None:
    created = run_training_result(
        replace(_training_result_happy_settings(tmp_path), allow_training_result=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "TRAINING_RESULT_STATUS"].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == TRAINING_RESULT_CREATED
    assert row["workflow_area"] == "TRAINING_RESULT"
    assert result.training_result_workflow_implemented is True
    assert result.training_result_views_implemented is True
    assert result.latest_training_result_run_id == created.training_result_run_id
    assert result.latest_training_result_status == TRAINING_RESULT_CREATED
    assert result.latest_training_result_health_status == "PASS"
    assert result.latest_training_result_workflow_stage == TRAINING_RESULT_CREATED
    assert result.training_result_artifact_path.endswith(created.training_result_run_id)
    assert result.training_result_source_training_result_planning_run_id == "trp_plan"
    assert result.training_result_source_metric_extension_run_id == "metric_ext_plan"
    assert result.training_result_source_metric_computation_run_id == "metric_comp_plan"
    assert result.training_result_source_metric_evaluation_planning_run_id == "metric_eval_plan"
    assert result.training_result_source_training_evaluation_run_id == "train_eval_plan"
    assert result.training_result_source_forward_return_label_run_id == "label_plan"
    assert result.training_result_source_replay_decision_freeze_run_id == "freeze_plan"
    assert "benchmark_relative_return" in result.training_result_metric_evidence_names_present
    assert result.training_result_metric_evidence_reference_count == 7
    assert result.training_result_row_count == 1
    assert result.training_result_eligible_row_count == 1
    assert result.training_result_quarantined_row_count == 0
    assert result.training_result_limitations_created is True
    assert result.training_result_overfit_warnings_created is True
    assert result.training_result_created is True
    assert result.training_result_weights_trained is False
    assert result.training_result_model_version_created is False
    assert result.training_result_parameter_version_created is False
    assert result.training_result_thresholds_optimized is False
    assert result.training_result_predictions_created is False
    assert result.training_result_calibrated_probabilities_created is False
    assert result.training_result_feature_importance_created is False
    assert result.training_result_stock_profile_created is False
    assert result.training_result_real_buy_review_eligible is False
    assert result.training_result_approved_for_paper is False
    assert result.training_result_strategy_performance_validated is False
    assert result.training_result_trading_allowed is False
    assert result.training_result_report_only is True
    assert result.training_result_diagnostic_only is True
    assert result.training_result_no_live_trading is True
    assert result.training_result_no_broker_api is True
    assert result.training_result_no_order_placement is True
    assert result.training_result_no_message_sent is True
    assert metadata["latest_training_result_status"] == TRAINING_RESULT_CREATED
    assert metadata["training_result_created"] is True
    assert metadata["training_result_weights_trained"] is False
    assert metadata["training_result_model_version_created"] is False
    assert metadata["training_result_parameter_version_created"] is False
    assert metadata["training_result_approved_for_paper"] is False
    assert metadata["training_result_strategy_performance_validated"] is False
    assert metadata["training_result_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_actual_training_result(tmp_path: Path) -> None:
    run_training_result(replace(_training_result_happy_settings(tmp_path), allow_training_result=True))
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; actual training_result phase 1 "
            "is report-only metric evidence context."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_training_result_status == TRAINING_RESULT_CREATED
    assert result.training_result_created is True
    assert result.training_result_weights_trained is False
    assert result.training_result_model_version_created is False
    assert result.training_result_parameter_version_created is False
    assert result.training_result_thresholds_optimized is False
    assert result.training_result_predictions_created is False
    assert result.training_result_stock_profile_created is False
    assert result.training_result_real_buy_review_eligible is False
    assert result.training_result_approved_for_paper is False
    assert result.training_result_strategy_performance_validated is False
    assert result.training_result_trading_allowed is False


def test_cli_research_status_prints_actual_training_result_fields(tmp_path: Path, capsys) -> None:
    run_training_result(replace(_training_result_happy_settings(tmp_path), allow_training_result=True))
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "training_result_workflow_implemented: True" in output.out
    assert "training_result_views_implemented: True" in output.out
    assert f"latest_training_result_status: {TRAINING_RESULT_CREATED}" in output.out
    assert "training_result_source_training_result_planning_run_id: trp_plan" in output.out
    assert "training_result_metric_evidence_reference_count: 7" in output.out
    assert "training_result_created: True" in output.out
    assert "training_result_weights_trained: False" in output.out
    assert "training_result_model_version_created: False" in output.out
    assert "training_result_parameter_version_created: False" in output.out
    assert "training_result_thresholds_optimized: False" in output.out
    assert "training_result_predictions_created: False" in output.out
    assert "training_result_stock_profile_created: False" in output.out
    assert "training_result_approved_for_paper: False" in output.out
    assert "training_result_strategy_performance_validated: False" in output.out
    assert "training_result_trading_allowed: False" in output.out


def test_training_result_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/training_result.md"),
        Path("docs/release_checkpoint_v1.50.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_50_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "TRAINING_RESULT_CREATED" in text
        for phrase in [
            "not weights",
            "not model_version",
            "not parameter_version",
            "not thresholds",
            "not predictions",
            "not stock_profile",
            "not buy-review",
            "not paper approval",
            "not performance validation",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_50_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_model_weight_versioning_fields(tmp_path: Path) -> None:
    created = run_model_weight_versioning(
        replace(_model_weight_versioning_happy_settings(tmp_path), allow_model_weight_versioning=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MODEL_WEIGHT_VERSIONING_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED
    assert row["workflow_area"] == "MODEL_WEIGHT_VERSIONING"
    assert result.model_weight_versioning_workflow_implemented is True
    assert result.model_weight_versioning_views_implemented is True
    assert result.latest_model_workflow_run_id == created.model_workflow_run_id
    assert result.latest_model_weight_versioning_status == MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED
    assert result.latest_model_weight_versioning_health_status == "PASS"
    assert (
        result.latest_model_weight_versioning_workflow_stage
        == MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED
    )
    assert result.model_weight_versioning_artifact_path.endswith(created.model_workflow_run_id)
    assert result.model_weight_versioning_training_result_row_count == 1
    assert result.model_weight_versioning_eligible_training_result_row_count == 1
    assert result.model_weight_versioning_quarantined_training_result_row_count == 0
    assert "benchmark_relative_return" in result.model_weight_versioning_metric_evidence_names_present
    assert result.model_weight_versioning_metric_evidence_reference_count == 7
    assert result.model_weights_reference_created is True
    assert result.model_version_metadata_created is True
    assert result.parameter_version_metadata_created is True
    assert result.threshold_plan_created is True
    assert result.prediction_rows_created is True
    assert result.probability_calibration_report_created is True
    assert result.feature_importance_report_created is True
    assert result.active_model is False
    assert result.promoted_model is False
    assert result.production_model is False
    assert result.active_parameters is False
    assert result.active_thresholds is False
    assert result.advisory_predictions_created is False
    assert result.active_probabilities_created is False
    assert result.active_feature_importance_created is False
    assert result.model_weight_versioning_active_stock_profile_exists is False
    assert result.model_weight_versioning_real_buy_review_eligible is False
    assert result.model_weight_versioning_approved_for_paper is False
    assert result.model_weight_versioning_strategy_performance_validated is False
    assert result.model_weight_versioning_trading_allowed is False
    assert result.model_weight_versioning_report_only is True
    assert result.model_weight_versioning_diagnostic_only is True
    assert result.model_weight_versioning_no_live_trading is True
    assert result.model_weight_versioning_no_broker_api is True
    assert result.model_weight_versioning_no_order_placement is True
    assert result.model_weight_versioning_no_message_sent is True
    assert metadata["latest_model_weight_versioning_status"] == MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED
    assert metadata["model_weights_reference_created"] is True
    assert metadata["model_version_metadata_created"] is True
    assert metadata["active_model"] is False
    assert metadata["promoted_model"] is False
    assert metadata["production_model"] is False
    assert metadata["active_parameters"] is False
    assert metadata["active_thresholds"] is False
    assert metadata["advisory_predictions_created"] is False
    assert metadata["model_weight_versioning_approved_for_paper"] is False
    assert metadata["model_weight_versioning_strategy_performance_validated"] is False
    assert metadata["model_weight_versioning_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_model_weight_versioning(tmp_path: Path) -> None:
    run_model_weight_versioning(
        replace(_model_weight_versioning_happy_settings(tmp_path), allow_model_weight_versioning=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; model-weight-versioning is report-only "
            "research context, not active model promotion."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_model_weight_versioning_status == MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED
    assert result.model_weights_reference_created is True
    assert result.model_version_metadata_created is True
    assert result.active_model is False
    assert result.promoted_model is False
    assert result.production_model is False
    assert result.active_parameters is False
    assert result.active_thresholds is False
    assert result.advisory_predictions_created is False
    assert result.model_weight_versioning_real_buy_review_eligible is False
    assert result.model_weight_versioning_approved_for_paper is False
    assert result.model_weight_versioning_strategy_performance_validated is False
    assert result.model_weight_versioning_trading_allowed is False


def test_cli_research_status_prints_model_weight_versioning_fields(tmp_path: Path, capsys) -> None:
    run_model_weight_versioning(
        replace(_model_weight_versioning_happy_settings(tmp_path), allow_model_weight_versioning=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "model_weight_versioning_workflow_implemented: True" in output.out
    assert "model_weight_versioning_views_implemented: True" in output.out
    assert (
        f"latest_model_weight_versioning_status: {MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED}"
        in output.out
    )
    assert "model_weight_versioning_training_result_row_count: 1" in output.out
    assert "model_weights_reference_created: True" in output.out
    assert "model_version_metadata_created: True" in output.out
    assert "parameter_version_metadata_created: True" in output.out
    assert "threshold_plan_created: True" in output.out
    assert "prediction_rows_created: True" in output.out
    assert "probability_calibration_report_created: True" in output.out
    assert "feature_importance_report_created: True" in output.out
    assert "active_model: False" in output.out
    assert "promoted_model: False" in output.out
    assert "production_model: False" in output.out
    assert "active_parameters: False" in output.out
    assert "active_thresholds: False" in output.out
    assert "advisory_predictions_created: False" in output.out
    assert "model_weight_versioning_approved_for_paper: False" in output.out
    assert "model_weight_versioning_strategy_performance_validated: False" in output.out
    assert "model_weight_versioning_trading_allowed: False" in output.out


def test_model_weight_versioning_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/model_weight_versioning.md"),
        Path("docs/release_checkpoint_v1.51.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_51_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED" in text
        for phrase in [
            "not an active model",
            "not a promoted model",
            "not a production model",
            "not active parameters",
            "not active thresholds",
            "not advisory predictions",
            "not active probabilities",
            "not stock_profile",
            "not buy-review",
            "not paper approval",
            "not performance validation",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_51_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_active_model_fields_without_promotion_or_trading(tmp_path: Path) -> None:
    created = run_active_model(replace(_active_model_happy_settings(tmp_path), allow_active_model=True))
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "ACTIVE_MODEL_STATUS"].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED
    assert row["workflow_area"] == "ACTIVE_MODEL"
    assert result.active_model_workflow_implemented is True
    assert result.active_model_views_implemented is True
    assert result.latest_active_model_run_id == created.active_model_run_id
    assert result.latest_active_model_status == ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED
    assert result.latest_active_model_health_status == "PASS"
    assert result.latest_active_model_workflow_stage == ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED
    assert result.active_model_artifact_path.endswith(created.active_model_run_id)
    assert result.ready_for_active_model is True
    assert result.active_model_executed is True
    assert result.active_model_artifacts_created is True
    assert result.active_model_pointer_created is True
    assert result.active_model_registry_entry_created is True
    assert result.active_parameter_pointer_created is True
    assert result.active_model_activation_status_created is True
    assert result.active_model_rollback_plan_created is True
    assert result.active_model_input_index_created is True
    assert result.active_model_lineage_matrix_created is True
    assert result.active_model_limitations_created is True
    assert result.active_model_overfit_warnings_created is True
    assert result.active_model_safety_flags_created is True
    assert result.active_model_source_model_workflow_run_id == created.source_model_workflow_run_id
    assert result.active_model_source_model_weight_versioning_status == "MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED"
    assert result.active_model_source_model_weight_versioning_health_status == "PASS"
    assert result.active_model_model_weight_reference_id == created.model_weight_reference_id
    assert result.active_model_model_version_id == created.model_version_id
    assert result.active_model_parameter_version_id == created.parameter_version_id
    assert result.active_model_promoted_model_created is False
    assert result.active_model_production_model_created is False
    assert result.active_model_active_thresholds_created is False
    assert result.active_model_advisory_predictions_created is False
    assert result.active_model_active_probabilities_created is False
    assert result.active_model_stock_profile_created is False
    assert result.active_model_buy_review_allowed is False
    assert result.active_model_real_buy_review_eligible is False
    assert result.active_model_approved_for_paper is False
    assert result.active_model_strategy_performance_validated is False
    assert result.active_model_trading_allowed is False
    assert result.active_model_order_placed is False
    assert result.active_model_broker_api_called is False
    assert result.active_model_message_sent is False
    assert result.active_model_llm_api_called is False
    assert result.active_model_external_api_called is False
    assert result.active_model_cache_mutated is False
    assert result.active_model_data_raw_written is False
    assert result.active_model_data_processed_written is False
    assert result.active_model_data_cache_written is False
    assert result.active_model_current_candidates_run is False
    assert result.active_model_snapshot_built is False
    assert result.active_model_signal_semantics_changed is False
    assert result.active_model_research_governed is True
    assert result.active_model_diagnostic_output is True
    assert result.active_model_no_live_trading is True
    assert result.active_model_no_broker_api is True
    assert result.active_model_no_order_placement is True
    assert result.active_model_no_message_sent is True
    assert metadata["latest_active_model_status"] == ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED
    assert metadata["active_model_artifacts_created"] is True
    assert metadata["active_model_promoted_model_created"] is False
    assert metadata["active_model_production_model_created"] is False
    assert metadata["active_model_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_active_model_context(tmp_path: Path) -> None:
    run_active_model(replace(_active_model_happy_settings(tmp_path), allow_active_model=True))
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; active-model is research-governed context, "
            "not promoted model, production model, stock_profile, paper approval, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_active_model_status == ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED
    assert result.active_model_artifacts_created is True
    assert result.active_model_promoted_model_created is False
    assert result.active_model_production_model_created is False
    assert result.active_model_active_thresholds_created is False
    assert result.active_model_advisory_predictions_created is False
    assert result.active_model_active_probabilities_created is False
    assert result.active_model_stock_profile_created is False
    assert result.active_model_buy_review_allowed is False
    assert result.active_model_real_buy_review_eligible is False
    assert result.active_model_approved_for_paper is False
    assert result.active_model_strategy_performance_validated is False
    assert result.active_model_trading_allowed is False
    assert result.active_model_current_candidates_run is False
    assert result.active_model_snapshot_built is False
    assert result.active_model_signal_semantics_changed is False


def test_cli_research_status_prints_active_model_fields(tmp_path: Path, capsys) -> None:
    run_active_model(replace(_active_model_happy_settings(tmp_path), allow_active_model=True))
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "active_model_workflow_implemented: True" in output.out
    assert "active_model_views_implemented: True" in output.out
    assert f"latest_active_model_status: {ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED}" in output.out
    assert "active_model_artifacts_created: True" in output.out
    assert "active_model_pointer_created: True" in output.out
    assert "active_model_promoted_model_created: False" in output.out
    assert "active_model_production_model_created: False" in output.out
    assert "active_model_active_thresholds_created: False" in output.out
    assert "active_model_advisory_predictions_created: False" in output.out
    assert "active_model_active_probabilities_created: False" in output.out
    assert "active_model_stock_profile_created: False" in output.out
    assert "active_model_buy_review_allowed: False" in output.out
    assert "active_model_approved_for_paper: False" in output.out
    assert "active_model_strategy_performance_validated: False" in output.out
    assert "active_model_trading_allowed: False" in output.out
    assert "active_model_current_candidates_run: False" in output.out
    assert "active_model_snapshot_built: False" in output.out
    assert "active_model_signal_semantics_changed: False" in output.out


def test_active_model_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/active_model.md"),
        Path("docs/release_checkpoint_v1.52.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_52_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED" in text
        for phrase in [
            "not promoted model",
            "not production model",
            "not active thresholds",
            "not advisory predictions",
            "not active probabilities",
            "not stock_profile",
            "not buy-review",
            "not paper approval",
            "not performance validation",
            "not current-candidates",
            "not snapshot",
            "not signal_semantics",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_52_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT Project Source is maintained separately" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_stock_profile_fields_without_buy_review_or_trading(tmp_path: Path) -> None:
    created = run_stock_profile(replace(_stock_profile_happy_settings(tmp_path), allow_stock_profile=True))
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "STOCK_PROFILE_STATUS"].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert row["workflow_area"] == "STOCK_PROFILE"
    assert result.stock_profile_workflow_implemented is True
    assert result.stock_profile_views_implemented is True
    assert result.latest_stock_profile_run_id == created.stock_profile_run_id
    assert result.latest_stock_profile_status == STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.latest_stock_profile_health_status == "PASS"
    assert result.latest_stock_profile_workflow_stage == STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.stock_profile_artifact_path.endswith(created.stock_profile_run_id)
    assert result.ready_for_stock_profile_phase1 is True
    assert result.stock_profile_phase1_executed is True
    assert result.stock_profile_phase1_report_only_artifacts_created is True
    assert result.stock_profile_metadata_created is True
    assert result.stock_profile_input_index_created is True
    assert result.stock_profile_lineage_matrix_created is True
    assert result.stock_profile_factor_coverage_summary_created is True
    assert result.stock_profile_symbol_coverage_created is True
    assert result.stock_profile_market_regime_coverage_created is True
    assert result.stock_profile_metric_summary_created is True
    assert result.stock_profile_limitations_created is True
    assert result.stock_profile_overfit_warnings_created is True
    assert result.stock_profile_safety_flags_created is True
    assert result.stock_profile_source_active_model_run_id == created.source_active_model_run_id
    assert result.stock_profile_source_active_model_status == "ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED"
    assert result.stock_profile_source_active_model_health_status == "PASS"
    assert result.stock_profile_source_model_workflow_run_id == created.source_model_workflow_run_id
    assert result.stock_profile_source_model_weight_versioning_status == "MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED"
    assert result.stock_profile_source_model_weight_versioning_health_status == "PASS"
    assert result.stock_profile_model_weight_reference_id == created.model_weight_reference_id
    assert result.stock_profile_model_version_id == created.model_version_id
    assert result.stock_profile_parameter_version_id == created.parameter_version_id
    assert result.stock_profile_active_stock_profile_created is False
    assert result.stock_profile_real_buy_review_eligible is False
    assert result.stock_profile_buy_review_allowed is False
    assert result.stock_profile_approved_for_paper is False
    assert result.stock_profile_strategy_performance_validated is False
    assert result.stock_profile_trading_allowed is False
    assert result.stock_profile_current_candidates_run is False
    assert result.stock_profile_snapshot_built is False
    assert result.stock_profile_signal_semantics_changed is False
    assert result.stock_profile_promoted_model_created is False
    assert result.stock_profile_production_model_created is False
    assert result.stock_profile_active_thresholds_created is False
    assert result.stock_profile_advisory_predictions_created is False
    assert result.stock_profile_active_probabilities_created is False
    assert result.stock_profile_order_placed is False
    assert result.stock_profile_broker_api_called is False
    assert result.stock_profile_message_sent is False
    assert result.stock_profile_llm_api_called is False
    assert result.stock_profile_external_api_called is False
    assert result.stock_profile_cache_mutated is False
    assert result.stock_profile_data_raw_written is False
    assert result.stock_profile_data_processed_written is False
    assert result.stock_profile_data_cache_written is False
    assert result.stock_profile_report_only is True
    assert result.stock_profile_research_governed is True
    assert result.stock_profile_diagnostic_output is True
    assert result.stock_profile_no_live_trading is True
    assert result.stock_profile_no_broker_api is True
    assert result.stock_profile_no_order_placement is True
    assert result.stock_profile_no_message_sent is True
    assert metadata["latest_stock_profile_status"] == STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert metadata["stock_profile_phase1_report_only_artifacts_created"] is True
    assert metadata["stock_profile_active_stock_profile_created"] is False
    assert metadata["stock_profile_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_stock_profile_context(tmp_path: Path) -> None:
    run_stock_profile(replace(_stock_profile_happy_settings(tmp_path), allow_stock_profile=True))
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; stock-profile is report-only context, "
            "not active stock_profile, buy-review, paper approval, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_stock_profile_status == STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.stock_profile_phase1_report_only_artifacts_created is True
    assert result.stock_profile_active_stock_profile_created is False
    assert result.stock_profile_real_buy_review_eligible is False
    assert result.stock_profile_buy_review_allowed is False
    assert result.stock_profile_approved_for_paper is False
    assert result.stock_profile_strategy_performance_validated is False
    assert result.stock_profile_trading_allowed is False
    assert result.stock_profile_current_candidates_run is False
    assert result.stock_profile_snapshot_built is False
    assert result.stock_profile_signal_semantics_changed is False


def test_cli_research_status_prints_stock_profile_fields(tmp_path: Path, capsys) -> None:
    run_stock_profile(replace(_stock_profile_happy_settings(tmp_path), allow_stock_profile=True))
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "stock_profile_workflow_implemented: True" in output.out
    assert "stock_profile_views_implemented: True" in output.out
    assert f"latest_stock_profile_status: {STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED}" in output.out
    assert "stock_profile_phase1_report_only_artifacts_created: True" in output.out
    assert "stock_profile_active_stock_profile_created: False" in output.out
    assert "stock_profile_real_buy_review_eligible: False" in output.out
    assert "stock_profile_buy_review_allowed: False" in output.out
    assert "stock_profile_approved_for_paper: False" in output.out
    assert "stock_profile_strategy_performance_validated: False" in output.out
    assert "stock_profile_trading_allowed: False" in output.out
    assert "stock_profile_current_candidates_run: False" in output.out
    assert "stock_profile_snapshot_built: False" in output.out
    assert "stock_profile_signal_semantics_changed: False" in output.out
    assert "stock_profile_promoted_model_created: False" in output.out
    assert "stock_profile_production_model_created: False" in output.out
    assert "stock_profile_active_thresholds_created: False" in output.out
    assert "stock_profile_advisory_predictions_created: False" in output.out
    assert "stock_profile_active_probabilities_created: False" in output.out


def test_stock_profile_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/stock_profile.md"),
        Path("docs/release_checkpoint_v1.53.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_53_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED" in text
        for phrase in [
            "not active stock_profile",
            "not real buy-review",
            "not paper approval",
            "not performance validation",
            "not current-candidates",
            "not snapshot",
            "not signal_semantics",
            "not promoted model",
            "not production model",
            "not active thresholds",
            "not advisory predictions",
            "not active probabilities",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_53_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT Project Source is maintained separately" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_paper_workflow_phase1_fields_without_paper_approval(
    tmp_path: Path,
) -> None:
    created = run_paper_workflow_phase1(
        replace(_paper_workflow_phase1_happy_settings(tmp_path), allow_paper_workflow_phase1=True)
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "PAPER_WORKFLOW_PHASE1_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert row["workflow_area"] == "PAPER_WORKFLOW_PHASE1"
    assert result.paper_workflow_phase1_workflow_implemented is True
    assert result.paper_workflow_phase1_views_implemented is True
    assert result.latest_paper_workflow_phase1_run_id == created.paper_workflow_run_id
    assert result.latest_paper_workflow_phase1_status == PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.latest_paper_workflow_phase1_health_status == "PASS"
    assert result.latest_paper_workflow_phase1_workflow_stage == PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.paper_workflow_phase1_artifact_path.endswith(created.paper_workflow_run_id)
    assert result.ready_for_paper_workflow_phase1 is True
    assert result.paper_workflow_phase1_executed is True
    assert result.paper_workflow_phase1_report_only_artifacts_created is True
    assert result.paper_workflow_metadata_created is True
    assert result.paper_workflow_input_index_created is True
    assert result.paper_workflow_lineage_matrix_created is True
    assert result.paper_candidate_review_context_created is True
    assert result.paper_decision_draft_created is True
    assert result.paper_review_queue_created is True
    assert result.paper_workflow_limitations_created is True
    assert result.paper_workflow_overfit_warnings_created is True
    assert result.paper_workflow_safety_flags_created is True
    assert result.source_stock_profile_run_id == created.source_stock_profile_run_id
    assert result.source_stock_profile_status == "STOCK_PROFILE_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED"
    assert result.source_stock_profile_health_status == "PASS"
    assert result.source_active_model_run_id == created.source_active_model_run_id
    assert result.source_active_model_status == "ACTIVE_MODEL_RESEARCH_GOVERNED_ARTIFACTS_CREATED"
    assert result.source_active_model_health_status == "PASS"
    assert result.source_model_workflow_run_id == created.source_model_workflow_run_id
    assert result.source_model_weight_versioning_status == "MODEL_WEIGHT_VERSIONING_RESEARCH_ARTIFACTS_CREATED"
    assert result.source_model_weight_versioning_health_status == "PASS"
    assert result.model_weight_reference_id == created.model_weight_reference_id
    assert result.model_version_id == created.model_version_id
    assert result.parameter_version_id == created.parameter_version_id
    assert result.approved_for_paper is False
    assert result.approved_for_paper_created is False
    assert result.paper_approval_created is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.active_stock_profile_created is False
    assert result.promoted_model_created is False
    assert result.production_model_created is False
    assert result.active_thresholds_created is False
    assert result.advisory_predictions_created is False
    assert result.active_probabilities_created is False
    assert result.order_placed is False
    assert result.broker_api_called is False
    assert result.message_sent is False
    assert result.llm_api_called is False
    assert result.external_api_called is False
    assert result.cache_mutated is False
    assert result.data_raw_written is False
    assert result.data_processed_written is False
    assert result.data_cache_written is False
    assert result.report_only is True
    assert result.research_governed is True
    assert result.diagnostic_output is True
    assert result.no_live_trading is True
    assert result.no_broker_api is True
    assert result.no_order_placement is True
    assert result.no_message_sent is True
    assert metadata["latest_paper_workflow_phase1_status"] == PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert metadata["paper_workflow_phase1_report_only_artifacts_created"] is True
    assert metadata["approved_for_paper"] is False
    assert metadata["real_buy_review_eligible"] is False
    assert metadata["strategy_performance_validated"] is False
    assert metadata["trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_paper_workflow_phase1_context(
    tmp_path: Path,
) -> None:
    run_paper_workflow_phase1(
        replace(_paper_workflow_phase1_happy_settings(tmp_path), allow_paper_workflow_phase1=True)
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Paper Workflow Phase 1 is report-only "
            "context, not APPROVED_FOR_PAPER, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_paper_workflow_phase1_status == PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.paper_workflow_phase1_report_only_artifacts_created is True
    assert result.approved_for_paper is False
    assert result.approved_for_paper_created is False
    assert result.paper_approval_created is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.active_stock_profile_created is False
    assert result.promoted_model_created is False
    assert result.production_model_created is False
    assert result.active_thresholds_created is False
    assert result.advisory_predictions_created is False
    assert result.active_probabilities_created is False


def test_cli_research_status_prints_paper_workflow_phase1_fields(tmp_path: Path, capsys) -> None:
    run_paper_workflow_phase1(
        replace(_paper_workflow_phase1_happy_settings(tmp_path), allow_paper_workflow_phase1=True)
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "paper_workflow_phase1_workflow_implemented: True" in output.out
    assert "paper_workflow_phase1_views_implemented: True" in output.out
    assert (
        f"latest_paper_workflow_phase1_status: {PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED}"
        in output.out
    )
    assert "paper_workflow_phase1_report_only_artifacts_created: True" in output.out
    assert "paper_workflow_metadata_created: True" in output.out
    assert "paper_candidate_review_context_created: True" in output.out
    assert "paper_decision_draft_created: True" in output.out
    assert "approved_for_paper: False" in output.out
    assert "approved_for_paper_created: False" in output.out
    assert "paper_approval_created: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "strategy_performance_validated: False" in output.out
    assert "trading_allowed: False" in output.out
    assert "current_candidates_run: False" in output.out
    assert "snapshot_built: False" in output.out
    assert "signal_semantics_changed: False" in output.out
    assert "active_stock_profile_created: False" in output.out
    assert "promoted_model_created: False" in output.out
    assert "production_model_created: False" in output.out
    assert "active_thresholds_created: False" in output.out
    assert "advisory_predictions_created: False" in output.out
    assert "active_probabilities_created: False" in output.out


def test_paper_workflow_phase1_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/paper_workflow_phase1.md"),
        Path("docs/release_checkpoint_v1.54.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_54_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED" in text
        for phrase in [
            "not APPROVED_FOR_PAPER",
            "not real buy-review",
            "not strategy performance validation",
            "not current-candidates",
            "not snapshot",
            "not signal_semantics",
            "not active stock_profile",
            "not promoted model",
            "not production model",
            "not active thresholds",
            "not advisory predictions",
            "not active probabilities",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_54_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT Project Source is maintained separately" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_approved_for_paper_phase1_fields_without_global_approval(
    tmp_path: Path,
) -> None:
    created = run_approved_for_paper_phase1(
        replace(
            _approved_for_paper_phase1_happy_settings(tmp_path),
            allow_approved_for_paper_phase1=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "APPROVED_FOR_PAPER_PHASE1_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert row["workflow_area"] == "APPROVED_FOR_PAPER_PHASE1"
    assert result.approved_for_paper_phase1_workflow_implemented is True
    assert result.approved_for_paper_phase1_views_implemented is True
    assert result.latest_approved_for_paper_phase1_run_id == created.approved_for_paper_run_id
    assert result.latest_approved_for_paper_phase1_status == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.latest_approved_for_paper_phase1_health_status == "PASS"
    assert result.latest_approved_for_paper_phase1_workflow_stage == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.approved_for_paper_phase1_artifact_path.endswith(created.approved_for_paper_run_id)
    assert result.ready_for_approved_for_paper_phase1 is True
    assert result.approved_for_paper_phase1_executed is True
    assert result.approved_for_paper_phase1_report_only_artifacts_created is True
    assert result.approved_for_paper_metadata_created is True
    assert result.approved_for_paper_input_index_created is True
    assert result.approved_for_paper_lineage_matrix_created is True
    assert result.approved_for_paper_review_context_created is True
    assert result.approved_for_paper_decision_draft_created is True
    assert result.approved_for_paper_limitations_created is True
    assert result.approved_for_paper_overfit_warnings_created is True
    assert result.approved_for_paper_safety_flags_created is True
    assert result.scoped_approved_for_paper_phase1 is True
    assert result.scoped_approved_for_paper is True
    assert result.approved_for_paper_scope == "report_only_phase1_artifact_state_only"
    assert result.approved_for_paper_phase1_source_paper_workflow_run_id == created.source_paper_workflow_phase1_run_id
    assert result.approved_for_paper_phase1_source_paper_workflow_status == "PAPER_WORKFLOW_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED"
    assert result.approved_for_paper_phase1_source_paper_workflow_health_status == "PASS"
    assert result.approved_for_paper_phase1_model_weight_reference_id == created.model_weight_reference_id
    assert result.approved_for_paper_phase1_model_version_id == created.model_version_id
    assert result.approved_for_paper_phase1_parameter_version_id == created.parameter_version_id
    assert result.approved_for_paper_phase1_real_buy_review_eligible is False
    assert result.approved_for_paper_phase1_buy_review_allowed is False
    assert result.approved_for_paper_phase1_strategy_performance_validated is False
    assert result.approved_for_paper_phase1_trading_allowed is False
    assert result.approved_for_paper_phase1_current_candidates_run is False
    assert result.approved_for_paper_phase1_snapshot_built is False
    assert result.approved_for_paper_phase1_signal_semantics_changed is False
    assert result.approved_for_paper_phase1_active_stock_profile_created is False
    assert result.approved_for_paper_phase1_promoted_model_created is False
    assert result.approved_for_paper_phase1_production_model_created is False
    assert result.approved_for_paper_phase1_active_thresholds_created is False
    assert result.approved_for_paper_phase1_advisory_predictions_created is False
    assert result.approved_for_paper_phase1_active_probabilities_created is False
    assert result.approved_for_paper_phase1_order_placed is False
    assert result.approved_for_paper_phase1_broker_api_called is False
    assert result.approved_for_paper_phase1_message_sent is False
    assert result.approved_for_paper_phase1_llm_api_called is False
    assert result.approved_for_paper_phase1_external_api_called is False
    assert result.approved_for_paper_phase1_cache_mutated is False
    assert result.approved_for_paper_phase1_data_raw_written is False
    assert result.approved_for_paper_phase1_data_processed_written is False
    assert result.approved_for_paper_phase1_data_cache_written is False
    assert result.approved_for_paper_phase1_report_only is True
    assert result.approved_for_paper_phase1_research_governed is True
    assert result.approved_for_paper_phase1_diagnostic_output is True
    assert result.approved_for_paper_phase1_no_live_trading is True
    assert result.approved_for_paper_phase1_no_broker_api is True
    assert result.approved_for_paper_phase1_no_order_placement is True
    assert result.approved_for_paper_phase1_no_message_sent is True
    assert result.approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert metadata["latest_approved_for_paper_phase1_status"] == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert metadata["approved_for_paper_phase1_report_only_artifacts_created"] is True
    assert metadata["approved_for_paper_phase1_real_buy_review_eligible"] is False
    assert metadata["approved_for_paper_phase1_trading_allowed"] is False
    assert metadata["approved_for_paper"] is False
    assert metadata["real_buy_review_eligible"] is False
    assert metadata["strategy_performance_validated"] is False
    assert metadata["trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_approved_for_paper_phase1_context(
    tmp_path: Path,
) -> None:
    run_approved_for_paper_phase1(
        replace(
            _approved_for_paper_phase1_happy_settings(tmp_path),
            allow_approved_for_paper_phase1=True,
        )
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; APPROVED_FOR_PAPER Phase 1 is scoped "
            "report-only context, not global approval, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.latest_approved_for_paper_phase1_status == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    assert result.approved_for_paper_phase1_report_only_artifacts_created is True
    assert result.approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert result.current_candidates_run is False
    assert result.snapshot_built is False
    assert result.signal_semantics_changed is False
    assert result.active_stock_profile_created is False
    assert result.promoted_model_created is False
    assert result.production_model_created is False
    assert result.active_thresholds_created is False
    assert result.advisory_predictions_created is False
    assert result.active_probabilities_created is False


def test_cli_research_status_prints_approved_for_paper_phase1_fields(tmp_path: Path, capsys) -> None:
    run_approved_for_paper_phase1(
        replace(
            _approved_for_paper_phase1_happy_settings(tmp_path),
            allow_approved_for_paper_phase1=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "approved_for_paper_phase1_workflow_implemented: True" in output.out
    assert "approved_for_paper_phase1_views_implemented: True" in output.out
    assert (
        f"latest_approved_for_paper_phase1_status: {APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED}"
        in output.out
    )
    assert "approved_for_paper_phase1_report_only_artifacts_created: True" in output.out
    assert "approved_for_paper_metadata_created: True" in output.out
    assert "approved_for_paper_review_context_created: True" in output.out
    assert "approved_for_paper_decision_draft_created: True" in output.out
    assert "scoped_approved_for_paper_phase1: True" in output.out
    assert "approved_for_paper_phase1_real_buy_review_eligible: False" in output.out
    assert "approved_for_paper_phase1_buy_review_allowed: False" in output.out
    assert "approved_for_paper_phase1_strategy_performance_validated: False" in output.out
    assert "approved_for_paper_phase1_trading_allowed: False" in output.out
    assert "approved_for_paper_phase1_current_candidates_run: False" in output.out
    assert "approved_for_paper_phase1_snapshot_built: False" in output.out
    assert "approved_for_paper_phase1_signal_semantics_changed: False" in output.out
    assert "approved_for_paper: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "strategy_performance_validated: False" in output.out
    assert "trading_allowed: False" in output.out


def test_research_status_includes_global_approval_review_report_only_fields(
    tmp_path: Path,
) -> None:
    created = run_global_approved_for_paper_approval_review(
        replace(
            _global_approved_for_paper_approval_review_happy_settings(tmp_path),
            allow_global_approved_for_paper_approval_review=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED
    assert row["workflow_area"] == "GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW"
    assert result.global_approved_for_paper_approval_review_workflow_implemented is True
    assert result.global_approved_for_paper_approval_review_views_implemented is True
    assert (
        result.latest_global_approved_for_paper_approval_review_id
        == created.global_approved_for_paper_approval_review_id
    )
    assert (
        result.latest_global_approved_for_paper_approval_review_status
        == GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED
    )
    assert result.latest_global_approved_for_paper_approval_review_health_status == "PASS"
    assert (
        result.latest_global_approved_for_paper_approval_review_workflow_stage
        == GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED
    )
    assert result.global_approved_for_paper_approval_review_artifact_path.endswith(
        created.global_approved_for_paper_approval_review_id
    )
    assert result.ready_for_global_approved_for_paper_approval_review is True
    assert result.global_approved_for_paper_approval_review_executed is True
    assert result.global_approved_for_paper_approval_review_report_only_artifacts_created is True
    assert result.scoped_global_approved_for_paper_approval_review is True
    assert result.global_approved_for_paper is False
    assert result.global_approved_for_paper_scope == "report_only_global_approval_review_only"
    assert (
        result.global_approved_for_paper_approval_review_source_approved_for_paper_phase1_status
        == APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED
    )
    assert result.global_approved_for_paper_approval_review_source_approved_for_paper_phase1_health_status == "PASS"
    assert result.global_approved_for_paper_approval_review_real_buy_review_eligible is False
    assert result.global_approved_for_paper_approval_review_buy_review_allowed is False
    assert result.global_approved_for_paper_approval_review_strategy_performance_validated is False
    assert result.global_approved_for_paper_approval_review_trading_allowed is False
    assert result.global_approved_for_paper_approval_review_current_candidates_run is False
    assert result.global_approved_for_paper_approval_review_snapshot_built is False
    assert result.global_approved_for_paper_approval_review_signal_semantics_changed is False
    assert result.global_approved_for_paper_approval_review_active_stock_profile_created is False
    assert result.global_approved_for_paper_approval_review_promoted_model_created is False
    assert result.global_approved_for_paper_approval_review_production_model_created is False
    assert result.global_approved_for_paper_approval_review_active_thresholds_created is False
    assert result.global_approved_for_paper_approval_review_advisory_predictions_created is False
    assert result.global_approved_for_paper_approval_review_active_probabilities_created is False
    assert result.global_approved_for_paper_approval_review_broker_api_called is False
    assert result.global_approved_for_paper_approval_review_order_placed is False
    assert result.global_approved_for_paper_approval_review_message_sent is False
    assert result.global_approved_for_paper_approval_review_llm_api_called is False
    assert result.global_approved_for_paper_approval_review_external_api_called is False
    assert result.global_approved_for_paper_approval_review_cache_mutated is False
    assert result.global_approved_for_paper_approval_review_data_raw_written is False
    assert result.global_approved_for_paper_approval_review_data_processed_written is False
    assert result.global_approved_for_paper_approval_review_data_cache_written is False
    assert result.approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert metadata["latest_global_approved_for_paper_approval_review_status"] == (
        GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED
    )
    assert metadata["global_approved_for_paper_approval_review_report_only_artifacts_created"] is True
    assert metadata["global_approved_for_paper"] is False
    assert metadata["global_approved_for_paper_approval_review_buy_review_allowed"] is False
    assert metadata["global_approved_for_paper_approval_review_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_global_approval_review_context(
    tmp_path: Path,
) -> None:
    run_global_approved_for_paper_approval_review(
        replace(
            _global_approved_for_paper_approval_review_happy_settings(tmp_path),
            allow_global_approved_for_paper_approval_review=True,
        )
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Global APPROVED_FOR_PAPER approval review "
            "is report-only context, not buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert (
        result.latest_global_approved_for_paper_approval_review_status
        == GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED
    )
    assert result.global_approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False


def test_cli_research_status_prints_global_approval_review_fields(tmp_path: Path, capsys) -> None:
    run_global_approved_for_paper_approval_review(
        replace(
            _global_approved_for_paper_approval_review_happy_settings(tmp_path),
            allow_global_approved_for_paper_approval_review=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "global_approved_for_paper_approval_review_workflow_implemented: True" in output.out
    assert "global_approved_for_paper_approval_review_views_implemented: True" in output.out
    assert (
        "latest_global_approved_for_paper_approval_review_status: "
        f"{GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED}"
    ) in output.out
    assert "global_approved_for_paper_approval_review_report_only_artifacts_created: True" in output.out
    assert "scoped_global_approved_for_paper_approval_review: True" in output.out
    assert "global_approved_for_paper: False" in output.out
    assert "global_approved_for_paper_approval_review_buy_review_allowed: False" in output.out
    assert "global_approved_for_paper_approval_review_strategy_performance_validated: False" in output.out
    assert "global_approved_for_paper_approval_review_trading_allowed: False" in output.out
    assert "global_approved_for_paper_approval_review_current_candidates_run: False" in output.out
    assert "global_approved_for_paper_approval_review_snapshot_built: False" in output.out
    assert "global_approved_for_paper_approval_review_signal_semantics_changed: False" in output.out
    assert "global_approved_for_paper_approval_review_active_stock_profile_created: False" in output.out
    assert "global_approved_for_paper_approval_review_promoted_model_created: False" in output.out
    assert "global_approved_for_paper_approval_review_production_model_created: False" in output.out
    assert "global_approved_for_paper_approval_review_active_thresholds_created: False" in output.out
    assert "global_approved_for_paper_approval_review_advisory_predictions_created: False" in output.out
    assert "global_approved_for_paper_approval_review_active_probabilities_created: False" in output.out
    assert "approved_for_paper: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "strategy_performance_validated: False" in output.out
    assert "trading_allowed: False" in output.out


def test_global_approval_review_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("docs/global_approved_for_paper_approval_review.md"),
        Path("docs/release_checkpoint_v1.56.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_56_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "GLOBAL_APPROVED_FOR_PAPER_APPROVAL_REVIEW_REPORT_ONLY_APPROVED" in text
        for phrase in [
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize active stock_profile",
            "does not authorize promoted/production models",
            "does not authorize active thresholds",
            "does not authorize advisory predictions/probabilities",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    source_text = Path("SOURCE_UPDATE_NOTES_v1_56_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_research_status_includes_operational_global_approved_for_paper_planning_fields(
    tmp_path: Path,
) -> None:
    created = run_operational_global_approved_for_paper(
        replace(
            _operational_global_approved_for_paper_happy_settings(tmp_path),
            allow_operational_global_approved_for_paper_planning=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert row["status"] == OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED
    assert row["workflow_area"] == "OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER"
    assert result.operational_global_approved_for_paper_workflow_implemented is True
    assert result.operational_global_approved_for_paper_views_implemented is True
    assert result.latest_operational_global_approved_for_paper_id == created.operational_global_approved_for_paper_id
    assert (
        result.latest_operational_global_approved_for_paper_status
        == OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED
    )
    assert result.latest_operational_global_approved_for_paper_health_status == "PASS"
    assert (
        result.latest_operational_global_approved_for_paper_workflow_stage
        == OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED
    )
    assert result.operational_global_approved_for_paper_context_visible is True
    assert result.ready_for_operational_global_approved_for_paper_review is True
    assert result.operational_global_approved_for_paper_executed is True
    assert result.operational_global_approved_for_paper_planning_artifacts_created is True
    assert result.operational_global_approved_for_paper_granted is False
    assert result.operational_global_approved_for_paper_scope == "OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_REPORT_ONLY_PLANNING"
    assert result.operational_global_approved_for_paper_approval_expiry == "2026-07-23T00:00:00Z"
    assert result.operational_global_approved_for_paper_revocation_path == "manual_revocation_report_only_path"
    assert result.global_approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert result.operational_global_approved_for_paper_current_candidates_run is False
    assert result.operational_global_approved_for_paper_snapshot_built is False
    assert result.operational_global_approved_for_paper_signal_semantics_changed is False
    assert result.operational_global_approved_for_paper_active_stock_profile_created is False
    assert result.operational_global_approved_for_paper_promoted_model_created is False
    assert result.operational_global_approved_for_paper_production_model_created is False
    assert result.operational_global_approved_for_paper_active_thresholds_created is False
    assert result.operational_global_approved_for_paper_advisory_predictions_created is False
    assert result.operational_global_approved_for_paper_active_probabilities_created is False
    assert result.operational_global_approved_for_paper_broker_api_called is False
    assert result.operational_global_approved_for_paper_order_placed is False
    assert result.operational_global_approved_for_paper_message_sent is False
    assert result.operational_global_approved_for_paper_llm_api_called is False
    assert result.operational_global_approved_for_paper_external_api_called is False
    assert result.operational_global_approved_for_paper_cache_mutated is False
    assert result.operational_global_approved_for_paper_data_raw_written is False
    assert result.operational_global_approved_for_paper_data_processed_written is False
    assert result.operational_global_approved_for_paper_data_cache_written is False
    assert metadata["latest_operational_global_approved_for_paper_status"] == (
        OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED
    )
    assert metadata["operational_global_approved_for_paper_context_visible"] is True
    assert metadata["operational_global_approved_for_paper_granted"] is False
    assert metadata["operational_global_approved_for_paper_buy_review_allowed"] is False
    assert metadata["operational_global_approved_for_paper_trading_allowed"] is False


def test_research_status_preserves_paper_priority_with_operational_global_approval_context(
    tmp_path: Path,
) -> None:
    run_operational_global_approved_for_paper(
        replace(
            _operational_global_approved_for_paper_happy_settings(tmp_path),
            allow_operational_global_approved_for_paper_planning=True,
        )
    )
    root = _workflow_to_daily(tmp_path / "outputs" / "reports")
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Operational Global APPROVED_FOR_PAPER planning "
            "is report-only context, not operational approval, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert (
        result.latest_operational_global_approved_for_paper_status
        == OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED
    )
    assert result.operational_global_approved_for_paper_granted is False
    assert result.global_approved_for_paper is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False


def test_cli_research_status_prints_operational_global_approval_fields(tmp_path: Path, capsys) -> None:
    run_operational_global_approved_for_paper(
        replace(
            _operational_global_approved_for_paper_happy_settings(tmp_path),
            allow_operational_global_approved_for_paper_planning=True,
        )
    )
    root = tmp_path / "outputs" / "reports"

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "operational_global_approved_for_paper_workflow_implemented: True" in output.out
    assert "operational_global_approved_for_paper_views_implemented: True" in output.out
    assert (
        "latest_operational_global_approved_for_paper_status: "
        f"{OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED}"
    ) in output.out
    assert "operational_global_approved_for_paper_context_visible: True" in output.out
    assert "operational_global_approved_for_paper_planning_artifacts_created: True" in output.out
    assert "operational_global_approved_for_paper_granted: False" in output.out
    assert "global_approved_for_paper: False" in output.out
    assert "operational_global_approved_for_paper_buy_review_allowed: False" in output.out
    assert "operational_global_approved_for_paper_strategy_performance_validated: False" in output.out
    assert "operational_global_approved_for_paper_trading_allowed: False" in output.out
    assert "operational_global_approved_for_paper_current_candidates_run: False" in output.out
    assert "operational_global_approved_for_paper_snapshot_built: False" in output.out
    assert "operational_global_approved_for_paper_signal_semantics_changed: False" in output.out
    assert "operational_global_approved_for_paper_active_stock_profile_created: False" in output.out
    assert "operational_global_approved_for_paper_promoted_model_created: False" in output.out
    assert "operational_global_approved_for_paper_production_model_created: False" in output.out
    assert "operational_global_approved_for_paper_active_thresholds_created: False" in output.out
    assert "operational_global_approved_for_paper_advisory_predictions_created: False" in output.out
    assert "operational_global_approved_for_paper_active_probabilities_created: False" in output.out
    assert "real_buy_review_eligible: False" in output.out
    assert "buy_review_allowed: False" in output.out
    assert "strategy_performance_validated: False" in output.out
    assert "trading_allowed: False" in output.out


def test_operational_global_approved_for_paper_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/operational_global_approved_for_paper.md"),
        Path("docs/release_checkpoint_v1.57.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_57_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "OPERATIONAL_GLOBAL_APPROVED_FOR_PAPER_PLANNING_ARTIFACTS_CREATED" in text
        for phrase in [
            "does not grant operational global APPROVED_FOR_PAPER",
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize active stock_profile",
            "does not authorize promoted/production models",
            "does not authorize active thresholds",
            "does not authorize advisory predictions/probabilities",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    source_text = Path("SOURCE_UPDATE_NOTES_v1_57_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_source_registry_schema_fixture_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/source_registry_schema_fixture.md"),
        Path("docs/release_checkpoint_v1.58.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_58_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "SOURCE_REGISTRY_SCHEMA_FIXTURE_CREATED" in text
        for phrase in [
            "does not create real source permissions",
            "does not create production source registry state",
            "does not fetch real data",
            "does not write data/raw",
            "does not write data/processed",
            "does not write data/cache",
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize active stock_profile",
            "does not authorize promoted/production models",
            "does not authorize active thresholds",
            "does not authorize advisory predictions/probabilities",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    source_text = Path("SOURCE_UPDATE_NOTES_v1_58_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert not Path("docs/project_sources").exists()


def test_raw_document_store_schema_fixture_research_status_docs_and_algorithm_guard_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/raw_document_store_schema_fixture.md"),
        Path("docs/release_checkpoint_v1.59.0.md"),
        Path("docs/quant_research_design_pack_v0_1.md"),
        Path("SOURCE_UPDATE_NOTES_v1_59_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "RAW_DOCUMENT_STORE_SCHEMA_FIXTURE_CREATED" in text
        for phrase in [
            "synthetic/report-only",
            "not production raw_document_store",
            "not real data fetch",
            "not raw document ingestion",
            "not real source permission",
            "does not write data/raw",
            "does not write data/processed",
            "does not write data/cache",
            "does not create factor observations",
            "does not create event ingestion",
            "does not create company exposure",
            "does not create replay evidence bundles",
            "does not create buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    design_text = Path("docs/quant_research_design_pack_v0_1.md").read_text(encoding="utf-8")
    for phrase in [
        "signal_score formula is design reference only",
        "real weights are not calibrated yet",
        "thresholds are not active yet",
        "ML training must wait until PIT-valid factor observations and forward labels exist",
        "factor IC / Rank IC / CAR / event study metrics are evaluation methods, not strategy performance validation by themselves",
        "stock_profile is a validation dossier, not a trade instruction",
        "paper workflow must precede real buy-review",
        "buy-review does not equal trading",
        "no broker/order/API/trading integration is allowed in current scope",
    ]:
        assert phrase in design_text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_59_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT should generate the external Project Source update after user tags v1.59.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_event_structured_schema_fixture_research_status_docs_and_algorithm_guard_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/event_structured_schema_fixture.md"),
        Path("docs/release_checkpoint_v1.62.0.md"),
        Path("docs/quant_research_design_pack_v0_1.md"),
        Path("SOURCE_UPDATE_NOTES_v1_62_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "EVENT_STRUCTURED_SCHEMA_FIXTURE_CREATED" in text
        for phrase in [
            "synthetic/report-only",
            "not production event ingestion",
            "not active event library",
            "not real raw document ingestion",
            "not real source adapter",
            "not factor observation",
            "not production company exposure mapping",
            "not replay evidence bundle",
            "not signal_score implementation",
            "not model training",
            "not active weights",
            "not active thresholds",
            "not stock_profile validation",
            "not paper validation",
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    event_doc = Path("docs/event_structured_schema_fixture.md").read_text(encoding="utf-8")
    for phrase in [
        "event_time / publish_time / available_time are distinct",
        "available_time controls replay eligibility",
        "confidence is extraction/evidence confidence, not return probability",
        "direction is contextual and may require company exposure",
        "do not create factor observations",
        "LLM output must not become deterministic advisory logic",
    ]:
        assert phrase in event_doc
    design_text = Path("docs/quant_research_design_pack_v0_1.md").read_text(encoding="utf-8")
    for phrase in [
        "signal_score formula is design reference only",
        "real weights are not calibrated yet",
        "thresholds are not active yet",
        "ML training must wait until PIT-valid factor observations and forward labels exist",
        "factor IC / Rank IC / CAR / event study metrics are evaluation methods, not strategy performance validation by themselves",
        "stock_profile is a validation dossier, not a trade instruction",
        "paper workflow must precede real buy-review",
        "buy-review does not equal trading",
        "no broker/order/API/trading integration is allowed in current scope",
    ]:
        assert phrase in design_text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_62_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT should generate the external Project Source update after user tags v1.62.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_factor_observation_schema_fixture_research_status_docs_and_algorithm_guard_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/factor_observation_schema_fixture.md"),
        Path("docs/release_checkpoint_v1.63.0.md"),
        Path("docs/quant_research_design_pack_v0_1.md"),
        Path("SOURCE_UPDATE_NOTES_v1_63_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "FACTOR_OBSERVATION_SCHEMA_FIXTURE_CREATED" in text
        for phrase in [
            "synthetic/report-only",
            "not real factor observations",
            "not production factor registry",
            "not active factor library",
            "not production event ingestion",
            "not production company exposure mapping",
            "not real raw document ingestion",
            "not replay evidence bundle",
            "not replay decisions",
            "not forward labels",
            "not signal_score implementation",
            "not model training",
            "not active weights",
            "not active thresholds",
            "not stock_profile validation",
            "not paper validation",
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    factor_doc = Path("docs/factor_observation_schema_fixture.md").read_text(encoding="utf-8")
    for phrase in [
        "observation_date",
        "period_end",
        "source_publish_time",
        "available_time",
        "as_of_date",
        "available_time controls future replay eligibility",
        "x_{i,j,t} = O_j(E_{<=t}, exposure_{i,<=t}, event_{<=t})",
        "factor_definition defines the rule",
        "event_structured may provide context",
        "company_exposure may inform context",
        "confidence is evidence/calculation confidence, not return probability",
        "raw_value is not a signal score",
        "Normalization, winsorization, and direction adjustment are inactive",
        "Risk veto can block actionability",
        "Fixture rows are not buy/sell signals",
    ]:
        assert phrase in factor_doc
    design_text = Path("docs/quant_research_design_pack_v0_1.md").read_text(encoding="utf-8")
    for phrase in [
        "signal_score formula is design reference only",
        "real weights are not calibrated yet",
        "thresholds are not active yet",
        "ML training must wait until PIT-valid factor observations and forward labels exist",
        "factor IC / Rank IC / CAR / event study metrics are evaluation methods, not strategy performance validation by themselves",
        "stock_profile is a validation dossier, not a trade instruction",
        "paper workflow must precede real buy-review",
        "buy-review does not equal trading",
        "no broker/order/API/trading integration is allowed in current scope",
    ]:
        assert phrase in design_text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_63_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT should generate the external Project Source update after user tags v1.63.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_replay_evidence_bundle_schema_fixture_research_status_docs_and_algorithm_guard_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/replay_evidence_bundle_schema_fixture.md"),
        Path("docs/release_checkpoint_v1.64.0.md"),
        Path("docs/quant_research_design_pack_v0_1.md"),
        Path("SOURCE_UPDATE_NOTES_v1_64_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "REPLAY_EVIDENCE_BUNDLE_SCHEMA_FIXTURE_CREATED" in text
        for phrase in [
            "synthetic/report-only",
            "not real replay evidence bundles",
            "not replay decisions",
            "not forward labels",
            "not future labels",
            "not production factor observations",
            "not real factor observations",
            "not production factor registry",
            "not active factor library",
            "not production event ingestion",
            "not active event library",
            "not production company exposure mapping",
            "not real raw document ingestion",
            "not signal_score implementation",
            "not authorized signal_score input",
            "not model training",
            "not active weights",
            "not active thresholds",
            "not stock_profile validation",
            "not paper validation",
            "not real buy-review eligibility",
            "does not set buy_review_allowed",
            "not strategy performance validation",
            "does not authorize current-candidates",
            "does not authorize snapshots",
            "does not authorize signal_semantics mutation",
            "does not authorize broker/order/message/API/trading",
        ]:
            assert phrase in text, f"{path}: {phrase}"
    replay_doc = Path("docs/replay_evidence_bundle_schema_fixture.md").read_text(encoding="utf-8")
    for phrase in [
        "replay_decision_time",
        "replay_as_of_date",
        "available_time_max",
        "revision_id",
        "available_time_max must be less than or equal to the future replay decision time",
        "source registry references are schema references",
        "raw document references are schema references",
        "factor definition references are schema references",
        "company exposure references are schema references",
        "event structured references are schema references",
        "factor observation references are schema references",
        "Evidence admissibility and risk veto fields are governance placeholders",
        "Fixture rows are not buy/sell signals",
    ]:
        assert phrase in replay_doc
    design_text = Path("docs/quant_research_design_pack_v0_1.md").read_text(encoding="utf-8")
    for phrase in [
        "signal_score formula is design reference only",
        "real weights are not calibrated yet",
        "thresholds are not active yet",
        "ML training must wait until PIT-valid factor observations and forward labels exist",
        "factor IC / Rank IC / CAR / event study metrics are evaluation methods, not strategy performance validation by themselves",
        "stock_profile is a validation dossier, not a trade instruction",
        "paper workflow must precede real buy-review",
        "buy-review does not equal trading",
        "no broker/order/API/trading integration is allowed in current scope",
    ]:
        assert phrase in design_text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_64_0.md").read_text(encoding="utf-8")
    assert "do not include src/ or tests/ in ChatGPT Project Source upload lists" in source_text
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT should generate the external Project Source update after user tags v1.64.0" in source_text
    assert not Path("docs/project_sources").exists()


def test_approved_for_paper_phase1_research_status_docs_and_source_notes_are_safe() -> None:
    docs = [
        Path("README.md"),
        Path("docs/local_research_dashboard.md"),
        Path("docs/approved_for_paper_phase1.md"),
        Path("docs/release_checkpoint_v1.55.0.md"),
        Path("SOURCE_UPDATE_NOTES_v1_55_0.md"),
    ]
    for path in docs:
        text = path.read_text(encoding="utf-8")
        assert "APPROVED_FOR_PAPER_PHASE1_REPORT_ONLY_ARTIFACTS_CREATED" in text
        for phrase in [
            "not global APPROVED_FOR_PAPER",
            "not real buy-review",
            "not strategy performance validation",
            "not current-candidates",
            "not snapshot",
            "not signal_semantics",
            "not active stock_profile",
            "not promoted model",
            "not production model",
            "not active thresholds",
            "not advisory predictions",
            "not active probabilities",
            "not trading",
        ]:
            assert phrase in text
    source_text = Path("SOURCE_UPDATE_NOTES_v1_55_0.md").read_text(encoding="utf-8")
    assert "docs/project_sources/ is intentionally absent from Git" in source_text
    assert "ChatGPT Project Source is maintained separately" in source_text
    assert not Path("docs/project_sources").exists()


def test_cli_research_status_works(tmp_path: Path, capsys) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "Research status:" in output.out
    assert "Report path:" in output.out


def test_cli_prints_next_manual_action(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "next_manual_action: Run data-pipeline." in output.out


def test_cli_prints_no_live_trading_statement(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "No live trading or broker API was invoked." in output.out


def test_dashboard_output_is_deterministic(tmp_path: Path) -> None:
    root = _workflow_complete(_reports_root(tmp_path))

    first = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    second = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert first.dashboard_id == second.dashboard_id
    assert first.dashboard_frame.to_dict("records") == second.dashboard_frame.to_dict("records")
    assert first.summary_frame.to_dict("records") == second.summary_frame.to_dict("records")


def test_dashboard_no_live_trading_or_broker_integration_is_invoked(tmp_path: Path) -> None:
    root = _workflow_complete(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.audit_metadata["live_trading_enabled"] is False
    assert result.audit_metadata["broker_api_invoked"] is False
    assert result.audit_metadata["local_research_dashboard_only"] is True
    assert not any("broker" in module_name.lower() for module_name in sys.modules)


def test_dashboard_no_real_network_api_calls_are_used(tmp_path: Path) -> None:
    root = _workflow_complete(_reports_root(tmp_path))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.status == "PASS"
    assert result.audit_metadata["network_api_calls_used_in_tests"] is False


def test_dashboard_prefers_active_review_chain_over_stale_warn_artifacts(tmp_path: Path) -> None:
    root = _workflow_to_review_template(_reports_root(tmp_path))
    _review_template_health(root, health_id="health-stale", status="WARN", warnings=1)
    _paper_review(
        root,
        review_id="review-stale",
        template_health={
            "template_health_check_id": "health-stale",
            "template_health_status": "WARN",
            "template_health_report_path": str(
                root / "paper_trading" / "review_template_health" / "health-stale" / "review_template_health_report.md"
            ),
            "template_health_issue_count": 1,
            "template_health_error_count": 0,
            "template_health_warning_count": 1,
        },
        warnings=["stale review warning"],
    )
    _review_template_health(root, health_id="health-active", status="PASS")
    active_review = _paper_review(
        root,
        review_id="review-active",
        template_health={
            "template_health_check_id": "health-active",
            "template_health_status": "PASS",
            "template_health_report_path": str(
                root / "paper_trading" / "review_template_health" / "health-active" / "review_template_health_report.md"
            ),
            "template_health_issue_count": 0,
            "template_health_error_count": 0,
            "template_health_warning_count": 0,
        },
    )
    _daily_paper(root, reviewed_decisions_path=active_review / "reviewed_decisions.csv")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    by_component = {row["component"]: row for row in result.dashboard_frame.to_dict("records")}

    assert by_component["PAPER_REVIEW"]["latest_artifact_id"] == "review-active"
    assert by_component["REVIEW_TEMPLATE_HEALTH"]["latest_artifact_id"] == "health-active"
    assert result.paper_review_status == "READY"
    assert "stale_warning_count" in by_component["REVIEW_TEMPLATE_HEALTH"]["notes"]
    assert result.workflow_stage != "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert int(result.summary_frame.iloc[0]["stale_warning_count"]) > 0


def test_dashboard_prefers_active_reconciliation_over_unlinked_diagnostic_failure(tmp_path: Path) -> None:
    root = _workflow_to_review_template(_reports_root(tmp_path))
    _review_template_health(root, health_id="health-active", status="PASS")
    active_review = _paper_review(
        root,
        review_id="review-active",
        template_health={
            "template_health_check_id": "health-active",
            "template_health_status": "PASS",
            "template_health_report_path": str(
                root / "paper_trading" / "review_template_health" / "health-active" / "review_template_health_report.md"
            ),
            "template_health_issue_count": 0,
            "template_health_error_count": 0,
            "template_health_warning_count": 0,
        },
    )
    active_reconciliation = _reconciliation(root, reconciliation_id="recon-active", status="PASS")
    _reconciliation(
        root,
        reconciliation_id="recon-diagnostic",
        status="FAIL",
        errors=1,
        created_at=f"{DECISION_DATE}T16:20:00",
    )
    _daily_paper(
        root,
        reviewed_decisions_path=active_review / "reviewed_decisions.csv",
        reconciliation_report_path=active_reconciliation / "reconciliation_report.md",
        reconciliation_status="PASS",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    by_component = {row["component"]: row for row in result.dashboard_frame.to_dict("records")}

    assert by_component["RECONCILIATION"]["latest_artifact_id"] == "recon-active"
    assert by_component["RECONCILIATION"]["status"] == "PASS"
    assert result.reconciliation_status == "PASS"
    assert result.status != "FAIL"
    assert result.workflow_stage != "LOCAL_RESEARCH_NEEDS_ATTENTION"


def test_dashboard_inherits_expected_demo_warning_actionability_from_paper_workflow_status(tmp_path: Path) -> None:
    root = _workflow_to_daily(_reports_root(tmp_path))
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WORKFLOW_NEEDS_ATTENTION",
        expected_demo_warning_count=1,
        stale_warning_count=0,
        actionable_warning_count=0,
        blocking_error_count=0,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    paper_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "PAPER_WORKFLOW_STATUS"].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.status == "WARN"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action
    assert paper_row["warning_classification"] == "EXPECTED_DEMO_WARNING"
    assert summary["expected_demo_warning_count"] == 1
    assert summary["actionable_warning_count"] == 0


def test_dashboard_prefers_newer_paper_workflow_status_artifact_when_created_at_is_deterministic(
    tmp_path: Path,
) -> None:
    root = _workflow_to_daily(_reports_root(tmp_path))
    _reconciliation(root, status="PASS")
    stale = _paper_workflow_status(
        root,
        workflow_status_id="workflow-status-stale",
        status="FAIL",
        workflow_stage="WORKFLOW_NEEDS_ATTENTION",
        blocking_error_count=1,
        next_manual_action="Review warnings/errors in workflow status and health reports.",
        created_at="1970-01-01T00:00:00+00:00",
    )
    latest = _paper_workflow_status(
        root,
        workflow_status_id="workflow-status-latest",
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
        created_at="1970-01-01T00:00:00+00:00",
    )
    os.utime(stale / "metadata.json", (100, 100))
    os.utime(latest / "metadata.json", (200, 200))

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    paper_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "PAPER_WORKFLOW_STATUS"].iloc[0]

    assert paper_row["latest_artifact_id"] == "workflow-status-latest"
    assert result.paper_workflow_status == "WARN"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_classifies_prior_current_candidate_health_warning_as_stale(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))
    _current_candidate_health_with_issues(
        root,
        status="WARN",
        issues=[
            {
                "artifact_type": "CURRENT_CANDIDATES",
                "run_id": "old-dry-run",
                "path_field": "candidates_path",
                "path_value": str(root / "current_candidates" / "old" / "candidates.csv"),
                "severity": "WARN",
                "issue_code": "CSV_EMPTY",
                "issue_message": "candidates.csv has no rows.",
                "suggested_action": "Confirm no candidates passed filters.",
            }
        ],
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    health_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "CURRENT_CANDIDATE_HEALTH"].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "CURRENT_CANDIDATES_HEALTH_READY"
    assert result.next_manual_action == "Run current-to-paper."
    assert health_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert summary["stale_warning_count"] == 1
    assert summary["actionable_warning_count"] == 0


def test_dashboard_includes_market_update_handoff_status_when_no_paper_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_status(root, status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    handoff_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_UPDATE_HANDOFF_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST"
    assert result.market_update_handoff_status == "WARN"
    assert result.latest_market_update_handoff_id == "handoff-a"
    assert result.market_update_handoff_stage == "CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST"
    assert result.market_update_handoff_current_candidate_run_id == "candidate-a"
    assert "current-to-paper" in result.next_manual_action
    assert handoff_row["warning_classification"] == "EXPECTED_DEMO_WARNING"
    assert summary["market_update_handoff_pipeline_id"] == "pipeline-a"
    assert summary["market_update_handoff_snapshot_quality_status"] == "PASS"
    assert summary["actionable_warning_count"] == 0


def test_dashboard_includes_historical_backfill_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(root, status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    backfill_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_BACKFILL_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "BACKFILL_WARNINGS_NEED_REVIEW"
    assert result.status == "WARN"
    assert result.latest_historical_backfill_id == "backfill-a"
    assert result.historical_backfill_status == "WARN"
    assert result.historical_backfill_stage == "BACKFILL_WARNINGS_NEED_REVIEW"
    assert result.historical_backfill_task_count == 6
    assert result.historical_backfill_pass_count == 3
    assert result.historical_backfill_warn_count == 3
    assert result.historical_backfill_fail_count == 0
    assert result.historical_backfill_skipped_count == 0
    assert result.historical_backfill_cache_write_occurred is False
    assert "Review WARN tasks" in result.next_manual_action
    assert backfill_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert summary["expected_reviewable_warning_count"] == 1
    assert summary["actionable_warning_count"] == 0


def test_dashboard_preserves_paper_priority_with_historical_backfill_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(root, status="WARN", warning_count=1)
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    backfill_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_BACKFILL_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.paper_workflow_status == "PASS"
    assert result.latest_historical_backfill_id == "backfill-a"
    assert result.historical_backfill_status == "WARN"
    assert backfill_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Review completed" in result.next_manual_action


def test_dashboard_marks_active_failed_historical_backfill_as_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(
        root,
        status="FAIL",
        workflow_stage="BACKFILL_FAILED",
        health_status="FAIL",
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    backfill_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_BACKFILL_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "BACKFILL_FAILED"
    assert result.latest_historical_backfill_id == "backfill-a"
    assert backfill_row["warning_classification"] == "BLOCKING_ERROR"
    assert "failed backfill" in result.next_manual_action


def test_dashboard_treats_partial_backfill_rejections_as_reviewable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(
        root,
        status="WARN",
        workflow_stage="BACKFILL_PARTIAL_WITH_REJECTIONS",
        health_status="PASS",
        warning_count=1,
        cache_write_occurred=True,
        accepted_task_count=8,
        rejected_task_count=2,
        preflight_rejected_count=2,
        comparison_failed_count=2,
        cache_write_partial=True,
        rejected_symbols="300750,688981",
        rejected_sources="BAOSTOCK_OPTIONAL",
        rejected_issue_categories="BLOCKED_PREFLIGHT_REJECT,COMPARISON_FAIL",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    backfill_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_BACKFILL_STATUS"
    ].iloc[0]

    assert result.status == "WARN"
    assert result.workflow_stage == "BACKFILL_PARTIAL_WITH_REJECTIONS"
    assert result.historical_backfill_cache_write_partial is True
    assert result.historical_backfill_accepted_task_count == 8
    assert result.historical_backfill_rejected_task_count == 2
    assert result.historical_backfill_preflight_rejected_count == 2
    assert result.historical_backfill_comparison_failed_count == 2
    assert result.historical_backfill_rejected_symbols == "300750,688981"
    assert backfill_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Review rejected rows" in result.next_manual_action


def test_dashboard_preserves_paper_priority_with_partial_backfill_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(
        root,
        status="WARN",
        workflow_stage="BACKFILL_PARTIAL_WITH_REJECTIONS",
        health_status="PASS",
        warning_count=1,
        cache_write_occurred=True,
        accepted_task_count=8,
        rejected_task_count=2,
        preflight_rejected_count=2,
        comparison_failed_count=2,
        cache_write_partial=True,
        rejected_symbols="300750,688981",
        rejected_sources="BAOSTOCK_OPTIONAL",
        rejected_issue_categories="BLOCKED_PREFLIGHT_REJECT,COMPARISON_FAIL",
    )
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    backfill_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "HISTORICAL_BACKFILL_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.historical_backfill_stage == "BACKFILL_PARTIAL_WITH_REJECTIONS"
    assert result.historical_backfill_rejected_task_count == 2
    assert backfill_row["warning_classification"] == "STALE_ARTIFACT_WARNING"


def test_dashboard_summary_csv_exports_historical_backfill_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(root, backfill_id="backfill-export", status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    row = exported.iloc[0].to_dict()

    assert row["latest_historical_backfill_id"] == "backfill-export"
    assert row["historical_backfill_status"] == "WARN"
    assert row["historical_backfill_stage"] == "BACKFILL_WARNINGS_NEED_REVIEW"
    assert "Review WARN tasks" in row["historical_backfill_next_action"]
    assert row["historical_backfill_task_count"] == "6"
    assert row["historical_backfill_pass_count"] == "3"
    assert row["historical_backfill_warn_count"] == "3"
    assert row["historical_backfill_fail_count"] == "0"
    assert row["historical_backfill_skipped_count"] == "0"
    assert row["historical_backfill_cache_write_occurred"] == "False"
    assert row["historical_backfill_rejected_task_count"] == "0"
    assert row["historical_backfill_cache_write_partial"] == "False"
    assert row["historical_backfill_report_path"]


def test_dashboard_metadata_exports_historical_backfill_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(root, backfill_id="backfill-metadata", status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    component_statuses = metadata["component_statuses"]

    assert metadata["latest_historical_backfill_id"] == "backfill-metadata"
    assert metadata["historical_backfill_status"] == "WARN"
    assert metadata["historical_backfill_stage"] == "BACKFILL_WARNINGS_NEED_REVIEW"
    assert "Review WARN tasks" in metadata["historical_backfill_next_action"]
    assert metadata["historical_backfill_task_count"] == 6
    assert metadata["historical_backfill_warn_count"] == 3
    assert metadata["historical_backfill_cache_write_occurred"] is False
    assert metadata["historical_backfill_rejected_task_count"] == 0
    assert metadata["historical_backfill_cache_write_partial"] is False
    assert component_statuses["latest_historical_backfill_id"] == "backfill-metadata"
    assert component_statuses["expected_reviewable_warning_count"] == 1


def test_dashboard_includes_market_cache_export_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-a", status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    export_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_EXPORT"
    assert result.market_cache_export_status == "PASS"
    assert result.latest_market_cache_export_id == "export-a"
    assert result.market_cache_export_stage == "SNAPSHOT_READY_FROM_EXPORT"
    assert result.market_cache_export_pipeline_id == "pipeline-export-a"
    assert result.market_cache_export_data_pipeline_status == "PASS"
    assert result.market_cache_export_data_quality_status == "PASS"
    assert result.market_cache_export_snapshot_quality_status == "PASS"
    assert result.linked_snapshot_quality_status == "PASS"
    assert result.active_snapshot_chain == "MARKET_CACHE_EXPORT_STATUS"
    assert result.market_cache_export_snapshot_manifest_path.endswith("snapshot_manifest.json")
    assert "current-candidates" in result.next_manual_action
    assert export_row["warning_classification"] == ""
    assert summary["market_cache_export_report_path"]


def test_dashboard_preserves_current_candidate_priority_with_market_cache_export_context(tmp_path: Path) -> None:
    root = _workflow_to_current_candidates(_reports_root(tmp_path))
    _market_cache_export_status(root, export_id="export-context", status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "CURRENT_CANDIDATES_READY"
    assert result.current_candidate_status == "READY"
    assert result.latest_market_cache_export_id == "export-context"
    assert result.market_cache_export_status == "PASS"
    assert result.market_cache_export_snapshot_quality_status == "PASS"


def test_dashboard_preserves_paper_priority_with_market_cache_export_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-paper-context", status="FAIL", error_count=1)
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    export_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.status == "WARN"
    assert result.paper_workflow_status == "PASS"
    assert result.latest_market_cache_export_id == "export-paper-context"
    assert export_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Review completed" in result.next_manual_action


def test_dashboard_uses_latest_market_cache_export_when_older_export_failed(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(
        root,
        export_id="export-old",
        status="FAIL",
        workflow_stage="CACHE_EXPORT_FAILED",
        health_status="FAIL",
        error_count=1,
        created_at=f"{DECISION_DATE}T14:00:00",
    )
    _market_cache_export_status(
        root,
        export_id="export-new",
        status="PASS",
        created_at=f"{DECISION_DATE}T15:00:00",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    export_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_EXPORT"
    assert result.latest_market_cache_export_id == "export-new"
    assert result.market_cache_export_status == "PASS"
    assert export_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "stale_error_count=" in export_row["notes"]
    assert summary["stale_warning_count"] > 0
    assert summary["blocking_error_count"] == 0


def test_dashboard_active_export_snapshot_pass_not_overridden_by_stale_snapshot_warn(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-pass", status="PASS", snapshot_quality_status="PASS")
    _snapshot_quality(root, status="WARN")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    snapshot_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SNAPSHOT_QUALITY"].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_EXPORT"
    assert result.next_manual_action == "Run current-candidates with the reviewed cache export snapshot manifest."
    assert result.linked_snapshot_quality_status == "PASS"
    assert result.active_snapshot_chain == "MARKET_CACHE_EXPORT_STATUS"
    assert result.active_snapshot_warning_count == 0
    assert result.active_snapshot_error_count == 0
    assert result.unrelated_snapshot_warning_count > 0
    assert snapshot_row["warning_classification"] == "LINKED_SNAPSHOT_PASS;UNRELATED_SNAPSHOT_WARNING"
    assert summary["actionable_warning_count"] == 0
    assert summary["blocking_error_count"] == 0


def test_dashboard_active_export_snapshot_warn_remains_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-warn", status="PASS", snapshot_quality_status="WARN")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    snapshot_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SNAPSHOT_QUALITY"].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.linked_snapshot_quality_status == "WARN"
    assert result.active_snapshot_chain == "MARKET_CACHE_EXPORT_STATUS"
    assert result.active_snapshot_warning_count == 1
    assert result.active_snapshot_error_count == 0
    assert snapshot_row["warning_classification"] == "ACTIVE_SNAPSHOT_WARNING"
    assert result.next_manual_action == "Review warnings/errors."


def test_dashboard_active_export_snapshot_fail_is_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-fail-snapshot", status="PASS", snapshot_quality_status="FAIL")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    snapshot_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SNAPSHOT_QUALITY"].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.linked_snapshot_quality_status == "FAIL"
    assert result.active_snapshot_error_count == 1
    assert snapshot_row["warning_classification"] == "ACTIVE_SNAPSHOT_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_dashboard_standalone_snapshot_warn_remains_actionable_without_active_chain(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _snapshot_quality(root, status="WARN")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    snapshot_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SNAPSHOT_QUALITY"].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.linked_snapshot_quality_status == "WARN"
    assert result.active_snapshot_chain == "SNAPSHOT_QUALITY"
    assert result.active_snapshot_warning_count == 1
    assert snapshot_row["warning_classification"] == "ACTIVE_SNAPSHOT_WARNING"


def test_dashboard_paper_priority_treats_unlinked_snapshot_warn_as_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _paper_workflow_status(root, status="PASS")
    _snapshot_quality(root, status="WARN")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    snapshot_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SNAPSHOT_QUALITY"].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.paper_workflow_status == "PASS"
    assert result.active_snapshot_chain == "PAPER_WORKFLOW_STATUS"
    assert result.unrelated_snapshot_warning_count > 0
    assert snapshot_row["warning_classification"] == "MISSING_LINKED_SNAPSHOT;UNRELATED_SNAPSHOT_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0


def test_dashboard_marks_active_failed_market_cache_export_as_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(
        root,
        export_id="export-fail",
        status="FAIL",
        workflow_stage="CACHE_EXPORT_FAILED",
        health_status="FAIL",
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    export_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "CACHE_EXPORT_FAILED"
    assert result.latest_market_cache_export_id == "export-fail"
    assert export_row["warning_classification"] == "BLOCKING_ERROR"
    assert "market-cache-export-health errors" in result.next_manual_action


def test_dashboard_summary_csv_exports_market_cache_export_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-summary", status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    row = exported.iloc[0].to_dict()

    assert row["latest_market_cache_export_id"] == "export-summary"
    assert row["market_cache_export_status"] == "PASS"
    assert row["market_cache_export_stage"] == "SNAPSHOT_READY_FROM_EXPORT"
    assert "current-candidates" in row["market_cache_export_next_action"]
    assert row["market_cache_export_pipeline_id"] == "pipeline-export-summary"
    assert row["market_cache_export_data_pipeline_status"] == "PASS"
    assert row["market_cache_export_data_quality_status"] == "PASS"
    assert row["market_cache_export_snapshot_quality_status"] == "PASS"
    assert row["market_cache_export_snapshot_manifest_path"].endswith("snapshot_manifest.json")
    assert row["market_cache_export_report_path"]
    assert row["linked_snapshot_quality_status"] == "PASS"
    assert row["active_snapshot_chain"] == "MARKET_CACHE_EXPORT_STATUS"
    assert row["active_snapshot_warning_count"] == "0"
    assert row["active_snapshot_error_count"] == "0"
    assert row["stale_snapshot_warning_count"] == "0"
    assert row["unrelated_snapshot_warning_count"] == "0"


def test_dashboard_includes_market_cache_export_plan_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-a", status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_POLICY_PLAN"
    assert result.status == "WARN"
    assert result.latest_market_cache_export_plan_id == "plan-a"
    assert result.market_cache_export_plan_status == "WARN"
    assert result.market_cache_export_plan_stage == "SNAPSHOT_READY_FROM_POLICY_PLAN"
    assert result.market_cache_export_plan_recommendation_count == 2
    assert result.market_cache_export_plan_recommended_count == 1
    assert result.market_cache_export_plan_warning_count == 1
    assert result.market_cache_export_plan_comparison_pass_count == 1
    assert result.market_cache_export_plan_comparison_unavailable_count == 1
    assert result.market_cache_export_plan_comparison_fail_count == 0
    assert result.market_cache_export_plan_comparison_supported_recommendation_count == 1
    assert result.market_cache_export_plan_comparison_unsupported_recommendation_count == 1
    assert result.market_cache_export_plan_downstream_export_id == "export-plan-a"
    assert result.market_cache_export_plan_downstream_snapshot_quality_status == "PASS"
    assert result.linked_snapshot_quality_status == "PASS"
    assert result.active_snapshot_chain == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    assert "current-candidates" in result.next_manual_action
    assert plan_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert summary["actionable_warning_count"] == 0


def test_dashboard_marks_active_policy_plan_stock_comparison_fail_as_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(
        root,
        plan_id="plan-comparison-fail",
        status="WARN",
        workflow_stage="POLICY_PLAN_COMPARISON_WARNINGS_NEED_REVIEW",
        warning_count=1,
        comparison_pass_count=0,
        comparison_fail_count=1,
        comparison_unavailable_count=0,
        comparison_supported_recommendation_count=1,
        comparison_unsupported_recommendation_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    ].iloc[0]
    summary = result.summary_frame.iloc[0].to_dict()

    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.latest_market_cache_export_plan_id == "plan-comparison-fail"
    assert result.market_cache_export_plan_comparison_fail_count == 1
    assert plan_row["warning_classification"] == "ACTIONABLE_WARNING"
    assert summary["actionable_warning_count"] > 0


def test_dashboard_preserves_market_cache_export_priority_with_policy_plan_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-context", status="FAIL", error_count=1)
    _market_cache_export_status(root, export_id="export-context", status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_EXPORT"
    assert result.market_cache_export_status == "PASS"
    assert result.latest_market_cache_export_plan_id == "plan-context"
    assert result.market_cache_export_plan_comparison_pass_count == 1
    assert result.market_cache_export_plan_comparison_unavailable_count == 1
    assert plan_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0


def test_dashboard_preserves_paper_priority_with_market_cache_export_plan_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-paper-context", status="WARN", warning_count=1)
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.paper_workflow_status == "PASS"
    assert result.latest_market_cache_export_plan_id == "plan-paper-context"
    assert result.market_cache_export_plan_status == "WARN"
    assert result.market_cache_export_plan_comparison_pass_count == 1
    assert result.market_cache_export_plan_comparison_unavailable_count == 1
    assert result.summary_frame.iloc[0]["stale_warning_count"] > 0
    assert "Review completed" in result.next_manual_action


def test_dashboard_marks_active_failed_market_cache_export_plan_as_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(
        root,
        plan_id="plan-fail",
        status="FAIL",
        workflow_stage="POLICY_PLAN_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "POLICY_PLAN_FAILED"
    assert result.latest_market_cache_export_plan_id == "plan-fail"
    assert plan_row["warning_classification"] == "BLOCKING_ERROR"
    assert "market-cache-export-plan-health errors" in result.next_manual_action


def test_dashboard_uses_latest_market_cache_export_plan_when_older_plan_failed(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(
        root,
        plan_id="plan-old",
        status="FAIL",
        workflow_stage="POLICY_PLAN_FAILED",
        health_status="FAIL",
        error_count=1,
        created_at=f"{DECISION_DATE}T14:00:00",
    )
    _market_cache_export_policy_status(
        root,
        plan_id="plan-new",
        status="WARN",
        workflow_stage="SNAPSHOT_READY_FROM_POLICY_PLAN",
        warning_count=1,
        created_at=f"{DECISION_DATE}T15:00:00",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    plan_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_CACHE_EXPORT_POLICY_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "SNAPSHOT_READY_FROM_POLICY_PLAN"
    assert result.latest_market_cache_export_plan_id == "plan-new"
    assert result.market_cache_export_plan_status == "WARN"
    assert plan_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "stale_error_count=" in plan_row["notes"]
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0


def test_dashboard_summary_csv_exports_market_cache_export_plan_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-summary", status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    row = exported.iloc[0].to_dict()

    assert row["latest_market_cache_export_plan_id"] == "plan-summary"
    assert row["market_cache_export_plan_status"] == "WARN"
    assert row["market_cache_export_plan_stage"] == "SNAPSHOT_READY_FROM_POLICY_PLAN"
    assert row["market_cache_export_plan_recommendation_count"] == "2"
    assert row["market_cache_export_plan_recommended_count"] == "1"
    assert row["market_cache_export_plan_warning_count"] == "1"
    assert row["market_cache_export_plan_comparison_pass_count"] == "1"
    assert row["market_cache_export_plan_comparison_warn_count"] == "0"
    assert row["market_cache_export_plan_comparison_fail_count"] == "0"
    assert row["market_cache_export_plan_comparison_unavailable_count"] == "1"
    assert row["market_cache_export_plan_comparison_required_but_missing_count"] == "0"
    assert row["market_cache_export_plan_comparison_supported_recommendation_count"] == "1"
    assert row["market_cache_export_plan_comparison_unsupported_recommendation_count"] == "1"
    assert row["market_cache_export_plan_generated_manifest_path"].endswith(
        "market_cache_export_recommended_plan-summary.csv"
    )
    assert row["market_cache_export_plan_downstream_export_id"] == "export-plan-summary"
    assert row["market_cache_export_plan_downstream_snapshot_quality_status"] == "PASS"


def test_dashboard_metadata_exports_market_cache_export_plan_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-metadata", status="WARN", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    component_statuses = metadata["component_statuses"]

    assert metadata["latest_market_cache_export_plan_id"] == "plan-metadata"
    assert metadata["market_cache_export_plan_status"] == "WARN"
    assert metadata["market_cache_export_plan_stage"] == "SNAPSHOT_READY_FROM_POLICY_PLAN"
    assert metadata["market_cache_export_plan_recommendation_count"] == 2
    assert metadata["market_cache_export_plan_recommended_count"] == 1
    assert metadata["market_cache_export_plan_warning_count"] == 1
    assert metadata["market_cache_export_plan_comparison_pass_count"] == 1
    assert metadata["market_cache_export_plan_comparison_warn_count"] == 0
    assert metadata["market_cache_export_plan_comparison_fail_count"] == 0
    assert metadata["market_cache_export_plan_comparison_unavailable_count"] == 1
    assert metadata["market_cache_export_plan_comparison_required_but_missing_count"] == 0
    assert metadata["market_cache_export_plan_comparison_supported_recommendation_count"] == 1
    assert metadata["market_cache_export_plan_comparison_unsupported_recommendation_count"] == 1
    assert metadata["market_cache_export_plan_downstream_export_id"] == "export-plan-metadata"
    assert metadata["market_cache_export_plan_downstream_snapshot_quality_status"] == "PASS"
    assert component_statuses["latest_market_cache_export_plan_id"] == "plan-metadata"
    assert component_statuses["market_cache_export_plan_comparison_pass_count"] == 1
    assert component_statuses["market_cache_export_plan_comparison_unavailable_count"] == 1
    assert component_statuses["market_cache_export_plan_generated_manifest_path"].endswith(
        "market_cache_export_recommended_plan-metadata.csv"
    )


def test_dashboard_metadata_exports_market_cache_export_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-metadata", status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    component_statuses = metadata["component_statuses"]

    assert metadata["latest_market_cache_export_id"] == "export-metadata"
    assert metadata["market_cache_export_status"] == "PASS"
    assert metadata["market_cache_export_stage"] == "SNAPSHOT_READY_FROM_EXPORT"
    assert "current-candidates" in metadata["market_cache_export_next_action"]
    assert metadata["market_cache_export_pipeline_id"] == "pipeline-export-metadata"
    assert metadata["market_cache_export_data_pipeline_status"] == "PASS"
    assert metadata["market_cache_export_data_quality_status"] == "PASS"
    assert metadata["market_cache_export_snapshot_quality_status"] == "PASS"
    assert metadata["market_cache_export_snapshot_manifest_path"].endswith("snapshot_manifest.json")
    assert metadata["linked_snapshot_quality_status"] == "PASS"
    assert metadata["active_snapshot_chain"] == "MARKET_CACHE_EXPORT_STATUS"
    assert metadata["active_snapshot_warning_count"] == 0
    assert metadata["active_snapshot_error_count"] == 0
    assert metadata["stale_snapshot_warning_count"] == 0
    assert metadata["unrelated_snapshot_warning_count"] == 0
    assert component_statuses["latest_market_cache_export_id"] == "export-metadata"
    assert component_statuses["market_cache_export_pipeline_id"] == "pipeline-export-metadata"


def test_cli_research_status_prints_market_cache_export_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_status(root, export_id="export-cli", status="PASS")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_market_cache_export_id: export-cli" in output.out
    assert "market_cache_export_status: PASS" in output.out
    assert "market_cache_export_stage: SNAPSHOT_READY_FROM_EXPORT" in output.out
    assert "market_cache_export_pipeline_id: pipeline-export-cli" in output.out
    assert "market_cache_export_snapshot_quality_status: PASS" in output.out
    assert "active_snapshot_chain: MARKET_CACHE_EXPORT_STATUS" in output.out
    assert "linked_snapshot_quality_status: PASS" in output.out


def test_cli_research_status_prints_market_cache_export_plan_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _market_cache_export_policy_status(root, plan_id="plan-cli", status="WARN", warning_count=1)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_market_cache_export_plan_id: plan-cli" in output.out
    assert "market_cache_export_plan_status: WARN" in output.out
    assert "market_cache_export_plan_stage: SNAPSHOT_READY_FROM_POLICY_PLAN" in output.out
    assert "market_cache_export_plan_comparison_pass_count: 1" in output.out
    assert "market_cache_export_plan_comparison_unavailable_count: 1" in output.out
    assert "market_cache_export_plan_downstream_export_id: export-plan-cli" in output.out
    assert "market_cache_export_plan_downstream_snapshot_quality_status: PASS" in output.out


def test_cli_research_status_prints_historical_backfill_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _historical_backfill_status(root, backfill_id="backfill-cli", status="WARN", warning_count=1)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_historical_backfill_id: backfill-cli" in output.out
    assert "historical_backfill_status: WARN" in output.out
    assert "historical_backfill_stage: BACKFILL_WARNINGS_NEED_REVIEW" in output.out
    assert "historical_backfill_rejected_task_count: 0" in output.out


def test_dashboard_does_not_regress_from_paper_workflow_to_market_update_handoff(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_status(
        root,
        status="FAIL",
        workflow_stage="HANDOFF_ARTIFACTS_NEED_REPAIR",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )
    _paper_workflow_status(root, status="PASS")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    handoff_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_UPDATE_HANDOFF_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_WORKFLOW_COMPLETE"
    assert result.status == "WARN"
    assert result.paper_workflow_status == "PASS"
    assert handoff_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Review completed" in result.next_manual_action


def test_dashboard_marks_active_broken_market_update_handoff_as_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_status(
        root,
        status="FAIL",
        workflow_stage="HANDOFF_ARTIFACTS_NEED_REPAIR",
        health_status="FAIL",
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    handoff_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "MARKET_UPDATE_HANDOFF_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "LOCAL_RESEARCH_NEEDS_ATTENTION"
    assert result.status == "FAIL"
    assert result.next_manual_action == "Review warnings/errors."
    assert handoff_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_cli_research_status_prints_market_update_handoff_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _market_update_handoff_status(root, status="WARN", warning_count=1)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_market_update_handoff_id: handoff-a" in output.out
    assert "market_update_handoff_status: WARN" in output.out
    assert "market_update_handoff_stage: CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST" in output.out
    assert "market_update_handoff_current_candidate_run_id: candidate-a" in output.out


def test_dashboard_includes_advisory_profile_calibration_status_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(root, calibration_id="calibration-reviewed")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    calibration_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_PROFILE_CALIBRATION_STATUS"
    ].iloc[0]

    assert result.latest_advisory_profile_calibration_run_id == "calibration-reviewed"
    assert result.advisory_profile_calibration_status == "WARN"
    assert result.advisory_profile_calibration_stage == "ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW"
    assert result.advisory_profile_calibration_health_status == "PASS"
    assert result.advisory_profile_calibration_profile == "balanced"
    assert result.advisory_profile_calibration_review_buy_candidate_count == 1
    assert result.advisory_profile_calibration_watch_count == 2
    assert result.advisory_profile_calibration_no_action_count == 1
    assert result.advisory_profile_calibration_blocked_count == 2
    assert result.advisory_profile_calibration_issue_count == 2
    assert result.workflow_stage == "ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW"
    assert calibration_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "REVIEW_BUY_CANDIDATE is not an order" in result.next_manual_action


def test_dashboard_advisory_profile_calibration_demo_is_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(
        root,
        calibration_id="calibration-demo",
        workflow_stage="DEMO_ADVISORY_PROFILE_CALIBRATION_VALIDATED",
        profile="balanced",
        row_count=9,
        review_buy_candidate_count=0,
        watch_count=0,
        no_action_count=0,
        blocked_count=0,
        demo_only_count=9,
        issue_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    calibration_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_PROFILE_CALIBRATION_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "DEMO_ADVISORY_PROFILE_CALIBRATION_VALIDATED"
    assert result.advisory_profile_calibration_demo_only_count == 9
    assert result.advisory_profile_calibration_review_buy_candidate_count == 0
    assert calibration_row["warning_classification"] == "EXPECTED_DEMO_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "DEMO_ONLY labels" in result.next_manual_action


def test_dashboard_failed_advisory_profile_calibration_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(
        root,
        calibration_id="calibration-fail",
        status="FAIL",
        workflow_stage="ADVISORY_PROFILE_CALIBRATION_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    calibration_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_PROFILE_CALIBRATION_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "ADVISORY_PROFILE_CALIBRATION_FAILED"
    assert calibration_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair advisory profile calibration artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_advisory_profile_calibration(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(root, calibration_id="calibration-reviewed")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    calibration_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_PROFILE_CALIBRATION_STATUS"
    ].iloc[0]

    assert result.latest_advisory_profile_calibration_run_id == "calibration-reviewed"
    assert result.advisory_profile_calibration_stage == "ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert calibration_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_advisory_profile_calibration_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(root, calibration_id="calibration-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_advisory_profile_calibration_run_id"] == "calibration-export"
    assert row["advisory_profile_calibration_status"] == "WARN"
    assert row["advisory_profile_calibration_stage"] == "ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW"
    assert row["advisory_profile_calibration_health_status"] == "PASS"
    assert row["advisory_profile_calibration_review_buy_candidate_count"] == "1"
    assert row["advisory_profile_calibration_watch_count"] == "2"
    assert row["advisory_profile_calibration_blocked_count"] == "2"
    assert row["advisory_profile_calibration_profile"] == "balanced"
    assert metadata["latest_advisory_profile_calibration_run_id"] == "calibration-export"
    assert metadata["advisory_profile_calibration_health_status"] == "PASS"
    assert metadata["advisory_profile_calibration_review_buy_candidate_count"] == 1
    assert metadata["component_statuses"]["latest_advisory_profile_calibration_run_id"] == "calibration-export"


def test_cli_research_status_prints_advisory_profile_calibration_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _advisory_profile_calibration_status(root, calibration_id="calibration-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_advisory_profile_calibration_run_id: calibration-cli" in output.out
    assert "advisory_profile_calibration_status: WARN" in output.out
    assert "advisory_profile_calibration_stage: ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW" in output.out
    assert "advisory_profile_calibration_health_status: PASS" in output.out
    assert "advisory_profile_calibration_review_buy_candidate_count: 1" in output.out


def test_dashboard_includes_calibration_to_signal_semantics_status_when_no_later_workflow_exists(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(root, proposal_id="proposal-reviewed")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    proposal_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CALIBRATION_TO_SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.latest_calibration_to_signal_semantics_proposal_run_id == "proposal-reviewed"
    assert result.calibration_to_signal_semantics_status == "WARN"
    assert result.calibration_to_signal_semantics_stage == "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
    assert result.calibration_to_signal_semantics_health_status == "PASS"
    assert result.calibration_to_signal_semantics_defaults_changed is False
    assert "KEEP_CURRENT_DEFAULTS" in result.calibration_to_signal_semantics_proposal_categories
    assert "DO_NOT_EXPAND_BUY_REVIEW_YET" in result.calibration_to_signal_semantics_proposal_categories
    assert result.calibration_to_signal_semantics_calibration_run_count == 10
    assert result.calibration_to_signal_semantics_observed_review_buy_candidate_count == 7
    assert result.calibration_to_signal_semantics_observed_watch_count == 8
    assert result.calibration_to_signal_semantics_observed_blocked_count == 24
    assert result.workflow_stage == "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
    assert proposal_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "Keep current defaults" in result.next_manual_action
    assert "do not expand BUY review yet" in result.next_manual_action


def test_dashboard_calibration_to_signal_semantics_defaults_changed_is_actionable(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(
        root,
        proposal_id="proposal-defaults-changed",
        status="FAIL",
        workflow_stage="CALIBRATION_TO_SEMANTICS_FAILED",
        health_status="FAIL",
        defaults_changed=True,
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    proposal_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CALIBRATION_TO_SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "CALIBRATION_TO_SEMANTICS_FAILED"
    assert result.calibration_to_signal_semantics_defaults_changed is True
    assert proposal_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_dashboard_failed_calibration_to_signal_semantics_health_is_actionable_when_active(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(
        root,
        proposal_id="proposal-health-fail",
        status="FAIL",
        workflow_stage="CALIBRATION_TO_SEMANTICS_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    proposal_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CALIBRATION_TO_SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "CALIBRATION_TO_SEMANTICS_FAILED"
    assert result.calibration_to_signal_semantics_health_status == "FAIL"
    assert proposal_row["warning_classification"] == "BLOCKING_ERROR"
    assert "Repair calibration-to-semantics proposal artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_calibration_to_signal_semantics(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(root, proposal_id="proposal-reviewed")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    proposal_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "CALIBRATION_TO_SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.latest_calibration_to_signal_semantics_proposal_run_id == "proposal-reviewed"
    assert result.calibration_to_signal_semantics_stage == "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert proposal_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_calibration_to_signal_semantics_fields_to_summary_and_metadata(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(root, proposal_id="proposal-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_calibration_to_signal_semantics_proposal_run_id"] == "proposal-export"
    assert row["calibration_to_signal_semantics_status"] == "WARN"
    assert row["calibration_to_signal_semantics_stage"] == "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
    assert row["calibration_to_signal_semantics_health_status"] == "PASS"
    assert row["calibration_to_signal_semantics_defaults_changed"] == "False"
    assert "REQUIRE_MORE_EVIDENCE" in row["calibration_to_signal_semantics_proposal_categories"]
    assert row["calibration_to_signal_semantics_calibration_run_count"] == "10"
    assert row["calibration_to_signal_semantics_observed_review_buy_candidate_count"] == "7"
    assert metadata["latest_calibration_to_signal_semantics_proposal_run_id"] == "proposal-export"
    assert metadata["calibration_to_signal_semantics_defaults_changed"] is False
    assert metadata["calibration_to_signal_semantics_observed_blocked_count"] == 24
    assert (
        metadata["component_statuses"]["latest_calibration_to_signal_semantics_proposal_run_id"]
        == "proposal-export"
    )


def test_cli_research_status_prints_calibration_to_signal_semantics_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _calibration_to_signal_semantics_status(root, proposal_id="proposal-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_calibration_to_signal_semantics_proposal_run_id: proposal-cli" in output.out
    assert "calibration_to_signal_semantics_status: WARN" in output.out
    assert (
        "calibration_to_signal_semantics_stage: CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
        in output.out
    )
    assert "calibration_to_signal_semantics_defaults_changed: False" in output.out
    assert "calibration_to_signal_semantics_observed_review_buy_candidate_count: 7" in output.out


def test_dashboard_includes_signal_semantics_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(root, semantics_id="semantics-reviewed")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    semantics_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.latest_signal_semantics_run_id == "semantics-reviewed"
    assert result.signal_semantics_status == "WARN"
    assert result.signal_semantics_stage == "SIGNAL_SEMANTICS_READY_FOR_REVIEW"
    assert result.signal_semantics_health_status == "PASS"
    assert result.signal_semantics_review_buy_candidate_count == 1
    assert result.signal_semantics_watch_count == 1
    assert result.signal_semantics_blocked_count == 2
    assert result.signal_semantics_issue_count == 2
    assert result.signal_semantics_profile == "reviewed_local_v0"
    assert result.workflow_stage == "SIGNAL_SEMANTICS_READY_FOR_REVIEW"
    assert semantics_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "REVIEW_BUY_CANDIDATE is not an order" in result.next_manual_action
    assert "auto-order remains disabled" in result.next_manual_action


def test_dashboard_signal_semantics_demo_is_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(
        root,
        semantics_id="semantics-demo",
        workflow_stage="DEMO_SIGNAL_SEMANTICS_VALIDATED",
        profile="demo",
        row_count=9,
        demo_only_count=9,
        review_buy_candidate_count=0,
        review_sell_candidate_count=0,
        watch_count=0,
        blocked_count=0,
        issue_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    semantics_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.workflow_stage == "DEMO_SIGNAL_SEMANTICS_VALIDATED"
    assert result.signal_semantics_demo_only_count == 9
    assert result.signal_semantics_review_buy_candidate_count == 0
    assert result.signal_semantics_review_sell_candidate_count == 0
    assert semantics_row["warning_classification"] == "EXPECTED_DEMO_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "DEMO_ONLY labels" in result.next_manual_action


def test_dashboard_failed_signal_semantics_health_is_actionable_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(
        root,
        semantics_id="semantics-fail",
        status="FAIL",
        workflow_stage="SIGNAL_SEMANTICS_FAILED",
        health_status="FAIL",
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    semantics_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "SIGNAL_SEMANTICS_FAILED"
    assert semantics_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1
    assert "Repair signal semantics artifacts" in result.next_manual_action


def test_dashboard_demo_signal_semantics_buy_sell_leakage_is_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(
        root,
        semantics_id="semantics-unsafe-demo",
        status="FAIL",
        workflow_stage="SIGNAL_SEMANTICS_FAILED",
        health_status="FAIL",
        profile="demo",
        row_count=2,
        demo_only_count=1,
        review_buy_candidate_count=1,
        review_sell_candidate_count=0,
        warning_count=0,
        error_count=1,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.status == "FAIL"
    assert result.workflow_stage == "SIGNAL_SEMANTICS_FAILED"
    assert result.signal_semantics_profile == "demo"
    assert result.signal_semantics_review_buy_candidate_count == 1
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_dashboard_preserves_later_paper_priority_over_signal_semantics_review_labels(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(root, semantics_id="semantics-reviewed")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    semantics_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SIGNAL_SEMANTICS_STATUS"
    ].iloc[0]

    assert result.latest_signal_semantics_run_id == "semantics-reviewed"
    assert result.signal_semantics_stage == "SIGNAL_SEMANTICS_READY_FOR_REVIEW"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert semantics_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_signal_semantics_fields_to_summary_and_metadata(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(root, semantics_id="semantics-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_signal_semantics_run_id"] == "semantics-export"
    assert row["signal_semantics_status"] == "WARN"
    assert row["signal_semantics_stage"] == "SIGNAL_SEMANTICS_READY_FOR_REVIEW"
    assert row["signal_semantics_health_status"] == "PASS"
    assert row["signal_semantics_review_buy_candidate_count"] == "1"
    assert row["signal_semantics_watch_count"] == "1"
    assert row["signal_semantics_blocked_count"] == "2"
    assert row["signal_semantics_profile"] == "reviewed_local_v0"
    assert metadata["latest_signal_semantics_run_id"] == "semantics-export"
    assert metadata["signal_semantics_health_status"] == "PASS"
    assert metadata["signal_semantics_review_buy_candidate_count"] == 1
    assert metadata["component_statuses"]["latest_signal_semantics_run_id"] == "semantics-export"


def test_cli_research_status_prints_signal_semantics_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _signal_semantics_status(root, semantics_id="semantics-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_signal_semantics_run_id: semantics-cli" in output.out
    assert "signal_semantics_status: WARN" in output.out
    assert "signal_semantics_stage: SIGNAL_SEMANTICS_READY_FOR_REVIEW" in output.out
    assert "signal_semantics_health_status: PASS" in output.out
    assert "signal_semantics_review_buy_candidate_count: 1" in output.out


def test_dashboard_includes_signal_advisory_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_signal_run_id == "signal-a"
    assert result.signal_advisory_status == "WARN"
    assert result.signal_advisory_stage == "DEMO_SIGNAL_ADVISORY_VALIDATED"
    assert result.signal_health_status == "PASS"
    assert result.signal_count == 9
    assert result.demo_signal_count == 9
    assert result.workflow_stage == "DEMO_SIGNAL_ADVISORY_VALIDATED"
    assert "DEMO_ONLY" in result.advisory_action_counts
    assert "Review local alert preview" in result.next_manual_action


def test_dashboard_signal_demo_is_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(root, status="WARN", workflow_stage="DEMO_SIGNAL_ADVISORY_VALIDATED", warning_count=1)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    signal_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SIGNAL_ADVISORY_STATUS"].iloc[0]

    assert signal_row["warning_classification"] == "EXPECTED_DEMO_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "REVIEW_BUY_CANDIDATE\": 0" in result.advisory_action_counts
    assert "REVIEW_SELL_CANDIDATE\": 0" in result.advisory_action_counts


def test_dashboard_preserves_later_paper_priority_over_signal_advisory_demo(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(root, status="WARN", warning_count=1)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_signal_run_id == "signal-a"
    assert result.signal_advisory_stage == "DEMO_SIGNAL_ADVISORY_VALIDATED"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_failed_signal_health_is_actionable_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(
        root,
        status="FAIL",
        workflow_stage="SIGNAL_ADVISORY_FAILED",
        health_status="FAIL",
        error_count=1,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    signal_row = result.dashboard_frame.loc[result.dashboard_frame["component"] == "SIGNAL_ADVISORY_STATUS"].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "SIGNAL_ADVISORY_FAILED"
    assert signal_row["warning_classification"] == "BLOCKING_ERROR"
    assert "Repair signal advisory artifacts" in result.next_manual_action


def test_dashboard_exports_signal_advisory_fields_to_summary_and_metadata(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(root, signal_id="signal-export")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_signal_run_id"] == "signal-export"
    assert row["signal_advisory_status"] == "WARN"
    assert row["signal_advisory_stage"] == "DEMO_SIGNAL_ADVISORY_VALIDATED"
    assert row["signal_health_status"] == "PASS"
    assert row["signal_count"] == "9"
    assert row["demo_signal_count"] == "9"
    assert row["source_candidate_run_id"] == "candidate-a"
    assert row["selection_profile"] == "demo"
    assert row["demo_mode"] == "True"
    assert row["not_strategy_recommendation"] == "True"
    assert metadata["latest_signal_run_id"] == "signal-export"
    assert metadata["signal_health_status"] == "PASS"
    assert metadata["demo_mode"] is True
    assert metadata["component_statuses"]["latest_signal_run_id"] == "signal-export"


def test_cli_research_status_prints_signal_advisory_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _signal_advisory_status(root, signal_id="signal-cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_signal_run_id: signal-cli" in output.out
    assert "signal_advisory_status: WARN" in output.out
    assert "signal_advisory_stage: DEMO_SIGNAL_ADVISORY_VALIDATED" in output.out
    assert "signal_health_status: PASS" in output.out
    assert "demo_mode: True" in output.out


def test_dashboard_includes_single_symbol_advisory_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(root, advisory_id="single-000001", symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_single_symbol_advisory_run_id == "single-000001"
    assert result.latest_single_symbol_advisory_symbol == "000001"
    assert result.single_symbol_advisory_status == "WARN"
    assert result.single_symbol_advisory_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED"
    assert result.single_symbol_advisory_action == "DEMO_ONLY"
    assert result.single_symbol_advisory_health_status == "PASS"
    assert result.single_symbol_advisory_demo_mode is True
    assert result.single_symbol_advisory_not_strategy_recommendation is True
    assert result.workflow_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED"
    assert "Review local single-symbol advisory preview" in result.next_manual_action


def test_dashboard_single_symbol_not_found_is_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(
        root,
        advisory_id="single-missing",
        symbol="999999",
        status="WARN",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_NOT_FOUND",
        latest_status="NOT_FOUND",
        advisory_action="NO_ACTION",
        final_score="",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    advisory_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_STATUS"
    ].iloc[0]

    assert result.latest_single_symbol_advisory_symbol == "999999"
    assert result.single_symbol_advisory_action == "NO_ACTION"
    assert result.workflow_stage == "SINGLE_SYMBOL_ADVISORY_NOT_FOUND"
    assert result.status == "WARN"
    assert advisory_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "no recommendation was invented" in result.next_manual_action


def test_dashboard_failed_single_symbol_health_is_actionable_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(
        root,
        advisory_id="single-fail",
        status="FAIL",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_FAILED",
        health_status="FAIL",
        error_count=1,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    advisory_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "SINGLE_SYMBOL_ADVISORY_FAILED"
    assert advisory_row["warning_classification"] == "BLOCKING_ERROR"
    assert "Repair single-symbol advisory artifacts" in result.next_manual_action


def test_dashboard_preserves_later_paper_priority_over_single_symbol_not_found(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(
        root,
        advisory_id="single-missing",
        symbol="999999",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_NOT_FOUND",
        latest_status="NOT_FOUND",
        advisory_action="NO_ACTION",
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    advisory_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_STATUS"
    ].iloc[0]

    assert result.latest_single_symbol_advisory_symbol == "999999"
    assert result.single_symbol_advisory_stage == "SINGLE_SYMBOL_ADVISORY_NOT_FOUND"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert advisory_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_single_symbol_advisory_fields_to_summary_and_metadata(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(root, advisory_id="single-export", symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_single_symbol_advisory_run_id"] == "single-export"
    assert row["latest_single_symbol_advisory_symbol"] == "000001"
    assert row["single_symbol_advisory_status"] == "WARN"
    assert row["single_symbol_advisory_stage"] == "DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED"
    assert row["single_symbol_advisory_action"] == "DEMO_ONLY"
    assert row["single_symbol_advisory_health_status"] == "PASS"
    assert row["single_symbol_advisory_demo_mode"] == "True"
    assert row["single_symbol_advisory_not_strategy_recommendation"] == "True"
    assert metadata["latest_single_symbol_advisory_run_id"] == "single-export"
    assert metadata["latest_single_symbol_advisory_symbol"] == "000001"
    assert metadata["single_symbol_advisory_health_status"] == "PASS"
    assert metadata["single_symbol_advisory_demo_mode"] is True
    assert metadata["component_statuses"]["latest_single_symbol_advisory_symbol"] == "000001"


def test_cli_research_status_prints_single_symbol_advisory_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_status(root, advisory_id="single-cli", symbol="000001")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_single_symbol_advisory_run_id: single-cli" in output.out
    assert "latest_single_symbol_advisory_symbol: 000001" in output.out
    assert "single_symbol_advisory_status: WARN" in output.out
    assert "single_symbol_advisory_stage: DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED" in output.out
    assert "single_symbol_advisory_health_status: PASS" in output.out


def test_dashboard_includes_single_symbol_answer_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(root, answer_id="answer-000001", symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_single_symbol_advisory_answer_run_id == "answer-000001"
    assert result.latest_single_symbol_advisory_answer_symbol == "000001"
    assert result.single_symbol_advisory_answer_status == "WARN"
    assert result.single_symbol_advisory_answer_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED"
    assert result.single_symbol_advisory_answer_action == "DEMO_ONLY"
    assert result.single_symbol_advisory_answer_health_status == "PASS"
    assert result.single_symbol_advisory_answer_question == "should I buy?"
    assert result.single_symbol_advisory_answer_style == "concise"
    assert result.single_symbol_advisory_answer_demo_mode is True
    assert result.single_symbol_advisory_answer_not_strategy_recommendation is True
    assert result.workflow_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED"
    assert "Review local question-style answer" in result.next_manual_action


def test_dashboard_single_symbol_answer_not_found_is_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(
        root,
        answer_id="answer-missing",
        symbol="999999",
        status="WARN",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_ANSWER_NOT_FOUND",
        latest_status="NOT_FOUND",
        advisory_action="NO_ACTION",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    answer_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_ANSWER_STATUS"
    ].iloc[0]

    assert result.latest_single_symbol_advisory_answer_symbol == "999999"
    assert result.single_symbol_advisory_answer_action == "NO_ACTION"
    assert result.workflow_stage == "SINGLE_SYMBOL_ADVISORY_ANSWER_NOT_FOUND"
    assert result.status == "WARN"
    assert answer_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "no recommendation was invented" in result.next_manual_action


def test_dashboard_failed_single_symbol_answer_health_is_actionable_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(
        root,
        answer_id="answer-fail",
        status="FAIL",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_ANSWER_FAILED",
        health_status="FAIL",
        error_count=1,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    answer_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_ANSWER_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "SINGLE_SYMBOL_ADVISORY_ANSWER_FAILED"
    assert answer_row["warning_classification"] == "BLOCKING_ERROR"
    assert "Repair question-style answer artifacts" in result.next_manual_action


def test_dashboard_computed_answer_llm_api_called_is_actionable(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_answer_artifact(root, answer_id="answer-llm", symbol="000001", metadata_updates={"llm_api_called": True})

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    answer_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_ANSWER_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "SINGLE_SYMBOL_ADVISORY_ANSWER_FAILED"
    assert result.latest_single_symbol_advisory_answer_symbol == "000001"
    assert answer_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_dashboard_preserves_later_paper_priority_over_single_symbol_answer_not_found(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(
        root,
        answer_id="answer-missing",
        symbol="999999",
        workflow_stage="SINGLE_SYMBOL_ADVISORY_ANSWER_NOT_FOUND",
        latest_status="NOT_FOUND",
        advisory_action="NO_ACTION",
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    answer_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "SINGLE_SYMBOL_ADVISORY_ANSWER_STATUS"
    ].iloc[0]

    assert result.latest_single_symbol_advisory_answer_symbol == "999999"
    assert result.single_symbol_advisory_answer_stage == "SINGLE_SYMBOL_ADVISORY_ANSWER_NOT_FOUND"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert answer_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_single_symbol_answer_fields_to_summary_and_metadata(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(root, answer_id="answer-export", symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_single_symbol_advisory_answer_run_id"] == "answer-export"
    assert row["latest_single_symbol_advisory_answer_symbol"] == "000001"
    assert row["single_symbol_advisory_answer_status"] == "WARN"
    assert row["single_symbol_advisory_answer_stage"] == "DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED"
    assert row["single_symbol_advisory_answer_action"] == "DEMO_ONLY"
    assert row["single_symbol_advisory_answer_health_status"] == "PASS"
    assert row["single_symbol_advisory_answer_question"] == "should I buy?"
    assert row["single_symbol_advisory_answer_style"] == "concise"
    assert row["single_symbol_advisory_answer_demo_mode"] == "True"
    assert row["single_symbol_advisory_answer_not_strategy_recommendation"] == "True"
    assert metadata["latest_single_symbol_advisory_answer_run_id"] == "answer-export"
    assert metadata["latest_single_symbol_advisory_answer_symbol"] == "000001"
    assert metadata["single_symbol_advisory_answer_health_status"] == "PASS"
    assert metadata["single_symbol_advisory_answer_demo_mode"] is True
    assert metadata["component_statuses"]["latest_single_symbol_advisory_answer_symbol"] == "000001"


def test_cli_research_status_prints_single_symbol_answer_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _single_symbol_advisory_answer_status(root, answer_id="answer-cli", symbol="000001")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_single_symbol_advisory_answer_run_id: answer-cli" in output.out
    assert "latest_single_symbol_advisory_answer_symbol: 000001" in output.out
    assert "single_symbol_advisory_answer_status: WARN" in output.out
    assert "single_symbol_advisory_answer_stage: DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED" in output.out
    assert "single_symbol_advisory_answer_health_status: PASS" in output.out


def test_dashboard_includes_advisory_conversation_status_when_no_later_workflow_exists(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(root, conversation_id="conv-000001", parsed_symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.latest_advisory_conversation_run_id == "conv-000001"
    assert result.advisory_conversation_original_question == "000001 now can buy?"
    assert result.advisory_conversation_parsed_symbol == "000001"
    assert result.advisory_conversation_parsed_intent == "BUY_REVIEW"
    assert result.advisory_conversation_status == "WARN"
    assert result.advisory_conversation_stage == "DEMO_ADVISORY_CONVERSATION_VALIDATED"
    assert result.advisory_conversation_action == "DEMO_ONLY"
    assert result.advisory_conversation_health_status == "PASS"
    assert result.advisory_conversation_parser_type == "deterministic_rule_based"
    assert result.advisory_conversation_llm_api_called is False
    assert result.advisory_conversation_no_message_sent is True
    assert result.workflow_stage == "DEMO_ADVISORY_CONVERSATION_VALIDATED"
    assert "Review local conversational advisory answer" in result.next_manual_action


def test_dashboard_advisory_conversation_parse_failed_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(
        root,
        conversation_id="conv-parse-failed",
        question="can I buy this?",
        parsed_symbol="",
        status="WARN",
        workflow_stage="ADVISORY_CONVERSATION_PARSE_FAILED",
        latest_status="PARSE_FAILED",
        advisory_action="NO_ACTION",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    conversation_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_CONVERSATION_STATUS"
    ].iloc[0]

    assert result.advisory_conversation_parsed_symbol == ""
    assert result.advisory_conversation_action == "NO_ACTION"
    assert result.workflow_stage == "ADVISORY_CONVERSATION_PARSE_FAILED"
    assert result.status == "WARN"
    assert conversation_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 0
    assert "no symbol or recommendation was invented" in result.next_manual_action


def test_dashboard_advisory_conversation_not_found_visible_but_not_blocking(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(
        root,
        conversation_id="conv-not-found",
        parsed_symbol="999999",
        status="WARN",
        workflow_stage="ADVISORY_CONVERSATION_NOT_FOUND",
        latest_status="NOT_FOUND",
        advisory_action="NO_ACTION",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    conversation_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_CONVERSATION_STATUS"
    ].iloc[0]

    assert result.advisory_conversation_parsed_symbol == "999999"
    assert result.advisory_conversation_action == "NO_ACTION"
    assert result.workflow_stage == "ADVISORY_CONVERSATION_NOT_FOUND"
    assert conversation_row["warning_classification"] == "EXPECTED_REVIEWABLE_WARNING"
    assert "no recommendation was invented" in result.next_manual_action


def test_dashboard_failed_advisory_conversation_health_is_actionable_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(
        root,
        conversation_id="conv-fail",
        status="FAIL",
        workflow_stage="ADVISORY_CONVERSATION_FAILED",
        health_status="FAIL",
        llm_api_called=True,
        error_count=1,
        warning_count=0,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    conversation_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_CONVERSATION_STATUS"
    ].iloc[0]

    assert result.status == "FAIL"
    assert result.workflow_stage == "ADVISORY_CONVERSATION_FAILED"
    assert result.advisory_conversation_llm_api_called is True
    assert conversation_row["warning_classification"] == "BLOCKING_ERROR"
    assert result.summary_frame.iloc[0]["blocking_error_count"] == 1


def test_dashboard_preserves_later_paper_priority_over_advisory_conversation_parse_failed(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(
        root,
        conversation_id="conv-parse-failed",
        question="can I buy this?",
        parsed_symbol="",
        workflow_stage="ADVISORY_CONVERSATION_PARSE_FAILED",
        latest_status="PARSE_FAILED",
        advisory_action="NO_ACTION",
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    conversation_row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "ADVISORY_CONVERSATION_STATUS"
    ].iloc[0]

    assert result.advisory_conversation_stage == "ADVISORY_CONVERSATION_PARSE_FAILED"
    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert conversation_row["warning_classification"] == "STALE_ARTIFACT_WARNING"
    assert "Demo WATCH_ONLY paper workflow validated" in result.next_manual_action


def test_dashboard_exports_advisory_conversation_fields_to_summary_and_metadata(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(root, conversation_id="conv-export", parsed_symbol="000001")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    exported = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    row = exported.iloc[0].to_dict()

    assert row["latest_advisory_conversation_run_id"] == "conv-export"
    assert row["advisory_conversation_original_question"] == "000001 now can buy?"
    assert row["advisory_conversation_parsed_symbol"] == "000001"
    assert row["advisory_conversation_parsed_intent"] == "BUY_REVIEW"
    assert row["advisory_conversation_status"] == "WARN"
    assert row["advisory_conversation_stage"] == "DEMO_ADVISORY_CONVERSATION_VALIDATED"
    assert row["advisory_conversation_action"] == "DEMO_ONLY"
    assert row["advisory_conversation_health_status"] == "PASS"
    assert row["advisory_conversation_llm_api_called"] == "False"
    assert row["advisory_conversation_no_message_sent"] == "True"
    assert metadata["latest_advisory_conversation_run_id"] == "conv-export"
    assert metadata["advisory_conversation_parsed_symbol"] == "000001"
    assert metadata["advisory_conversation_health_status"] == "PASS"
    assert metadata["advisory_conversation_llm_api_called"] is False
    assert metadata["component_statuses"]["advisory_conversation_parsed_symbol"] == "000001"


def test_cli_research_status_prints_advisory_conversation_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _advisory_conversation_status(root, conversation_id="conv-cli", parsed_symbol="000001")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "latest_advisory_conversation_run_id: conv-cli" in output.out
    assert "advisory_conversation_parsed_symbol: 000001" in output.out
    assert "advisory_conversation_status: WARN" in output.out
    assert "advisory_conversation_stage: DEMO_ADVISORY_CONVERSATION_VALIDATED" in output.out
    assert "advisory_conversation_llm_api_called: False" in output.out
    assert "advisory_conversation_no_message_sent: True" in output.out


def _reports_root(tmp_path: Path) -> Path:
    root = tmp_path / "reports"
    root.mkdir(parents=True, exist_ok=True)
    return root


def _run_personal_mvp_daily_advisory_review_context(root: Path):
    source_root = root / "_daily_review_source"
    _personal_mvp_daily_advisory_review_signal_artifact(source_root)
    result = run_personal_mvp_daily_advisory_review(
        root=source_root,
        output_dir=root / "personal_mvp_daily_advisory_review",
        review_date="2024-05-20",
        run_id="daily-review-001",
    )
    build_personal_mvp_daily_advisory_review_index(root=root / "personal_mvp_daily_advisory_review")
    check_personal_mvp_daily_advisory_review_health(root=root / "personal_mvp_daily_advisory_review")
    run_personal_mvp_daily_advisory_review_status(root=root / "personal_mvp_daily_advisory_review")
    return result


def _personal_mvp_daily_advisory_review_signal_artifact(root: Path) -> None:
    artifact = root / "signals" / "daily-review-source"
    artifact.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {
                "signal_id": "daily-signal-000001",
                "signal_run_id": "daily-review-source",
                "signal_date": "2024-05-20",
                "decision_date": "2024-05-20",
                "symbol": "000001",
                "name": "Name 000001",
                "instrument_type": "stock",
                "universe_name": "stock_core",
                "advisory_action": "WATCH",
                "final_score": 60.0,
                "confidence_level": "medium",
                "reason_summary": "watch context",
                "risk_notes": "risk context",
                "data_source_notes": "local report-only fixture",
                "demo_mode": False,
                "not_strategy_recommendation": False,
                "requires_manual_confirmation": True,
                "auto_order_allowed": False,
                "no_live_trading": True,
                "no_broker_api": True,
                "no_message_sent": True,
            }
        ]
    ).to_csv(artifact / "signals.csv", index=False)
    (artifact / "signal_advisory_report.md").write_text("# Signal report\n", encoding="utf-8")
    (artifact / "metadata.json").write_text(
        json.dumps(
            {
                "signal_run_id": "daily-review-source",
                "status": "SIGNAL_ADVISORY_READY_FOR_REVIEW",
                "workflow_stage": "SIGNAL_ADVISORY_READY_FOR_REVIEW",
                "signal_count": 1,
                "report_only": True,
                "diagnostic_only": True,
                "local_only": True,
                "buy_review_allowed": False,
                "trading_allowed": False,
                "broker_api_called": False,
                "order_placed": False,
                "message_sent": False,
            },
            indent=2,
            sort_keys=True,
        ),
        encoding="utf-8",
    )


def _metadata_reference_following_manifest(root: Path) -> Path:
    metadata_path = root / "metadata_refs" / "source_registry_snapshot.json"
    metadata_path.parent.mkdir(parents=True, exist_ok=True)
    metadata_path.write_text(
        json.dumps(
            {
                "schema_version": "v0.1",
                "metadata_kind": "syntactic_fixture_metadata",
                "available_time": "2024-04-02T15:00:00+08:00",
                "source_hash": "sha256:metadata-reference-following-fixture",
                "revision_id": "rev-001",
                "reviewer_id": "reviewer-fixture",
                "quality_status": "accepted_for_review_context_only",
                "limitation_note": "Synthetic metadata-only dashboard fixture.",
                "forbidden_downstream_flags": {
                    flag: False for flag in METADATA_REFERENCE_FOLLOWING_REQUIRED_FALSE_FLAGS
                },
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    manifest_path = root / "manifest.json"
    manifest_path.write_text(
        json.dumps(
            {
                "package_id": "tiny-pit-metadata-reference-following-dashboard",
                "package_schema_version": "v0.1",
                "created_at": "2026-06-30T00:00:00Z",
                "prepared_by": "synthetic-reviewer",
                "report_only": True,
                "diagnostic_only": True,
                "inspection_level": INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY,
                "metadata_references": [
                    {
                        "reference_type": "source_registry_snapshot_ref",
                        "reference_name": "source-registry-snapshot-fixture",
                        "path": str(metadata_path),
                        "required": True,
                        "expected_schema_version": "v0.1",
                        "declared_only": False,
                        "notes": "synthetic metadata-only reference",
                    }
                ],
                "forbidden_downstream_flags": {
                    flag: False for flag in METADATA_REFERENCE_FOLLOWING_REQUIRED_FALSE_FLAGS
                },
                "limitations": ["metadata-reference-following is report-only and CSV_READ_NONE"],
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    return manifest_path


def _csv_structural_header_only_manifest(root: Path) -> Path:
    csv_path = root / "reviewed" / "tiny_header_only.csv"
    csv_path.parent.mkdir(parents=True, exist_ok=True)
    csv_path.write_text(
        "symbol,signal_date,review_note\n"
        "000001,2024-04-02,SENTINEL_DASHBOARD_ROW_VALUE_DO_NOT_READ\n",
        encoding="utf-8",
        newline="",
    )
    manifest_path = root / "manifest.json"
    manifest_path.write_text(
        json.dumps(
            {
                "package_id": "tiny-pit-csv-structural-header-only-dashboard",
                "package_schema_version": "v0.1",
                "created_at": "2026-07-01T00:00:00Z",
                "prepared_by": "synthetic-reviewer",
                "report_only": True,
                "diagnostic_only": True,
                "requested_file_touch_level": "CSV_STRUCTURAL_HEADER_ONLY",
                "requested_csv_read_level": "CSV_HEADER_ONLY",
                "requested_local_file_hash_level": "LOCAL_FILE_HASH_NONE",
                "csv_file_references": [
                    {
                        "reference_type": "reviewed_local_csv_file_ref",
                        "reference_name": "tiny-header-only-dashboard",
                        "path": str(csv_path),
                        "required": True,
                        "intended_touch_level": "CSV_HEADER_ONLY",
                        "declared_only": False,
                    }
                ],
                "forbidden_downstream_flags": {
                    flag: False for flag in CSV_STRUCTURAL_HEADER_ONLY_REQUIRED_FALSE_FLAGS
                },
                "limitations": ["Header-only structural dashboard fixture; no row values consumed."],
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    return manifest_path


def _local_file_byte_hash_only_manifest(root: Path) -> tuple[Path, Path, bytes]:
    csv_path = root / "reviewed" / "tiny_hash_only.csv"
    csv_bytes = (
        b"symbol,signal_date,review_note\n"
        b"000001,2024-04-02,SENTINEL_HASH_ONLY_DASHBOARD_ROW_VALUE_DO_NOT_READ\n"
    )
    csv_path.parent.mkdir(parents=True, exist_ok=True)
    csv_path.write_bytes(csv_bytes)
    manifest_path = root / "manifest.json"
    manifest_path.write_text(
        json.dumps(
            {
                "package_id": "tiny-pit-local-file-byte-hash-only-dashboard",
                "package_schema_version": "v0.1",
                "created_at": "2026-07-01T00:00:00Z",
                "prepared_by": "synthetic-reviewer",
                "report_only": True,
                "diagnostic_only": True,
                "requested_file_touch_level": LOCAL_FILE_BYTE_HASH_ONLY,
                "requested_csv_read_level": LOCAL_FILE_BYTE_HASH_ONLY_CSV_READ_NONE,
                "requested_local_file_hash_level": LOCAL_FILE_BYTE_HASH_SHA256_ONLY,
                "local_file_references": [
                    {
                        "reference_type": "reviewed_local_csv_file_ref",
                        "reference_name": "tiny-hash-only-dashboard",
                        "path": str(csv_path),
                        "required": True,
                        "intended_touch_level": LOCAL_FILE_BYTE_HASH_ONLY,
                        "declared_only": False,
                    }
                ],
                "forbidden_downstream_flags": {
                    flag: False for flag in LOCAL_FILE_BYTE_HASH_ONLY_REQUIRED_FALSE_FLAGS
                },
                "limitations": ["Byte-hash-only dashboard fixture; no CSV parsing or PIT validation."],
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    return manifest_path, csv_path, csv_bytes


def _expected_hash_verification_inputs(
    root: Path,
    *,
    expected_hash: str,
    actual_hash: str,
) -> tuple[Path, Path]:
    metadata_path = root / "byte_hash_metadata" / "metadata.json"
    metadata_path.parent.mkdir(parents=True, exist_ok=True)
    metadata_payload = {
        "local_file_byte_hash_computed": True,
        "local_file_byte_hash_algorithm": "SHA-256",
        "local_file_byte_hash_value": actual_hash,
        "csv_read_level": EXPECTED_HASH_VERIFICATION_CSV_READ_NONE,
        "csv_file_opened_structurally": False,
        "csv_header_read": False,
        "csv_row_count_computed": False,
        "csv_values_read": False,
        "csv_full_content_read": False,
        "real_csv_consumed": False,
        "source_hash_validated": False,
        "revision_id_validated": False,
        "available_time_validated": False,
        "pit_admissibility_validated": False,
        "source_reliability_scored": False,
        "reviewer_authority_validated": False,
        **{flag: False for flag in EXPECTED_HASH_VERIFICATION_REQUIRED_FALSE_FLAGS},
    }
    metadata_path.write_text(
        json.dumps(metadata_payload, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    manifest_path = root / "expected_hash_manifest.json"
    manifest_payload = {
        "verification_id": "expected-hash-dashboard-fixture",
        "package_id": "tiny-pit-expected-hash-dashboard",
        "package_schema_version": "v0.1",
        "created_at": "2026-07-02T00:00:00Z",
        "prepared_by": "synthetic-reviewer",
        "report_only": True,
        "diagnostic_only": True,
        "requested_expected_hash_verification_level": EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY,
        "requested_csv_read_level": EXPECTED_HASH_VERIFICATION_CSV_READ_NONE,
        "requested_local_file_hash_level": LOCAL_FILE_HASH_SHA256_METADATA_REFERENCE_ONLY,
        "source_local_file_byte_hash_artifact_metadata_path": str(metadata_path),
        "expected_hash_algorithm": "SHA-256",
        "expected_hash_value": expected_hash,
        "expected_hash_disclosure_level": "PREVIEW_ONLY_STATUS",
        "forbidden_downstream_flags": {
            flag: False for flag in EXPECTED_HASH_VERIFICATION_REQUIRED_FALSE_FLAGS
        },
        "limitations": [
            "Expected-hash dashboard fixture compares manifest metadata to local byte-hash metadata only.",
        ],
    }
    manifest_path.write_text(
        json.dumps(manifest_payload, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    return manifest_path, metadata_path


def _source_revision_time_inputs(
    root: Path,
    *,
    source_hash: str = "a" * 64,
    available_time: str = "2024-04-02T09:15:00+08:00",
    available_time_timezone: str = "Asia/Shanghai",
) -> tuple[Path, Path]:
    metadata_path = root / "source_lineage_metadata" / "metadata.json"
    metadata_payload = {
        "source_id": "official-source-revision-dashboard-fixture",
        "source_name": "Official Source Revision Dashboard Fixture",
        "source_type": "official_local_metadata",
        "permission_class": "reviewed_local_reference",
        "source_hash_algorithm": "SHA-256",
        "source_hash_value": source_hash,
        "source_hash_disclosure_level": "PREVIEW_ONLY_STATUS",
        "revision_id": "provider-revision-20240402",
        "revision_id_type": "provider_revision_id",
        "available_time": available_time,
        "available_time_timezone": available_time_timezone,
        "available_time_policy": "metadata_present_only_no_decision_time_comparison",
        "quality_status": "REVIEW_CONTEXT_ONLY",
        "manual_review_status": "REVIEWED_METADATA_CONTEXT_ONLY",
        "report_only": True,
        "diagnostic_only": True,
        "forbidden_downstream_flags": {
            flag: False for flag in SOURCE_REVISION_TIME_REQUIRED_FALSE_FLAGS
        },
        "limitations": [
            "Source hash/revision/available-time dashboard fixture records metadata presence only.",
        ],
    }
    _write_json(metadata_path, metadata_payload)

    manifest_path = root / "source_revision_time_manifest.json"
    manifest_payload = {
        "package_id": "tiny-pit-source-revision-time-dashboard",
        "package_schema_version": "source_revision_time_v0_1",
        "created_at": "2026-07-03T00:00:00Z",
        "prepared_by": "synthetic-reviewer",
        "report_only": True,
        "diagnostic_only": True,
        "requested_source_hash_validation_level": SOURCE_HASH_METADATA_PRESENT_ONLY,
        "requested_revision_id_validation_level": REVISION_ID_METADATA_PRESENT_ONLY,
        "requested_available_time_validation_level": AVAILABLE_TIME_METADATA_PRESENT_ONLY,
        "requested_pit_admissibility_level": PIT_ADMISSIBILITY_NONE,
        "source_lineage_metadata_reference": {
            "path": str(metadata_path),
            "required": True,
            "reference_type": "source_lineage_metadata_ref",
            "intended_touch_level": SOURCE_REVISION_TIME_METADATA_PRESENT_ONLY,
            "declared_only": False,
        },
        "source_hash_policy": "metadata_presence_and_preview_only",
        "revision_id_policy": "metadata_presence_only",
        "available_time_policy": "parseability_and_timezone_metadata_only",
        "timezone_policy": "timezone_required_for_pass_context",
        "forbidden_downstream_flags": {
            flag: False for flag in SOURCE_REVISION_TIME_REQUIRED_FALSE_FLAGS
        },
        "limitations": [
            "Dashboard fixture does not validate PIT admissibility or source reliability.",
        ],
    }
    _write_json(manifest_path, manifest_payload)
    return manifest_path, metadata_path


def _reviewer_quality_limitation_output_root(root: Path) -> Path:
    return (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_reviewer_authority_quality_limitation_v0_1"
    )


def _reviewer_quality_limitation_inputs(
    root: Path,
    *,
    reviewer_id_preview: str = "private-revi",
    quality_warning_count: int = 0,
    quality_blocker_count: int = 0,
    limitation_severity_max: str = "INFO",
    limitation_categories: list[str] | None = None,
    unresolved_limitation_count: int = 0,
    blocking_limitation_count: int = 0,
    permission_class: str = "public",
    legality_flag: str = "public_confirmed",
) -> tuple[Path, Path]:
    root.mkdir(parents=True, exist_ok=True)
    categories = limitation_categories or ["schema_assumption"]
    metadata_path = root / "reviewer_quality_metadata.json"
    metadata_payload = {
        "reviewer_id_recorded": True,
        "reviewer_id_preview": reviewer_id_preview,
        "reviewer_role": "reviewer",
        "reviewer_type": "human_declared_only",
        "reviewer_attestation_present": True,
        "reviewer_authority_scope_declared": True,
        "reviewer_authority_validated": False,
        "manual_review_status": "declared_context_only",
        "quality_status": "QUALITY_METADATA_PRESENT_ONLY",
        "quality_status_validated": False,
        "quality_issue_count": quality_warning_count + quality_blocker_count,
        "quality_warning_count": quality_warning_count,
        "quality_blocker_count": quality_blocker_count,
        "limitations_present": True,
        "limitation_count": len(categories),
        "limitation_severity_max": limitation_severity_max,
        "limitation_categories": categories,
        "unresolved_limitation_count": unresolved_limitation_count,
        "blocking_limitation_count": blocking_limitation_count,
        "limitation_policy": "visible-non-override",
        "limitations_overridden_by_reviewer": False,
        "limitations_overridden_by_quality": False,
        "assumptions_present": True,
        "assumption_count": 1,
        "permission_class": permission_class,
        "legality_flag": legality_flag,
        "permission_class_validated": False,
        "report_only": True,
        "diagnostic_only": True,
        "forbidden_downstream_flags": {
            flag: False for flag in REVIEWER_QUALITY_LIMITATION_REQUIRED_FALSE_FLAGS
        },
        "limitations": ["Synthetic reviewer/quality/limitation context."],
    }
    _write_json(metadata_path, metadata_payload)

    manifest_path = root / "reviewer_quality_manifest.json"
    manifest_payload = {
        "package_id": "tiny-pit-reviewer-quality-dashboard",
        "package_schema_version": "reviewer_quality_limitation_v0_1",
        "created_at": "2026-07-03T00:00:00Z",
        "prepared_by": "synthetic-test",
        "report_only": True,
        "diagnostic_only": True,
        "requested_reviewer_authority_level": REVIEWER_METADATA_PRESENT_ONLY,
        "requested_quality_status_level": QUALITY_METADATA_PRESENT_ONLY,
        "requested_limitation_review_level": LIMITATION_METADATA_PRESENT_ONLY,
        "requested_permission_review_level": PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        "requested_package_promotion_level": PACKAGE_PROMOTION_NONE,
        "reviewer_quality_metadata_reference": {
            "path": str(metadata_path),
            "required": True,
            "reference_type": "reviewer_quality_limitation_metadata_ref",
            "intended_touch_level": "REVIEWER_QUALITY_LIMITATION_METADATA_PRESENT_ONLY",
            "declared_only": False,
        },
        "reviewer_policy": "metadata-present-only",
        "quality_policy": "declared-only",
        "limitation_policy": "visible-non-override",
        "permission_policy": "declared-permission-class-only",
        "disclosure_policy": "preview-only",
        "forbidden_downstream_flags": {
            flag: False for flag in REVIEWER_QUALITY_LIMITATION_REQUIRED_FALSE_FLAGS
        },
        "limitations": ["Synthetic reviewer quality limitation manifest context."],
    }
    _write_json(manifest_path, manifest_payload)
    return manifest_path, metadata_path


def _preflight_output_root(root: Path) -> Path:
    return (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_v0_1"
    )


def _preflight_inputs(
    root: Path,
    *,
    missing_optional: str | None = None,
    missing_required: str | None = None,
    metadata_overrides: dict[str, dict[str, object]] | None = None,
) -> tuple[Path, dict[str, Path], Path, str, str]:
    root.mkdir(parents=True, exist_ok=True)
    metadata_root = root / "metadata"
    metadata_root.mkdir(parents=True, exist_ok=True)
    full_source_hash = "abc123def4567890" * 4
    full_reviewer_id = "private-reviewer-id-preflight-dashboard-000001"
    overrides = metadata_overrides or {}
    refs: dict[str, Path] = {}
    evidence_references: list[dict[str, object]] = []
    for reference_name in [*PREFLIGHT_REQUIRED_REFERENCE_NAMES, *PREFLIGHT_OPTIONAL_REFERENCE_NAMES]:
        required = reference_name in PREFLIGHT_REQUIRED_REFERENCE_NAMES
        path = metadata_root / f"{reference_name}.json"
        if reference_name != missing_required and reference_name != missing_optional:
            _write_json(
                path,
                _preflight_reference_metadata(
                    reference_name,
                    source_hash_preview=full_source_hash,
                    reviewer_id_preview=full_reviewer_id[:12],
                    **overrides.get(reference_name, {}),
                ),
            )
            refs[f"{reference_name}_path"] = path
        evidence_references.append(
            {
                "reference_name": reference_name,
                "reference_type": f"{reference_name}_ref",
                "path": str(path),
                "required": required,
                "expected_workflow_area": reference_name.upper(),
                "expected_report_only": True,
                "expected_diagnostic_only": True,
                "expected_metadata_only": True,
                "expected_negative_flags": PREFLIGHT_NEGATIVE_FALSE_FIELDS,
                "allow_statuses": ["PASS", "WARN", "METADATA_PRESENT_REPORT_ONLY"],
                "warn_statuses": ["WARN", "EXPECTED_HASH_VERIFICATION_WARN_HASH_MISMATCH"],
                "block_statuses": ["FAIL"],
                "disclosure_level": "metadata_only_no_private_path_or_full_hash",
            }
        )
    manifest_path = root / "preflight_manifest.json"
    _write_json(
        manifest_path,
        {
            "preflight_id": "preflight-dashboard-0001",
            "declared_package_id": "declared-package-dashboard-0001",
            "package_schema_version": "preflight_v0_1",
            "created_at": "2026-07-04T00:00:00Z",
            "prepared_by": "synthetic-reviewer",
            "report_only": True,
            "diagnostic_only": True,
            "requested_preflight_level": PREFLIGHT_METADATA_REFERENCES_ONLY,
            "requested_package_creation_level": PREFLIGHT_PACKAGE_CREATION_NONE,
            "requested_csv_read_level": PREFLIGHT_CSV_READ_NONE,
            "requested_source_hash_validation_level": PREFLIGHT_SOURCE_HASH_VALIDATION_NONE,
            "requested_revision_id_validation_level": PREFLIGHT_REVISION_ID_VALIDATION_NONE,
            "requested_available_time_validation_level": PREFLIGHT_AVAILABLE_TIME_VALIDATION_NONE,
            "requested_pit_admissibility_level": PREFLIGHT_PIT_ADMISSIBILITY_NONE,
            "requested_reviewer_authority_level": PREFLIGHT_REVIEWER_AUTHORITY_NONE,
            "requested_quality_status_level": PREFLIGHT_QUALITY_STATUS_NONE,
            "requested_limitation_review_level": PREFLIGHT_LIMITATION_REVIEW_NONE,
            "requested_permission_review_level": PREFLIGHT_PERMISSION_REVIEW_NONE,
            "requested_source_reliability_level": PREFLIGHT_SOURCE_RELIABILITY_NONE,
            "requested_active_input_level": PREFLIGHT_ACTIVE_INPUT_NONE,
            "requested_replay_readiness_level": PREFLIGHT_REPLAY_READINESS_NONE,
            "evidence_references": evidence_references,
            "required_evidence_policy": "strict_metadata_complete",
            "warning_policy": "warnings_do_not_create_package_candidate",
            "blocker_policy": "blockers_remain_report_only",
            "disclosure_policy": "metadata_counts_and_booleans_only",
            "forbidden_downstream_flags": {field: False for field in PREFLIGHT_NEGATIVE_FALSE_FIELDS},
            "limitations": ["preflight dashboard fixture is metadata-reference context only"],
        },
    )
    return manifest_path, refs, metadata_root, full_source_hash, full_reviewer_id


def _preflight_reference_metadata(
    reference_name: str,
    *,
    source_hash_preview: str,
    reviewer_id_preview: str,
    **overrides: object,
) -> dict[str, object]:
    metadata: dict[str, object] = {
        "runtime_status": "METADATA_PRESENT_REPORT_ONLY",
        "health_status": "PASS",
        "workflow_stage": f"{reference_name.upper()}_REPORT_ONLY",
        "report_only": True,
        "diagnostic_only": True,
        "metadata_only": True,
        "source_hash_preview": source_hash_preview,
        "reviewer_id_preview": reviewer_id_preview,
        "issue_count": 0,
        "warning_count": 0,
        "blocker_count": 0,
        "limitations": ["synthetic preflight dashboard metadata reference"],
        "forbidden_downstream_flags": {field: False for field in PREFLIGHT_NEGATIVE_FALSE_FIELDS},
    }
    metadata.update({field: False for field in PREFLIGHT_NEGATIVE_FALSE_FIELDS})
    metadata.update(overrides)
    return metadata


def _run_preflight_metadata_context(
    tmp_path: Path,
    root: Path,
    *,
    run_id: str,
    missing_optional: str | None = None,
    missing_required: str | None = None,
    metadata_overrides: dict[str, dict[str, object]] | None = None,
) -> tuple[dict[str, object], Path, str, str]:
    manifest_path, refs, metadata_root, full_source_hash, full_reviewer_id = _preflight_inputs(
        tmp_path / f"{run_id}_inputs",
        missing_optional=missing_optional,
        missing_required=missing_required,
        metadata_overrides=metadata_overrides,
    )
    result = run_real_reviewed_local_csv_package_candidate_preflight(
        output_root=_preflight_output_root(root),
        run_id=run_id,
        preflight_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_path.parent],
        allow_real_reviewed_local_csv_package_candidate_preflight=True,
        **refs,
    )
    for path in [manifest_path, *refs.values()]:
        path.unlink(missing_ok=True)
    return result, metadata_root, full_source_hash, full_reviewer_id


def _csv_physical_data_line_count_only_inputs(
    root: Path,
    *,
    data_lines: list[str],
) -> tuple[Path, Path, Path]:
    csv_path = root / "reviewed" / "tiny_physical_line_count.csv"
    csv_path.parent.mkdir(parents=True, exist_ok=True)
    content = "symbol,signal_date,review_note\n" + "\n".join(data_lines)
    if data_lines:
        content += "\n"
    csv_path.write_text(content, encoding="utf-8")

    header_metadata_path = root / "header_metadata" / "metadata.json"
    header_metadata_path.parent.mkdir(parents=True, exist_ok=True)
    header_metadata_path.write_text(
        json.dumps(
            {
                "report_only": True,
                "diagnostic_only": True,
                "csv_header_read": True,
                "csv_header_values_recorded": False,
                "csv_header_column_count": 3,
                "target_csv_path": str(csv_path),
                "csv_row_count_computed": False,
                "csv_values_read": False,
                "csv_full_content_read": False,
                "real_csv_consumed": False,
                "local_file_byte_hash_computed": False,
                "expected_hash_verification_performed": False,
                "source_hash_validated": False,
                "revision_id_validated": False,
                "available_time_validated": False,
                "pit_admissibility_validated": False,
                "source_reliability_scored": False,
                "reviewer_authority_validated": False,
                **csv_physical_data_line_count_safety_flags(),
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )

    manifest_path = root / "manifest.json"
    manifest_path.write_text(
        json.dumps(
            {
                "package_id": "tiny-pit-csv-physical-data-line-count-only-dashboard",
                "package_schema_version": "v0.1",
                "created_at": "2026-07-02T00:00:00Z",
                "prepared_by": "synthetic-reviewer",
                "report_only": True,
                "diagnostic_only": True,
                "requested_file_touch_level": CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
                "requested_csv_read_level": CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
                "requested_csv_physical_data_line_count_level": CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
                "requested_local_file_hash_level": CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_LOCAL_HASH_NONE,
                "requested_expected_hash_verification_level": (
                    CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_EXPECTED_HASH_NONE
                ),
                "csv_file_references": [
                    {
                        "reference_type": "reviewed_local_csv_file_ref",
                        "reference_name": "tiny-physical-line-count-dashboard",
                        "path": str(csv_path),
                        "required": True,
                        "intended_touch_level": CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
                        "declared_only": False,
                        "notes": "Synthetic dashboard fixture; values must not be surfaced.",
                    }
                ],
                "header_metadata_reference": str(header_metadata_path),
                "row_count_policy": PHYSICAL_NON_HEADER_LINE_COUNT,
                "forbidden_downstream_flags": {
                    flag: False for flag in CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_REQUIRED_FALSE_FLAGS
                },
                "limitations": [
                    "Physical data-line count-only dashboard fixture; no CSV parsing or value reading.",
                ],
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    return manifest_path, header_metadata_path, csv_path


def _active_replay_input_promotion_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_promotion_test_inputs"
    validator = _active_replay_input_promotion_validator_artifact(input_root / "validator")
    smoke = _active_replay_input_promotion_smoke_artifact(input_root / "smoke", validator)
    request = _active_replay_input_promotion_request_manifest(input_root / "promotion_request.json", validator, smoke)
    review = _active_replay_input_promotion_review_manifest(input_root / "human_review.json")
    return run_active_replay_input_promotion(
        ActiveReplayInputPromotionSettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_promotion_v0_1",
            validator_artifact=validator,
            smoke_artifact=smoke,
            promotion_request_manifest=request,
            human_review_manifest=review,
        )
    )


def _active_replay_input_promotion_validator_artifact(path: Path) -> Path:
    path.mkdir(parents=True, exist_ok=True)
    metadata = {
        "validator_run_id": "validator_001",
        "status": "REPLAY_INPUT_GATE_PASS_CANDIDATE",
        "workflow_stage": "REPLAY_INPUT_GATE_PASS_CANDIDATE",
        "input_package_path": str(path.parent / "input_package"),
        "pass_candidate": True,
        "active_replay_input_ready": False,
        "active_replay_input": False,
        "forward_labels_exist": False,
        "weights_trained": False,
        "active_stock_profile_exists": False,
        "real_buy_review_eligible": False,
        "approval_applied": False,
        "order_placed": False,
        "llm_api_called": False,
        "external_api_called": False,
        "cache_mutated": False,
        "current_candidates_run": False,
        "snapshot_built": False,
        "signal_semantics_changed": False,
        "report_only": True,
        "diagnostic_only": True,
        "artifact_path": str(path),
    }
    (path / "metadata.json").write_text(json.dumps(metadata, indent=2, sort_keys=True), encoding="utf-8")
    return path


def _active_replay_input_promotion_smoke_artifact(path: Path, validator_path: Path) -> Path:
    path.mkdir(parents=True, exist_ok=True)
    validator_metadata = json.loads((validator_path / "metadata.json").read_text(encoding="utf-8"))
    metadata = {
        "smoke_run_id": "smoke_001",
        "validator_run_id": validator_metadata["validator_run_id"],
        "validator_artifact_path": str(validator_path),
        "validator_status": "REPLAY_INPUT_GATE_PASS_CANDIDATE",
        "validation_status": "PASS",
        "workflow_stage": "SMOKE_PASS_CANDIDATE_READY",
        "smoke_stage": "SMOKE_PASS_CANDIDATE_READY",
        "pass_candidate": True,
        "active_replay_input_ready": False,
        "active_replay_input": False,
        "forward_labels_exist": False,
        "weights_trained": False,
        "active_stock_profile_exists": False,
        "real_buy_review_eligible": False,
        "report_only": True,
        "diagnostic_only": True,
        "artifact_path": str(path),
    }
    (path / "smoke_metadata.json").write_text(json.dumps(metadata, indent=2, sort_keys=True), encoding="utf-8")
    return path


def _active_replay_input_promotion_request_manifest(path: Path, validator_artifact: Path, smoke_artifact: Path) -> Path:
    path.parent.mkdir(parents=True, exist_ok=True)
    request = {
        "promotion_request_id": "promotion_request_001",
        "requested_by": "fixture_reviewer",
        "requested_at": "2024-04-02T16:10:00+08:00",
        "request_reason": "fixture report-only promotion review",
        "validator_artifact_ref": str(validator_artifact),
        "smoke_artifact_ref": str(smoke_artifact),
        "input_package_ref": str(validator_artifact.parent / "input_package"),
        "requested_status": "PROMOTION_READY_FOR_HUMAN_REVIEW",
        "report_only": True,
        "diagnostic_only": True,
        "active_replay_input_ready": False,
        "active_replay_input": False,
        "forward_labels_exist": False,
        "weights_trained": False,
        "active_stock_profile_exists": False,
        "real_buy_review_eligible": False,
        "approval_applied": False,
        "order_placed": False,
        "llm_api_called": False,
        "external_api_called": False,
        "cache_mutated": False,
        "current_candidates_run": False,
        "snapshot_built": False,
        "signal_semantics_changed": False,
    }
    path.write_text(json.dumps(request, indent=2, sort_keys=True), encoding="utf-8")
    return path


def _active_replay_input_promotion_review_manifest(path: Path) -> Path:
    path.parent.mkdir(parents=True, exist_ok=True)
    review = {
        "human_review_id": "human_review_001",
        "reviewer": "fixture_reviewer",
        "reviewed_at": "2024-04-02T16:20:00+08:00",
        "review_scope": "report-only active replay input promotion readiness",
        "pit_universe_reviewed": True,
        "source_permission_reviewed": True,
        "raw_evidence_reviewed": True,
        "factor_definition_reviewed": True,
        "factor_observation_reviewed": True,
        "event_structured_reviewed": True,
        "company_exposure_reviewed": True,
        "leakage_reviewed": True,
        "side_effect_reviewed": True,
        "promotion_decision_reviewed": True,
        "review_result": "READY_FOR_HUMAN_REVIEW",
        "report_only": True,
        "diagnostic_only": True,
        "notes": "fixture only",
    }
    path.write_text(json.dumps(review, indent=2, sort_keys=True), encoding="utf-8")
    return path


def _active_replay_input_acceptance_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_acceptance_test_inputs"
    promotion = _active_replay_input_promotion_ready(root)
    promotion_artifact = promotion.artifact_path
    request = _active_replay_input_acceptance_request_manifest(
        input_root / "acceptance_request.json",
        promotion_artifact,
    )
    authority = _active_replay_input_acceptance_authority_manifest(input_root / "authority.json")
    attestation = _active_replay_input_acceptance_attestation_manifest(input_root / "attestation.json")
    second_review = _active_replay_input_acceptance_second_review_manifest(input_root / "second_review.json")
    red_team = _active_replay_input_acceptance_red_team_manifest(input_root / "red_team.json")
    promotion_health = _write_json(input_root / "promotion_health.json", {"health_status": "PASS"})
    promotion_status = _write_json(
        input_root / "promotion_status.json",
        {"status": "PROMOTION_READY_FOR_HUMAN_REVIEW", "ready_for_human_review": True},
    )
    return run_active_replay_input_acceptance(
        ActiveReplayInputAcceptanceSettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_acceptance_v0_1",
            promotion_artifact=promotion_artifact,
            promotion_health_artifact=promotion_health,
            promotion_status_artifact=promotion_status,
            acceptance_request_manifest=request,
            reviewer_authority_manifest=authority,
            manual_attestation_manifest=attestation,
            second_review_manifest=second_review,
            red_team_review_manifest=red_team,
        )
    )


def _active_replay_input_active_ready_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_active_ready_test_inputs"
    acceptance = _active_replay_input_acceptance_ready(root)
    acceptance_artifact = acceptance.artifact_path
    acceptance_health = _write_json(input_root / "acceptance_health.json", {"health_status": "PASS"})
    acceptance_status = _write_json(
        input_root / "acceptance_status.json",
        {
            "status": "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW",
            "ready_for_active_ready_review": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
        },
    )
    return run_active_replay_input_active_ready(
        ActiveReplayInputActiveReadySettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_active_ready_v0_1",
            acceptance_artifact=acceptance_artifact,
            acceptance_health_artifact=acceptance_health,
            acceptance_status_artifact=acceptance_status,
            active_ready_request_manifest=_active_ready_request_manifest(
                input_root / "active_ready_request.json",
                acceptance_artifact,
            ),
            active_ready_authority_manifest=_active_ready_authority_manifest(input_root / "authority.json"),
            pit_coverage_manifest=_active_ready_pit_coverage_manifest(input_root / "pit_coverage.json"),
            source_coverage_manifest=_active_ready_source_coverage_manifest(input_root / "source_coverage.json"),
            evidence_coverage_manifest=_active_ready_evidence_coverage_manifest(input_root / "evidence_coverage.json"),
            taxonomy_compliance_manifest=_active_ready_taxonomy_manifest(input_root / "taxonomy.json"),
            leakage_review_manifest=_active_ready_leakage_manifest(input_root / "leakage.json"),
            side_effect_review_manifest=_active_ready_side_effect_manifest(input_root / "side_effect.json"),
            overclaim_review_manifest=_active_ready_overclaim_manifest(input_root / "overclaim.json"),
        )
    )


def _active_ready_request_manifest(path: Path, acceptance_artifact: Path) -> Path:
    return _write_json(
        path,
        {
            "active_ready_request_id": "active_ready_request_001",
            "requested_by": "fixture_reviewer",
            "requested_at": "2024-04-02T18:00:00+08:00",
            "request_reason": "fixture final-review-only active-ready governance",
            "acceptance_artifact_ref": str(acceptance_artifact),
            "requested_status": "ACTIVE_READY_READY_FOR_FINAL_REVIEW",
            "report_only": True,
            "diagnostic_only": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
            "forward_labels_exist": False,
            "weights_trained": False,
            "active_stock_profile_exists": False,
            "real_buy_review_eligible": False,
            "approval_applied": False,
            "order_placed": False,
            "message_sent": False,
            "llm_api_called": False,
            "external_api_called": False,
            "cache_mutated": False,
            "data_raw_written": False,
            "data_processed_written": False,
            "data_cache_written": False,
            "current_candidates_run": False,
            "snapshot_built": False,
            "signal_semantics_changed": False,
        },
    )


def _active_ready_authority_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "active_ready_authority_id": "authority_001",
            "primary_approver": "primary_fixture_reviewer",
            "second_approver": "second_fixture_reviewer",
            "pit_reviewer": "pit_fixture_reviewer",
            "source_reviewer": "source_fixture_reviewer",
            "evidence_reviewer": "evidence_fixture_reviewer",
            "risk_compliance_reviewer": "risk_fixture_reviewer",
            "strategy_owner": "strategy_fixture_owner",
            "authority_scope": "review-only final-review-ready active-ready governance",
            "authority_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_pit_coverage_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pit_coverage_id": "pit_001",
            "available_time_coverage_complete": True,
            "universe_coverage_complete": True,
            "suspension_st_delist_coverage_complete": True,
            "corporate_action_policy_reviewed": True,
            "pit_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_source_coverage_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "source_coverage_id": "source_001",
            "source_id_coverage_complete": True,
            "source_hash_coverage_complete": True,
            "revision_id_coverage_complete": True,
            "permission_class_coverage_complete": True,
            "quality_status_coverage_complete": True,
            "source_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_evidence_coverage_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "evidence_coverage_id": "evidence_001",
            "raw_evidence_refs_complete": True,
            "replay_evidence_bundle_complete": True,
            "factor_definition_coverage_complete": True,
            "factor_observation_coverage_complete": True,
            "event_structured_coverage_complete": True,
            "company_exposure_coverage_complete": True,
            "evidence_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_taxonomy_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "taxonomy_compliance_id": "taxonomy_001",
            "uses_8_layer_taxonomy": True,
            "not_fixed_12_only": True,
            "factor_layer_metadata_complete": True,
            "trade_usage_metadata_complete": True,
            "compliance_metadata_complete": True,
            "taxonomy_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_leakage_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "leakage_review_id": "leakage_001",
            "no_future_labels": True,
            "no_forward_returns": True,
            "no_training_outputs": True,
            "no_model_weights": True,
            "no_stock_profile_artifacts": True,
            "no_buy_review_eligibility": True,
            "leakage_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_side_effect_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "side_effect_review_id": "side_effect_001",
            "no_approval_applied": True,
            "no_order_placed": True,
            "no_message_sent": True,
            "no_llm_api_called": True,
            "no_external_api_called": True,
            "no_cache_mutated": True,
            "no_data_raw_written": True,
            "no_data_processed_written": True,
            "no_data_cache_written": True,
            "no_current_candidates_run": True,
            "no_snapshot_built": True,
            "no_signal_semantics_changed": True,
            "side_effect_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_ready_overclaim_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "overclaim_review_id": "overclaim_001",
            "pass_candidate_not_active_ready": True,
            "smoke_not_active_ready": True,
            "promotion_not_active_ready": True,
            "acceptance_not_active_ready": True,
            "final_review_not_active_ready": True,
            "active_ready_not_replay": True,
            "active_ready_not_labels": True,
            "active_ready_not_training": True,
            "active_ready_not_stock_profile": True,
            "active_ready_not_buy_review": True,
            "active_ready_not_trading": True,
            "active_ready_not_performance_validation": True,
            "overclaim_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_final_review_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_final_review_test_inputs"
    active_ready = _active_replay_input_active_ready_ready(root)
    active_ready_artifact = active_ready.artifact_path
    active_ready_health = _write_json(input_root / "active_ready_health.json", {"health_status": "PASS"})
    active_ready_status = _write_json(
        input_root / "active_ready_status.json",
        {
            "status": "ACTIVE_READY_READY_FOR_FINAL_REVIEW",
            "health_status": "PASS",
            "workflow_stage": "ACTIVE_READY_READY_FOR_FINAL_REVIEW",
            "ready_for_final_review": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
        },
    )
    return run_active_replay_input_final_review(
        ActiveReplayInputFinalReviewSettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_final_review_v0_1",
            active_ready_artifact=active_ready_artifact,
            active_ready_health_artifact=active_ready_health,
            active_ready_status_artifact=active_ready_status,
            final_review_package_manifest=_final_review_package_manifest(
                input_root / "final_review_package.json",
                active_ready_artifact,
            ),
            final_review_authority_manifest=_final_review_authority_manifest(input_root / "authority.json"),
            final_review_attestation_manifest=_final_review_attestation_manifest(input_root / "attestation.json"),
            pit_source_evidence_attachment_bundle=_final_review_pit_source_bundle(input_root / "pit_source.json"),
            taxonomy_attachment_bundle=_final_review_taxonomy_bundle(input_root / "taxonomy.json"),
            leakage_side_effect_evidence_bundle=_final_review_leakage_side_effect_bundle(
                input_root / "leakage_side_effect.json"
            ),
            overclaim_evidence_bundle=_final_review_overclaim_bundle(input_root / "overclaim.json"),
            emission_request_manifest=_final_review_emission_request_manifest(input_root / "emission.json"),
        )
    )


def _active_replay_input_emission_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_emission_test_inputs"
    final_review = _active_replay_input_final_review_ready(root)
    final_review_artifact = final_review.artifact_path
    final_review_health = _write_json(input_root / "final_review_health.json", {"health_status": "PASS"})
    final_review_status = _write_json(
        input_root / "final_review_status.json",
        {
            "status": "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW",
            "workflow_stage": "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW",
            "ready_for_emission_review": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
        },
    )
    return run_active_replay_input_emission(
        ActiveReplayInputEmissionSettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_emission_v0_1",
            final_review_artifact_path=final_review_artifact,
            final_review_health_artifact_path=final_review_health,
            final_review_status_artifact_path=final_review_status,
            emission_request_manifest_path=_emission_request_manifest(input_root / "emission_request.json"),
            emission_authority_manifest_path=_emission_authority_manifest(input_root / "authority.json"),
            emission_attestation_manifest_path=_emission_attestation_manifest(input_root / "attestation.json"),
            pit_source_evidence_bundle_path=_emission_pit_source_evidence(input_root / "pit_source.json"),
            taxonomy_evidence_bundle_path=_emission_taxonomy_bundle(input_root / "taxonomy.json"),
            leakage_side_effect_evidence_bundle_path=_emission_leakage_side_effect_bundle(
                input_root / "leakage_side_effect.json"
            ),
            overclaim_evidence_bundle_path=_emission_overclaim_bundle(input_root / "overclaim.json"),
        )
    )


def _active_replay_input_ready_ready(root: Path):
    input_root = root / "manual_diagnostics" / "active_replay_input_ready_test_inputs"
    ready_decision = _write_json(input_root / "ready_decision.json", _active_replay_input_ready_decision_payload())
    ready_decision_health = _write_json(input_root / "ready_decision_health.json", {"health_status": "PASS"})
    ready_decision_status = _write_json(
        input_root / "ready_decision_status.json",
        {
            "status": "READY_FOR_ACTIVE_REPLAY_INPUT_READY_DECISION",
            "workflow_stage": "READY_FOR_ACTIVE_REPLAY_INPUT_READY_DECISION",
            "ready_for_active_replay_input_ready_decision": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
            "replay_execution_allowed": False,
            "replay_decisions_exist": False,
        },
    )
    governance_audit = input_root / "governance_audit.md"
    governance_audit.write_text("Report-only governance audit accepted for fixture.", encoding="utf-8")
    return run_active_replay_input_ready(
        ActiveReplayInputReadySettings(
            output_dir=root / "manual_diagnostics" / "active_replay_input_ready_v0_1",
            ready_decision_artifact_path=ready_decision,
            ready_decision_health_artifact_path=ready_decision_health,
            ready_decision_status_artifact_path=ready_decision_status,
            governance_audit_path=governance_audit,
            governance_request_manifest_path=_active_replay_input_ready_request_manifest(
                input_root / "governance_request.json"
            ),
            final_authority_manifest_path=_active_replay_input_ready_authority_manifest(
                input_root / "authority.json"
            ),
            final_attestation_manifest_path=_active_replay_input_ready_attestation_manifest(
                input_root / "attestation.json"
            ),
            pit_source_evidence_bundle_path=_active_replay_input_ready_pit_source_bundle(
                input_root / "pit_source.json"
            ),
            taxonomy_evidence_bundle_path=_active_replay_input_ready_taxonomy_bundle(
                input_root / "taxonomy.json"
            ),
            leakage_side_effect_evidence_bundle_path=_active_replay_input_ready_leakage_bundle(
                input_root / "leakage_side_effect.json"
            ),
            overclaim_evidence_bundle_path=_active_replay_input_ready_overclaim_bundle(
                input_root / "overclaim.json"
            ),
        )
    )


def _active_replay_input_ready_decision_payload() -> dict[str, object]:
    return {
        "decision_run_id": "ready_decision_fixture",
        "status": "READY_FOR_ACTIVE_REPLAY_INPUT_READY_DECISION",
        "workflow_stage": "READY_FOR_ACTIVE_REPLAY_INPUT_READY_DECISION",
        "ready_for_active_replay_input_ready_decision": True,
        "active_replay_input_ready": False,
        "active_replay_input": False,
        "active_ready_emitted": False,
        "replay_execution_allowed": False,
        "replay_decisions_exist": False,
        "forward_labels_allowed": False,
        "forward_labels_exist": False,
        "forward_returns_exist": False,
        "training_allowed": False,
        "training_outputs_exist": False,
        "model_weights_exist": False,
        "weights_trained": False,
        "stock_profile_allowed": False,
        "stock_profile_artifacts_exist": False,
        "active_stock_profile_exists": False,
        "buy_review_allowed": False,
        "real_buy_review_eligible": False,
        "trading_allowed": False,
        "order_placed": False,
        "broker_api_called": False,
        "message_sent": False,
        "llm_api_called": False,
        "external_api_called": False,
        "cache_mutated": False,
        "data_raw_written": False,
        "data_processed_written": False,
        "data_cache_written": False,
        "current_candidates_run": False,
        "snapshot_built": False,
        "approved_for_paper": False,
        "report_only": True,
        "diagnostic_only": True,
    }


def _active_replay_input_ready_request_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "request_result": "PASS",
            "requested_status": "READY_TO_EMIT_ACTIVE_REPLAY_INPUT_READY",
            "allow_active_replay_input_ready_emission": False,
            "allow_active_replay_input_creation": False,
            "allow_replay_execution": False,
            "allow_forward_labels": False,
            "allow_training": False,
            "allow_stock_profile": False,
            "allow_buy_review": False,
            "allow_trading": False,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_authority_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "authority_result": "PASS",
            "primary_reviewer": "primary_fixture_reviewer",
            "second_reviewer": "second_fixture_reviewer",
            "authority_scope": "report-only ACTIVE_REPLAY_INPUT_READY governance",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_attestation_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "primary_reviewer_attested": True,
            "second_reviewer_attested": True,
            "no_active_input_creation_attested": True,
            "no_replay_execution_attested": True,
            "no_trading_authority_attested": True,
            "no_performance_claim_attested": True,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_pit_source_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "accepted_pit_universe_evidence_attached": True,
            "source_id_coverage_attached": True,
            "source_hash_coverage_attached": True,
            "revision_id_coverage_attached": True,
            "permission_class_coverage_attached": True,
            "factor_observation_coverage_attached": True,
            "raw_evidence_refs_attached": True,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_taxonomy_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "uses_8_layer_taxonomy": True,
            "not_fixed_12_only": True,
            "factor_layer_metadata_attached": True,
            "trade_usage_metadata_attached": True,
            "compliance_metadata_attached": True,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_leakage_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "no_future_labels": True,
            "no_forward_returns": True,
            "no_replay_decisions": True,
            "no_replay_execution": True,
            "no_training_outputs": True,
            "no_model_weights": True,
            "no_stock_profile_artifacts": True,
            "no_buy_review_eligibility": True,
            "no_approved_for_paper": True,
            "no_broker_api_called": True,
            "no_order_placed": True,
            "no_message_sent": True,
            "no_llm_api_called": True,
            "no_external_api_called": True,
            "no_cache_mutated": True,
            "no_data_raw_written": True,
            "no_data_processed_written": True,
            "no_data_cache_written": True,
            "no_current_candidates_run": True,
            "no_snapshot_built": True,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_ready_overclaim_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pass_candidate_not_active_ready": True,
            "smoke_not_active_ready": True,
            "promotion_not_active_ready": True,
            "acceptance_not_active_ready": True,
            "active_ready_final_review_not_active_ready": True,
            "final_review_ready_not_active_input_ready": True,
            "emission_ready_review_not_active_input_ready": True,
            "ready_decision_not_active_replay_input_ready": True,
            "ready_to_emit_not_active_replay_input_ready": True,
            "active_input_ready_not_replay": True,
            "active_input_ready_not_labels": True,
            "active_input_ready_not_training": True,
            "active_input_ready_not_stock_profile": True,
            "active_input_ready_not_buy_review": True,
            "active_input_ready_not_trading": True,
            "active_input_ready_not_performance_validation": True,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_request_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "emission_request_id": "emission_request_001",
            "requested_status": "EMISSION_READY_FOR_ACTIVE_REPLAY_INPUT_READY_REVIEW",
            "requested_by": "fixture_reviewer",
            "requested_at": "2024-04-02T20:00:00+08:00",
            "request_reason": "fixture report-only active replay input emission review",
            "allow_active_replay_input_ready_emission": False,
            "allow_active_replay_input_creation": False,
            "allow_replay_execution": False,
            "allow_forward_labels": False,
            "allow_training": False,
            "allow_stock_profile": False,
            "allow_buy_review": False,
            "allow_trading": False,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_authority_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "emission_authority_id": "emission_authority_001",
            "primary_reviewer": "primary_fixture_reviewer",
            "second_reviewer": "second_fixture_reviewer",
            "pit_source_reviewer": "pit_fixture_reviewer",
            "evidence_taxonomy_reviewer": "evidence_fixture_reviewer",
            "risk_compliance_reviewer": "risk_fixture_reviewer",
            "system_operator": "operator_fixture",
            "strategy_owner": "strategy_fixture_owner",
            "authority_scope": "report-only active replay input emission review",
            "authority_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_attestation_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "attestation_id": "emission_attestation_001",
            "primary_reviewer_attested": True,
            "second_reviewer_attested": True,
            "pit_source_reviewer_attested": True,
            "evidence_taxonomy_reviewer_attested": True,
            "risk_compliance_reviewer_attested": True,
            "no_trading_authority_attested": True,
            "no_performance_claim_attested": True,
            "no_replay_execution_attested": True,
            "attestation_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_pit_source_evidence(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pit_universe_evidence_attached": True,
            "available_time_coverage_attached": True,
            "source_id_coverage_attached": True,
            "source_hash_coverage_attached": True,
            "revision_id_coverage_attached": True,
            "permission_class_coverage_attached": True,
            "quality_status_coverage_attached": True,
            "raw_evidence_refs_attached": True,
            "replay_evidence_bundle_ref_attached": True,
            "factor_definition_coverage_attached": True,
            "factor_observation_coverage_attached": True,
            "event_structured_coverage_attached": True,
            "company_exposure_coverage_attached": True,
            "attachment_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_taxonomy_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "uses_8_layer_taxonomy": True,
            "not_fixed_12_only": True,
            "factor_layer_metadata_attached": True,
            "trade_usage_metadata_attached": True,
            "compliance_metadata_attached": True,
            "taxonomy_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_leakage_side_effect_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "no_future_labels": True,
            "no_forward_returns": True,
            "no_training_outputs": True,
            "no_model_weights": True,
            "no_stock_profile_artifacts": True,
            "no_buy_review_eligibility": True,
            "no_approved_for_paper": True,
            "no_order_placed": True,
            "no_message_sent": True,
            "no_broker_api_called": True,
            "no_llm_api_called": True,
            "no_external_api_called": True,
            "no_cache_mutated": True,
            "no_data_raw_written": True,
            "no_data_processed_written": True,
            "no_data_cache_written": True,
            "no_current_candidates_run": True,
            "no_snapshot_built": True,
            "no_signal_semantics_changed": True,
            "leakage_side_effect_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _emission_overclaim_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pass_candidate_not_active_ready": True,
            "smoke_not_active_ready": True,
            "promotion_not_active_ready": True,
            "acceptance_not_active_ready": True,
            "active_ready_final_review_not_active_ready": True,
            "final_review_ready_not_active_input_ready": True,
            "emission_ready_review_not_active_input_ready": True,
            "active_input_ready_not_replay": True,
            "active_input_ready_not_labels": True,
            "active_input_ready_not_training": True,
            "active_input_ready_not_stock_profile": True,
            "active_input_ready_not_buy_review": True,
            "active_input_ready_not_trading": True,
            "active_input_ready_not_performance_validation": True,
            "overclaim_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_package_manifest(path: Path, active_ready_artifact: Path) -> Path:
    return _write_json(
        path,
        {
            "final_review_package_id": "final_review_package_001",
            "requested_by": "fixture_reviewer",
            "requested_at": "2024-04-02T19:00:00+08:00",
            "package_reason": "fixture final review emission-readiness context",
            "active_ready_artifact_ref": str(active_ready_artifact),
            "requested_status": "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW",
            "report_only": True,
            "diagnostic_only": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
            "forward_labels_exist": False,
            "weights_trained": False,
            "active_stock_profile_exists": False,
            "real_buy_review_eligible": False,
            "approval_applied": False,
            "order_placed": False,
            "message_sent": False,
            "llm_api_called": False,
            "external_api_called": False,
            "cache_mutated": False,
            "data_raw_written": False,
            "data_processed_written": False,
            "data_cache_written": False,
            "current_candidates_run": False,
            "snapshot_built": False,
            "signal_semantics_changed": False,
        },
    )


def _final_review_authority_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "final_review_authority_id": "final_authority_001",
            "primary_reviewer": "primary_fixture_reviewer",
            "second_reviewer": "second_fixture_reviewer",
            "pit_source_reviewer": "pit_fixture_reviewer",
            "evidence_taxonomy_reviewer": "evidence_fixture_reviewer",
            "risk_compliance_reviewer": "risk_fixture_reviewer",
            "system_operator": "operator_fixture",
            "strategy_owner": "strategy_fixture_owner",
            "authority_scope": "report-only final-review emission-readiness context",
            "authority_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_attestation_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "primary_reviewer_attested": True,
            "second_reviewer_attested": True,
            "pit_source_reviewer_attested": True,
            "evidence_taxonomy_reviewer_attested": True,
            "risk_compliance_reviewer_attested": True,
            "no_trading_authority_attested": True,
            "no_performance_claim_attested": True,
            "no_replay_execution_attested": True,
            "attestation_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_pit_source_bundle(path: Path) -> Path:
    payload = {
        "pit_universe_evidence_attached": True,
        "available_time_coverage_attached": True,
        "source_id_coverage_attached": True,
        "source_hash_coverage_attached": True,
        "revision_id_coverage_attached": True,
        "permission_class_coverage_attached": True,
        "quality_status_coverage_attached": True,
        "raw_evidence_refs_attached": True,
        "replay_evidence_bundle_ref_attached": True,
        "factor_definition_coverage_attached": True,
        "factor_observation_coverage_attached": True,
        "event_structured_coverage_attached": True,
        "company_exposure_coverage_attached": True,
        "attachment_result": "PASS",
        "report_only": True,
        "diagnostic_only": True,
    }
    return _write_json(path, payload)


def _final_review_taxonomy_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "uses_8_layer_taxonomy": True,
            "not_fixed_12_only": True,
            "factor_layer_metadata_attached": True,
            "trade_usage_metadata_attached": True,
            "compliance_metadata_attached": True,
            "taxonomy_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_leakage_side_effect_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "no_future_labels": True,
            "no_forward_returns": True,
            "no_training_outputs": True,
            "no_model_weights": True,
            "no_stock_profile_artifacts": True,
            "no_buy_review_eligibility": True,
            "no_approved_for_paper": True,
            "no_order_placed": True,
            "no_message_sent": True,
            "no_broker_api_called": True,
            "no_llm_api_called": True,
            "no_external_api_called": True,
            "no_cache_mutated": True,
            "no_data_raw_written": True,
            "no_data_processed_written": True,
            "no_data_cache_written": True,
            "no_current_candidates_run": True,
            "no_snapshot_built": True,
            "no_signal_semantics_changed": True,
            "leakage_side_effect_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_overclaim_bundle(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pass_candidate_not_active_ready": True,
            "smoke_not_active_ready": True,
            "promotion_not_active_ready": True,
            "acceptance_not_active_ready": True,
            "active_ready_final_review_not_active_ready": True,
            "final_review_ready_not_active_input_ready": True,
            "active_input_ready_not_replay": True,
            "active_input_ready_not_labels": True,
            "active_input_ready_not_training": True,
            "active_input_ready_not_stock_profile": True,
            "active_input_ready_not_buy_review": True,
            "active_input_ready_not_trading": True,
            "active_input_ready_not_performance_validation": True,
            "overclaim_result": "PASS",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _final_review_emission_request_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "requested_status": "FINAL_REVIEW_READY_FOR_EMISSION_REVIEW",
            "allow_active_replay_input_ready_emission": False,
            "allow_active_replay_input_creation": False,
            "allow_replay_execution": False,
            "allow_forward_labels": False,
            "allow_training": False,
            "allow_stock_profile": False,
            "allow_buy_review": False,
            "allow_trading": False,
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_acceptance_request_manifest(path: Path, promotion_artifact: Path) -> Path:
    return _write_json(
        path,
        {
            "acceptance_request_id": "acceptance_request_001",
            "requested_by": "fixture_reviewer",
            "requested_at": "2024-04-02T17:00:00+08:00",
            "request_reason": "fixture report-only acceptance",
            "promotion_artifact_ref": str(promotion_artifact),
            "requested_status": "ACCEPTANCE_READY_FOR_ACTIVE_READY_REVIEW",
            "report_only": True,
            "diagnostic_only": True,
            "active_replay_input_ready": False,
            "active_replay_input": False,
            "active_ready_emitted": False,
            "forward_labels_exist": False,
            "weights_trained": False,
            "active_stock_profile_exists": False,
            "real_buy_review_eligible": False,
            "approval_applied": False,
            "order_placed": False,
            "llm_api_called": False,
            "external_api_called": False,
            "cache_mutated": False,
            "current_candidates_run": False,
            "snapshot_built": False,
            "signal_semantics_changed": False,
        },
    )


def _active_replay_input_acceptance_authority_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "reviewer_authority_id": "authority_001",
            "primary_reviewer": "primary_fixture_reviewer",
            "primary_reviewer_role": "research_governance_reviewer",
            "second_reviewer": "second_fixture_reviewer",
            "red_team_reviewer": "red_team_fixture_reviewer",
            "data_source_reviewer": "source_fixture_reviewer",
            "strategy_owner": "strategy_fixture_owner",
            "authority_scope": "review-only acceptance for active-ready review",
            "authority_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_acceptance_attestation_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "pit_validity_attested": True,
            "source_permission_attested": True,
            "source_hash_revision_attested": True,
            "no_future_labels_attested": True,
            "no_training_leakage_attested": True,
            "no_stock_profile_leakage_attested": True,
            "no_buy_review_eligibility_attested": True,
            "no_active_ready_attested": True,
            "no_side_effects_attested": True,
            "no_trading_authorization_attested": True,
            "report_only": True,
            "diagnostic_only": True,
            "attestation_result": "ACCEPTED_FOR_REVIEW_ONLY",
        },
    )


def _active_replay_input_acceptance_second_review_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "second_review_id": "second_review_001",
            "reviewer": "second_fixture_reviewer",
            "reviewed_at": "2024-04-02T17:10:00+08:00",
            "pit_reviewed": True,
            "source_reviewed": True,
            "evidence_reviewed": True,
            "leakage_reviewed": True,
            "side_effect_reviewed": True,
            "overclaim_wording_reviewed": True,
            "review_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _active_replay_input_acceptance_red_team_manifest(path: Path) -> Path:
    return _write_json(
        path,
        {
            "red_team_review_id": "red_team_001",
            "reviewer": "red_team_fixture_reviewer",
            "reviewed_at": "2024-04-02T17:20:00+08:00",
            "attempted_to_find_future_leakage": True,
            "attempted_to_find_permission_gap": True,
            "attempted_to_find_overclaim": True,
            "attempted_to_find_side_effect_risk": True,
            "red_team_result": "ACCEPTED_FOR_REVIEW_ONLY",
            "report_only": True,
            "diagnostic_only": True,
        },
    )


def _workflow_to_current_candidates(root: Path) -> Path:
    _data_preparation_status(root)
    _snapshot_quality(root)
    _current_candidate(root)
    return root


def _workflow_to_current_candidate_health(root: Path) -> Path:
    _workflow_to_current_candidates(root)
    _current_candidate_health(root, status="PASS")
    return root


def _workflow_to_paper_handoff(root: Path) -> Path:
    _workflow_to_current_candidate_health(root)
    _current_to_paper_handoff(root)
    return root


def _workflow_to_review_template(root: Path) -> Path:
    _workflow_to_paper_handoff(root)
    _review_template(root)
    return root


def _workflow_to_review(root: Path) -> Path:
    _workflow_to_review_template(root)
    _review_template_health(root, status="PASS")
    _paper_review(root)
    return root


def _workflow_to_daily(root: Path) -> Path:
    _workflow_to_review(root)
    _daily_paper(root)
    return root


def _workflow_complete(root: Path) -> Path:
    _workflow_to_daily(root)
    _reconciliation(root, status="PASS")
    _paper_workflow_status(root, status="PASS")
    return root


def _data_preparation_status(root: Path) -> Path:
    folder = root / "data_preparation" / "workflow_status" / "data-prep-status-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "data_preparation_workflow_status_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "workflow_status_id": "data-prep-status-a",
            "created_at": f"{DECISION_DATE}T09:00:00",
            "status": "PASS",
            "workflow_stage": "DATA_PREP_WORKFLOW_COMPLETE",
            "latest_decision_date": DECISION_DATE,
            "latest_pipeline_id": "pipeline-a",
            "latest_snapshot_id": "snapshot-a",
            "next_manual_action": "Proceed to current-to-paper.",
            "output_files": {"data_preparation_workflow_status_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _snapshot_quality(root: Path, *, status: str = "PASS") -> Path:
    folder = root / "snapshot_quality" / "snapshot-a_gate-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "snapshot_quality_gate_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "snapshot_id": "snapshot-a",
            "quality_gate_id": "gate-a",
            "created_at": f"{DECISION_DATE}T09:10:00",
            "status": status,
            "warning_count": 1 if status == "WARN" else 0,
            "error_count": 1 if status == "FAIL" else 0,
            "output_files": {"snapshot_quality_gate_report": str(report)},
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _current_candidate(root: Path, *, run_id: str = "run-a") -> Path:
    folder = root / "current_candidates" / f"{DECISION_DATE}_{UNIVERSE}_{run_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "current_candidates_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "run_id": run_id,
            "decision_date": DECISION_DATE,
            "universe_name": UNIVERSE,
            "created_at": f"{DECISION_DATE}T15:30:00",
            "row_counts": {"factor_dataset": 2, "scored_dataset": 2, "candidates": 1},
            "output_files": {
                "current_candidates_report": str(report),
                "candidates": str(folder / "candidates.csv"),
                "factor_dataset": str(folder / "factor_dataset.csv"),
                "scored_dataset": str(folder / "scored_dataset.csv"),
            },
            "snapshot_quality": {"status": "PASS", "report_path": str(root / "snapshot_quality" / "snapshot-a_gate-a" / "snapshot_quality_gate_report.md")},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _current_candidate_health(root: Path, *, status: str = "PASS", errors: int = 0, warnings: int = 0) -> Path:
    folder = root / "current_candidates" / "health" / "cch-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "current_candidate_artifact_health_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "health_check_id": "cch-a",
            "created_at": f"{DECISION_DATE}T15:40:00",
            "status": status,
            "issue_count": errors + warnings,
            "error_count": errors,
            "warning_count": warnings,
            "output_files": {"current_candidate_artifact_health_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _current_candidate_health_with_issues(
    root: Path,
    *,
    status: str,
    issues: list[dict],
) -> Path:
    folder = root / "current_candidates" / "health" / "cch-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "current_candidate_artifact_health_report.md"
    issues_path = folder / "current_candidate_artifact_health_issues.csv"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    pd.DataFrame(issues).to_csv(issues_path, index=False)
    error_count = sum(1 for issue in issues if issue.get("severity") == "ERROR")
    warning_count = sum(1 for issue in issues if issue.get("severity") == "WARN")
    _write_json(
        folder / "metadata.json",
        {
            "health_check_id": "cch-a",
            "created_at": f"{DECISION_DATE}T15:40:00",
            "status": status,
            "issue_count": len(issues),
            "error_count": error_count,
            "warning_count": warning_count,
            "output_files": {
                "current_candidate_artifact_health_report": str(report),
                "current_candidate_artifact_health_issues": str(issues_path),
            },
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _current_to_paper_handoff(root: Path) -> Path:
    folder = root / "current_to_paper_handoff" / "handoff-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "handoff_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "handoff_metadata.json",
        {
            "handoff_id": "handoff-a",
            "created_at": f"{DECISION_DATE}T15:45:00",
            "selected_decision_date": DECISION_DATE,
            "selected_universe_name": UNIVERSE,
            "selected_run_id": "run-a",
            "paper_journal_id": "journal-a",
            "output_files": {"handoff_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _review_template(root: Path) -> Path:
    folder = root / "current_to_paper_review_handoff" / "review-template-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "review_handoff_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "review_handoff_id": "review-template-a",
            "created_at": f"{DECISION_DATE}T15:50:00",
            "decision_count": 1,
            "output_files": {
                "review_handoff_report": str(report),
                "review_updates_template": str(folder / "review_updates_template.csv"),
            },
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _review_template_health(
    root: Path,
    *,
    health_id: str = "template-health-a",
    status: str = "PASS",
    errors: int = 0,
    warnings: int = 0,
) -> Path:
    folder = root / "paper_trading" / "review_template_health" / health_id
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "review_template_health_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "health_check_id": health_id,
            "created_at": f"{DECISION_DATE}T15:55:00",
            "status": status,
            "issue_count": errors + warnings,
            "error_count": errors,
            "warning_count": warnings,
            "output_files": {"review_template_health_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _paper_review(
    root: Path,
    *,
    review_id: str = "review-a",
    template_health: dict | None = None,
    warnings: list[str] | None = None,
) -> Path:
    folder = root / "paper_trading" / "reviews" / review_id
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "paper_review_report.md"
    reviewed = folder / "reviewed_decisions.csv"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "decision_id": "d1",
                "symbol": "510300",
                "manual_review_status": "WATCH_ONLY",
            }
        ]
    ).to_csv(reviewed, index=False)
    metadata = {
        "review_id": review_id,
        "created_at": f"{DECISION_DATE}T16:00:00",
        "output_files": {
            "paper_review_report": str(report),
            "reviewed_decisions": str(reviewed),
        },
        "warnings": warnings or [],
        "live_trading_enabled": False,
        "broker_api_invoked": False,
        "paper_trading_only": True,
    }
    if template_health is not None:
        metadata["template_health"] = template_health
    _write_json(folder / "metadata.json", metadata)
    return folder


def _daily_paper(
    root: Path,
    *,
    reviewed_decisions_path: Path | None = None,
    reconciliation_report_path: Path | None = None,
    reconciliation_status: str = "",
    reconciliation_issue_count: int = 0,
    reconciliation_error_count: int = 0,
    reconciliation_warning_count: int = 0,
) -> Path:
    folder = root / "paper_trading" / "daily" / f"{DECISION_DATE}_journal-a"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "paper_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    reconciliation = {
        "status": reconciliation_status,
        "issue_count": reconciliation_issue_count,
        "error_count": reconciliation_error_count,
        "warning_count": reconciliation_warning_count,
    }
    if reconciliation_report_path is not None:
        reconciliation["report_path"] = str(reconciliation_report_path)
    _write_json(
        folder / "metadata.json",
        {
            "paper_date": DECISION_DATE,
            "journal_id": "journal-a",
            "created_at": f"{DECISION_DATE}T16:05:00",
            "reviewed_decisions_used": True,
            "reconciliation": reconciliation,
            "output_files": {"paper_report": str(report), "decisions": str(folder / "decisions.csv")},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "paper_trading_only": True,
        },
    )
    if reviewed_decisions_path is not None:
        metadata_path = folder / "metadata.json"
        metadata = json.loads(metadata_path.read_text(encoding="utf-8"))
        metadata["reviewed_decisions_path"] = str(reviewed_decisions_path)
        metadata["output_files"]["reviewed_decisions"] = str(reviewed_decisions_path)
        metadata_path.write_text(json.dumps(metadata, indent=2, sort_keys=True), encoding="utf-8")
    return folder


def _reconciliation(
    root: Path,
    *,
    reconciliation_id: str = "recon-a",
    status: str = "PASS",
    errors: int = 0,
    warnings: int = 0,
    created_at: str | None = None,
) -> Path:
    folder = root / "paper_trading" / "reconciliation" / reconciliation_id
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "reconciliation_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    _write_json(
        folder / "metadata.json",
        {
            "reconciliation_id": reconciliation_id,
            "created_at": created_at or f"{DECISION_DATE}T16:10:00",
            "status": status,
            "issue_count": errors + warnings,
            "error_count": errors,
            "warning_count": warnings,
            "output_files": {"reconciliation_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "paper_trading_only": True,
        },
    )
    return folder


def _paper_workflow_status(
    root: Path,
    *,
    workflow_status_id: str = "paper-workflow-status-a",
    status: str = "PASS",
    workflow_stage: str | None = None,
    expected_demo_warning_count: int = 0,
    stale_warning_count: int = 0,
    actionable_warning_count: int = 0,
    blocking_error_count: int = 0,
    next_manual_action: str = "Review completed workflow artifacts.",
    created_at: str | None = None,
) -> Path:
    folder = root / "paper_trading" / "workflow_status" / workflow_status_id
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "paper_workflow_status_report.md"
    report.write_text("No broker or live trading integration was invoked.", encoding="utf-8")
    total_warning_count = expected_demo_warning_count + stale_warning_count + actionable_warning_count
    resolved_stage = workflow_stage or ("WORKFLOW_COMPLETE" if status == "PASS" else "WORKFLOW_NEEDS_ATTENTION")
    _write_json(
        folder / "metadata.json",
        {
            "workflow_status_id": workflow_status_id,
            "created_at": created_at or f"{DECISION_DATE}T16:15:00",
            "status": status,
            "workflow_stage": resolved_stage,
            "latest_decision_date": DECISION_DATE,
            "next_manual_action": next_manual_action,
            "total_warning_count": total_warning_count,
            "expected_demo_warning_count": expected_demo_warning_count,
            "stale_warning_count": stale_warning_count,
            "actionable_warning_count": actionable_warning_count,
            "blocking_error_count": blocking_error_count,
            "component_statuses": {
                "total_warning_count": total_warning_count,
                "expected_demo_warning_count": expected_demo_warning_count,
                "stale_warning_count": stale_warning_count,
                "actionable_warning_count": actionable_warning_count,
                "blocking_error_count": blocking_error_count,
            },
            "output_files": {"paper_workflow_status_report": str(report)},
            "warnings": [],
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _historical_backfill_status(
    root: Path,
    *,
    status: str = "WARN",
    workflow_stage: str = "BACKFILL_WARNINGS_NEED_REVIEW",
    backfill_id: str = "backfill-a",
    health_status: str = "WARN",
    warning_count: int = 0,
    error_count: int = 0,
    cache_write_occurred: bool = False,
    accepted_task_count: int = 0,
    rejected_task_count: int = 0,
    preflight_rejected_count: int = 0,
    comparison_failed_count: int = 0,
    cache_write_partial: bool = False,
    rejected_symbols: str = "",
    rejected_sources: str = "",
    rejected_issue_categories: str = "",
    created_at: str = f"{DECISION_DATE}T14:30:00",
) -> Path:
    folder = root / "historical_backfill" / "status" / f"status-{backfill_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "historical_backfill_status_report.md"
    status_csv = folder / "historical_backfill_status.csv"
    summary_csv = folder / "historical_backfill_status_summary.csv"
    metadata_path = folder / "metadata.json"
    next_action = (
        "Review rejected rows; accepted rows were cache-written. Use reviewed export/snapshot path if downstream validation passed."
        if workflow_stage == "BACKFILL_PARTIAL_WITH_REJECTIONS"
        else "Review WARN tasks and only rerun with --accept-cache-write after manual approval."
    )
    report.write_text("No live trading or broker API was invoked.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "HISTORICAL_BACKFILL_INDEX",
                "status": "PASS",
                "latest_backfill_id": backfill_id,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "HISTORICAL_BACKFILL_HEALTH",
                "status": health_status,
                "latest_backfill_id": backfill_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
            },
            {
                "component": "LATEST_HISTORICAL_BACKFILL",
                "status": status,
                "latest_backfill_id": backfill_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "status": status,
                "workflow_stage": workflow_stage,
                "latest_backfill_id": backfill_id,
                "task_count": 6,
                "pass_count": 3,
                "warn_count": 3 if status == "WARN" else 0,
                "fail_count": 1 if status == "FAIL" else 0,
                "skipped_count": 0,
                "cache_write_occurred": cache_write_occurred,
                "accepted_task_count": accepted_task_count,
                "rejected_task_count": rejected_task_count,
                "preflight_rejected_count": preflight_rejected_count,
                "comparison_failed_count": comparison_failed_count,
                "cache_write_partial": cache_write_partial,
                "rejected_symbols": rejected_symbols,
                "rejected_sources": rejected_sources,
                "rejected_issue_categories": rejected_issue_categories,
                "health_status": health_status,
                "issue_count": warning_count + error_count,
                "warning_count": warning_count,
                "error_count": error_count,
                "next_manual_action": next_action,
                "report_path": str(root / "historical_backfill" / backfill_id / "historical_backfill_report.md"),
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{backfill_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_backfill_id": backfill_id,
            "next_manual_action": next_action,
            "accepted_task_count": accepted_task_count,
            "rejected_task_count": rejected_task_count,
            "preflight_rejected_count": preflight_rejected_count,
            "comparison_failed_count": comparison_failed_count,
            "cache_write_partial": cache_write_partial,
            "rejected_symbols": rejected_symbols,
            "rejected_sources": rejected_sources,
            "rejected_issue_categories": rejected_issue_categories,
            "warnings": ["Expected dry-run WARN tasks need review."] if warning_count else [],
            "output_files": {
                "historical_backfill_status_report": str(report),
                "historical_backfill_status_csv": str(status_csv),
                "historical_backfill_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _market_cache_export_status(
    root: Path,
    *,
    export_id: str = "export-a",
    status: str = "PASS",
    workflow_stage: str = "SNAPSHOT_READY_FROM_EXPORT",
    health_status: str = "PASS",
    snapshot_quality_status: str = "PASS",
    warning_count: int = 0,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:00:00",
) -> Path:
    folder = root / "market_cache_export" / "status" / f"status-{export_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "market_cache_export_status_report.md"
    status_csv = folder / "market_cache_export_status.csv"
    summary_csv = folder / "market_cache_export_status_summary.csv"
    metadata_path = folder / "metadata.json"
    snapshot_manifest = root / "market_cache_export" / export_id / "snapshot_manifest.json"
    export_report = root / "market_cache_export" / export_id / "market_cache_export_report.md"
    next_action = "Use the snapshot manifest for current-candidates or link this export into research-status."
    report.write_text("No live trading or broker API was invoked.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "MARKET_CACHE_EXPORT_INDEX",
                "status": "PASS",
                "latest_export_id": export_id,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "MARKET_CACHE_EXPORT_HEALTH",
                "status": health_status,
                "latest_export_id": export_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
            },
            {
                "component": "LATEST_MARKET_CACHE_EXPORT",
                "status": status,
                "latest_export_id": export_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "status": status,
                "workflow_stage": workflow_stage,
                "latest_export_id": export_id,
                "exported_row_count": 93,
                "duplicate_key_count": 0,
                "generated_pipeline_manifest_path": str(
                    root / "market_cache_export" / export_id / "market_cache_export_manifest.json"
                ),
                "pipeline_id": f"pipeline-{export_id}",
                "data_pipeline_status": "PASS",
                "data_quality_status": "PASS",
                "snapshot_quality_status": snapshot_quality_status,
                "snapshot_manifest_path": str(snapshot_manifest),
                "health_status": health_status,
                "issue_count": warning_count + error_count,
                "warning_count": warning_count,
                "error_count": error_count,
                "next_manual_action": next_action,
                "report_path": str(export_report),
                "no_live_trading": True,
                "no_broker_api": True,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{export_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_export_id": export_id,
            "next_manual_action": next_action,
            "warnings": ["Reviewed cache export needs attention."] if warning_count else [],
            "output_files": {
                "market_cache_export_status_report": str(report),
                "market_cache_export_status_csv": str(status_csv),
                "market_cache_export_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _market_cache_export_policy_status(
    root: Path,
    *,
    plan_id: str = "plan-a",
    status: str = "WARN",
    workflow_stage: str = "SNAPSHOT_READY_FROM_POLICY_PLAN",
    health_status: str = "WARN",
    warning_count: int = 0,
    error_count: int = 0,
    comparison_pass_count: int = 1,
    comparison_warn_count: int = 0,
    comparison_fail_count: int = 0,
    comparison_unavailable_count: int = 1,
    comparison_required_but_missing_count: int = 0,
    comparison_supported_recommendation_count: int = 1,
    comparison_unsupported_recommendation_count: int = 1,
    created_at: str = f"{DECISION_DATE}T14:50:00",
) -> Path:
    folder = root / "market_cache_export_policy" / "status" / f"status-{plan_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "market_cache_export_policy_status_report.md"
    status_csv = folder / "market_cache_export_policy_status.csv"
    summary_csv = folder / "market_cache_export_policy_status_summary.csv"
    metadata_path = folder / "metadata.json"
    generated_manifest = root / "market_cache_export_policy" / "_manifests" / f"market_cache_export_recommended_{plan_id}.csv"
    downstream_export_id = f"export-{plan_id}"
    next_action = "Review policy warnings, then use the linked snapshot/export outputs for downstream research if appropriate."
    report.write_text("No live trading or broker API was invoked.", encoding="utf-8")
    generated_manifest.parent.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {
                "symbol": "000001",
                "start_date": "2024-01-02",
                "end_date": "2024-01-05",
                "source": "AKSHARE_OPTIONAL",
                "upstream_source": "TENCENT",
                "enabled": "true",
            }
        ]
    ).to_csv(generated_manifest, index=False)
    pd.DataFrame(
        [
            {
                "component": "MARKET_CACHE_EXPORT_POLICY_INDEX",
                "status": "PASS",
                "latest_plan_id": plan_id,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
                "comparison_pass_count": comparison_pass_count,
                "comparison_warn_count": comparison_warn_count,
                "comparison_fail_count": comparison_fail_count,
                "comparison_unavailable_count": comparison_unavailable_count,
                "comparison_supported_recommendation_count": comparison_supported_recommendation_count,
                "comparison_unsupported_recommendation_count": comparison_unsupported_recommendation_count,
            },
            {
                "component": "MARKET_CACHE_EXPORT_POLICY_HEALTH",
                "status": health_status,
                "latest_plan_id": plan_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
                "comparison_pass_count": comparison_pass_count,
                "comparison_warn_count": comparison_warn_count,
                "comparison_fail_count": comparison_fail_count,
                "comparison_unavailable_count": comparison_unavailable_count,
                "comparison_supported_recommendation_count": comparison_supported_recommendation_count,
                "comparison_unsupported_recommendation_count": comparison_unsupported_recommendation_count,
            },
            {
                "component": "LATEST_MARKET_CACHE_EXPORT_POLICY_PLAN",
                "status": status,
                "latest_plan_id": plan_id,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
                "comparison_pass_count": comparison_pass_count,
                "comparison_warn_count": comparison_warn_count,
                "comparison_fail_count": comparison_fail_count,
                "comparison_unavailable_count": comparison_unavailable_count,
                "comparison_supported_recommendation_count": comparison_supported_recommendation_count,
                "comparison_unsupported_recommendation_count": comparison_unsupported_recommendation_count,
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "status": status,
                "workflow_stage": workflow_stage,
                "latest_plan_id": plan_id,
                "recommendation_count": 2,
                "recommended_count": 1,
                "recommended_with_warnings_count": 1 if warning_count else 0,
                "no_reliable_source_count": 0,
                "no_cache_rows_count": 0,
                "comparison_pass_count": comparison_pass_count,
                "comparison_warn_count": comparison_warn_count,
                "comparison_fail_count": comparison_fail_count,
                "comparison_unavailable_count": comparison_unavailable_count,
                "comparison_required_but_missing_count": comparison_required_but_missing_count,
                "comparison_supported_recommendation_count": comparison_supported_recommendation_count,
                "comparison_unsupported_recommendation_count": comparison_unsupported_recommendation_count,
                "generated_reviewed_manifest_path": str(generated_manifest),
                "downstream_export_id": downstream_export_id,
                "downstream_export_status": "PASS",
                "downstream_pipeline_id": f"pipeline-{plan_id}",
                "downstream_snapshot_quality_status": "PASS",
                "health_status": health_status,
                "issue_count": warning_count + error_count,
                "warning_count": warning_count,
                "error_count": error_count,
                "next_manual_action": next_action,
                "report_path": str(root / "market_cache_export_policy" / plan_id / "market_cache_export_policy_report.md"),
                "no_live_trading": True,
                "no_broker_api": True,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{plan_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_plan_id": plan_id,
            "next_manual_action": next_action,
            "warnings": ["PROVISIONAL recommendation needs review."] if warning_count else [],
            "output_files": {
                "market_cache_export_policy_status_report": str(report),
                "market_cache_export_policy_status_csv": str(status_csv),
                "market_cache_export_policy_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _calibration_to_signal_semantics_status(
    root: Path,
    *,
    proposal_id: str = "proposal-a",
    status: str = "WARN",
    workflow_stage: str = "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE",
    health_status: str = "PASS",
    proposal_categories: str = (
        "KEEP_CURRENT_DEFAULTS;CONSIDER_WATCH_EXPANSION;DO_NOT_EXPAND_BUY_REVIEW_YET;"
        "REQUIRE_MORE_EVIDENCE;NEED_MULTI_DATE_VALIDATION;NEED_MORE_SYMBOLS;"
        "NEED_BACKTEST_OR_PAPER_EVIDENCE"
    ),
    defaults_changed: bool = False,
    calibration_run_count: int = 10,
    observed_review_buy_candidate_count: int = 7,
    observed_watch_count: int = 8,
    observed_blocked_count: int = 24,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:40:30",
) -> Path:
    folder = root / "calibration_to_signal_semantics" / "status" / f"status-{proposal_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "calibration_to_signal_semantics_status_report.md"
    status_csv = folder / "calibration_to_signal_semantics_status.csv"
    summary_csv = folder / "calibration_to_signal_semantics_status_summary.csv"
    metadata_path = folder / "metadata.json"
    proposal_report = (
        root / "calibration_to_signal_semantics" / proposal_id / "calibration_to_signal_semantics_report.md"
    )
    next_action = (
        "Keep current defaults; consider WATCH expansion only after more evidence; do not expand BUY review yet."
        if workflow_stage == "CALIBRATION_TO_SEMANTICS_NEEDS_MORE_EVIDENCE"
        else "Repair calibration-to-semantics proposal artifacts before dashboard integration."
        if status == "FAIL"
        else "Review proposal manually; do not change signal_semantics defaults without explicit implementation work."
    )
    report.write_text(
        "No live trading, broker API, order placement, message delivery, LLM API, or external API was invoked.",
        encoding="utf-8",
    )
    proposal_report.parent.mkdir(parents=True, exist_ok=True)
    proposal_report.write_text(
        "Proposal context only. REVIEW_BUY_CANDIDATE remains human-review-only, not an order.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "CALIBRATION_TO_SIGNAL_SEMANTICS_PROPOSAL",
                "status": "READY" if status != "FAIL" else "FAIL",
                "latest_artifact_id": proposal_id,
                "row_count": calibration_run_count,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "CALIBRATION_TO_SIGNAL_SEMANTICS_HEALTH",
                "status": health_status,
                "latest_artifact_id": "proposal-health-a",
                "row_count": calibration_run_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_proposal_run_id": proposal_id,
                "latest_status": "READY" if status != "FAIL" else "FAIL",
                "health_status": health_status,
                "proposal_categories": proposal_categories,
                "defaults_changed": defaults_changed,
                "calibration_run_count": calibration_run_count,
                "observed_review_buy_candidate_count": observed_review_buy_candidate_count,
                "observed_watch_count": observed_watch_count,
                "observed_blocked_count": observed_blocked_count,
                "report_path": str(proposal_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{proposal_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_proposal_run_id": proposal_id,
            "health_status": health_status,
            "proposal_categories": proposal_categories.split(";"),
            "defaults_changed": defaults_changed,
            "next_manual_action": next_action,
            "warnings": ["Proposal says more evidence is needed."] if warning_count else [],
            "output_files": {
                "calibration_to_signal_semantics_status_report": str(report),
                "calibration_to_signal_semantics_status_csv": str(status_csv),
                "calibration_to_signal_semantics_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "external_api_called": False,
            "llm_api_called": False,
            "config_mutated": False,
            "auto_order_allowed": False,
        },
    )
    return folder


def _current_candidates_backfill_plan_status(
    root: Path,
    *,
    plan_id: str = "plan-a",
    status: str = "PASS",
    workflow_stage: str = "CURRENT_CANDIDATES_BACKFILL_PLAN_READY",
    health_status: str = "PASS",
    selected_date_count: int = 8,
    first_signal_date: str = "2024-04-02",
    last_signal_date: str = "2024-05-06",
    warmup_trading_days: int = 60,
    warmup_feasible_count: int = 8,
    forward_1d_available_count: int = 8,
    forward_3d_available_count: int = 8,
    forward_5d_available_count: int = 8,
    forward_10d_available_count: int = 8,
    legacy_plan_count: int = 0,
    stale_plan_warning_count: int = 0,
    active_plan_issue_count: int = 0,
    active_plan_error_count: int = 0,
    legacy_missing_warmup_count: int = 0,
    latest_plan_is_warmup_aware: bool = True,
    warning_count: int = 0,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:39:30",
) -> Path:
    folder = root / "current_candidates_backfill_plan" / "status" / f"status-{plan_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "current_candidates_backfill_plan_status_report.md"
    status_csv = folder / "current_candidates_backfill_plan_status.csv"
    summary_csv = folder / "current_candidates_backfill_plan_status_summary.csv"
    metadata_path = folder / "metadata.json"
    plan_report = (
        root / "current_candidates_backfill_plan" / plan_id / "current_candidates_backfill_plan_report.md"
    )
    next_action = (
        "Repair current-candidates backfill plan artifacts before any backfill execution."
        if status == "FAIL"
        else "Review the backfill plan, source policy, warmup coverage, and forward horizons before candidate generation."
    )
    report.write_text(
        "Plan-only status. No current-candidates generation, live trading, broker API, order placement, message delivery, or network/API call was invoked.",
        encoding="utf-8",
    )
    plan_report.parent.mkdir(parents=True, exist_ok=True)
    plan_report.write_text("Plan-only artifact. It does not generate candidates or forward labels.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "CURRENT_CANDIDATES_BACKFILL_PLAN",
                "status": "READY" if status != "FAIL" else "FAIL",
                "latest_artifact_id": plan_id,
                "selected_date_count": selected_date_count,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "CURRENT_CANDIDATES_BACKFILL_PLAN_HEALTH",
                "status": health_status,
                "latest_artifact_id": "backfill-plan-health-a",
                "selected_date_count": selected_date_count,
                "warning_count": warning_count,
                "error_count": error_count,
                "issue_count": warning_count + error_count,
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_plan_id": plan_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "selected_date_count": selected_date_count,
                "first_signal_date": first_signal_date,
                "last_signal_date": last_signal_date,
                "warmup_trading_days": warmup_trading_days,
                "warmup_feasible_count": warmup_feasible_count,
                "forward_1d_available_count": forward_1d_available_count,
                "forward_3d_available_count": forward_3d_available_count,
                "forward_5d_available_count": forward_5d_available_count,
                "forward_10d_available_count": forward_10d_available_count,
                "legacy_plan_count": legacy_plan_count,
                "stale_plan_warning_count": stale_plan_warning_count,
                "active_plan_issue_count": active_plan_issue_count,
                "active_plan_error_count": active_plan_error_count,
                "legacy_missing_warmup_count": legacy_missing_warmup_count,
                "latest_plan_is_warmup_aware": latest_plan_is_warmup_aware,
                "report_path": str(plan_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{plan_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_plan_id": plan_id,
            "health_status": health_status,
            "selected_date_count": selected_date_count,
            "next_manual_action": next_action,
            "warnings": ["Backfill plan health warnings are present."] if warning_count else [],
            "output_files": {
                "current_candidates_backfill_plan_status_report": str(report),
                "current_candidates_backfill_plan_status_csv": str(status_csv),
                "current_candidates_backfill_plan_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "current_candidates_executed": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
        },
    )
    return folder


def _current_candidates_backfill_execution_manifest_status(
    root: Path,
    *,
    execution_manifest_id: str = "manifest-a",
    plan_id: str = "plan-a",
    status: str = "WARN",
    workflow_stage: str = "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_BLOCKED",
    health_status: str = "PASS",
    row_count: int = 8,
    ready_count: int = 0,
    blocked_count: int = 8,
    blocked_missing_snapshot_count: int = 0,
    blocked_snapshot_quality_count: int = 0,
    blocked_universe_as_of_count: int = 8,
    blocked_plan_infeasible_count: int = 0,
    reviewed_execution_required_count: int = 8,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:39:45",
) -> Path:
    folder = root / "current_candidates_backfill_execution_manifest" / "status" / f"status-{execution_manifest_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "current_candidates_backfill_execution_manifest_status_report.md"
    status_csv = folder / "current_candidates_backfill_execution_manifest_status.csv"
    summary_csv = folder / "current_candidates_backfill_execution_manifest_status_summary.csv"
    metadata_path = folder / "metadata.json"
    manifest_report = (
        root
        / "current_candidates_backfill_execution_manifest"
        / execution_manifest_id
        / "current_candidates_backfill_execution_manifest_report.md"
    )
    next_action = (
        "Repair execution manifest artifacts before reviewed candidate generation."
        if status == "FAIL"
        else "Review READY_FOR_REVIEW signal dates manually before any separate candidate generation step."
        if workflow_stage == "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_READY_FOR_REVIEW"
        else "Resolve blocked signal-date inputs before candidate generation; no current-candidates were run."
    )
    report.write_text(
        "Manifest-only status. No current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, or network/API call was invoked.",
        encoding="utf-8",
    )
    manifest_report.parent.mkdir(parents=True, exist_ok=True)
    manifest_report.write_text(
        "Execution readiness manifest only. It does not generate candidates, build snapshots, or compute forward labels.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST",
                "status": "WARN" if blocked_count else ("READY" if status != "FAIL" else "FAIL"),
                "latest_artifact_id": execution_manifest_id,
                "row_count": row_count,
                "ready_count": ready_count,
                "blocked_count": blocked_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "CURRENT_CANDIDATES_BACKFILL_EXECUTION_MANIFEST_HEALTH",
                "status": health_status,
                "latest_artifact_id": "execution-manifest-health-a",
                "row_count": row_count,
                "ready_count": ready_count,
                "blocked_count": blocked_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_execution_manifest_id": execution_manifest_id,
                "plan_id": plan_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "row_count": row_count,
                "ready_count": ready_count,
                "blocked_count": blocked_count,
                "blocked_missing_snapshot_count": blocked_missing_snapshot_count,
                "blocked_snapshot_quality_count": blocked_snapshot_quality_count,
                "blocked_universe_as_of_count": blocked_universe_as_of_count,
                "blocked_plan_infeasible_count": blocked_plan_infeasible_count,
                "reviewed_execution_required_count": reviewed_execution_required_count,
                "report_path": str(manifest_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{execution_manifest_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_execution_manifest_id": execution_manifest_id,
            "plan_id": plan_id,
            "health_status": health_status,
            "row_count": row_count,
            "ready_count": ready_count,
            "blocked_count": blocked_count,
            "next_manual_action": next_action,
            "warnings": ["Latest execution manifest has blocked signal-date rows."] if warning_count else [],
            "output_files": {
                "current_candidates_backfill_execution_manifest_status_report": str(report),
                "current_candidates_backfill_execution_manifest_status_csv": str(status_csv),
                "current_candidates_backfill_execution_manifest_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "execution_manifest_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_overlay_plan_status(
    root: Path,
    *,
    overlay_plan_id: str = "overlay-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_OVERLAY_PLAN_NEEDS_REVIEW",
    health_status: str = "PASS",
    row_count: int = 72,
    signal_date_count: int = 8,
    symbol_count: int = 9,
    needs_manual_review_count: int = 72,
    valid_for_signal_date_count: int = 0,
    survivorship_bias_warning_count: int = 72,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:49:45",
) -> Path:
    folder = root / "point_in_time_universe_overlay_plan" / "status" / f"status-{overlay_plan_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "point_in_time_universe_overlay_plan_status_report.md"
    status_csv = folder / "point_in_time_universe_overlay_plan_status.csv"
    summary_csv = folder / "point_in_time_universe_overlay_plan_status_summary.csv"
    metadata_path = folder / "metadata.json"
    overlay_report = (
        root
        / "point_in_time_universe_overlay_plan"
        / overlay_plan_id
        / "point_in_time_universe_overlay_plan_report.md"
    )
    next_action = (
        "Repair PIT universe overlay plan artifacts before manual PIT universe review."
        if status == "FAIL"
        else "Review PIT-valid overlay rows before any separate snapshot preparation step."
        if workflow_stage == "PIT_UNIVERSE_OVERLAY_PLAN_READY_FOR_REVIEW"
        else "Complete manual review for point-in-time universe rows; generated rows are not valid for execution yet."
    )
    report.write_text(
        "Plan-only PIT universe overlay status. No current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM API, or external API was invoked.",
        encoding="utf-8",
    )
    overlay_report.parent.mkdir(parents=True, exist_ok=True)
    overlay_report.write_text(
        "PIT universe overlay plan only. NEEDS_MANUAL_REVIEW rows are not valid point-in-time universe rows yet.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_OVERLAY_PLAN",
                "status": "NEEDS_REVIEW" if needs_manual_review_count else ("READY" if status != "FAIL" else "FAIL"),
                "latest_artifact_id": overlay_plan_id,
                "row_count": row_count,
                "needs_manual_review_count": needs_manual_review_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "survivorship_bias_warning_count": survivorship_bias_warning_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_OVERLAY_PLAN_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-overlay-health-a",
                "row_count": row_count,
                "needs_manual_review_count": needs_manual_review_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "survivorship_bias_warning_count": survivorship_bias_warning_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_overlay_plan_id": overlay_plan_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "row_count": row_count,
                "signal_date_count": signal_date_count,
                "symbol_count": symbol_count,
                "needs_manual_review_count": needs_manual_review_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "survivorship_bias_warning_count": survivorship_bias_warning_count,
                "report_path": str(overlay_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{overlay_plan_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_overlay_plan_id": overlay_plan_id,
            "health_status": health_status,
            "row_count": row_count,
            "needs_manual_review_count": needs_manual_review_count,
            "valid_for_signal_date_count": valid_for_signal_date_count,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe overlay plan still requires manual review."] if warning_count else [],
            "output_files": {
                "point_in_time_universe_overlay_plan_status_report": str(report),
                "point_in_time_universe_overlay_plan_status_csv": str(status_csv),
                "point_in_time_universe_overlay_plan_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_overlay_plan_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_overlay_review_status(
    root: Path,
    *,
    review_id: str = "review-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_OVERLAY_REVIEW_NEEDS_MORE_EVIDENCE",
    health_status: str = "PASS",
    approved_count: int = 1,
    valid_for_signal_date_count: int = 1,
    needs_more_evidence_count: int = 1,
    unresolved_survivorship_warning_count: int = 1,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:52:45",
) -> Path:
    folder = root / "point_in_time_universe_overlay_review" / "status" / f"status-{review_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_overlay_review_status_report.md"
    status_csv = folder / "pit_universe_overlay_review_status.csv"
    summary_csv = folder / "pit_universe_overlay_review_status_summary.csv"
    metadata_path = folder / "metadata.json"
    review_report = (
        root
        / "point_in_time_universe_overlay_review"
        / review_id
        / "pit_universe_overlay_review_report.md"
    )
    next_action = (
        "Repair PIT universe overlay review artifacts before snapshot preparation planning."
        if status == "FAIL"
        else "Review approved PIT universe rows before separate snapshot preparation planning."
        if workflow_stage
        in {
            "PIT_UNIVERSE_OVERLAY_REVIEW_HAS_APPROVED_ROWS",
            "PIT_UNIVERSE_OVERLAY_REVIEW_ALL_APPROVED",
        }
        else "Resolve PIT universe overlay rows that need more evidence before snapshot preparation planning."
    )
    report.write_text(
        "Review-only PIT universe overlay status. No current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM API, or external API was invoked.",
        encoding="utf-8",
    )
    review_report.parent.mkdir(parents=True, exist_ok=True)
    review_report.write_text(
        "Reviewed PIT universe overlay evidence only. Approved rows do not imply candidate generation.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_OVERLAY_REVIEW",
                "status": "NEEDS_MORE_EVIDENCE"
                if needs_more_evidence_count
                else ("HAS_APPROVED_ROWS" if approved_count else "WARN"),
                "latest_artifact_id": review_id,
                "approved_count": approved_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "needs_more_evidence_count": needs_more_evidence_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_OVERLAY_REVIEW_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-overlay-review-health-a",
                "approved_count": approved_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "needs_more_evidence_count": needs_more_evidence_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_review_id": review_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "approved_count": approved_count,
                "valid_for_signal_date_count": valid_for_signal_date_count,
                "needs_more_evidence_count": needs_more_evidence_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "report_path": str(review_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{review_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_review_id": review_id,
            "health_status": health_status,
            "approved_count": approved_count,
            "valid_for_signal_date_count": valid_for_signal_date_count,
            "needs_more_evidence_count": needs_more_evidence_count,
            "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe overlay review still needs more evidence."] if warning_count else [],
            "output_files": {
                "pit_universe_overlay_review_status_report": str(report),
                "pit_universe_overlay_review_status_csv": str(status_csv),
                "pit_universe_overlay_review_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_overlay_review_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_overlay_export_readiness_status(
    root: Path,
    *,
    export_readiness_id: str = "export-a",
    review_id: str = "review-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS",
    health_status: str = "PASS",
    approved_count: int = 0,
    export_ready_count: int = 0,
    blocked_count: int = 72,
    no_approved_rows: bool = True,
    missing_required_columns_count: int = 72,
    unresolved_survivorship_warning_count: int = 72,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:53:45",
) -> Path:
    folder = root / "point_in_time_universe_overlay_export_readiness" / "status" / f"status-{export_readiness_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_overlay_export_readiness_status_report.md"
    status_csv = folder / "pit_universe_overlay_export_readiness_status.csv"
    summary_csv = folder / "pit_universe_overlay_export_readiness_status_summary.csv"
    metadata_path = folder / "metadata.json"
    readiness_report = (
        root
        / "point_in_time_universe_overlay_export_readiness"
        / export_readiness_id
        / "pit_universe_overlay_export_readiness_report.md"
    )
    next_action = (
        "Repair PIT universe overlay export-readiness artifacts before any universe export planning."
        if status == "FAIL"
        else "Review export-ready PIT universe rows before a separate explicit universe export workflow."
        if workflow_stage == "PIT_UNIVERSE_EXPORT_READY_FOR_DRY_RUN"
        else "Approve PIT universe rows with evidence before export readiness can proceed."
        if workflow_stage == "PIT_UNIVERSE_EXPORT_BLOCKED_NO_APPROVED_ROWS"
        else "Resolve PIT universe review evidence and required universe columns before export readiness can proceed."
    )
    report.write_text(
        "Export-readiness status only. No universe export, data/raw write, data/processed write, current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM/API, external API, or cache mutation was invoked.",
        encoding="utf-8",
    )
    readiness_report.parent.mkdir(parents=True, exist_ok=True)
    readiness_report.write_text(
        "PIT universe overlay export readiness only. No usable universe export occurred.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_EXPORT_READINESS",
                "status": "BLOCKED_NO_APPROVED_ROWS" if no_approved_rows else "READY_FOR_DRY_RUN",
                "latest_artifact_id": export_readiness_id,
                "review_id": review_id,
                "approved_count": approved_count,
                "export_ready_count": export_ready_count,
                "blocked_count": blocked_count,
                "no_approved_rows": no_approved_rows,
                "missing_required_columns_count": missing_required_columns_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_EXPORT_READINESS_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-export-readiness-health-a",
                "review_id": review_id,
                "approved_count": approved_count,
                "export_ready_count": export_ready_count,
                "blocked_count": blocked_count,
                "no_approved_rows": no_approved_rows,
                "missing_required_columns_count": missing_required_columns_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_export_readiness_id": export_readiness_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "review_id": review_id,
                "approved_count": approved_count,
                "export_ready_count": export_ready_count,
                "blocked_count": blocked_count,
                "no_approved_rows": no_approved_rows,
                "missing_required_columns_count": missing_required_columns_count,
                "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
                "report_path": str(readiness_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{export_readiness_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_export_readiness_id": export_readiness_id,
            "health_status": health_status,
            "review_id": review_id,
            "approved_count": approved_count,
            "export_ready_count": export_ready_count,
            "blocked_count": blocked_count,
            "no_approved_rows": no_approved_rows,
            "missing_required_columns_count": missing_required_columns_count,
            "unresolved_survivorship_warning_count": unresolved_survivorship_warning_count,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe overlay export readiness is blocked."] if warning_count else [],
            "output_files": {
                "pit_universe_overlay_export_readiness_status_report": str(report),
                "pit_universe_overlay_export_readiness_status_csv": str(status_csv),
                "pit_universe_overlay_export_readiness_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "universe_exported": False,
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_overlay_export_readiness_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_export_staging_status(
    root: Path,
    *,
    staging_id: str = "stage-a",
    export_readiness_id: str = "export-a",
    review_id: str = "review-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_NO_READY_ROWS",
    health_status: str = "PASS",
    export_ready_input_count: int = 0,
    staged_row_count: int = 0,
    blocked_count: int = 72,
    source_is_diagnostic: bool = False,
    no_ready_rows: bool = True,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:54:10",
) -> Path:
    folder = root / "point_in_time_universe_export_staging" / "status" / f"status-{staging_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_export_staging_status_report.md"
    status_csv = folder / "pit_universe_export_staging_status.csv"
    summary_csv = folder / "pit_universe_export_staging_status_summary.csv"
    metadata_path = folder / "metadata.json"
    staging_report = (
        root
        / "point_in_time_universe_export_staging"
        / staging_id
        / "pit_universe_export_staging_report.md"
    )
    next_action = (
        "Repair PIT universe export staging artifacts before any accepted export planning."
        if status == "FAIL"
        else "Review staged PIT universe previews before any separate accepted export workflow."
        if workflow_stage == "PIT_UNIVERSE_EXPORT_STAGING_READY_FOR_REVIEW"
        else "Use only active non-diagnostic export-readiness artifacts for staging."
        if workflow_stage == "PIT_UNIVERSE_EXPORT_STAGING_BLOCKED_DIAGNOSTIC_SOURCE"
        else "Complete PIT universe review evidence before staging can create previews."
    )
    report.write_text(
        "PIT universe export staging status only. No data/raw write, data/processed write, current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, network/API, LLM/API, or cache mutation was invoked.",
        encoding="utf-8",
    )
    staging_report.parent.mkdir(parents=True, exist_ok=True)
    staging_report.write_text("PIT universe export staging preview only.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_EXPORT_STAGING",
                "status": "BLOCKED_NO_READY_ROWS" if no_ready_rows else "READY_FOR_REVIEW",
                "latest_artifact_id": staging_id,
                "export_readiness_id": export_readiness_id,
                "review_id": review_id,
                "export_ready_input_count": export_ready_input_count,
                "staged_row_count": staged_row_count,
                "blocked_count": blocked_count,
                "source_is_diagnostic": source_is_diagnostic,
                "no_ready_rows": no_ready_rows,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_EXPORT_STAGING_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-export-staging-health-a",
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_staging_id": staging_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "export_readiness_id": export_readiness_id,
                "review_id": review_id,
                "export_ready_input_count": export_ready_input_count,
                "staged_row_count": staged_row_count,
                "blocked_count": blocked_count,
                "source_is_diagnostic": source_is_diagnostic,
                "no_ready_rows": no_ready_rows,
                "report_path": str(staging_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{staging_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_staging_id": staging_id,
            "health_status": health_status,
            "export_readiness_id": export_readiness_id,
            "review_id": review_id,
            "export_ready_input_count": export_ready_input_count,
            "staged_row_count": staged_row_count,
            "blocked_count": blocked_count,
            "source_is_diagnostic": source_is_diagnostic,
            "no_ready_rows": no_ready_rows,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe export staging is blocked."] if warning_count else [],
            "output_files": {
                "pit_universe_export_staging_status_report": str(report),
                "pit_universe_export_staging_status_csv": str(status_csv),
                "pit_universe_export_staging_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_export_staging_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_evidence_completion_helper_status(
    root: Path,
    *,
    helper_id: str = "helper-a",
    review_id: str = "review-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_NEEDS_REVIEW",
    health_status: str = "PASS",
    row_count: int = 72,
    needs_evidence_count: int = 72,
    rows_with_base_hints_count: int = 72,
    future_dated_hint_count: int = 72,
    authoritative_hint_count: int = 0,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:54:45",
) -> Path:
    folder = (
        root
        / "point_in_time_universe_evidence_completion_helper"
        / "status"
        / f"status-{helper_id}"
    )
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_evidence_completion_helper_status_report.md"
    status_csv = folder / "pit_universe_evidence_completion_helper_status.csv"
    summary_csv = folder / "pit_universe_evidence_completion_helper_status_summary.csv"
    metadata_path = folder / "metadata.json"
    helper_report = (
        root
        / "point_in_time_universe_evidence_completion_helper"
        / helper_id
        / "pit_universe_evidence_gap_report.md"
    )
    next_action = (
        "Repair PIT universe evidence completion helper artifacts before using completion templates."
        if status == "FAIL"
        else "Review PIT universe evidence completion helper health warnings before using completion templates."
        if workflow_stage == "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_HEALTH_WARN"
        else "Complete PIT universe evidence fields manually; helper hints are non-authoritative and do not approve rows."
    )
    report.write_text(
        "Evidence completion helper status only. No universe export, data/raw write, data/processed write, current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM/API, external API, or cache mutation was invoked.",
        encoding="utf-8",
    )
    helper_report.parent.mkdir(parents=True, exist_ok=True)
    helper_report.write_text(
        "PIT universe evidence completion helper only. No approvals or usable universe export occurred.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER",
                "status": "NEEDS_REVIEW",
                "latest_artifact_id": helper_id,
                "review_id": review_id,
                "row_count": row_count,
                "needs_evidence_count": needs_evidence_count,
                "rows_with_base_hints_count": rows_with_base_hints_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_EVIDENCE_COMPLETION_HELPER_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-evidence-helper-health-a",
                "review_id": review_id,
                "row_count": row_count,
                "needs_evidence_count": needs_evidence_count,
                "rows_with_base_hints_count": rows_with_base_hints_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_helper_id": helper_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "review_id": review_id,
                "row_count": row_count,
                "needs_evidence_count": needs_evidence_count,
                "rows_with_base_hints_count": rows_with_base_hints_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "report_path": str(helper_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{helper_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_helper_id": helper_id,
            "health_status": health_status,
            "review_id": review_id,
            "row_count": row_count,
            "needs_evidence_count": needs_evidence_count,
            "rows_with_base_hints_count": rows_with_base_hints_count,
            "future_dated_hint_count": future_dated_hint_count,
            "authoritative_hint_count": authoritative_hint_count,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe evidence helper rows still need evidence."] if warning_count else [],
            "output_files": {
                "pit_universe_evidence_completion_helper_status_report": str(report),
                "pit_universe_evidence_completion_helper_status_csv": str(status_csv),
                "pit_universe_evidence_completion_helper_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "universe_exported": False,
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_evidence_completion_helper_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_evidence_review_worklist_status(
    root: Path,
    *,
    worklist_id: str = "worklist-a",
    review_id: str = "review-a",
    helper_id: str = "helper-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_NEEDS_REVIEW",
    health_status: str = "PASS",
    row_count: int = 72,
    symbol_count: int = 9,
    signal_date_count: int = 8,
    needs_evidence_count: int = 72,
    future_dated_hint_count: int = 72,
    authoritative_hint_count: int = 0,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:55:00",
) -> Path:
    folder = root / "point_in_time_universe_evidence_review_worklist" / "status" / f"status-{worklist_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_evidence_review_worklist_status_report.md"
    status_csv = folder / "pit_universe_evidence_review_worklist_status.csv"
    summary_csv = folder / "pit_universe_evidence_review_worklist_status_summary.csv"
    metadata_path = folder / "metadata.json"
    worklist_report = (
        root
        / "point_in_time_universe_evidence_review_worklist"
        / worklist_id
        / "pit_universe_evidence_review_worklist_report.md"
    )
    next_action = (
        "Repair PIT universe evidence review worklist artifacts before using reviewer templates."
        if status == "FAIL"
        else "Review PIT universe evidence review worklist health warnings before using reviewer templates."
        if workflow_stage == "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_HEALTH_WARN"
        else "Complete PIT universe evidence fields manually; worklist hints are non-authoritative and do not approve rows."
    )
    report.write_text(
        "Evidence review worklist status only. No approval, universe export, data/raw write, data/processed write, current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM/API, external API, or cache mutation was invoked.",
        encoding="utf-8",
    )
    worklist_report.parent.mkdir(parents=True, exist_ok=True)
    worklist_report.write_text(
        "PIT universe evidence review worklist only. No approvals or usable universe export occurred.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST",
                "status": "NEEDS_REVIEW",
                "latest_artifact_id": worklist_id,
                "review_id": review_id,
                "helper_id": helper_id,
                "row_count": row_count,
                "symbol_count": symbol_count,
                "signal_date_count": signal_date_count,
                "needs_evidence_count": needs_evidence_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_EVIDENCE_REVIEW_WORKLIST_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-evidence-worklist-health-a",
                "review_id": review_id,
                "helper_id": helper_id,
                "row_count": row_count,
                "symbol_count": symbol_count,
                "signal_date_count": signal_date_count,
                "needs_evidence_count": needs_evidence_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_worklist_id": worklist_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "review_id": review_id,
                "helper_id": helper_id,
                "row_count": row_count,
                "symbol_count": symbol_count,
                "signal_date_count": signal_date_count,
                "needs_evidence_count": needs_evidence_count,
                "future_dated_hint_count": future_dated_hint_count,
                "authoritative_hint_count": authoritative_hint_count,
                "report_path": str(worklist_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{worklist_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_worklist_id": worklist_id,
            "health_status": health_status,
            "review_id": review_id,
            "helper_id": helper_id,
            "row_count": row_count,
            "symbol_count": symbol_count,
            "signal_date_count": signal_date_count,
            "needs_evidence_count": needs_evidence_count,
            "future_dated_hint_count": future_dated_hint_count,
            "authoritative_hint_count": authoritative_hint_count,
            "next_manual_action": next_action,
            "warnings": ["Latest PIT universe evidence review worklist rows still need evidence."]
            if warning_count
            else [],
            "output_files": {
                "pit_universe_evidence_review_worklist_status_report": str(report),
                "pit_universe_evidence_review_worklist_status_csv": str(status_csv),
                "pit_universe_evidence_review_worklist_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "approved_rows_created": False,
            "universe_exported": False,
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_evidence_review_worklist_artifacts_only": True,
        },
    )
    return folder


def _pit_universe_evidence_update_ingestion_status(
    root: Path,
    *,
    ingestion_id: str = "ingest-a",
    status: str = "WARN",
    workflow_stage: str = "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_NO_READY_UPDATES",
    health_status: str = "PASS",
    row_count: int = 72,
    ready_for_review_update_count: int = 0,
    blocked_count: int = 72,
    approval_requested_count: int = 0,
    approved_ready_count: int = 0,
    duplicate_identity_count: int = 0,
    suggested_copy_risk_count: int = 0,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T16:05:00",
) -> Path:
    folder = root / "point_in_time_universe_evidence_update_ingestion" / "status" / f"status-{ingestion_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "pit_universe_evidence_update_ingestion_status_report.md"
    status_csv = folder / "pit_universe_evidence_update_ingestion_status.csv"
    summary_csv = folder / "pit_universe_evidence_update_ingestion_status_summary.csv"
    metadata_path = folder / "metadata.json"
    ingestion_report = (
        root
        / "point_in_time_universe_evidence_update_ingestion"
        / ingestion_id
        / "pit_universe_evidence_update_ingestion_report.md"
    )
    review_updates = (
        root
        / "point_in_time_universe_evidence_update_ingestion"
        / ingestion_id
        / "pit_universe_review_updates.csv"
    )
    ingestion_report.parent.mkdir(parents=True, exist_ok=True)
    ingestion_report.write_text("PIT universe evidence update ingestion report only.", encoding="utf-8")
    review_updates.write_text("signal_date,symbol,universe_name,review_status\n", encoding="utf-8")
    next_action = (
        "Repair PIT universe evidence update ingestion artifacts before using clean review updates."
        if status == "FAIL"
        else "Reviewer has not completed usable PIT universe evidence update rows yet."
        if ready_for_review_update_count == 0
        else "Review clean review_updates artifact manually before a separate pit-universe-overlay-review run."
    )
    report.write_text(
        "Evidence update ingestion status only. No approval applied, universe export, data/raw write, data/processed write, current-candidates generation, snapshot build, forward labels, live trading, broker API, order placement, message delivery, LLM/API, external API, or cache mutation was invoked.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION",
                "status": "WARN" if ready_for_review_update_count == 0 else "READY",
                "latest_artifact_id": ingestion_id,
                "row_count": row_count,
                "ready_for_review_update_count": ready_for_review_update_count,
                "blocked_count": blocked_count,
                "approval_requested_count": approval_requested_count,
                "approved_ready_count": approved_ready_count,
                "duplicate_identity_count": duplicate_identity_count,
                "suggested_copy_risk_count": suggested_copy_risk_count,
                "warning_count": warning_count if status != "PASS" else 0,
                "error_count": 0,
                "issue_count": warning_count if status != "PASS" else 0,
            },
            {
                "component": "PIT_UNIVERSE_EVIDENCE_UPDATE_INGESTION_HEALTH",
                "status": health_status,
                "latest_artifact_id": "pit-evidence-update-ingestion-health-a",
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "latest_ingestion_id": ingestion_id,
                "status": status,
                "workflow_stage": workflow_stage,
                "health_status": health_status,
                "row_count": row_count,
                "ready_for_review_update_count": ready_for_review_update_count,
                "blocked_count": blocked_count,
                "approval_requested_count": approval_requested_count,
                "approved_ready_count": approved_ready_count,
                "duplicate_identity_count": duplicate_identity_count,
                "suggested_copy_risk_count": suggested_copy_risk_count,
                "report_path": str(ingestion_report),
                "review_updates_path": str(review_updates),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{ingestion_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_ingestion_id": ingestion_id,
            "health_status": health_status,
            "row_count": row_count,
            "ready_for_review_update_count": ready_for_review_update_count,
            "blocked_count": blocked_count,
            "approval_requested_count": approval_requested_count,
            "approved_ready_count": approved_ready_count,
            "duplicate_identity_count": duplicate_identity_count,
            "suggested_copy_risk_count": suggested_copy_risk_count,
            "next_manual_action": next_action,
            "output_files": {
                "pit_universe_evidence_update_ingestion_status_report": str(report),
                "pit_universe_evidence_update_ingestion_status_csv": str(status_csv),
                "pit_universe_evidence_update_ingestion_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "approval_applied": False,
            "universe_exported": False,
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "cache_mutated": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "network_api_called": False,
            "llm_api_called": False,
            "pit_universe_evidence_update_ingestion_artifacts_only": True,
        },
    )
    return folder


def _pit_evidence_checklist_validator_artifact(
    root: Path,
    *,
    validator_id: str = "validator-a",
    status: str = "WARN",
    row_count: int = 16,
    checklist_pass_count: int = 0,
    blocked_count: int = 16,
    stock_core_blocked_count: int = 8,
    etf_core_blocked_count: int = 8,
) -> Path:
    folder = root / "pit_evidence_checklist_validator" / validator_id
    folder.mkdir(parents=True, exist_ok=True)
    validation_csv = folder / "pit_evidence_checklist_validation.csv"
    summary_csv = folder / "pit_evidence_checklist_validation_summary.csv"
    missing_csv = folder / "missing_evidence_matrix.csv"
    preview_csv = folder / "approval_candidate_preview.csv"
    report = folder / "report.md"
    metadata = folder / "metadata.json"
    pd.DataFrame(
        [
            {
                "validator_id": validator_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "profile": "stock_core",
                "review_status": "NEEDS_MORE_EVIDENCE",
                "checklist_status": "CHECKLIST_BLOCKED_MISSING_EVIDENCE",
                "checklist_pass": False,
                "blocked": True,
                "blocker_reason": "missing evidence",
                "missing_required_fields": "is_active_evidence",
                "unacceptable_source_fields": "",
                "pit_timing_blocker": True,
                "survivorship_blocker": True,
                "stock_st_blocker": True,
                "no_approval_applied": True,
                "no_universe_export": True,
                "no_data_raw_write": True,
                "no_data_processed_write": True,
                "no_current_candidates_generated": True,
                "no_snapshot_built": True,
                "no_forward_labels": True,
                "no_live_trading": True,
                "no_broker_api": True,
                "no_order_placement": True,
                "no_message_sent": True,
                "checklist_validation_only": True,
            }
        ],
        columns=VALIDATION_COLUMNS,
    ).to_csv(validation_csv, index=False)
    pd.DataFrame(
        [
            {
                "validator_id": validator_id,
                "status": status,
                "row_count": row_count,
                "checklist_pass_count": checklist_pass_count,
                "blocked_count": blocked_count,
                "stock_core_blocked_count": stock_core_blocked_count,
                "etf_core_blocked_count": etf_core_blocked_count,
                "missing_evidence_count": blocked_count,
                "unacceptable_source_count": 0,
                "pit_timing_blocked_count": blocked_count,
                "survivorship_blocked_count": blocked_count,
                "stock_st_blocked_count": stock_core_blocked_count,
            }
        ],
        columns=SUMMARY_COLUMNS,
    ).to_csv(summary_csv, index=False)
    pd.DataFrame(
        [
            {
                "validator_id": validator_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "field_name": "is_active_evidence",
                "issue_code": "MISSING_REQUIRED_EVIDENCE",
                "issue_message": "is_active_evidence is required.",
                "acceptable_sources": "official",
                "notes": "",
            }
        ]
    ).to_csv(missing_csv, index=False)
    pd.DataFrame().to_csv(preview_csv, index=False)
    report.write_text("No approval applied. No universe export. No current-candidates were run.", encoding="utf-8")
    _write_json(
        metadata,
        {
            "validator_id": validator_id,
            "created_at": "2026-06-04T10:00:00+08:00",
            "status": status,
            "row_count": row_count,
            "checklist_pass_count": checklist_pass_count,
            "blocked_count": blocked_count,
            "stock_core_blocked_count": stock_core_blocked_count,
            "etf_core_blocked_count": etf_core_blocked_count,
            "output_files": {
                "validation_csv": str(validation_csv),
                "summary_csv": str(summary_csv),
                "missing_evidence_matrix": str(missing_csv),
                "approval_candidate_preview": str(preview_csv),
                "report": str(report),
                "metadata": str(metadata),
            },
            "no_approval_applied": True,
            "no_universe_export": True,
            "no_data_raw_write": True,
            "no_data_processed_write": True,
            "no_current_candidates_generated": True,
            "no_snapshot_built": True,
            "no_forward_labels": True,
            "no_live_trading": True,
            "no_broker_api": True,
            "no_order_placement": True,
            "no_message_sent": True,
            "checklist_validation_only": True,
            "would_write_data_raw": False,
            "would_write_data_processed": False,
            "current_candidates_executed": False,
            "snapshot_manifest_built": False,
            "forward_returns_computed": False,
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "order_placement_enabled": False,
            "message_sent": False,
        },
    )
    return folder


def _pit_evidence_policy_profile_comparison_artifact(
    root: Path,
    *,
    comparison_id: str = "comparison-a",
    status: str = "WARN",
    row_count: int = 16,
    strict_pass_count: int = 0,
    eod_low_budget_pass_count: int = 0,
    reviewed_no_hit_support_pass_count: int = 0,
    no_hit_context_supported_count: int = 0,
    reviewer_acceptance_required_count: int = 0,
    relaxed_blocker_count: int = 16,
    remaining_blocked_count: int = 16,
    created_at: str = "2026-06-04T16:10:00+08:00",
) -> Path:
    folder = root / "pit_evidence_policy_profile_comparison" / comparison_id
    folder.mkdir(parents=True, exist_ok=True)
    comparison_csv = folder / "pit_evidence_policy_profile_comparison.csv"
    summary_csv = folder / "pit_evidence_policy_profile_summary.csv"
    relaxed_csv = folder / "relaxed_blocker_matrix.csv"
    remaining_csv = folder / "remaining_blocker_matrix.csv"
    snapshot_csv = folder / "eod_post_close_policy_profile_snapshot.csv"
    report = folder / "report.md"
    metadata = folder / "metadata.json"
    pd.DataFrame(
        [
            {
                "comparison_id": comparison_id,
                "symbol": "000001",
                "signal_date": "2024-04-02",
                "recommended_future_universe": "stock_core",
                "profile_name": "EOD_POST_CLOSE_LOW_BUDGET_PIT",
                "strict_status": "CHECKLIST_BLOCKED_PIT_TIMING",
                "eod_low_budget_status": "EOD_LOW_BUDGET_BLOCKED",
                "strict_blockers": "PIT timing blocked; survivorship unresolved",
                "relaxed_blockers": "PIT_TIMING_BLOCKED",
                "remaining_blockers": "survivorship unresolved",
                "available_time": "2024-04-02 15:30:00",
                "decision_time": "2024-04-02 16:00:00",
                "available_time_within_decision_time": True,
                "same_day_market_cache_used_as_support": True,
                "active_context_supported_by_cache": True,
                "suspension_context_supported_by_cache": True,
                "not_delisted_still_required": True,
                "st_no_st_still_required": True,
                "survivorship_still_required": True,
                "checklist_pass_under_strict": False,
                "checklist_pass_under_eod_low_budget": False,
                "checklist_pass_under_reviewed_no_hit_support": False,
                "no_hit_context_supported": False,
                "reviewer_acceptance_required": False,
                "approval_candidate_preview_only": False,
                "should_apply_approval": False,
                "no_pit_review_run": True,
                "no_export_readiness_run": True,
                "no_staging_run": True,
                "no_universe_export": True,
                "no_data_raw_write": True,
                "no_data_processed_write": True,
                "no_current_candidates_generated": True,
                "comparison_only": True,
            }
        ]
    ).to_csv(comparison_csv, index=False)
    pd.DataFrame(
        [
            {
                "comparison_id": comparison_id,
                "status": status,
                "reference_profile_name": "STRICT_PIT",
                "profile_name": "EOD_POST_CLOSE_LOW_BUDGET_PIT",
                "profile_is_opt_in": True,
                "strict_default_unchanged": True,
                "row_count": row_count,
                "strict_checklist_pass_count": strict_pass_count,
                "eod_low_budget_checklist_pass_count": eod_low_budget_pass_count,
                "reviewed_no_hit_support_pass_count": reviewed_no_hit_support_pass_count,
                "no_hit_context_supported_count": no_hit_context_supported_count,
                "reviewer_acceptance_required_count": reviewer_acceptance_required_count,
                "relaxed_blocker_count": relaxed_blocker_count,
                "remaining_blocked_count": remaining_blocked_count,
                "approval_candidate_preview_count": eod_low_budget_pass_count,
            }
        ]
    ).to_csv(summary_csv, index=False)
    pd.DataFrame([{"comparison_id": comparison_id, "symbol": "000001", "signal_date": "2024-04-02", "blocker": "PIT_TIMING_BLOCKED"}]).to_csv(relaxed_csv, index=False)
    pd.DataFrame([{"comparison_id": comparison_id, "symbol": "000001", "signal_date": "2024-04-02", "blocker": "survivorship unresolved"}]).to_csv(remaining_csv, index=False)
    pd.DataFrame([{"field_name": "available_time", "rule": "available_time <= decision_time"}]).to_csv(snapshot_csv, index=False)
    report.write_text("No approval applied. Comparison only.", encoding="utf-8")
    _write_json(
        metadata,
        {
            "comparison_id": comparison_id,
            "created_at": created_at,
            "status": status,
            "reference_profile_name": "STRICT_PIT",
            "profile_name": "EOD_POST_CLOSE_LOW_BUDGET_PIT",
            "profile_is_opt_in": True,
            "strict_default_unchanged": True,
            "row_count": row_count,
            "strict_checklist_pass_count": strict_pass_count,
            "eod_low_budget_checklist_pass_count": eod_low_budget_pass_count,
            "reviewed_no_hit_support_pass_count": reviewed_no_hit_support_pass_count,
            "no_hit_context_supported_count": no_hit_context_supported_count,
            "reviewer_acceptance_required_count": reviewer_acceptance_required_count,
            "relaxed_blocker_count": relaxed_blocker_count,
            "remaining_blocked_count": remaining_blocked_count,
            "approval_applied": False,
            "pit_review_run": False,
            "export_readiness_run": False,
            "export_staging_run": False,
            "universe_exported": False,
            "active_worklist_mutated": False,
            "no_data_raw_write": True,
            "no_data_processed_write": True,
            "no_current_candidates_generated": True,
            "comparison_only": True,
            "output_files": {
                "comparison_csv": str(comparison_csv),
                "summary_csv": str(summary_csv),
                "relaxed_blocker_matrix": str(relaxed_csv),
                "remaining_blocker_matrix": str(remaining_csv),
                "policy_snapshot": str(snapshot_csv),
                "report": str(report),
                "metadata": str(metadata),
            },
        },
    )
    return folder


def _pit_official_status_evidence_packet_artifact(
    root: Path,
    *,
    packet_id: str = "packet-a",
    status: str = "WARN",
    row_count: int = 16,
    evidence_packet_row_count: int = 64,
    strong_official_date_specific_count: int = 0,
    supporting_official_symbol_level_count: int = 16,
    supporting_local_eod_cache_count: int = 16,
    context_only_count: int = 0,
    missing_count: int = 32,
    checklist_pass_count: int = 0,
    blocked_count: int = 16,
    eod_low_budget_checklist_pass_count: int = 0,
    created_at: str = "2026-06-05T09:10:00+08:00",
) -> Path:
    folder = root / "pit_official_status_evidence_packet" / packet_id
    folder.mkdir(parents=True, exist_ok=True)
    packet_csv = folder / "pit_official_status_evidence_packet.csv"
    source_csv = folder / "source_coverage_summary.csv"
    per_date_csv = folder / "per_symbol_date_status_evidence.csv"
    matrix_csv = folder / "evidence_strength_matrix.csv"
    draft_csv = folder / "updated_draft_completed_updates.csv"
    report = folder / "report.md"
    metadata = folder / "metadata.json"
    pd.DataFrame(
        [
            {
                "packet_id": packet_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "evidence_field": "not_delisted",
                "evidence_strength": "MISSING",
                "source_name": "",
                "source_url_or_path": "",
                "source_type": "",
                "accessed_at": "",
                "pit_suitability": "MISSING",
                "fields_supported": "",
                "field_status": "missing",
                "blocker_status": "missing not-delisted/ST/survivorship evidence",
                "evidence_reference": "",
                "context_only_or_approval_candidate": "missing",
                "approval_candidate_preview_only": False,
                "should_apply_approval": False,
                "no_approval_applied": True,
                "no_pit_review_run": True,
                "no_export_readiness_run": True,
                "no_staging_run": True,
                "no_universe_export": True,
                "no_data_raw_write": True,
                "no_data_processed_write": True,
                "no_current_candidates_generated": True,
                "no_snapshot_built": True,
                "no_forward_labels": True,
                "no_live_trading": True,
                "no_broker_api": True,
                "no_order_placement": True,
                "no_message_sent": True,
                "packet_only": True,
            }
        ]
    ).to_csv(packet_csv, index=False)
    pd.DataFrame(
        [
            {
                "packet_id": packet_id,
                "source_name": "local_market_cache",
                "source_url_or_path": "data/cache/market/daily_bars.csv",
                "source_type": "local_market_cache",
                "access_status": "readable",
                "parseable": True,
                "symbols_observed": "000001,159915",
                "dates_observed": "2024-04",
                "pit_suitability": "EOD_SUPPORT_ONLY",
                "strong_official_date_specific_count": 0,
                "supporting_official_symbol_level_count": 0,
                "supporting_local_eod_cache_count": supporting_local_eod_cache_count,
                "context_only_count": 0,
                "missing_count": 0,
            }
        ]
    ).to_csv(source_csv, index=False)
    pd.DataFrame(
        [
            {
                "packet_id": packet_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "strong_official_date_specific_count": 0,
                "supporting_official_symbol_level_count": 1,
                "supporting_local_eod_cache_count": 1,
                "context_only_count": 0,
                "missing_count": 2,
                "checklist_pass": False,
                "blocked": True,
                "blocker_reason": "missing not-delisted/ST/survivorship evidence",
                "review_status": "NEEDS_MORE_EVIDENCE",
                "include_flag": False,
                "survivorship_bias_resolved": False,
            }
        ]
    ).to_csv(per_date_csv, index=False)
    pd.DataFrame(
        [
            {
                "packet_id": packet_id,
                "symbol": "000001",
                "universe_name": "stock_core",
                "evidence_field": "not_delisted",
                "evidence_strength": "MISSING",
                "row_count": 1,
            }
        ]
    ).to_csv(matrix_csv, index=False)
    pd.DataFrame(
        [
            {
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "review_status": "NEEDS_MORE_EVIDENCE",
                "include_flag": False,
                "reviewer": "",
                "reviewed_at": "",
                "review_reason": "packet fixture remains blocked",
                "evidence_source": "local_market_cache_context",
                "evidence_path": "data/cache/market/daily_bars.csv",
                "evidence_reference": "local_market_cache:000001:2024-04-02",
                "listed_date": "",
                "delisted_date": "",
                "is_active": "",
                "is_st": "",
                "is_suspended": "",
                "listed_date_evidence": "",
                "delisted_date_evidence": "",
                "is_active_evidence": "",
                "survivorship_bias_resolved": False,
                "as_of_date": "",
                "name": "",
                "instrument_type": "STOCK",
                "exchange": "SZSE",
                "industry": "",
                "min_lot": "",
                "t_plus_rule": "",
                "available_time": "",
                "revision_id": "",
                "source": "",
            }
        ]
    ).to_csv(draft_csv, index=False)
    report.write_text("No approval applied. Packet only.", encoding="utf-8")
    _write_json(
        metadata,
        {
            "packet_id": packet_id,
            "created_at": created_at,
            "status": status,
            "row_count": row_count,
            "evidence_packet_row_count": evidence_packet_row_count,
            "strong_official_date_specific_count": strong_official_date_specific_count,
            "supporting_official_symbol_level_count": supporting_official_symbol_level_count,
            "supporting_local_eod_cache_count": supporting_local_eod_cache_count,
            "context_only_count": context_only_count,
            "missing_count": missing_count,
            "checklist_pass_count": checklist_pass_count,
            "blocked_count": blocked_count,
            "eod_low_budget_checklist_pass_count": eod_low_budget_checklist_pass_count,
            "approval_applied": False,
            "pit_review_run": False,
            "export_readiness_run": False,
            "export_staging_run": False,
            "universe_exported": False,
            "active_worklist_mutated": False,
            "no_data_raw_write": True,
            "no_data_processed_write": True,
            "no_current_candidates_generated": True,
            "no_snapshot_built": True,
            "no_forward_labels": True,
            "cache_mutated": False,
            "packet_only": True,
            "output_files": {
                "packet_csv": str(packet_csv),
                "source_coverage_summary": str(source_csv),
                "per_symbol_date_status_evidence": str(per_date_csv),
                "evidence_strength_matrix": str(matrix_csv),
                "updated_draft_completed_updates": str(draft_csv),
                "report": str(report),
                "metadata": str(metadata),
            },
        },
    )
    return folder


def _pit_official_status_evidence_packet_enrichment_artifact(
    root: Path,
    *,
    enrichment_id: str = "enrich-a",
    status: str = "WARN",
    source_packet_id: str = "packet-a",
    policy_comparison_id: str = "comparison-a",
    row_count: int = 16,
    strong_official_date_specific_quotation_count: int = 16,
    reviewed_no_hit_context_supported_count: int = 16,
    reviewer_acceptance_required_count: int = 16,
    checklist_pass_count: int = 0,
    remaining_blocked_count: int = 16,
    created_at: str = "2026-06-05T09:20:00+08:00",
) -> Path:
    folder = root / "pit_official_status_evidence_packet_enrichment" / enrichment_id
    folder.mkdir(parents=True, exist_ok=True)
    enriched_csv = folder / "pit_official_status_evidence_packet_enrichment.csv"
    summary_csv = folder / "pit_official_status_evidence_packet_enrichment_summary.csv"
    blockers_csv = folder / "remaining_enrichment_blockers.csv"
    report = folder / "report.md"
    metadata = folder / "metadata.json"
    pd.DataFrame(
        [
            {
                "enrichment_id": enrichment_id,
                "source_packet_id": source_packet_id,
                "policy_comparison_id": policy_comparison_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "strong_official_date_specific_quotation": True,
                "quotation_source_url": "https://www.szse.cn/api/report/ShowReport?symbol=000001",
                "quotation_fields_observed": "symbol,date,close,volume",
                "reviewed_no_hit_context_supported": True,
                "reviewer_acceptance_required": True,
                "prior_official_symbol_level_context": True,
                "local_eod_cache_context": True,
                "missing_evidence_categories": "reviewer_no_hit_acceptance; survivorship_bias_resolution",
                "remaining_blocked": True,
                "checklist_pass": False,
                "no_approval_applied": True,
                "no_universe_export": True,
                "no_current_candidates_generated": True,
            }
        ]
    ).to_csv(enriched_csv, index=False)
    pd.DataFrame(
        [
            {
                "enrichment_id": enrichment_id,
                "status": status,
                "source_packet_id": source_packet_id,
                "policy_comparison_id": policy_comparison_id,
                "row_count": row_count,
                "strong_official_date_specific_quotation_count": (
                    strong_official_date_specific_quotation_count
                ),
                "reviewed_no_hit_context_supported_count": reviewed_no_hit_context_supported_count,
                "reviewer_acceptance_required_count": reviewer_acceptance_required_count,
                "prior_official_symbol_level_context_count": 16,
                "local_eod_cache_context_count": 16,
                "checklist_pass_count": checklist_pass_count,
                "remaining_blocked_count": remaining_blocked_count,
            }
        ]
    ).to_csv(summary_csv, index=False)
    pd.DataFrame(
        [
            {
                "enrichment_id": enrichment_id,
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "blocker": "reviewer_no_hit_acceptance",
            }
        ]
    ).to_csv(blockers_csv, index=False)
    report.write_text("No approval applied. Enrichment only.", encoding="utf-8")
    _write_json(
        metadata,
        {
            "enrichment_id": enrichment_id,
            "created_at": created_at,
            "status": status,
            "source_packet_id": source_packet_id,
            "policy_comparison_id": policy_comparison_id,
            "row_count": row_count,
            "strong_official_date_specific_quotation_count": (
                strong_official_date_specific_quotation_count
            ),
            "reviewed_no_hit_context_supported_count": reviewed_no_hit_context_supported_count,
            "reviewer_acceptance_required_count": reviewer_acceptance_required_count,
            "prior_official_symbol_level_context_count": 16,
            "local_eod_cache_context_count": 16,
            "checklist_pass_count": checklist_pass_count,
            "remaining_blocked_count": remaining_blocked_count,
            "approval_applied": False,
            "pit_review_run": False,
            "export_readiness_run": False,
            "export_staging_run": False,
            "universe_exported": False,
            "active_worklist_mutated": False,
            "no_data_raw_write": True,
            "no_data_processed_write": True,
            "no_current_candidates_generated": True,
            "no_snapshot_built": True,
            "no_forward_labels": True,
            "cache_mutated": False,
            "enrichment_only": True,
            "output_files": {
                "enriched_csv": str(enriched_csv),
                "summary_csv": str(summary_csv),
                "blocker_matrix": str(blockers_csv),
                "report": str(report),
                "metadata": str(metadata),
            },
        },
    )
    return folder


def _universe_profile_policy_audit(root: Path) -> Path:
    worklist = root / "manual_diagnostics" / "policy_audit_test_worklist.csv"
    worklist.parent.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {
                "worklist_id": "worklist001",
                "review_id": "review001",
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "etf_core",
                "suggested_instrument_type": "STOCK",
                "current_review_status": "NEEDS_MANUAL_REVIEW",
                "no_live_trading": True,
                "no_broker_api": True,
                "no_order_placement": True,
                "no_message_sent": True,
                "worklist_only": True,
            },
            {
                "worklist_id": "worklist001",
                "review_id": "review001",
                "signal_date": "2024-04-02",
                "symbol": "510300",
                "universe_name": "etf_core",
                "suggested_instrument_type": "ETF",
                "current_review_status": "NEEDS_MANUAL_REVIEW",
                "no_live_trading": True,
                "no_broker_api": True,
                "no_order_placement": True,
                "no_message_sent": True,
                "worklist_only": True,
            },
        ]
    ).to_csv(worklist, index=False)
    result = build_universe_profile_policy_audit(
        worklist=worklist,
        output_dir=root / "universe_profile_policy_audit",
    )
    return Path(result.artifact_paths["artifact_dir"])


def _signal_semantics_status(
    root: Path,
    *,
    semantics_id: str = "semantics-a",
    status: str = "WARN",
    workflow_stage: str = "SIGNAL_SEMANTICS_READY_FOR_REVIEW",
    health_status: str = "PASS",
    row_count: int = 4,
    demo_only_count: int = 0,
    watch_count: int = 1,
    review_buy_candidate_count: int = 1,
    review_sell_candidate_count: int = 0,
    hold_review_count: int = 0,
    no_action_count: int = 0,
    blocked_count: int = 2,
    issue_count: int = 2,
    profile: str = "reviewed_local_v0",
    input_path: str = "outputs/reports/manual_diagnostics/signal_semantics_synthetic_reviewed_fixture.csv",
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:41:00",
) -> Path:
    folder = root / "signal_semantics" / "status" / f"status-{semantics_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "signal_semantics_status_report.md"
    status_csv = folder / "signal_semantics_status.csv"
    summary_csv = folder / "signal_semantics_status_summary.csv"
    metadata_path = folder / "metadata.json"
    semantics_report = root / "signal_semantics" / semantics_id / "signal_semantics_report.md"
    next_action = (
        "Demo signal semantics validated; do not treat DEMO_ONLY labels as strategy recommendations."
        if workflow_stage == "DEMO_SIGNAL_SEMANTICS_VALIDATED"
        else "Repair signal semantics artifacts before using advisory labels."
        if status == "FAIL"
        else "Review signal semantics labels manually; REVIEW_BUY_CANDIDATE is not an order and auto-order remains disabled."
    )
    report.write_text(
        "No live trading, broker API, order placement, or message delivery was invoked.",
        encoding="utf-8",
    )
    semantics_report.parent.mkdir(parents=True, exist_ok=True)
    semantics_report.write_text(
        "Signal semantics labels are advisory human-review labels, not orders.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "SIGNAL_SEMANTICS",
                "status": "READY" if status != "FAIL" else "FAIL",
                "latest_artifact_id": semantics_id,
                "row_count": row_count,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": issue_count,
            },
            {
                "component": "SIGNAL_SEMANTICS_HEALTH",
                "status": health_status,
                "latest_artifact_id": "semantics-health-a",
                "row_count": row_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_semantics_run_id": semantics_id,
                "latest_status": "READY" if status != "FAIL" else "FAIL",
                "health_status": health_status,
                "row_count": row_count,
                "demo_only_count": demo_only_count,
                "watch_count": watch_count,
                "review_buy_candidate_count": review_buy_candidate_count,
                "review_sell_candidate_count": review_sell_candidate_count,
                "hold_review_count": hold_review_count,
                "no_action_count": no_action_count,
                "blocked_count": blocked_count,
                "issue_count": issue_count,
                "profile": profile,
                "input_path": input_path,
                "input_type": "candidates",
                "report_path": str(semantics_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{semantics_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_semantics_run_id": semantics_id,
            "health_status": health_status,
            "row_count": row_count,
            "next_manual_action": next_action,
            "warnings": ["Latest signal semantics artifact is review-only."] if warning_count else [],
            "output_files": {
                "signal_semantics_status_report": str(report),
                "signal_semantics_status_csv": str(status_csv),
                "signal_semantics_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "auto_order_allowed": False,
        },
    )
    return folder


def _advisory_profile_calibration_status(
    root: Path,
    *,
    calibration_id: str = "calibration-a",
    status: str = "WARN",
    workflow_stage: str = "ADVISORY_PROFILE_CALIBRATION_READY_FOR_REVIEW",
    health_status: str = "PASS",
    row_count: int = 6,
    review_buy_candidate_count: int = 1,
    watch_count: int = 2,
    no_action_count: int = 1,
    blocked_count: int = 2,
    demo_only_count: int = 0,
    issue_count: int = 2,
    profile: str = "balanced",
    input_path: str = "outputs/reports/manual_diagnostics/advisory_profile_calibration_synthetic_fixture.csv",
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:40:00",
) -> Path:
    folder = root / "advisory_profile_calibration" / "status" / f"status-{calibration_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "advisory_profile_calibration_status_report.md"
    status_csv = folder / "advisory_profile_calibration_status.csv"
    summary_csv = folder / "advisory_profile_calibration_status_summary.csv"
    metadata_path = folder / "metadata.json"
    calibration_report = root / "advisory_profile_calibration" / calibration_id / "advisory_profile_calibration_report.md"
    next_action = (
        "Demo advisory profile calibration validated; do not treat DEMO_ONLY labels as strategy recommendations."
        if workflow_stage == "DEMO_ADVISORY_PROFILE_CALIBRATION_VALIDATED"
        else "Repair advisory profile calibration artifacts before using threshold analysis."
        if status == "FAIL"
        else "Review calibration labels manually; REVIEW_BUY_CANDIDATE is not an order and auto-order remains disabled."
    )
    report.write_text(
        "No live trading, broker API, order placement, or message delivery was invoked.",
        encoding="utf-8",
    )
    calibration_report.parent.mkdir(parents=True, exist_ok=True)
    calibration_report.write_text(
        "Calibration labels are threshold-analysis labels, not orders.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "ADVISORY_PROFILE_CALIBRATION",
                "status": "READY" if status != "FAIL" else "FAIL",
                "latest_artifact_id": calibration_id,
                "row_count": row_count,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": issue_count,
            },
            {
                "component": "ADVISORY_PROFILE_CALIBRATION_HEALTH",
                "status": health_status,
                "latest_artifact_id": "calibration-health-a",
                "row_count": row_count,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_calibration_run_id": calibration_id,
                "latest_status": "READY" if status != "FAIL" else "FAIL",
                "health_status": health_status,
                "row_count": row_count,
                "review_buy_candidate_count": review_buy_candidate_count,
                "watch_count": watch_count,
                "no_action_count": no_action_count,
                "blocked_count": blocked_count,
                "demo_only_count": demo_only_count,
                "issue_count": issue_count,
                "profile": profile,
                "input_path": input_path,
                "input_type": "candidates",
                "report_path": str(calibration_report),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{calibration_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_calibration_run_id": calibration_id,
            "health_status": health_status,
            "row_count": row_count,
            "next_manual_action": next_action,
            "warnings": ["Latest advisory profile calibration artifact is review-only."] if warning_count else [],
            "output_files": {
                "advisory_profile_calibration_status_report": str(report),
                "advisory_profile_calibration_status_csv": str(status_csv),
                "advisory_profile_calibration_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "auto_order_allowed": False,
        },
    )
    return folder


def _signal_advisory_status(
    root: Path,
    *,
    signal_id: str = "signal-a",
    status: str = "WARN",
    workflow_stage: str = "DEMO_SIGNAL_ADVISORY_VALIDATED",
    health_status: str = "PASS",
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:42:00",
) -> Path:
    folder = root / "signals" / "status" / f"status-{signal_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "signal_advisory_status_report.md"
    status_csv = folder / "signal_advisory_status.csv"
    summary_csv = folder / "signal_advisory_status_summary.csv"
    metadata_path = folder / "metadata.json"
    alert_preview = root / "signals" / signal_id / "signal_alert_preview.md"
    signal_report = root / "signals" / signal_id / "signal_advisory_report.md"
    next_action = (
        "Review local alert preview; do not treat DEMO_ONLY signals as strategy recommendations."
        if workflow_stage == "DEMO_SIGNAL_ADVISORY_VALIDATED"
        else "Repair signal advisory artifacts before using alert previews."
        if status == "FAIL"
        else "Review local alert preview and require manual confirmation before any human action."
    )
    report.write_text("No live trading, broker API, order placement, or message delivery was invoked.", encoding="utf-8")
    alert_preview.parent.mkdir(parents=True, exist_ok=True)
    alert_preview.write_text("Manual confirmation required. No auto-order.", encoding="utf-8")
    signal_report.write_text("Signals are advisory artifacts, not orders.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "SIGNAL_ADVISORY",
                "status": "READY",
                "latest_artifact_id": signal_id,
                "signal_count": 9,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "SIGNAL_ADVISORY_HEALTH",
                "status": health_status,
                "latest_artifact_id": "signal-health-a",
                "signal_count": 9,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_signal_run_id": signal_id,
                "latest_status": "READY",
                "health_status": health_status,
                "signal_count": 9,
                "demo_signal_count": 9,
                "watch_count": 0,
                "review_buy_candidate_count": 0,
                "review_sell_candidate_count": 0,
                "blocked_count": 0,
                "source_candidate_run_id": "candidate-a",
                "selection_profile": "demo",
                "demo_mode": True,
                "not_strategy_recommendation": True,
                "alert_preview_path": str(alert_preview),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{signal_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_signal_run_id": signal_id,
            "health_status": health_status,
            "signal_count": 9,
            "next_manual_action": next_action,
            "warnings": ["Latest signal advisory artifact is DEMO_ONLY."] if warning_count else [],
            "output_files": {
                "signal_advisory_status_report": str(report),
                "signal_advisory_status_csv": str(status_csv),
                "signal_advisory_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
        },
    )
    return folder


def _single_symbol_advisory_status(
    root: Path,
    *,
    advisory_id: str = "single-a",
    symbol: str = "000001",
    status: str = "WARN",
    workflow_stage: str = "DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED",
    latest_status: str = "READY",
    advisory_action: str = "DEMO_ONLY",
    health_status: str = "PASS",
    final_score: str = "55.6",
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:43:00",
) -> Path:
    folder = root / "single_symbol_advisory" / "status" / f"status-{advisory_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "single_symbol_advisory_status_report.md"
    status_csv = folder / "single_symbol_advisory_status.csv"
    summary_csv = folder / "single_symbol_advisory_status_summary.csv"
    metadata_path = folder / "metadata.json"
    alert_preview = root / "single_symbol_advisory" / advisory_id / "alert_preview.md"
    advisory_report = root / "single_symbol_advisory" / advisory_id / "single_symbol_advisory_report.md"
    next_action = (
        "Review local single-symbol alert preview; do not treat DEMO_ONLY output as a strategy recommendation."
        if workflow_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_VALIDATED"
        else "Symbol was not found in the provided local artifact; provide a relevant candidates/scored/signals artifact before reviewing."
        if workflow_stage == "SINGLE_SYMBOL_ADVISORY_NOT_FOUND"
        else "Repair single-symbol advisory artifacts before using this review."
        if status == "FAIL"
        else "Review local single-symbol advisory report; manual confirmation remains required."
    )
    report.write_text("No live trading, broker API, order placement, or message delivery was invoked.", encoding="utf-8")
    alert_preview.parent.mkdir(parents=True, exist_ok=True)
    alert_preview.write_text("Manual confirmation required. No auto-order.", encoding="utf-8")
    advisory_report.write_text("Single-symbol advisory is not an order.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "SINGLE_SYMBOL_ADVISORY",
                "status": latest_status,
                "latest_artifact_id": advisory_id,
                "symbol": symbol,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "SINGLE_SYMBOL_ADVISORY_HEALTH",
                "status": health_status,
                "latest_artifact_id": "single-health-a",
                "symbol": symbol,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_advisory_run_id": advisory_id,
                "latest_symbol": symbol,
                "latest_status": latest_status,
                "latest_advisory_action": advisory_action,
                "health_status": health_status,
                "final_score": final_score,
                "demo_mode": True,
                "not_strategy_recommendation": True,
                "alert_preview_path": str(alert_preview),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{advisory_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_advisory_run_id": advisory_id,
            "latest_symbol": symbol,
            "latest_advisory_action": advisory_action,
            "health_status": health_status,
            "next_manual_action": next_action,
            "warnings": ["Latest single-symbol advisory artifact is review-only."] if warning_count else [],
            "output_files": {
                "single_symbol_advisory_status_report": str(report),
                "single_symbol_advisory_status_csv": str(status_csv),
                "single_symbol_advisory_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
        },
    )
    return folder


def _single_symbol_advisory_answer_status(
    root: Path,
    *,
    answer_id: str = "answer-a",
    advisory_id: str = "single-a",
    symbol: str = "000001",
    status: str = "WARN",
    workflow_stage: str = "DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED",
    latest_status: str = "READY",
    advisory_action: str = "DEMO_ONLY",
    health_status: str = "PASS",
    question: str = "should I buy?",
    answer_style: str = "concise",
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:44:00",
) -> Path:
    folder = root / "single_symbol_advisory_answer" / "status" / f"status-{answer_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "single_symbol_advisory_answer_status_report.md"
    status_csv = folder / "single_symbol_advisory_answer_status.csv"
    summary_csv = folder / "single_symbol_advisory_answer_status_summary.csv"
    metadata_path = folder / "metadata.json"
    answer_markdown = root / "single_symbol_advisory_answer" / answer_id / "single_symbol_advisory_answer.md"
    next_action = (
        "Review local question-style answer; do not treat DEMO_ONLY output as a strategy recommendation."
        if workflow_stage == "DEMO_SINGLE_SYMBOL_ADVISORY_ANSWER_VALIDATED"
        else "Symbol was not found in the provided local artifact; no recommendation was invented."
        if workflow_stage == "SINGLE_SYMBOL_ADVISORY_ANSWER_NOT_FOUND"
        else "Repair question-style answer artifacts before using this response."
        if status == "FAIL"
        else "Review local question-style answer; manual confirmation remains required."
    )
    report.write_text("No live trading, broker API, order placement, LLM API, or message delivery was invoked.", encoding="utf-8")
    answer_markdown.parent.mkdir(parents=True, exist_ok=True)
    answer_markdown.write_text(
        "Manual confirmation required. No auto-order. No LLM/API call. No message sent.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "SINGLE_SYMBOL_ADVISORY_ANSWER",
                "status": latest_status,
                "latest_artifact_id": answer_id,
                "symbol": symbol,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "SINGLE_SYMBOL_ADVISORY_ANSWER_HEALTH",
                "status": health_status,
                "latest_artifact_id": "answer-health-a",
                "symbol": symbol,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_answer_run_id": answer_id,
                "latest_advisory_run_id": advisory_id,
                "latest_symbol": symbol,
                "latest_status": latest_status,
                "latest_advisory_action": advisory_action,
                "latest_question": question,
                "answer_style": answer_style,
                "health_status": health_status,
                "demo_mode": True,
                "not_strategy_recommendation": True,
                "answer_markdown_path": str(answer_markdown),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{answer_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_answer_run_id": answer_id,
            "latest_advisory_run_id": advisory_id,
            "latest_symbol": symbol,
            "latest_advisory_action": advisory_action,
            "health_status": health_status,
            "next_manual_action": next_action,
            "warnings": ["Latest question-style answer is review-only."] if warning_count else [],
            "output_files": {
                "single_symbol_advisory_answer_status_report": str(report),
                "single_symbol_advisory_answer_status_csv": str(status_csv),
                "single_symbol_advisory_answer_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "llm_api_called": False,
        },
    )
    return folder


def _advisory_conversation_status(
    root: Path,
    *,
    conversation_id: str = "conv-a",
    question: str = "000001 now can buy?",
    parsed_symbol: str = "000001",
    parsed_intent: str = "BUY_REVIEW",
    status: str = "WARN",
    workflow_stage: str = "DEMO_ADVISORY_CONVERSATION_VALIDATED",
    latest_status: str = "READY",
    advisory_action: str = "DEMO_ONLY",
    health_status: str = "PASS",
    parser_type: str = "deterministic_rule_based",
    llm_api_called: bool = False,
    no_message_sent: bool = True,
    no_live_trading: bool = True,
    no_broker_api: bool = True,
    auto_order_allowed: bool = False,
    warning_count: int = 1,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:46:00",
) -> Path:
    folder = root / "advisory_conversation" / "status" / f"status-{conversation_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "advisory_conversation_status_report.md"
    status_csv = folder / "advisory_conversation_status.csv"
    summary_csv = folder / "advisory_conversation_status_summary.csv"
    metadata_path = folder / "metadata.json"
    linked_answer = root / "single_symbol_advisory_answer" / f"answer-{conversation_id}" / "single_symbol_advisory_answer.md"
    linked_answer.parent.mkdir(parents=True, exist_ok=True)
    linked_answer.write_text(
        "Manual confirmation required. No auto-order. No LLM/API call. No message sent.",
        encoding="utf-8",
    )
    next_action = (
        "Review local conversational advisory answer; do not treat DEMO_ONLY output as a strategy recommendation."
        if workflow_stage == "DEMO_ADVISORY_CONVERSATION_VALIDATED"
        else "Provide a six-digit local symbol in the question; no symbol or recommendation was invented."
        if workflow_stage == "ADVISORY_CONVERSATION_PARSE_FAILED"
        else "Parsed symbol was not found in the provided local artifact; no recommendation was invented."
        if workflow_stage == "ADVISORY_CONVERSATION_NOT_FOUND"
        else "Repair advisory conversation artifacts before using local conversational answers."
        if status == "FAIL"
        else "Review local conversational advisory answer; manual confirmation remains required."
    )
    report.write_text(
        "No live trading, broker API, order placement, LLM/API call, external API call, or message delivery was invoked.",
        encoding="utf-8",
    )
    pd.DataFrame(
        [
            {
                "component": "ADVISORY_CONVERSATION",
                "status": latest_status,
                "latest_artifact_id": conversation_id,
                "symbol": parsed_symbol,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "ADVISORY_CONVERSATION_HEALTH",
                "status": health_status,
                "latest_artifact_id": "conversation-health-a",
                "symbol": parsed_symbol,
                "warning_count": 0 if health_status == "PASS" else warning_count,
                "error_count": error_count,
                "issue_count": error_count + (0 if health_status == "PASS" else warning_count),
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "workflow_stage": workflow_stage,
                "status": status,
                "latest_conversation_run_id": conversation_id,
                "latest_original_question": question,
                "latest_parsed_symbol": parsed_symbol,
                "latest_parsed_intent": parsed_intent,
                "latest_advisory_action": advisory_action,
                "parser_type": parser_type,
                "health_status": health_status,
                "llm_api_called": llm_api_called,
                "no_message_sent": no_message_sent,
                "no_live_trading": no_live_trading,
                "no_broker_api": no_broker_api,
                "auto_order_allowed": auto_order_allowed,
                "linked_answer_markdown_path": str(linked_answer),
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{conversation_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_conversation_run_id": conversation_id,
            "latest_original_question": question,
            "latest_parsed_symbol": parsed_symbol,
            "latest_parsed_intent": parsed_intent,
            "latest_advisory_action": advisory_action,
            "health_status": health_status,
            "next_manual_action": next_action,
            "warnings": ["Latest advisory conversation artifact is review-only."] if warning_count else [],
            "output_files": {
                "advisory_conversation_status_report": str(report),
                "advisory_conversation_status_csv": str(status_csv),
                "advisory_conversation_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "message_delivery_enabled": False,
            "message_sent": False,
            "llm_api_called": llm_api_called,
        },
    )
    return folder


def _single_symbol_answer_artifact(
    root: Path,
    *,
    answer_id: str,
    advisory_id: str = "single-a",
    symbol: str = "000001",
    metadata_updates: dict | None = None,
) -> Path:
    folder = root / "single_symbol_advisory_answer" / answer_id
    folder.mkdir(parents=True, exist_ok=True)
    answer_md = folder / "single_symbol_advisory_answer.md"
    answer_json = folder / "single_symbol_advisory_answer.json"
    metadata_path = folder / "metadata.json"
    body = (
        "Demo-only review for workflow validation; not a real trading recommendation.\n"
        "Manual confirmation required. No auto-order. No message sent. No LLM/API call."
    )
    answer_md.write_text(body, encoding="utf-8")
    payload = {
        "answer_run_id": answer_id,
        "advisory_run_id": advisory_id,
        "symbol": symbol,
        "status": "READY",
        "advisory_action": "DEMO_ONLY",
        **_semantics_provenance("DEMO_ONLY"),
        "question": "should I buy?",
        "answer_style": "concise",
        "short_answer": "Demo-only review for workflow validation; not a real trading recommendation.",
        "answer_body": body,
        "requires_manual_confirmation": True,
        "auto_order_allowed": False,
        "no_live_trading": True,
        "no_broker_api": True,
        "no_message_sent": True,
        "audit_metadata": {
            "demo_mode": True,
            "not_strategy_recommendation": True,
            "llm_api_called": False,
            **_semantics_provenance("DEMO_ONLY"),
        },
        "advisory_record": {
            "symbol": symbol,
            "status": "READY",
            "advisory_action": "DEMO_ONLY",
            **_semantics_provenance("DEMO_ONLY"),
            "demo_mode": True,
            "not_strategy_recommendation": True,
            "requires_manual_confirmation": True,
            "auto_order_allowed": False,
            "no_live_trading": True,
            "no_broker_api": True,
            "no_message_sent": True,
        },
    }
    answer_json.write_text(json.dumps(payload, indent=2), encoding="utf-8")
    metadata = {
        "answer_run_id": answer_id,
        "created_at": f"{DECISION_DATE}T15:44:00",
        "advisory_run_id": advisory_id,
        "symbol": symbol,
        "status": "READY",
        "advisory_action": "DEMO_ONLY",
        **_semantics_provenance("DEMO_ONLY"),
        "question": "should I buy?",
        "answer_style": "concise",
        "short_answer": "Demo-only review for workflow validation; not a real trading recommendation.",
        "requires_manual_confirmation": True,
        "auto_order_allowed": False,
        "no_live_trading": True,
        "no_broker_api": True,
        "no_message_sent": True,
        "message_delivery_enabled": False,
        "message_sent": False,
        "live_trading_enabled": False,
        "broker_api_invoked": False,
        "approved_for_paper_applied": False,
        "llm_api_called": False,
        "external_api_called": False,
        "demo_mode": True,
        "not_strategy_recommendation": True,
        "output_files": {
            "single_symbol_advisory_answer": str(answer_md),
            "single_symbol_advisory_answer_json": str(answer_json),
            "metadata": str(metadata_path),
        },
    }
    metadata.update(metadata_updates or {})
    metadata_path.write_text(json.dumps(metadata, indent=2), encoding="utf-8")
    return folder


def _semantics_provenance(action: str) -> dict:
    return {
        "semantics_policy_source": "signal_semantics",
        "semantics_policy_version": "v0.1",
        "semantics_classifier": "classify_signal_semantics_action",
        "semantics_settings_profile": "demo",
        "semantics_action": action,
        "semantics_reason": "Dashboard fixture classified by shared signal semantics.",
        "semantics_manual_confirmation_required": True,
        "semantics_auto_order_allowed": False,
        "semantics_no_live_trading": True,
        "semantics_no_broker_api": True,
    }


def _market_update_handoff_status(
    root: Path,
    *,
    status: str = "WARN",
    workflow_stage: str = "CURRENT_CANDIDATES_READY_FOR_PAPER_SMOKE_TEST",
    handoff_id: str = "handoff-a",
    health_status: str = "PASS",
    warning_count: int = 0,
    error_count: int = 0,
    created_at: str = f"{DECISION_DATE}T15:35:00",
) -> Path:
    folder = root / "market_update_handoff" / "status" / f"status-{handoff_id}"
    folder.mkdir(parents=True, exist_ok=True)
    report = folder / "market_update_handoff_status_report.md"
    status_csv = folder / "market_update_handoff_status.csv"
    summary_csv = folder / "market_update_handoff_status_summary.csv"
    metadata_path = folder / "metadata.json"
    next_action = "Run current-to-paper on the latest current-candidates artifact, then continue paper review smoke testing."
    report.write_text("No live trading or broker API was invoked.", encoding="utf-8")
    pd.DataFrame(
        [
            {
                "component": "MARKET_UPDATE_HANDOFF_INDEX",
                "status": "PASS",
                "latest_handoff_id": handoff_id,
                "warning_count": 0,
                "error_count": 0,
                "issue_count": 0,
            },
            {
                "component": "MARKET_UPDATE_HANDOFF_HEALTH",
                "status": health_status,
                "latest_handoff_id": handoff_id,
                "warning_count": 0,
                "error_count": error_count,
                "issue_count": error_count,
            },
            {
                "component": "LATEST_MARKET_UPDATE_HANDOFF",
                "status": status,
                "latest_handoff_id": handoff_id,
                "warning_count": warning_count,
                "error_count": 0,
                "issue_count": 0,
            },
        ]
    ).to_csv(status_csv, index=False)
    pd.DataFrame(
        [
            {
                "status": status,
                "workflow_stage": workflow_stage,
                "latest_handoff_id": handoff_id,
                "pipeline_id": "pipeline-a",
                "snapshot_quality_status": "PASS",
                "current_candidate_run_id": "candidate-a",
                "candidate_count": 2,
                "health_status": health_status,
                "issue_count": error_count,
                "warning_count": 0,
                "error_count": error_count,
                "next_manual_action": next_action,
            }
        ]
    ).to_csv(summary_csv, index=False)
    _write_json(
        metadata_path,
        {
            "status_id": f"status-{handoff_id}",
            "created_at": created_at,
            "status": status,
            "workflow_stage": workflow_stage,
            "latest_handoff_id": handoff_id,
            "next_manual_action": next_action,
            "warnings": ["Provisional WARN_ACCEPT rows were included."] if warning_count else [],
            "output_files": {
                "market_update_handoff_status_report": str(report),
                "market_update_handoff_status_csv": str(status_csv),
                "market_update_handoff_status_summary": str(summary_csv),
                "metadata": str(metadata_path),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
        },
    )
    return folder


def _market_update_handoff_artifact(
    root: Path,
    *,
    handoff_id: str = "handoff-a",
    status: str = "PASS",
    pipeline_id: str = "pipeline-a",
    snapshot_quality_status: str = "PASS",
    current_candidate_run_id: str = "candidate-a",
    candidate_count: int = 2,
    created_at: str = f"{DECISION_DATE}T15:35:00",
) -> Path:
    artifact_dir = root / "market_update_handoff" / handoff_id
    artifact_dir.mkdir(parents=True, exist_ok=True)
    linked_dir = root / "market_update_handoff" / "_linked" / handoff_id
    linked_dir.mkdir(parents=True, exist_ok=True)
    no_live = "No live trading or broker API was invoked."

    batch_market_csv = linked_dir / "market_raw_data.csv"
    pd.DataFrame(
        [
            {
                "symbol": "510300",
                "trade_date": DECISION_DATE,
                "open": 1.0,
                "high": 1.1,
                "low": 0.9,
                "close": 1.0,
                "volume": 1000,
                "amount": 1000,
                "available_time": f"{DECISION_DATE} 15:30:00",
                "source": "AKSHARE_OPTIONAL",
            }
        ]
    ).to_csv(batch_market_csv, index=False)
    pipeline_manifest = linked_dir / "market_update_handoff_manifest.json"
    pipeline_manifest.write_text(
        json.dumps(
            {
                "datasets": [
                    {"dataset_type": "market", "source": "LOCAL_CSV", "input_path": str(batch_market_csv)}
                ]
            }
        ),
        encoding="utf-8",
    )
    pipeline_report = linked_dir / "data_pipeline_report.md"
    snapshot_manifest = linked_dir / "snapshot_manifest.json"
    snapshot_report = linked_dir / "snapshot_quality_gate_report.md"
    current_report = linked_dir / "current_candidates_report.md"
    current_metadata = linked_dir / "current_candidate_metadata.json"
    factor_dataset = linked_dir / "factor_dataset.csv"
    scored_dataset = linked_dir / "scored_dataset.csv"
    candidates = linked_dir / "candidates.csv"
    pipeline_report.write_text(no_live, encoding="utf-8")
    snapshot_manifest.write_text("{}", encoding="utf-8")
    snapshot_report.write_text(no_live, encoding="utf-8")
    current_report.write_text(no_live, encoding="utf-8")
    current_metadata.write_text(json.dumps({"run_id": current_candidate_run_id}), encoding="utf-8")
    pd.DataFrame([{"symbol": "510300"}] * candidate_count).to_csv(factor_dataset, index=False)
    pd.DataFrame([{"symbol": "510300", "final_score": 50.0}] * candidate_count).to_csv(scored_dataset, index=False)
    pd.DataFrame([{"symbol": "510300"}] * candidate_count).to_csv(candidates, index=False)

    handoff_report = artifact_dir / "market_update_handoff_report.md"
    handoff_rows = artifact_dir / "market_update_handoff_rows.csv"
    manifest_artifact = artifact_dir / "generated_pipeline_manifest.json"
    handoff_report.write_text(no_live, encoding="utf-8")
    pd.DataFrame(
        [
            {
                "symbol": "510300",
                "included": True,
                "handoff_status": "INCLUDED_WARN_ACCEPT" if status == "WARN" else "INCLUDED_ACCEPT",
                "raw_data_path": str(batch_market_csv),
            }
        ]
    ).to_csv(handoff_rows, index=False)
    manifest_artifact.write_text(pipeline_manifest.read_text(encoding="utf-8"), encoding="utf-8")

    _write_json(
        artifact_dir / "metadata.json",
        {
            "handoff_id": handoff_id,
            "status": status,
            "created_at": created_at,
            "summary": [{"included_row_count": 1}],
            "batch_market_csv_path": str(batch_market_csv),
            "generated_pipeline_manifest_path": str(pipeline_manifest),
            "pipeline_id": pipeline_id,
            "pipeline_status": "PASS",
            "data_pipeline_report_path": str(pipeline_report),
            "snapshot_manifest_path": str(snapshot_manifest),
            "snapshot_quality_status": snapshot_quality_status,
            "snapshot_quality_report_path": str(snapshot_report),
            "current_candidate_run_id": current_candidate_run_id,
            "current_candidate_artifact_paths": {
                "current_candidates_report": str(current_report),
                "metadata": str(current_metadata),
                "factor_dataset": str(factor_dataset),
                "scored_dataset": str(scored_dataset),
                "candidates": str(candidates),
            },
            "factor_dataset_shape": [candidate_count, 1],
            "scored_dataset_shape": [candidate_count, 2],
            "candidates_shape": [candidate_count, 1],
            "candidate_count": candidate_count,
            "warnings": ["Provisional WARN_ACCEPT rows were included."] if status == "WARN" else [],
            "artifact_paths": {
                "market_update_handoff_report": str(handoff_report),
                "market_update_handoff_rows": str(handoff_rows),
                "generated_pipeline_manifest": str(manifest_artifact),
                "metadata": str(artifact_dir / "metadata.json"),
            },
            "live_trading_enabled": False,
            "broker_api_invoked": False,
            "no_live_trading": True,
            "no_broker_api": True,
            "no_live_trading_statement": no_live,
        },
    )
    return artifact_dir


def _write_json(path: Path, payload: dict) -> Path:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(payload, indent=2, sort_keys=True), encoding="utf-8")
    return path


def test_research_status_includes_reviewer_no_hit_acceptance_when_active(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _reviewer_no_hit_acceptance_artifact(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.reviewer_no_hit_acceptance_status == "WARN"
    assert result.reviewer_no_hit_acceptance_stage == "REVIEWER_NO_HIT_SOURCE_COVERAGE_ACCEPTANCE_NEEDS_REVIEW"
    assert result.reviewer_no_hit_acceptance_row_count == 8
    assert result.reviewer_no_hit_acceptance_reviewer_acceptance_required_count == 8
    assert result.reviewer_no_hit_acceptance_checklist_pass_count == 0
    assert result.workflow_stage == "REVIEWER_NO_HIT_SOURCE_COVERAGE_ACCEPTANCE_NEEDS_REVIEW"


def test_research_status_preserves_later_paper_priority_with_reviewer_no_hit_acceptance(tmp_path: Path) -> None:
    root = _workflow_to_daily(_reports_root(tmp_path))
    _reviewer_no_hit_acceptance_artifact(root)
    _reconciliation(root, status="PASS")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="WATCH_ONLY_DEMO_VALIDATED_NO_FILLS",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Demo WATCH_ONLY paper workflow validated; no fills were supplied. Proceed to fill reconciliation "
            "only if testing fills, or return to data-source / strategy research."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.reviewer_no_hit_acceptance_stage == "REVIEWER_NO_HIT_SOURCE_COVERAGE_ACCEPTANCE_NEEDS_REVIEW"
    row = result.dashboard_frame.loc[
        result.dashboard_frame["component"] == "REVIEWER_NO_HIT_SOURCE_COVERAGE_ACCEPTANCE_STATUS"
    ].iloc[0]
    assert row["stale_warning_count"] >= 1
    assert row["actionable_warning_count"] == 0


def test_research_status_metadata_exports_reviewer_no_hit_acceptance_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _reviewer_no_hit_acceptance_artifact(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], keep_default_na=False)
    metadata = json.loads(Path(result.artifact_paths["metadata"]).read_text(encoding="utf-8"))

    assert summary.loc[0, "latest_reviewer_no_hit_acceptance_id"]
    assert summary.loc[0, "reviewer_no_hit_acceptance_row_count"] == 8
    assert metadata["reviewer_no_hit_acceptance_status"] == "WARN"
    assert metadata["reviewer_no_hit_acceptance_checklist_pass_count"] == 0


def _reviewer_no_hit_acceptance_artifact(root: Path) -> Path:
    enrichment = root / "_fixtures" / "enrichment-a"
    enrichment.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {"signal_date": "2024-04-02", "symbol": "000001", "universe_name": "stock_core"},
            {"signal_date": "2024-04-02", "symbol": "159915", "universe_name": "etf_core"},
        ]
    ).to_csv(enrichment / "pit_official_status_evidence_packet_enrichment.csv", index=False)
    _write_json(
        enrichment / "metadata.json",
        {
            "enrichment_id": "enrichment-a",
            "source_packet_id": "packet-a",
            "policy_comparison_id": "comparison-a",
            "created_at": "2026-06-05T00:00:00Z",
        },
    )
    audit = root / "_fixtures" / "audit"
    audit.mkdir(parents=True, exist_ok=True)
    for name in [
        "source_coverage_acceptance_rules.csv",
        "query_window_rules.csv",
        "survivorship_rationale_template.csv",
        "blocker_after_acceptance_matrix.csv",
    ]:
        pd.DataFrame([{"rule": "review_required"}]).to_csv(audit / name, index=False)
    comparison = root / "_fixtures" / "comparison-a"
    comparison.mkdir(parents=True, exist_ok=True)
    _write_json(comparison / "metadata.json", {"comparison_id": "comparison-a"})
    result = build_reviewer_no_hit_source_coverage_acceptance(
        enrichment=enrichment,
        audit=audit,
        policy_comparison=comparison,
        output_dir=root / "reviewer_no_hit_source_coverage_acceptance",
    )
    return result.artifact_paths["artifact_dir"]


def _reviewer_no_hit_downstream_impact_artifact(root: Path) -> Path:
    acceptance = _reviewer_no_hit_acceptance_artifact(root)
    validator = root / "_fixtures" / "validator-a"
    validator.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "universe_name": "stock_core",
                "checklist_pass": False,
                "blocker_reason": "remaining PIT evidence required",
            },
            {
                "signal_date": "2024-04-02",
                "symbol": "159915",
                "universe_name": "etf_core",
                "checklist_pass": False,
                "blocker_reason": "remaining PIT evidence required",
            },
        ]
    ).to_csv(validator / "pit_evidence_checklist_validation.csv", index=False)
    _write_json(validator / "metadata.json", {"validator_id": "validator-a"})
    policy = root / "_fixtures" / "comparison-a"
    policy.mkdir(parents=True, exist_ok=True)
    pd.DataFrame(
        [
            {
                "signal_date": "2024-04-02",
                "symbol": "000001",
                "recommended_future_universe": "stock_core",
            },
            {
                "signal_date": "2024-04-02",
                "symbol": "159915",
                "recommended_future_universe": "etf_core",
            },
        ]
    ).to_csv(policy / "pit_evidence_policy_profile_comparison.csv", index=False)
    _write_json(policy / "metadata.json", {"comparison_id": "comparison-a"})
    result = build_reviewer_no_hit_acceptance_downstream_impact(
        acceptance=acceptance,
        enrichment=root / "_fixtures" / "enrichment-a",
        validator=validator,
        policy_comparison=policy,
        output_dir=root / "reviewer_no_hit_acceptance_downstream_impact",
    )
    return result.artifact_paths["artifact_dir"]


def test_research_status_includes_reviewed_local_csv_input_contract_fixture_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    fixture = build_reviewed_local_csv_replay_prototype_input_contract_fixture(
        output_dir=root
        / "manual_diagnostics"
        / "reviewed_local_csv_replay_prototype_input_contract_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "REVIEWED_LOCAL_CSV_REPLAY_PROTOTYPE_INPUT_CONTRACT_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_context_visible is True
    assert result.latest_reviewed_local_csv_replay_prototype_input_contract_fixture_id == (
        fixture.reviewed_local_csv_replay_prototype_input_contract_fixture_id
    )
    assert result.latest_reviewed_local_csv_replay_prototype_input_contract_fixture_status == "PASS"
    assert result.latest_reviewed_local_csv_replay_prototype_input_contract_fixture_health_status == "PASS"
    assert result.latest_reviewed_local_csv_replay_prototype_input_contract_fixture_workflow_stage == (
        "REVIEWED_LOCAL_CSV_REPLAY_PROTOTYPE_INPUT_CONTRACT_FIXTURE_CREATED"
    )
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_created is True
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_contract_count == 12
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_validation_issue_count == 0
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_report_only is True
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_diagnostic_only is True
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_contracts_created is True
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_reviewed_input_package_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_active_reviewed_input_candidate_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_pit_admissibility_validator_implemented is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_input_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_evidence_bundle_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_decision_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_replay_decision_frozen is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_forward_labels_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_future_labels_joined is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_future_label_joined_to_decision_input is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_training_dataset_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_metric_computation_performed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_signal_score_input_authorized is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_model_training_performed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_stock_profile_validation_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_paper_validation_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_buy_review_eligible is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_buy_review_allowed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_strategy_performance_validated is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_current_candidates_run is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_snapshot_built is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_signal_semantics_changed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_trading_allowed is False
    assert result.real_buy_review_eligible is False
    assert result.buy_review_allowed is False
    assert result.strategy_performance_validated is False
    assert result.trading_allowed is False
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "REVIEWED_LOCAL_CSV_REPLAY_PROTOTYPE_INPUT_CONTRACT_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_reviewed_local_csv_replay_prototype_input_contract_fixture_id"] == (
        fixture.reviewed_local_csv_replay_prototype_input_contract_fixture_id
    )
    assert summary.loc[
        0,
        "reviewed_local_csv_replay_prototype_input_contract_fixture_contract_count",
    ] == "12"
    assert metadata["research_status_final_workflow_stage"] == result.workflow_stage
    assert metadata["reviewed_local_csv_replay_prototype_input_contract_fixture_context_visible"] is True
    assert metadata["reviewed_local_csv_replay_prototype_input_contract_fixture_contracts_created"] is True
    assert metadata["reviewed_local_csv_replay_prototype_input_contract_fixture_buy_review_allowed"] is False
    assert metadata["reviewed_local_csv_replay_prototype_input_contract_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_reviewed_local_csv_input_contract_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_reviewed_local_csv_replay_prototype_input_contract_fixture(
        output_dir=root
        / "manual_diagnostics"
        / "reviewed_local_csv_replay_prototype_input_contract_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Reviewed LOCAL_CSV input contract fixture is report-only "
            "context, not real reviewed input, PIT validation, replay input, labels, training, "
            "stock_profile validation, paper validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_context_visible is True
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_reviewed_input_package_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_pit_admissibility_validator_implemented is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_input_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_real_forward_labels_created is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_future_labels_joined is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_signal_score_input_authorized is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_model_training_performed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_buy_review_allowed is False
    assert result.reviewed_local_csv_replay_prototype_input_contract_fixture_trading_allowed is False


def test_cli_research_status_prints_reviewed_local_csv_input_contract_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_reviewed_local_csv_replay_prototype_input_contract_fixture(
        output_dir=root
        / "manual_diagnostics"
        / "reviewed_local_csv_replay_prototype_input_contract_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Reviewed LOCAL_CSV input contract fixture is report-only "
            "context, not real reviewed input, PIT validation, replay input, labels, training, "
            "stock_profile validation, paper validation, buy-review, performance validation, or trading."
        ),
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_context_visible: True" in output.out
    assert (
        "latest_reviewed_local_csv_replay_prototype_input_contract_fixture_id: "
        f"{fixture.reviewed_local_csv_replay_prototype_input_contract_fixture_id}"
    ) in output.out
    assert "latest_reviewed_local_csv_replay_prototype_input_contract_fixture_status: PASS" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_contract_count: 12" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_validation_issue_count: 0" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_report_only: True" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_diagnostic_only: True" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_contracts_created: True" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_real_reviewed_input_package_created: False"
        in output.out
    )
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_pit_admissibility_validator_implemented: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_input_created: False" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_evidence_bundle_created: False"
        in output.out
    )
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_real_replay_decision_created: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_replay_decision_frozen: False" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_real_forward_labels_created: False" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_future_labels_joined: False" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_future_label_joined_to_decision_input: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_training_dataset_created: False" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_metric_computation_performed: False"
        in output.out
    )
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_signal_score_input_authorized: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_model_training_performed: False" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_stock_profile_validation_created: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_paper_validation_created: False" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_real_buy_review_eligible: False" in output.out
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_buy_review_allowed: False" in output.out
    assert (
        "reviewed_local_csv_replay_prototype_input_contract_fixture_strategy_performance_validated: False"
        in output.out
    )
    assert "reviewed_local_csv_replay_prototype_input_contract_fixture_trading_allowed: False" in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out


def test_research_status_includes_tiny_pit_admissibility_validator_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_synthetic_validator_artifacts(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "TINY_PIT_ADMISSIBILITY_VALIDATOR_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.tiny_pit_admissibility_validator_context_visible is True
    assert result.latest_tiny_pit_admissibility_validator_id == fixture.validator_run_id
    assert result.latest_tiny_pit_admissibility_validator_status == "PASS"
    assert result.latest_tiny_pit_admissibility_validator_health_status == "PASS"
    assert result.latest_tiny_pit_admissibility_validator_workflow_stage == (
        "TINY_PIT_ADMISSIBILITY_VALIDATOR_SYNTHETIC_CORE_CREATED"
    )
    assert result.latest_tiny_pit_admissibility_validator_case_count == 14
    assert result.latest_tiny_pit_admissibility_validator_pass_candidate_count == 1
    assert result.latest_tiny_pit_admissibility_validator_warning_count == 3
    assert result.latest_tiny_pit_admissibility_validator_blocker_count == 11
    assert result.latest_tiny_pit_admissibility_validator_report_only is True
    assert result.latest_tiny_pit_admissibility_validator_diagnostic_only is True
    assert result.latest_tiny_pit_admissibility_validator_synthetic_only is True
    assert result.latest_tiny_pit_admissibility_validator_active_replay_input is False
    assert result.latest_tiny_pit_admissibility_validator_active_replay_ready is False
    assert result.latest_tiny_pit_admissibility_validator_trading_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_real_reviewed_csv_package_created is False
    assert result.latest_tiny_pit_admissibility_validator_active_reviewed_input_candidate_created is False
    assert result.latest_tiny_pit_admissibility_validator_real_replay_input_created is False
    assert result.latest_tiny_pit_admissibility_validator_replay_execution_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_forward_labels_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_training_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_metric_computation_performed is False
    assert result.latest_tiny_pit_admissibility_validator_signal_score_implemented is False
    assert result.latest_tiny_pit_admissibility_validator_model_training_performed is False
    assert result.latest_tiny_pit_admissibility_validator_stock_profile_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_paper_validation_created is False
    assert result.latest_tiny_pit_admissibility_validator_real_buy_review_eligible is False
    assert result.latest_tiny_pit_admissibility_validator_buy_review_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_strategy_performance_validated is False
    assert result.latest_tiny_pit_admissibility_validator_broker_api_calls is False
    assert result.latest_tiny_pit_admissibility_validator_data_raw_written is False
    assert result.latest_tiny_pit_admissibility_validator_data_processed_written is False
    assert result.latest_tiny_pit_admissibility_validator_data_cache_written is False
    assert result.tiny_pit_admissibility_validator_next_action == (
        "Tiny PIT Admissibility Validator Post-Checkpoint Governance Audit Report-Only v0.1"
    )
    assert result.workflow_stage != "TINY_PIT_ADMISSIBILITY_VALIDATOR_SYNTHETIC_CORE_CREATED"
    assert row["status"] == "PASS"
    assert row["workflow_area"] == "TINY_PIT_ADMISSIBILITY_VALIDATOR"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_tiny_pit_admissibility_validator_id"] == fixture.validator_run_id
    assert summary.loc[0, "latest_tiny_pit_admissibility_validator_case_count"] == "14"
    assert summary.loc[0, "tiny_pit_admissibility_validator_context_visible"] == "True"
    assert metadata["research_status_final_workflow_stage"] == result.workflow_stage
    assert metadata["tiny_pit_admissibility_validator_context_visible"] is True
    assert metadata["latest_tiny_pit_admissibility_validator_report_only"] is True
    assert metadata["latest_tiny_pit_admissibility_validator_diagnostic_only"] is True
    assert metadata["latest_tiny_pit_admissibility_validator_synthetic_only"] is True
    assert metadata["latest_tiny_pit_admissibility_validator_buy_review_allowed"] is False
    assert metadata["latest_tiny_pit_admissibility_validator_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_tiny_pit_validator(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_synthetic_validator_artifacts(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Tiny PIT synthetic validator is report-only context, "
            "not real PIT validation, reviewed CSV, replay input, labels, training, stock_profile, "
            "paper validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.tiny_pit_admissibility_validator_context_visible is True
    assert result.latest_tiny_pit_admissibility_validator_status == "PASS"
    assert result.latest_tiny_pit_admissibility_validator_active_replay_input is False
    assert result.latest_tiny_pit_admissibility_validator_active_replay_ready is False
    assert result.latest_tiny_pit_admissibility_validator_real_reviewed_csv_package_created is False
    assert result.latest_tiny_pit_admissibility_validator_active_reviewed_input_candidate_created is False
    assert result.latest_tiny_pit_admissibility_validator_real_replay_input_created is False
    assert result.latest_tiny_pit_admissibility_validator_replay_execution_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_forward_labels_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_training_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_stock_profile_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_paper_validation_created is False
    assert result.latest_tiny_pit_admissibility_validator_buy_review_allowed is False
    assert result.latest_tiny_pit_admissibility_validator_strategy_performance_validated is False
    assert result.latest_tiny_pit_admissibility_validator_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_validator_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_synthetic_validator_artifacts(
        output_dir=root / "manual_diagnostics" / "tiny_pit_admissibility_validator_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Tiny PIT synthetic validator is report-only context."
        ),
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "tiny_pit_admissibility_validator_context_visible: True" in output.out
    assert f"latest_tiny_pit_admissibility_validator_id: {fixture.validator_run_id}" in output.out
    assert "latest_tiny_pit_admissibility_validator_status: PASS" in output.out
    assert "latest_tiny_pit_admissibility_validator_health_status: PASS" in output.out
    assert (
        "latest_tiny_pit_admissibility_validator_workflow_stage: "
        "TINY_PIT_ADMISSIBILITY_VALIDATOR_SYNTHETIC_CORE_CREATED"
    ) in output.out
    assert "latest_tiny_pit_admissibility_validator_case_count: 14" in output.out
    assert "latest_tiny_pit_admissibility_validator_pass_candidate_count: 1" in output.out
    assert "latest_tiny_pit_admissibility_validator_warning_count: 3" in output.out
    assert "latest_tiny_pit_admissibility_validator_blocker_count: 11" in output.out
    assert "latest_tiny_pit_admissibility_validator_report_only: True" in output.out
    assert "latest_tiny_pit_admissibility_validator_diagnostic_only: True" in output.out
    assert "latest_tiny_pit_admissibility_validator_synthetic_only: True" in output.out
    assert "latest_tiny_pit_admissibility_validator_active_replay_input: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_active_replay_ready: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_real_reviewed_csv_package_created: False" in output.out
    assert (
        "latest_tiny_pit_admissibility_validator_active_reviewed_input_candidate_created: False"
        in output.out
    )
    assert "latest_tiny_pit_admissibility_validator_real_replay_input_created: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_replay_execution_allowed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_forward_labels_allowed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_training_allowed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_metric_computation_performed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_signal_score_implemented: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_model_training_performed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_stock_profile_allowed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_paper_validation_created: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_buy_review_allowed: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_strategy_performance_validated: False" in output.out
    assert "latest_tiny_pit_admissibility_validator_trading_allowed: False" in output.out
    assert "tiny_pit_admissibility_validator_next_action: Tiny PIT Admissibility Validator Post-Checkpoint" in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_tiny_pit_reviewed_package_fixture_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_tiny_pit_reviewed_package_fixture_artifacts(
        output_root=root / "manual_diagnostics" / "tiny_pit_reviewed_package_fixture_v0_1"
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"] == "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.tiny_pit_reviewed_package_fixture_context_visible is True
    assert result.latest_tiny_pit_reviewed_package_fixture_id == fixture.fixture_id
    assert result.latest_tiny_pit_reviewed_package_fixture_status == (
        "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_reviewed_package_fixture_health_status == "PASS"
    assert result.latest_tiny_pit_reviewed_package_fixture_workflow_stage == (
        "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_reviewed_package_fixture_case_count == 15
    assert result.latest_tiny_pit_reviewed_package_fixture_pass_count == 9
    assert result.latest_tiny_pit_reviewed_package_fixture_warn_count == 2
    assert result.latest_tiny_pit_reviewed_package_fixture_fail_count == 4
    assert result.latest_tiny_pit_reviewed_package_fixture_blocker_count == 13
    assert result.latest_tiny_pit_reviewed_package_fixture_warning_count == 2
    assert result.latest_tiny_pit_reviewed_package_fixture_report_only is True
    assert result.latest_tiny_pit_reviewed_package_fixture_diagnostic_only is True
    assert result.latest_tiny_pit_reviewed_package_fixture_synthetic_only is True
    assert result.latest_tiny_pit_reviewed_package_fixture_real_reviewed_csv_package_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_reviewed_input_candidate_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_real_replay_input_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_replay_input is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_replay_ready is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_replay_input_ready_emitted is False
    assert result.latest_tiny_pit_reviewed_package_fixture_replay_execution_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_replay_decisions_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_forward_labels_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_future_labels_joined is False
    assert result.latest_tiny_pit_reviewed_package_fixture_training_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_training_dataset_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_metric_computation_performed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_signal_score_implemented is False
    assert result.latest_tiny_pit_reviewed_package_fixture_model_training_performed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_weights_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_thresholds_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_stock_profile_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_stock_profile_validation_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_paper_validation_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_real_buy_review_eligible is False
    assert result.latest_tiny_pit_reviewed_package_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_strategy_performance_validated is False
    assert result.latest_tiny_pit_reviewed_package_fixture_current_candidates_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_snapshots_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_signal_semantics_mutated is False
    assert result.latest_tiny_pit_reviewed_package_fixture_broker_api_called is False
    assert result.latest_tiny_pit_reviewed_package_fixture_order_placed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_message_sent is False
    assert result.latest_tiny_pit_reviewed_package_fixture_external_api_called is False
    assert result.latest_tiny_pit_reviewed_package_fixture_llm_api_called is False
    assert result.latest_tiny_pit_reviewed_package_fixture_trading_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_data_raw_written is False
    assert result.latest_tiny_pit_reviewed_package_fixture_data_processed_written is False
    assert result.latest_tiny_pit_reviewed_package_fixture_data_cache_written is False
    assert result.latest_tiny_pit_reviewed_package_fixture_recommended_next_task == (
        "Tiny PIT Reviewed Package Fixture Post-Checkpoint Governance Audit Report-Only v0.1"
    )
    assert result.workflow_stage != "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    assert row["status"] == "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    assert row["workflow_area"] == "TINY_PIT_REVIEWED_PACKAGE_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_tiny_pit_reviewed_package_fixture_id"] == fixture.fixture_id
    assert summary.loc[0, "latest_tiny_pit_reviewed_package_fixture_case_count"] == "15"
    assert summary.loc[0, "tiny_pit_reviewed_package_fixture_context_visible"] == "True"
    assert metadata["research_status_final_workflow_stage"] == result.workflow_stage
    assert metadata["tiny_pit_reviewed_package_fixture_context_visible"] is True
    assert metadata["latest_tiny_pit_reviewed_package_fixture_report_only"] is True
    assert metadata["latest_tiny_pit_reviewed_package_fixture_diagnostic_only"] is True
    assert metadata["latest_tiny_pit_reviewed_package_fixture_synthetic_only"] is True
    assert metadata["latest_tiny_pit_reviewed_package_fixture_buy_review_allowed"] is False
    assert metadata["latest_tiny_pit_reviewed_package_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_tiny_pit_reviewed_package_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_tiny_pit_reviewed_package_fixture_artifacts(
        output_root=root / "manual_diagnostics" / "tiny_pit_reviewed_package_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Tiny PIT reviewed package fixture is synthetic-only "
            "context, not a real reviewed CSV package, active replay input, replay, labels, training, "
            "stock_profile, paper validation, buy-review, performance validation, or trading."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.tiny_pit_reviewed_package_fixture_context_visible is True
    assert result.latest_tiny_pit_reviewed_package_fixture_status == (
        "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_reviewed_package_fixture_real_reviewed_csv_package_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_reviewed_input_candidate_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_real_replay_input_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_active_replay_input is False
    assert result.latest_tiny_pit_reviewed_package_fixture_replay_execution_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_forward_labels_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_training_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_stock_profile_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_paper_validation_created is False
    assert result.latest_tiny_pit_reviewed_package_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_reviewed_package_fixture_strategy_performance_validated is False
    assert result.latest_tiny_pit_reviewed_package_fixture_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_reviewed_package_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_tiny_pit_reviewed_package_fixture_artifacts(
        output_root=root / "manual_diagnostics" / "tiny_pit_reviewed_package_fixture_v0_1"
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority; Tiny PIT reviewed package fixture is report-only.",
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "tiny_pit_reviewed_package_fixture_context_visible: True" in output.out
    assert f"latest_tiny_pit_reviewed_package_fixture_id: {fixture.fixture_id}" in output.out
    assert (
        "latest_tiny_pit_reviewed_package_fixture_status: "
        "TINY_PIT_REVIEWED_PACKAGE_FIXTURE_CREATED_REPORT_ONLY"
    ) in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_health_status: PASS" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_case_count: 15" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_report_only: True" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_diagnostic_only: True" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_synthetic_only: True" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_real_reviewed_csv_package_created: False" in output.out
    assert (
        "latest_tiny_pit_reviewed_package_fixture_active_reviewed_input_candidate_created: False"
        in output.out
    )
    assert "latest_tiny_pit_reviewed_package_fixture_active_replay_input: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_replay_execution_allowed: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_forward_labels_created: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_training_allowed: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_stock_profile_allowed: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_buy_review_allowed: False" in output.out
    assert "latest_tiny_pit_reviewed_package_fixture_trading_allowed: False" in output.out
    assert (
        "latest_tiny_pit_reviewed_package_fixture_recommended_next_task: "
        "Tiny PIT Reviewed Package Fixture Post-Checkpoint Governance Audit Report-Only v0.1"
    ) in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_tiny_pit_metadata_reference_following_fields(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "metadata_reference_following_inputs"
    manifest_path = _metadata_reference_following_manifest(manifest_root)
    run = run_metadata_reference_following(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_v0_1"
        ),
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        inspection_level=INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_METADATA_REFERENCE_FOLLOWING_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.metadata_reference_following_context_visible is True
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_run_id
        == run["run_id"]
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_runtime_status
        == "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_METADATA_REFERENCES_FOLLOWED_REPORT_ONLY"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_health_status
        == "PASS"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_workflow_stage
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_METADATA_REFERENCE_FOLLOWING_CORE_CREATED_REPORT_ONLY"
    )
    assert result.metadata_reference_following_csv_read_level == "CSV_READ_NONE"
    assert result.metadata_reference_following_inspection_level == INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY
    assert result.metadata_reference_following_real_manifest_read is True
    assert result.metadata_reference_following_references_declared is True
    assert result.metadata_reference_following_references_followed is True
    assert result.metadata_reference_following_metadata_files_followed_count == 1
    assert result.metadata_reference_following_forbidden_data_references_count == 0
    assert result.metadata_reference_following_path_guard_blocker_count == 0
    assert result.metadata_reference_following_manifest_schema_blocker_count == 0
    assert result.metadata_reference_following_metadata_schema_blocker_count == 0
    assert result.metadata_reference_following_available_time_metadata_blocker_count == 0
    assert result.metadata_reference_following_source_hash_revision_metadata_blocker_count == 0
    assert result.metadata_reference_following_reviewer_quality_metadata_blocker_count == 0
    assert result.metadata_reference_following_limitation_warning_count == 0
    assert result.metadata_reference_following_local_file_hash_computed is False
    assert result.metadata_reference_following_external_source_validated is False
    assert result.metadata_reference_following_pit_admissibility_validated is False
    assert result.metadata_reference_following_real_csv_consumed is False
    assert result.metadata_reference_following_real_reviewed_csv_package_created is False
    assert result.metadata_reference_following_real_package_candidate_created is False
    assert result.metadata_reference_following_active_reviewed_input_candidate_created is False
    assert result.metadata_reference_following_real_replay_input_created is False
    assert result.metadata_reference_following_active_replay_input is False
    assert result.metadata_reference_following_active_replay_ready is False
    assert result.metadata_reference_following_active_replay_input_ready_emitted is False
    assert result.metadata_reference_following_replay_execution_allowed is False
    assert result.metadata_reference_following_trading_allowed is False
    assert result.metadata_reference_following_buy_review_allowed is False
    assert result.metadata_reference_following_data_raw_written is False
    assert result.metadata_reference_following_data_processed_written is False
    assert result.metadata_reference_following_data_cache_written is False
    assert (
        result.metadata_reference_following_recommended_next_task
        == EXPECTED_METADATA_REFERENCE_FOLLOWING_NEXT_BOUNDARY_DESIGN_TASK
    )
    for phrase in STALE_METADATA_REFERENCE_FOLLOWING_NEXT_ACTION_PHRASES:
        assert phrase not in result.metadata_reference_following_recommended_next_task
    assert "CSV header allowed" not in result.metadata_reference_following_recommended_next_task
    assert "row count allowed" not in result.metadata_reference_following_recommended_next_task
    assert "file hash allowed" not in result.metadata_reference_following_recommended_next_task
    assert row["status"] == "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_METADATA_REFERENCES_FOLLOWED_REPORT_ONLY"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_METADATA_REFERENCE_FOLLOWING"
    )
    assert row["blocking_error_count"] == 0
    assert (
        summary.loc[
            0,
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_run_id",
        ]
        == run["run_id"]
    )
    assert summary.loc[0, "metadata_reference_following_context_visible"] == "True"
    assert summary.loc[0, "metadata_reference_following_csv_read_level"] == "CSV_READ_NONE"
    assert summary.loc[0, "metadata_reference_following_references_followed"] == "True"
    assert summary.loc[0, "metadata_reference_following_real_csv_consumed"] == "False"
    assert summary.loc[0, "metadata_reference_following_trading_allowed"] == "False"
    assert metadata["metadata_reference_following_context_visible"] is True
    assert metadata["metadata_reference_following_csv_read_level"] == "CSV_READ_NONE"
    assert metadata["metadata_reference_following_references_followed"] is True
    assert metadata["metadata_reference_following_real_csv_consumed"] is False
    assert metadata["metadata_reference_following_trading_allowed"] is False
    assert "ACTIVE_REPLAY_INPUT_READY" not in json.dumps(metadata)


def test_research_status_preserves_paper_priority_over_tiny_pit_metadata_reference_following(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "metadata_reference_following_inputs"
    manifest_path = _metadata_reference_following_manifest(manifest_root)
    run_metadata_reference_following(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_v0_1"
        ),
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        inspection_level=INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.metadata_reference_following_context_visible is True
    assert result.metadata_reference_following_csv_read_level == "CSV_READ_NONE"
    assert result.metadata_reference_following_references_followed is True
    assert result.metadata_reference_following_active_replay_input is False
    assert result.metadata_reference_following_active_replay_input_ready_emitted is False
    assert result.metadata_reference_following_replay_execution_allowed is False
    assert result.metadata_reference_following_buy_review_allowed is False
    assert result.metadata_reference_following_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_metadata_reference_following_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "metadata_reference_following_inputs"
    manifest_path = _metadata_reference_following_manifest(manifest_root)
    run = run_metadata_reference_following(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_v0_1"
        ),
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        inspection_level=INSPECTION_LEVEL_FOLLOWED_METADATA_ONLY,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out
    metadata_paths = list((tmp_path / "dashboard").glob("*/metadata.json"))
    assert len(metadata_paths) == 1
    metadata = json.loads(metadata_paths[0].read_text(encoding="utf-8"))
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_metadata_reference_following_run_id"
        ]
        == run["run_id"]
    )
    assert metadata["metadata_reference_following_context_visible"] is True
    assert metadata["metadata_reference_following_csv_read_level"] == "CSV_READ_NONE"
    assert metadata["metadata_reference_following_references_followed"] is True
    assert metadata["metadata_reference_following_real_csv_consumed"] is False
    assert metadata["metadata_reference_following_active_replay_input"] is False
    assert metadata["metadata_reference_following_buy_review_allowed"] is False
    assert metadata["metadata_reference_following_trading_allowed"] is False


def test_research_status_includes_csv_structural_header_only_fields(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "csv_structural_header_only_inputs"
    manifest_path = _csv_structural_header_only_manifest(manifest_root)
    run = run_csv_structural_file_touch(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_file_touch_v0_1"
        ),
        run_id="002_header",
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        file_touch_level="CSV_STRUCTURAL_HEADER_ONLY",
        csv_read_level="CSV_HEADER_ONLY",
        local_file_hash_level="LOCAL_FILE_HASH_NONE",
        allow_csv_header_only=True,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_STRUCTURAL_HEADER_ONLY_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.csv_structural_header_only_context_visible is True
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_header_only_run_id
        == run["run_id"]
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_header_only_runtime_status
        == "CSV_STRUCTURAL_HEADER_ONLY_REPORT_ONLY"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_header_only_health_status
        == "PASS"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_header_only_workflow_stage
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_STRUCTURAL_HEADER_ONLY_CORE_CREATED_REPORT_ONLY"
    )
    assert result.csv_structural_header_only_file_touch_level == "CSV_STRUCTURAL_HEADER_ONLY"
    assert result.csv_structural_header_only_csv_read_level == "CSV_HEADER_ONLY"
    assert result.csv_structural_header_only_local_file_hash_level == "LOCAL_FILE_HASH_NONE"
    assert result.csv_structural_header_only_csv_file_opened_structurally is True
    assert result.csv_structural_header_only_csv_header_read is True
    assert result.csv_structural_header_only_csv_header_column_count == 3
    assert result.csv_structural_header_only_csv_row_count_computed is False
    assert result.csv_structural_header_only_csv_row_count == ""
    assert result.csv_structural_header_only_csv_values_read is False
    assert result.csv_structural_header_only_csv_full_content_read is False
    assert result.csv_structural_header_only_local_file_byte_hash_computed is False
    assert result.csv_structural_header_only_local_file_byte_hash_algorithm == ""
    assert result.csv_structural_header_only_real_csv_consumed is False
    assert result.csv_structural_header_only_real_package_candidate_created is False
    assert result.csv_structural_header_only_active_reviewed_input_candidate_created is False
    assert result.csv_structural_header_only_real_replay_input_created is False
    assert result.csv_structural_header_only_active_replay_input is False
    assert result.csv_structural_header_only_active_replay_ready is False
    assert result.csv_structural_header_only_active_replay_input_ready_emitted is False
    assert result.csv_structural_header_only_replay_execution_allowed is False
    assert result.csv_structural_header_only_trading_allowed is False
    assert result.csv_structural_header_only_buy_review_allowed is False
    assert result.csv_structural_header_only_data_raw_written is False
    assert result.csv_structural_header_only_data_processed_written is False
    assert result.csv_structural_header_only_data_cache_written is False
    next_task = result.csv_structural_header_only_recommended_next_task
    assert next_task == EXPECTED_CSV_STRUCTURAL_HEADER_ONLY_NEXT_TASK
    for stale in STALE_CSV_STRUCTURAL_HEADER_ONLY_NEXT_ACTION_PHRASES:
        assert stale not in next_task
    for unsafe in UNSAFE_CSV_STRUCTURAL_HEADER_ONLY_NEXT_ACTION_PHRASES:
        assert unsafe not in next_task
    assert row["status"] == "CSV_STRUCTURAL_HEADER_ONLY_REPORT_ONLY"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_STRUCTURAL_HEADER_ONLY"
    )
    assert row["blocking_error_count"] == 0
    assert (
        summary.loc[
            0,
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_header_only_run_id",
        ]
        == run["run_id"]
    )
    assert summary.loc[0, "csv_structural_header_only_context_visible"] == "True"
    assert summary.loc[0, "csv_structural_header_only_csv_header_read"] == "True"
    assert summary.loc[0, "csv_structural_header_only_csv_row_count_computed"] == "False"
    assert summary.loc[0, "csv_structural_header_only_csv_values_read"] == "False"
    assert summary.loc[0, "csv_structural_header_only_csv_full_content_read"] == "False"
    assert summary.loc[0, "csv_structural_header_only_local_file_byte_hash_computed"] == "False"
    assert summary.loc[0, "csv_structural_header_only_real_csv_consumed"] == "False"
    assert summary.loc[0, "csv_structural_header_only_trading_allowed"] == "False"
    assert summary.loc[0, "csv_structural_header_only_recommended_next_task"] == EXPECTED_CSV_STRUCTURAL_HEADER_ONLY_NEXT_TASK
    assert metadata["csv_structural_header_only_context_visible"] is True
    assert metadata["csv_structural_header_only_csv_header_read"] is True
    assert metadata["csv_structural_header_only_csv_row_count_computed"] is False
    assert metadata["csv_structural_header_only_csv_values_read"] is False
    assert metadata["csv_structural_header_only_csv_full_content_read"] is False
    assert metadata["csv_structural_header_only_local_file_byte_hash_computed"] is False
    assert metadata["csv_structural_header_only_real_csv_consumed"] is False
    assert metadata["csv_structural_header_only_trading_allowed"] is False
    assert metadata["csv_structural_header_only_recommended_next_task"] == EXPECTED_CSV_STRUCTURAL_HEADER_ONLY_NEXT_TASK
    assert "SENTINEL_DASHBOARD_ROW_VALUE_DO_NOT_READ" not in json.dumps(metadata)
    assert "ACTIVE_REPLAY_INPUT_READY" not in json.dumps(metadata)


def test_research_status_preserves_paper_priority_over_csv_structural_header_only(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "csv_structural_header_only_inputs"
    manifest_path = _csv_structural_header_only_manifest(manifest_root)
    run_csv_structural_file_touch(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_structural_file_touch_v0_1"
        ),
        run_id="002_header",
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        file_touch_level="CSV_STRUCTURAL_HEADER_ONLY",
        csv_read_level="CSV_HEADER_ONLY",
        local_file_hash_level="LOCAL_FILE_HASH_NONE",
        allow_csv_header_only=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.csv_structural_header_only_context_visible is True
    assert result.csv_structural_header_only_csv_header_read is True
    assert result.csv_structural_header_only_csv_values_read is False
    assert result.csv_structural_header_only_csv_full_content_read is False
    assert result.csv_structural_header_only_real_csv_consumed is False
    assert result.csv_structural_header_only_active_replay_input is False
    assert result.csv_structural_header_only_active_replay_input_ready_emitted is False
    assert result.csv_structural_header_only_replay_execution_allowed is False
    assert result.csv_structural_header_only_buy_review_allowed is False
    assert result.csv_structural_header_only_trading_allowed is False


def test_research_status_includes_local_file_byte_hash_only_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_local_file_byte_hash_only_v0_1"
    )
    run_local_file_byte_hash_only(output_root=output_root, run_id="003_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.local_file_byte_hash_only_context_visible is True
    assert result.latest_local_file_byte_hash_only_run_id == "003_no_input"
    assert result.latest_local_file_byte_hash_only_runtime_status == "NO_LOCAL_FILE_BYTE_HASH_INPUT"
    assert result.latest_local_file_byte_hash_only_health_status == "PASS"
    assert result.latest_local_file_byte_hash_only_file_touch_level == "FILE_TOUCH_NONE"
    assert result.latest_local_file_byte_hash_only_csv_read_level == "CSV_READ_NONE"
    assert result.latest_local_file_byte_hash_only_local_file_hash_level == "LOCAL_FILE_HASH_NONE"
    assert result.latest_local_file_byte_hash_only_hash_computed is False
    assert result.local_file_byte_hash_only_csv_file_opened_structurally is False
    assert result.local_file_byte_hash_only_csv_header_read is False
    assert result.local_file_byte_hash_only_csv_row_count_computed is False
    assert result.local_file_byte_hash_only_csv_values_read is False
    assert result.local_file_byte_hash_only_csv_full_content_read is False
    assert result.local_file_byte_hash_only_real_csv_consumed is False
    assert result.local_file_byte_hash_only_source_hash_validated is False
    assert result.local_file_byte_hash_only_revision_id_validated is False
    assert result.local_file_byte_hash_only_available_time_validated is False
    assert result.local_file_byte_hash_only_pit_admissibility_validated is False
    assert result.local_file_byte_hash_only_reviewer_authority_validated is False
    assert result.local_file_byte_hash_only_active_replay_input is False
    assert result.local_file_byte_hash_only_active_replay_input_ready_emitted is False
    assert result.local_file_byte_hash_only_replay_execution_allowed is False
    assert result.local_file_byte_hash_only_buy_review_allowed is False
    assert result.local_file_byte_hash_only_trading_allowed is False
    assert result.local_file_byte_hash_only_data_raw_written is False
    assert result.local_file_byte_hash_only_data_processed_written is False
    assert result.local_file_byte_hash_only_data_cache_written is False


def test_research_status_includes_local_file_byte_hash_only_preview_only_fields(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "local_file_byte_hash_only_inputs"
    manifest_path, csv_path, csv_bytes = _local_file_byte_hash_only_manifest(manifest_root)
    full_hash = hashlib.sha256(csv_bytes).hexdigest()
    preview = full_hash[:LOCAL_FILE_BYTE_HASH_ONLY_PREVIEW_CHARS]
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_local_file_byte_hash_only_v0_1"
    )
    run_local_file_byte_hash_only(
        output_root=output_root,
        run_id="003_hash",
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        file_touch_level=LOCAL_FILE_BYTE_HASH_ONLY,
        csv_read_level=LOCAL_FILE_BYTE_HASH_ONLY_CSV_READ_NONE,
        local_file_hash_level=LOCAL_FILE_BYTE_HASH_SHA256_ONLY,
        allow_local_file_byte_hash_only=True,
    )
    csv_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_LOCAL_FILE_BYTE_HASH_ONLY_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")

    assert result.local_file_byte_hash_only_context_visible is True
    assert result.latest_local_file_byte_hash_only_run_id == "003_hash"
    assert result.latest_local_file_byte_hash_only_runtime_status == "LOCAL_FILE_BYTE_HASH_ONLY_REPORT_ONLY"
    assert result.latest_local_file_byte_hash_only_health_status == "PASS"
    assert result.latest_local_file_byte_hash_only_workflow_stage == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_LOCAL_FILE_BYTE_HASH_ONLY_CORE_CREATED_REPORT_ONLY"
    )
    assert result.latest_local_file_byte_hash_only_file_touch_level == LOCAL_FILE_BYTE_HASH_ONLY
    assert result.latest_local_file_byte_hash_only_csv_read_level == "CSV_READ_NONE"
    assert result.latest_local_file_byte_hash_only_local_file_hash_level == LOCAL_FILE_BYTE_HASH_SHA256_ONLY
    assert result.latest_local_file_byte_hash_only_hash_computed is True
    assert result.latest_local_file_byte_hash_only_hash_algorithm == "SHA-256"
    assert result.latest_local_file_byte_hash_only_hash_preview == preview
    assert result.latest_local_file_byte_hash_only_hash_disclosure_level == "FULL_METADATA_PREVIEW_STATUS"
    assert result.latest_local_file_byte_hash_only_full_hash_recorded_in_metadata is True
    assert result.latest_local_file_byte_hash_only_verified_against_manifest is False
    assert result.latest_local_file_byte_hash_only_expected_hash_present is False
    assert result.latest_local_file_byte_hash_only_file_size_bytes == str(len(csv_bytes))
    assert result.local_file_byte_hash_only_csv_file_opened_structurally is False
    assert result.local_file_byte_hash_only_csv_header_read is False
    assert result.local_file_byte_hash_only_csv_row_count_computed is False
    assert result.local_file_byte_hash_only_csv_values_read is False
    assert result.local_file_byte_hash_only_csv_full_content_read is False
    assert result.local_file_byte_hash_only_real_csv_consumed is False
    assert result.local_file_byte_hash_only_source_hash_validated is False
    assert result.local_file_byte_hash_only_revision_id_validated is False
    assert result.local_file_byte_hash_only_available_time_validated is False
    assert result.local_file_byte_hash_only_pit_admissibility_validated is False
    assert result.local_file_byte_hash_only_source_reliability_scored is False
    assert result.local_file_byte_hash_only_reviewer_authority_validated is False
    assert result.local_file_byte_hash_only_real_reviewed_csv_package_created is False
    assert result.local_file_byte_hash_only_real_package_candidate_created is False
    assert result.local_file_byte_hash_only_active_reviewed_input_candidate_created is False
    assert result.local_file_byte_hash_only_real_replay_input_created is False
    assert result.local_file_byte_hash_only_active_replay_input is False
    assert result.local_file_byte_hash_only_active_replay_ready is False
    assert result.local_file_byte_hash_only_active_replay_input_ready_emitted is False
    assert result.local_file_byte_hash_only_replay_execution_allowed is False
    assert result.local_file_byte_hash_only_trading_allowed is False
    assert result.local_file_byte_hash_only_buy_review_allowed is False
    assert result.local_file_byte_hash_only_data_raw_written is False
    assert result.local_file_byte_hash_only_data_processed_written is False
    assert result.local_file_byte_hash_only_data_cache_written is False
    assert result.latest_local_file_byte_hash_only_recommended_next_task == (
        EXPECTED_LOCAL_FILE_BYTE_HASH_ONLY_NEXT_TASK
    )
    assert row["status"] == "LOCAL_FILE_BYTE_HASH_ONLY_REPORT_ONLY"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_LOCAL_FILE_BYTE_HASH_ONLY"
    )
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "local_file_byte_hash_only_context_visible"] == "True"
    assert summary.loc[0, "latest_local_file_byte_hash_only_hash_preview"] == preview
    assert summary.loc[0, "local_file_byte_hash_only_csv_header_read"] == "False"
    assert summary.loc[0, "local_file_byte_hash_only_real_csv_consumed"] == "False"
    assert metadata["local_file_byte_hash_only_context_visible"] is True
    assert metadata["latest_local_file_byte_hash_only_hash_preview"] == preview
    assert metadata["local_file_byte_hash_only_real_csv_consumed"] is False
    assert full_hash not in metadata_text
    assert full_hash not in report_text
    assert "SENTINEL_HASH_ONLY_DASHBOARD_ROW_VALUE_DO_NOT_READ" not in metadata_text
    assert "SENTINEL_HASH_ONLY_DASHBOARD_ROW_VALUE_DO_NOT_READ" not in report_text
    for stale in STALE_LOCAL_FILE_BYTE_HASH_ONLY_NEXT_ACTION_PHRASES:
        assert stale not in result.latest_local_file_byte_hash_only_recommended_next_task
    for unsafe in UNSAFE_LOCAL_FILE_BYTE_HASH_ONLY_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in result.latest_local_file_byte_hash_only_recommended_next_task
        assert unsafe not in row["notes"]


def test_research_status_preserves_paper_priority_over_local_file_byte_hash_only(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_root = tmp_path / "local_file_byte_hash_only_inputs"
    manifest_path, _, _ = _local_file_byte_hash_only_manifest(manifest_root)
    run_local_file_byte_hash_only(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_local_file_byte_hash_only_v0_1"
        ),
        run_id="003_hash",
        package_manifest_path=manifest_path,
        allowed_manifest_roots=[manifest_root],
        file_touch_level=LOCAL_FILE_BYTE_HASH_ONLY,
        csv_read_level=LOCAL_FILE_BYTE_HASH_ONLY_CSV_READ_NONE,
        local_file_hash_level=LOCAL_FILE_BYTE_HASH_SHA256_ONLY,
        allow_local_file_byte_hash_only=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.local_file_byte_hash_only_context_visible is True
    assert result.latest_local_file_byte_hash_only_hash_computed is True
    assert result.local_file_byte_hash_only_csv_header_read is False
    assert result.local_file_byte_hash_only_csv_row_count_computed is False
    assert result.local_file_byte_hash_only_csv_values_read is False
    assert result.local_file_byte_hash_only_csv_full_content_read is False
    assert result.local_file_byte_hash_only_real_csv_consumed is False
    assert result.local_file_byte_hash_only_active_replay_input is False
    assert result.local_file_byte_hash_only_active_replay_input_ready_emitted is False
    assert result.local_file_byte_hash_only_replay_execution_allowed is False
    assert result.local_file_byte_hash_only_buy_review_allowed is False
    assert result.local_file_byte_hash_only_trading_allowed is False


def test_research_status_includes_expected_hash_verification_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_expected_hash_verification_v0_1"
    )
    run_expected_hash_verification(output_root=output_root, run_id="004_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.expected_hash_verification_context_visible is True
    assert result.latest_expected_hash_verification_run_id == "004_no_input"
    assert result.latest_expected_hash_verification_runtime_status == "NO_EXPECTED_HASH_VERIFICATION_INPUT"
    assert result.latest_expected_hash_verification_health_status == "PASS"
    assert result.latest_expected_hash_verification_file_touch_level == "FILE_TOUCH_NONE"
    assert result.latest_expected_hash_verification_csv_read_level == "CSV_READ_NONE"
    assert result.latest_expected_hash_verification_local_file_hash_level == "LOCAL_FILE_HASH_NONE"
    assert result.latest_expected_hash_verification_level == "EXPECTED_HASH_VERIFICATION_NONE"
    assert result.latest_expected_hash_verification_performed is False
    assert result.latest_expected_hash_verification_matched is False
    assert result.latest_expected_hash_verification_mismatch is False
    assert result.latest_expected_hash_verification_actionable_mismatch is False
    assert result.expected_hash_verification_target_file_opened_for_expected_hash_verification is False
    assert result.expected_hash_verification_local_file_byte_hash_recomputed is False
    assert result.expected_hash_verification_csv_file_opened_structurally is False
    assert result.expected_hash_verification_csv_header_read is False
    assert result.expected_hash_verification_csv_row_count_computed is False
    assert result.expected_hash_verification_csv_values_read is False
    assert result.expected_hash_verification_csv_full_content_read is False
    assert result.expected_hash_verification_real_csv_consumed is False
    assert result.expected_hash_verification_source_hash_validated is False
    assert result.expected_hash_verification_revision_id_validated is False
    assert result.expected_hash_verification_available_time_validated is False
    assert result.expected_hash_verification_pit_admissibility_validated is False
    assert result.expected_hash_verification_reviewer_authority_validated is False
    assert result.expected_hash_verification_active_replay_input_ready_emitted is False
    assert result.expected_hash_verification_replay_execution_allowed is False
    assert result.expected_hash_verification_buy_review_allowed is False
    assert result.expected_hash_verification_trading_allowed is False
    assert result.expected_hash_verification_data_raw_written is False
    assert result.expected_hash_verification_data_processed_written is False
    assert result.expected_hash_verification_data_cache_written is False
    assert result.latest_expected_hash_verification_recommended_next_task == (
        EXPECTED_HASH_VERIFICATION_NEXT_TASK
    )


def test_research_status_includes_expected_hash_verification_matched_preview_only_fields(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "expected_hash_inputs"
    full_hash = "c" * 64
    preview = full_hash[:EXPECTED_HASH_VERIFICATION_PREVIEW_CHARS]
    manifest_path, metadata_path = _expected_hash_verification_inputs(
        input_root,
        expected_hash=full_hash,
        actual_hash=full_hash,
    )
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_expected_hash_verification_v0_1"
    )
    run_expected_hash_verification(
        output_root=output_root,
        run_id="004_matched",
        expected_hash_manifest_path=manifest_path,
        local_file_byte_hash_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        verification_level=EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY,
        allow_expected_hash_verification=True,
    )
    manifest_path.unlink()
    metadata_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_EXPECTED_HASH_VERIFICATION_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")
    summary_text = result.artifact_paths["local_research_summary"].read_text(encoding="utf-8")

    assert result.expected_hash_verification_context_visible is True
    assert result.latest_expected_hash_verification_run_id == "004_matched"
    assert result.latest_expected_hash_verification_runtime_status == (
        "EXPECTED_HASH_VERIFICATION_MATCHED_REPORT_ONLY"
    )
    assert result.latest_expected_hash_verification_health_status == "PASS"
    assert result.latest_expected_hash_verification_workflow_stage == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_"
        "EXPECTED_HASH_VERIFICATION_CORE_CREATED_REPORT_ONLY"
    )
    assert result.latest_expected_hash_verification_file_touch_level == "FILE_TOUCH_NONE"
    assert result.latest_expected_hash_verification_csv_read_level == "CSV_READ_NONE"
    assert result.latest_expected_hash_verification_local_file_hash_level == (
        LOCAL_FILE_HASH_SHA256_METADATA_REFERENCE_ONLY
    )
    assert result.latest_expected_hash_verification_level == EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY
    assert result.latest_expected_hash_verification_performed is True
    assert result.latest_expected_hash_verification_algorithm == "SHA-256"
    assert result.latest_expected_hash_verification_expected_hash_present is True
    assert result.latest_expected_hash_verification_expected_hash_preview == preview
    assert result.latest_expected_hash_verification_actual_hash_preview == preview
    assert result.latest_expected_hash_verification_matched is True
    assert result.latest_expected_hash_verification_mismatch is False
    assert result.latest_expected_hash_verification_actionable_mismatch is False
    assert result.latest_expected_hash_verification_issue_count == 0
    assert result.latest_expected_hash_verification_warning_count == 0
    assert result.expected_hash_verification_expected_hash_verified_against_source_hash is False
    assert result.expected_hash_verification_source_hash_validated is False
    assert result.expected_hash_verification_revision_id_validated is False
    assert result.expected_hash_verification_available_time_validated is False
    assert result.expected_hash_verification_pit_admissibility_validated is False
    assert result.expected_hash_verification_source_reliability_scored is False
    assert result.expected_hash_verification_reviewer_authority_validated is False
    assert result.expected_hash_verification_local_file_byte_hash_recomputed is False
    assert result.expected_hash_verification_target_file_opened_for_expected_hash_verification is False
    assert result.expected_hash_verification_csv_file_opened_structurally is False
    assert result.expected_hash_verification_csv_header_read is False
    assert result.expected_hash_verification_csv_row_count_computed is False
    assert result.expected_hash_verification_csv_values_read is False
    assert result.expected_hash_verification_csv_full_content_read is False
    assert result.expected_hash_verification_real_csv_consumed is False
    assert result.expected_hash_verification_real_reviewed_csv_package_created is False
    assert result.expected_hash_verification_real_package_candidate_created is False
    assert result.expected_hash_verification_active_reviewed_input_candidate_created is False
    assert result.expected_hash_verification_real_replay_input_created is False
    assert result.expected_hash_verification_active_replay_input is False
    assert result.expected_hash_verification_active_replay_ready is False
    assert result.expected_hash_verification_active_replay_input_ready_emitted is False
    assert result.expected_hash_verification_replay_execution_allowed is False
    assert result.expected_hash_verification_trading_allowed is False
    assert result.expected_hash_verification_buy_review_allowed is False
    assert result.expected_hash_verification_data_raw_written is False
    assert result.expected_hash_verification_data_processed_written is False
    assert result.expected_hash_verification_data_cache_written is False
    assert result.latest_expected_hash_verification_recommended_next_task == (
        EXPECTED_HASH_VERIFICATION_NEXT_TASK
    )
    assert row["status"] == "EXPECTED_HASH_VERIFICATION_MATCHED_REPORT_ONLY"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_EXPECTED_HASH_VERIFICATION"
    )
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "expected_hash_verification_context_visible"] == "True"
    assert summary.loc[0, "latest_expected_hash_verification_expected_hash_preview"] == preview
    assert summary.loc[0, "latest_expected_hash_verification_actual_hash_preview"] == preview
    assert summary.loc[0, "expected_hash_verification_csv_header_read"] == "False"
    assert summary.loc[0, "expected_hash_verification_real_csv_consumed"] == "False"
    assert metadata["expected_hash_verification_context_visible"] is True
    assert metadata["latest_expected_hash_verification_expected_hash_preview"] == preview
    assert metadata["expected_hash_verification_real_csv_consumed"] is False
    assert full_hash not in metadata_text
    assert full_hash not in report_text
    assert full_hash not in summary_text
    for stale in STALE_EXPECTED_HASH_VERIFICATION_NEXT_ACTION_PHRASES:
        assert stale not in result.latest_expected_hash_verification_recommended_next_task
    for unsafe in UNSAFE_EXPECTED_HASH_VERIFICATION_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in result.latest_expected_hash_verification_recommended_next_task
        assert unsafe not in row["notes"]


def test_research_status_includes_expected_hash_verification_mismatch_warn_actionable_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "expected_hash_inputs"
    expected_hash = "a" * 64
    actual_hash = "b" * 64
    manifest_path, metadata_path = _expected_hash_verification_inputs(
        input_root,
        expected_hash=expected_hash,
        actual_hash=actual_hash,
    )
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_expected_hash_verification_v0_1"
    )
    run_expected_hash_verification(
        output_root=output_root,
        run_id="004_mismatch",
        expected_hash_manifest_path=manifest_path,
        local_file_byte_hash_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        verification_level=EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY,
        allow_expected_hash_verification=True,
    )
    manifest_path.unlink()
    metadata_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_EXPECTED_HASH_VERIFICATION_STATUS"
    ].iloc[0]
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)

    assert result.expected_hash_verification_context_visible is True
    assert result.latest_expected_hash_verification_runtime_status == (
        "EXPECTED_HASH_VERIFICATION_MISMATCHED_REPORT_ONLY"
    )
    assert result.latest_expected_hash_verification_health_status == "WARN"
    assert result.latest_expected_hash_verification_expected_hash_preview == (
        expected_hash[:EXPECTED_HASH_VERIFICATION_PREVIEW_CHARS]
    )
    assert result.latest_expected_hash_verification_actual_hash_preview == (
        actual_hash[:EXPECTED_HASH_VERIFICATION_PREVIEW_CHARS]
    )
    assert result.latest_expected_hash_verification_matched is False
    assert result.latest_expected_hash_verification_mismatch is True
    assert result.latest_expected_hash_verification_actionable_mismatch is True
    assert result.latest_expected_hash_verification_issue_count == 1
    assert result.latest_expected_hash_verification_warning_count == 1
    assert row["status"] == "EXPECTED_HASH_VERIFICATION_MISMATCHED_REPORT_ONLY"
    assert row["warning_count"] == 1
    assert row["blocking_error_count"] == 0
    assert result.expected_hash_verification_pit_admissibility_validated is False
    assert result.expected_hash_verification_source_hash_validated is False
    assert result.expected_hash_verification_reviewer_authority_validated is False
    assert result.expected_hash_verification_replay_execution_allowed is False
    assert result.expected_hash_verification_buy_review_allowed is False
    assert result.expected_hash_verification_trading_allowed is False
    assert expected_hash not in metadata_text
    assert actual_hash not in metadata_text
    for unsafe in UNSAFE_EXPECTED_HASH_VERIFICATION_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in row["notes"]


def test_research_status_preserves_paper_priority_over_expected_hash_verification(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "expected_hash_inputs"
    manifest_path, metadata_path = _expected_hash_verification_inputs(
        input_root,
        expected_hash="a" * 64,
        actual_hash="b" * 64,
    )
    run_expected_hash_verification(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_expected_hash_verification_v0_1"
        ),
        run_id="004_mismatch",
        expected_hash_manifest_path=manifest_path,
        local_file_byte_hash_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        verification_level=EXPECTED_HASH_SHA256_AGAINST_LOCAL_METADATA_ONLY,
        allow_expected_hash_verification=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.expected_hash_verification_context_visible is True
    assert result.latest_expected_hash_verification_health_status == "WARN"
    assert result.latest_expected_hash_verification_actionable_mismatch is True
    assert result.expected_hash_verification_active_replay_input is False
    assert result.expected_hash_verification_active_replay_input_ready_emitted is False
    assert result.expected_hash_verification_replay_execution_allowed is False
    assert result.expected_hash_verification_buy_review_allowed is False
    assert result.expected_hash_verification_trading_allowed is False


def test_research_status_includes_csv_physical_data_line_count_only_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only_v0_1"
    )
    run_csv_physical_data_line_count_only(output_root=output_root, run_id="005_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.csv_physical_data_line_count_only_context_visible is True
    assert result.latest_csv_physical_data_line_count_only_run_id == "005_no_input"
    assert result.latest_csv_physical_data_line_count_only_runtime_status == (
        "NO_CSV_PHYSICAL_DATA_LINE_COUNT_INPUT"
    )
    assert result.latest_csv_physical_data_line_count_only_health_status == "PASS"
    assert result.latest_csv_physical_data_line_count_only_file_touch_level == "FILE_TOUCH_NONE"
    assert result.latest_csv_physical_data_line_count_only_csv_read_level == "CSV_READ_NONE"
    assert result.latest_csv_physical_data_line_count_only_local_file_hash_level == "LOCAL_FILE_HASH_NONE"
    assert result.latest_csv_physical_data_line_count_only_expected_hash_verification_level == (
        "EXPECTED_HASH_VERIFICATION_NONE"
    )
    assert result.latest_csv_physical_data_line_count_only_level == "CSV_PHYSICAL_DATA_LINE_COUNT_NONE"
    assert result.latest_csv_physical_data_line_count_only_computed is False
    assert result.latest_csv_physical_data_line_count_only_count == ""
    assert result.csv_physical_data_line_count_only_target_csv_opened_for_physical_data_line_count is False
    assert result.csv_physical_data_line_count_only_csv_header_read is False
    assert result.csv_physical_data_line_count_only_csv_values_read is False
    assert result.csv_physical_data_line_count_only_csv_full_content_read is False
    assert result.csv_physical_data_line_count_only_real_csv_consumed is False
    assert result.csv_physical_data_line_count_only_local_file_byte_hash_computed is False
    assert result.csv_physical_data_line_count_only_expected_hash_verification_performed is False
    assert result.csv_physical_data_line_count_only_source_hash_validated is False
    assert result.csv_physical_data_line_count_only_revision_id_validated is False
    assert result.csv_physical_data_line_count_only_available_time_validated is False
    assert result.csv_physical_data_line_count_only_pit_admissibility_validated is False
    assert result.csv_physical_data_line_count_only_reviewer_authority_validated is False
    assert result.csv_physical_data_line_count_only_active_replay_input is False
    assert result.csv_physical_data_line_count_only_replay_execution_allowed is False
    assert result.csv_physical_data_line_count_only_buy_review_allowed is False
    assert result.csv_physical_data_line_count_only_trading_allowed is False
    assert result.csv_physical_data_line_count_only_data_raw_written is False
    assert result.csv_physical_data_line_count_only_data_processed_written is False
    assert result.csv_physical_data_line_count_only_data_cache_written is False


def test_research_status_includes_csv_physical_data_line_count_only_count_context_without_reopen(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "csv_physical_data_line_count_only_inputs"
    manifest_path, header_metadata_path, csv_path = _csv_physical_data_line_count_only_inputs(
        input_root,
        data_lines=[
            "000001,2024-04-02,SENTINEL_PHYSICAL_COUNT_ROW_VALUE_DO_NOT_READ",
            "000002,2024-04-03,SENTINEL_PHYSICAL_COUNT_FULL_CONTENT_DO_NOT_READ",
        ],
    )
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only_v0_1"
    )
    run_csv_physical_data_line_count_only(
        output_root=output_root,
        run_id="005_count",
        package_manifest_path=manifest_path,
        header_metadata_path=header_metadata_path,
        allowed_manifest_roots=[input_root],
        file_touch_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_read_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_physical_data_line_count_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        allow_csv_physical_data_line_count_only=True,
    )
    csv_path.unlink()
    header_metadata_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")

    assert result.csv_physical_data_line_count_only_context_visible is True
    assert result.latest_csv_physical_data_line_count_only_run_id == "005_count"
    assert result.latest_csv_physical_data_line_count_only_runtime_status == (
        "CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_REPORT_ONLY"
    )
    assert result.latest_csv_physical_data_line_count_only_health_status == "PASS"
    assert result.latest_csv_physical_data_line_count_only_level == CSV_PHYSICAL_DATA_LINE_COUNT_ONLY
    assert result.latest_csv_physical_data_line_count_only_computed is True
    assert result.latest_csv_physical_data_line_count_only_count == 2
    assert result.latest_csv_physical_data_line_count_only_policy == PHYSICAL_NON_HEADER_LINE_COUNT
    assert result.latest_csv_physical_data_line_count_only_total_physical_line_count == 3
    assert result.latest_csv_physical_data_line_count_only_header_dependency_policy == (
        "HEADER_METADATA_REUSED_NO_HEADER_VALUE_READ"
    )
    assert result.latest_csv_physical_data_line_count_only_header_metadata_reused is True
    assert result.latest_csv_physical_data_line_count_only_header_line_skipped_by_policy is True
    assert result.latest_csv_physical_data_line_count_only_zero_data_line_warning is False
    assert result.latest_csv_physical_data_line_count_only_issue_count == 0
    assert result.latest_csv_physical_data_line_count_only_warning_count == 0
    assert (
        result.csv_physical_data_line_count_only_target_csv_opened_for_physical_data_line_count
        is True
    )
    assert result.csv_physical_data_line_count_only_csv_header_read is False
    assert result.csv_physical_data_line_count_only_csv_header_values_recorded is False
    assert result.csv_physical_data_line_count_only_csv_values_read is False
    assert result.csv_physical_data_line_count_only_csv_value_fields_parsed is False
    assert result.csv_physical_data_line_count_only_csv_row_values_stored is False
    assert result.csv_physical_data_line_count_only_csv_full_content_read is False
    assert result.csv_physical_data_line_count_only_csv_full_content_semantically_read is False
    assert result.csv_physical_data_line_count_only_real_csv_consumed is False
    assert result.csv_physical_data_line_count_only_local_file_byte_hash_computed is False
    assert result.csv_physical_data_line_count_only_local_file_byte_hash_recomputed is False
    assert result.csv_physical_data_line_count_only_expected_hash_verification_performed is False
    assert result.csv_physical_data_line_count_only_expected_hash_verified_against_local_metadata is False
    assert result.csv_physical_data_line_count_only_expected_hash_verified_against_source_hash is False
    assert result.csv_physical_data_line_count_only_source_hash_validated is False
    assert result.csv_physical_data_line_count_only_revision_id_validated is False
    assert result.csv_physical_data_line_count_only_available_time_validated is False
    assert result.csv_physical_data_line_count_only_pit_admissibility_validated is False
    assert result.csv_physical_data_line_count_only_source_reliability_scored is False
    assert result.csv_physical_data_line_count_only_reviewer_authority_validated is False
    assert result.csv_physical_data_line_count_only_real_reviewed_csv_package_created is False
    assert result.csv_physical_data_line_count_only_real_package_candidate_created is False
    assert result.csv_physical_data_line_count_only_active_reviewed_input_candidate_created is False
    assert result.csv_physical_data_line_count_only_real_replay_input_created is False
    assert result.csv_physical_data_line_count_only_active_replay_input is False
    assert result.csv_physical_data_line_count_only_active_replay_ready is False
    assert result.csv_physical_data_line_count_only_active_replay_input_ready_emitted is False
    assert result.csv_physical_data_line_count_only_replay_execution_allowed is False
    assert result.csv_physical_data_line_count_only_trading_allowed is False
    assert result.csv_physical_data_line_count_only_buy_review_allowed is False
    assert result.csv_physical_data_line_count_only_data_raw_written is False
    assert result.csv_physical_data_line_count_only_data_processed_written is False
    assert result.csv_physical_data_line_count_only_data_cache_written is False
    assert result.latest_csv_physical_data_line_count_only_recommended_next_task == (
        EXPECTED_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_NEXT_TASK
    )
    assert row["status"] == "CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_REPORT_ONLY"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY"
    )
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "csv_physical_data_line_count_only_context_visible"] == "True"
    assert summary.loc[0, "latest_csv_physical_data_line_count_only_count"] == "2"
    assert (
        summary.loc[
            0,
            "csv_physical_data_line_count_only_target_csv_opened_for_physical_data_line_count",
        ]
        == "True"
    )
    assert summary.loc[0, "csv_physical_data_line_count_only_real_csv_consumed"] == "False"
    assert metadata["csv_physical_data_line_count_only_context_visible"] is True
    assert metadata["latest_csv_physical_data_line_count_only_count"] == 2
    assert metadata["csv_physical_data_line_count_only_real_csv_consumed"] is False
    for sentinel in [
        "SENTINEL_PHYSICAL_COUNT_ROW_VALUE_DO_NOT_READ",
        "SENTINEL_PHYSICAL_COUNT_FULL_CONTENT_DO_NOT_READ",
    ]:
        assert sentinel not in metadata_text
        assert sentinel not in report_text
    for unsafe in UNSAFE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in result.latest_csv_physical_data_line_count_only_recommended_next_task
        assert unsafe not in row["notes"]


def test_research_status_includes_csv_physical_data_line_count_only_zero_line_warn(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "csv_physical_data_line_count_only_zero_inputs"
    manifest_path, header_metadata_path, _ = _csv_physical_data_line_count_only_inputs(
        input_root,
        data_lines=[],
    )
    run_csv_physical_data_line_count_only(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only_v0_1"
        ),
        run_id="005_zero",
        package_manifest_path=manifest_path,
        header_metadata_path=header_metadata_path,
        allowed_manifest_roots=[input_root],
        file_touch_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_read_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_physical_data_line_count_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        allow_csv_physical_data_line_count_only=True,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_STATUS"
    ].iloc[0]

    assert result.csv_physical_data_line_count_only_context_visible is True
    assert result.latest_csv_physical_data_line_count_only_runtime_status == (
        "CSV_PHYSICAL_DATA_LINE_COUNT_WARN_ZERO_DATA_LINES"
    )
    assert result.latest_csv_physical_data_line_count_only_health_status == "WARN"
    assert result.latest_csv_physical_data_line_count_only_count == 0
    assert result.latest_csv_physical_data_line_count_only_zero_data_line_warning is True
    assert result.latest_csv_physical_data_line_count_only_warning_count == 1
    assert row["blocking_error_count"] == 0
    assert row["warning_count"] >= 1
    assert result.csv_physical_data_line_count_only_real_package_candidate_created is False
    assert result.csv_physical_data_line_count_only_active_replay_input is False
    assert result.csv_physical_data_line_count_only_buy_review_allowed is False
    assert result.csv_physical_data_line_count_only_trading_allowed is False


def test_research_status_preserves_paper_priority_over_csv_physical_data_line_count_only(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "csv_physical_data_line_count_only_priority_inputs"
    manifest_path, header_metadata_path, _ = _csv_physical_data_line_count_only_inputs(
        input_root,
        data_lines=["000001,2024-04-02,SENTINEL_PRIORITY_ROW_VALUE_DO_NOT_READ"],
    )
    run_csv_physical_data_line_count_only(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only_v0_1"
        ),
        run_id="005_count",
        package_manifest_path=manifest_path,
        header_metadata_path=header_metadata_path,
        allowed_manifest_roots=[input_root],
        file_touch_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_read_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_physical_data_line_count_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        allow_csv_physical_data_line_count_only=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.csv_physical_data_line_count_only_context_visible is True
    assert result.latest_csv_physical_data_line_count_only_health_status == "PASS"
    assert result.csv_physical_data_line_count_only_real_csv_consumed is False
    assert result.csv_physical_data_line_count_only_active_replay_input_ready_emitted is False
    assert result.csv_physical_data_line_count_only_replay_execution_allowed is False
    assert result.csv_physical_data_line_count_only_buy_review_allowed is False
    assert result.csv_physical_data_line_count_only_trading_allowed is False


def test_research_status_surfaces_csv_physical_data_line_count_only_health_fail(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "csv_physical_data_line_count_only_fail_inputs"
    manifest_path, header_metadata_path, _ = _csv_physical_data_line_count_only_inputs(
        input_root,
        data_lines=["000001,2024-04-02,SENTINEL_FAIL_ROW_VALUE_DO_NOT_READ"],
    )
    artifact = run_csv_physical_data_line_count_only(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_csv_physical_data_line_count_only_v0_1"
        ),
        run_id="005_unsafe",
        package_manifest_path=manifest_path,
        header_metadata_path=header_metadata_path,
        allowed_manifest_roots=[input_root],
        file_touch_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_read_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        csv_physical_data_line_count_level=CSV_PHYSICAL_DATA_LINE_COUNT_ONLY,
        allow_csv_physical_data_line_count_only=True,
    )
    metadata_path = Path(artifact["artifact_paths"]["metadata"])
    metadata = json.loads(metadata_path.read_text(encoding="utf-8"))
    metadata["csv_physical_data_line_count_policy"] = "CSV_RECORD_COUNT_ONLY"
    metadata_path.write_text(json.dumps(metadata, indent=2, sort_keys=True) + "\n", encoding="utf-8")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_CSV_PHYSICAL_DATA_LINE_COUNT_ONLY_STATUS"
    ].iloc[0]

    assert result.csv_physical_data_line_count_only_context_visible is True
    assert result.latest_csv_physical_data_line_count_only_health_status == "FAIL"
    assert row["blocking_error_count"] == 1
    assert result.csv_physical_data_line_count_only_real_csv_consumed is False
    assert result.csv_physical_data_line_count_only_active_replay_input is False
    assert result.csv_physical_data_line_count_only_buy_review_allowed is False
    assert result.csv_physical_data_line_count_only_trading_allowed is False


def test_research_status_includes_source_revision_time_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time_v0_1"
    )
    run_source_hash_revision_available_time(output_root=output_root, run_id="006_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.source_revision_time_context_visible is True
    assert result.latest_source_revision_time_run_id == "006_no_input"
    assert result.latest_source_revision_time_runtime_status == "NO_SOURCE_REVISION_TIME_INPUT"
    assert result.latest_source_revision_time_health_status == "PASS"
    assert result.latest_source_revision_time_source_hash_validation_level == "SOURCE_HASH_VALIDATION_NONE"
    assert result.latest_source_revision_time_revision_id_validation_level == "REVISION_ID_VALIDATION_NONE"
    assert result.latest_source_revision_time_available_time_validation_level == "AVAILABLE_TIME_VALIDATION_NONE"
    assert result.latest_source_revision_time_pit_admissibility_level == "PIT_ADMISSIBILITY_NONE"
    assert result.latest_source_revision_time_source_hash_metadata_present is False
    assert result.latest_source_revision_time_revision_id_metadata_present is False
    assert result.latest_source_revision_time_available_time_metadata_present is False
    assert result.source_revision_time_source_artifact_opened is False
    assert result.source_revision_time_source_content_read is False
    assert result.source_revision_time_target_csv_opened is False
    assert result.source_revision_time_source_hash_recomputed is False
    assert result.source_revision_time_expected_hash_reverified is False
    assert result.source_revision_time_available_time_validated is False
    assert result.source_revision_time_pit_admissibility_validated is False
    assert result.source_revision_time_active_replay_input is False
    assert result.source_revision_time_replay_execution_allowed is False
    assert result.source_revision_time_buy_review_allowed is False
    assert result.source_revision_time_trading_allowed is False
    assert result.latest_source_revision_time_recommended_next_task == EXPECTED_SOURCE_REVISION_TIME_NEXT_TASK


def test_research_status_includes_source_revision_time_metadata_present_preview_only(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "source_revision_time_inputs"
    full_source_hash = "abcdef0123456789" * 4
    manifest_path, metadata_path = _source_revision_time_inputs(
        input_root,
        source_hash=full_source_hash,
    )
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time_v0_1"
    )
    run_source_hash_revision_available_time(
        output_root=output_root,
        run_id="006_metadata",
        source_lineage_manifest_path=manifest_path,
        source_lineage_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        source_hash_validation_level=SOURCE_HASH_METADATA_PRESENT_ONLY,
        revision_id_validation_level=REVISION_ID_METADATA_PRESENT_ONLY,
        available_time_validation_level=AVAILABLE_TIME_METADATA_PRESENT_ONLY,
        pit_admissibility_level=PIT_ADMISSIBILITY_NONE,
        allow_source_revision_time_metadata=True,
    )
    manifest_path.unlink()
    metadata_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SOURCE_HASH_REVISION_AVAILABLE_TIME_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")
    preview = full_source_hash[:16]

    assert result.source_revision_time_context_visible is True
    assert result.latest_source_revision_time_run_id == "006_metadata"
    assert result.latest_source_revision_time_runtime_status == (
        "SOURCE_REVISION_TIME_METADATA_PRESENT_REPORT_ONLY"
    )
    assert result.latest_source_revision_time_health_status == "PASS"
    assert result.latest_source_revision_time_source_hash_validation_level == (
        SOURCE_HASH_METADATA_PRESENT_ONLY
    )
    assert result.latest_source_revision_time_revision_id_validation_level == (
        REVISION_ID_METADATA_PRESENT_ONLY
    )
    assert result.latest_source_revision_time_available_time_validation_level == (
        AVAILABLE_TIME_METADATA_PRESENT_ONLY
    )
    assert result.latest_source_revision_time_pit_admissibility_level == PIT_ADMISSIBILITY_NONE
    assert result.latest_source_revision_time_source_hash_metadata_present is True
    assert result.latest_source_revision_time_source_hash_format_checked is True
    assert result.latest_source_revision_time_source_hash_algorithm_supported is True
    assert result.latest_source_revision_time_source_hash_algorithm == "SHA-256"
    assert result.latest_source_revision_time_source_hash_preview == preview
    assert result.latest_source_revision_time_revision_id_metadata_present is True
    assert result.latest_source_revision_time_revision_id_type == "provider_revision_id"
    assert result.latest_source_revision_time_revision_id_type_supported is True
    assert result.latest_source_revision_time_revision_id_value_recorded is True
    assert result.latest_source_revision_time_revision_consistency_checked is False
    assert result.latest_source_revision_time_available_time_metadata_present is True
    assert result.latest_source_revision_time_available_time_parseable is True
    assert result.latest_source_revision_time_available_time_timezone_present is True
    assert result.latest_source_revision_time_available_time_timezone_policy == "Asia/Shanghai"
    assert result.latest_source_revision_time_available_time_compared_to_decision_time is False
    assert result.latest_source_revision_time_issue_count == 0
    assert result.latest_source_revision_time_warning_count == 0
    assert result.source_revision_time_source_hash_recomputed is False
    assert result.source_revision_time_source_artifact_opened is False
    assert result.source_revision_time_source_content_read is False
    assert result.source_revision_time_local_file_hash_recomputed is False
    assert result.source_revision_time_expected_hash_reverified is False
    assert result.source_revision_time_target_csv_opened is False
    assert result.source_revision_time_real_csv_consumed is False
    assert result.source_revision_time_source_hash_validated is False
    assert result.source_revision_time_revision_id_validated is False
    assert result.source_revision_time_available_time_validated is False
    assert result.source_revision_time_pit_admissibility_validated is False
    assert result.source_revision_time_source_reliability_scored is False
    assert result.source_revision_time_reviewer_authority_validated is False
    assert result.source_revision_time_real_reviewed_csv_package_created is False
    assert result.source_revision_time_real_package_candidate_created is False
    assert result.source_revision_time_active_reviewed_input_candidate_created is False
    assert result.source_revision_time_real_replay_input_created is False
    assert result.source_revision_time_active_replay_input is False
    assert result.source_revision_time_active_replay_ready is False
    assert result.source_revision_time_active_replay_input_ready_emitted is False
    assert result.source_revision_time_replay_execution_allowed is False
    assert result.source_revision_time_trading_allowed is False
    assert result.source_revision_time_buy_review_allowed is False
    assert result.source_revision_time_data_raw_written is False
    assert result.source_revision_time_data_processed_written is False
    assert result.source_revision_time_data_cache_written is False
    assert result.latest_source_revision_time_recommended_next_task == EXPECTED_SOURCE_REVISION_TIME_NEXT_TASK
    assert row["status"] == "SOURCE_REVISION_TIME_METADATA_PRESENT_REPORT_ONLY"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "source_revision_time_context_visible"] == "True"
    assert summary.loc[0, "latest_source_revision_time_source_hash_preview"] == preview
    assert summary.loc[0, "source_revision_time_source_hash_validated"] == "False"
    assert summary.loc[0, "source_revision_time_target_csv_opened"] == "False"
    assert metadata["source_revision_time_context_visible"] is True
    assert metadata["latest_source_revision_time_source_hash_preview"] == preview
    assert metadata["source_revision_time_target_csv_opened"] is False
    assert full_source_hash not in metadata_text
    assert full_source_hash not in report_text
    assert str(input_root) not in metadata_text
    assert str(input_root) not in report_text
    for stale in STALE_SOURCE_REVISION_TIME_NEXT_ACTION_PHRASES:
        assert stale not in result.latest_source_revision_time_recommended_next_task
    for unsafe in UNSAFE_SOURCE_REVISION_TIME_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in result.latest_source_revision_time_recommended_next_task
        assert unsafe not in row["notes"]


def test_research_status_surfaces_source_revision_time_timezone_warn_without_pit_failure(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "source_revision_time_timezone_inputs"
    manifest_path, metadata_path = _source_revision_time_inputs(
        input_root,
        available_time="2024-04-02T09:15:00",
        available_time_timezone="",
    )
    run_source_hash_revision_available_time(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time_v0_1"
        ),
        run_id="006_timezone_warn",
        source_lineage_manifest_path=manifest_path,
        source_lineage_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        source_hash_validation_level=SOURCE_HASH_METADATA_PRESENT_ONLY,
        revision_id_validation_level=REVISION_ID_METADATA_PRESENT_ONLY,
        available_time_validation_level=AVAILABLE_TIME_METADATA_PRESENT_ONLY,
        pit_admissibility_level=PIT_ADMISSIBILITY_NONE,
        allow_source_revision_time_metadata=True,
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SOURCE_HASH_REVISION_AVAILABLE_TIME_STATUS"
    ].iloc[0]

    assert result.source_revision_time_context_visible is True
    assert result.latest_source_revision_time_runtime_status == (
        "SOURCE_REVISION_TIME_WARN_TIMEZONE_ASSUMPTION_REQUIRED"
    )
    assert result.latest_source_revision_time_health_status == "WARN"
    assert result.latest_source_revision_time_available_time_parseable is True
    assert result.latest_source_revision_time_available_time_timezone_present is False
    assert result.latest_source_revision_time_available_time_compared_to_decision_time is False
    assert result.latest_source_revision_time_warning_count == 1
    assert row["warning_count"] >= 1
    assert row["blocking_error_count"] == 0
    assert result.source_revision_time_available_time_validated is False
    assert result.source_revision_time_pit_admissibility_validated is False
    assert result.source_revision_time_real_package_candidate_created is False
    assert result.source_revision_time_active_replay_input is False
    assert result.source_revision_time_buy_review_allowed is False
    assert result.source_revision_time_trading_allowed is False


def test_research_status_preserves_paper_priority_over_source_revision_time_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "source_revision_time_priority_inputs"
    manifest_path, metadata_path = _source_revision_time_inputs(input_root)
    run_source_hash_revision_available_time(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time_v0_1"
        ),
        run_id="006_priority",
        source_lineage_manifest_path=manifest_path,
        source_lineage_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        source_hash_validation_level=SOURCE_HASH_METADATA_PRESENT_ONLY,
        revision_id_validation_level=REVISION_ID_METADATA_PRESENT_ONLY,
        available_time_validation_level=AVAILABLE_TIME_METADATA_PRESENT_ONLY,
        pit_admissibility_level=PIT_ADMISSIBILITY_NONE,
        allow_source_revision_time_metadata=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.source_revision_time_context_visible is True
    assert result.latest_source_revision_time_health_status == "PASS"
    assert result.source_revision_time_real_csv_consumed is False
    assert result.source_revision_time_active_replay_input_ready_emitted is False
    assert result.source_revision_time_replay_execution_allowed is False
    assert result.source_revision_time_buy_review_allowed is False
    assert result.source_revision_time_trading_allowed is False


def test_research_status_surfaces_source_revision_time_health_fail_safely(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "source_revision_time_fail_inputs"
    full_source_hash = "fedcba9876543210" * 4
    manifest_path, metadata_path = _source_revision_time_inputs(
        input_root,
        source_hash=full_source_hash,
    )
    artifact = run_source_hash_revision_available_time(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_source_hash_revision_available_time_v0_1"
        ),
        run_id="006_unsafe",
        source_lineage_manifest_path=manifest_path,
        source_lineage_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        source_hash_validation_level=SOURCE_HASH_METADATA_PRESENT_ONLY,
        revision_id_validation_level=REVISION_ID_METADATA_PRESENT_ONLY,
        available_time_validation_level=AVAILABLE_TIME_METADATA_PRESENT_ONLY,
        pit_admissibility_level=PIT_ADMISSIBILITY_NONE,
        allow_source_revision_time_metadata=True,
    )
    metadata_artifact_path = Path(artifact["artifact_paths"]["metadata"])
    artifact_metadata = json.loads(metadata_artifact_path.read_text(encoding="utf-8"))
    artifact_metadata["source_hash_preview"] = full_source_hash
    metadata_artifact_path.write_text(
        json.dumps(artifact_metadata, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SOURCE_HASH_REVISION_AVAILABLE_TIME_STATUS"
    ].iloc[0]
    metadata_text = result.artifact_paths["metadata"].read_text(encoding="utf-8")

    assert result.source_revision_time_context_visible is True
    assert result.latest_source_revision_time_health_status == "FAIL"
    assert row["blocking_error_count"] == 1
    assert full_source_hash not in metadata_text
    assert result.source_revision_time_source_artifact_opened is False
    assert result.source_revision_time_source_content_read is False
    assert result.source_revision_time_target_csv_opened is False
    assert result.source_revision_time_real_csv_consumed is False
    assert result.source_revision_time_active_replay_input is False
    assert result.source_revision_time_buy_review_allowed is False
    assert result.source_revision_time_trading_allowed is False


def test_research_status_surfaces_reviewer_quality_limitation_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = _reviewer_quality_limitation_output_root(root)
    run_reviewer_authority_quality_limitation(output_root=output_root, run_id="007_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_REVIEWER_AUTHORITY_QUALITY_LIMITATION_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")

    assert result.reviewer_quality_limitation_context_visible is True
    assert result.latest_reviewer_quality_limitation_run_id == "007_no_input"
    assert result.latest_reviewer_quality_limitation_runtime_status == "NO_REVIEWER_QUALITY_LIMITATION_INPUT"
    assert result.latest_reviewer_quality_limitation_health_status == "PASS"
    assert result.latest_reviewer_quality_limitation_reviewer_metadata_present is False
    assert result.latest_reviewer_quality_limitation_quality_status_present is False
    assert result.latest_reviewer_quality_limitation_limitations_present is False
    assert result.reviewer_quality_limitation_reviewer_authority_validated is False
    assert result.reviewer_quality_limitation_quality_status_validated is False
    assert result.reviewer_quality_limitation_permission_class_validated is False
    assert result.reviewer_quality_limitation_source_hash_validated is False
    assert result.reviewer_quality_limitation_revision_id_validated is False
    assert result.reviewer_quality_limitation_available_time_validated is False
    assert result.reviewer_quality_limitation_pit_admissibility_validated is False
    assert result.reviewer_quality_limitation_active_replay_input is False
    assert result.reviewer_quality_limitation_buy_review_allowed is False
    assert result.reviewer_quality_limitation_trading_allowed is False
    assert result.latest_reviewer_quality_limitation_recommended_next_task == (
        EXPECTED_REVIEWER_QUALITY_LIMITATION_NEXT_TASK
    )
    assert row["status"] == "NO_REVIEWER_QUALITY_LIMITATION_INPUT"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "reviewer_quality_limitation_context_visible"] == "True"
    assert summary.loc[0, "reviewer_quality_limitation_active_replay_input"] == "False"


def test_research_status_surfaces_reviewer_quality_limitation_metadata_without_reopening_inputs(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    input_root = tmp_path / "reviewer_quality_inputs"
    full_reviewer_id = "private-reviewer-identity-000001"
    manifest_path, metadata_path = _reviewer_quality_limitation_inputs(
        input_root,
        reviewer_id_preview=full_reviewer_id[:12],
    )
    run_reviewer_authority_quality_limitation(
        output_root=_reviewer_quality_limitation_output_root(root),
        run_id="007_metadata",
        reviewer_quality_manifest_path=manifest_path,
        reviewer_quality_metadata_path=metadata_path,
        allowed_manifest_roots=[input_root],
        reviewer_authority_level=REVIEWER_METADATA_PRESENT_ONLY,
        quality_status_level=QUALITY_METADATA_PRESENT_ONLY,
        limitation_review_level=LIMITATION_METADATA_PRESENT_ONLY,
        permission_review_level=PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        package_promotion_level=PACKAGE_PROMOTION_NONE,
        allow_reviewer_quality_limitation_metadata=True,
    )
    manifest_path.unlink()
    metadata_path.unlink()

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_REVIEWER_AUTHORITY_QUALITY_LIMITATION_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")

    assert result.reviewer_quality_limitation_context_visible is True
    assert result.latest_reviewer_quality_limitation_run_id == "007_metadata"
    assert result.latest_reviewer_quality_limitation_runtime_status == (
        "REVIEWER_QUALITY_LIMITATION_METADATA_PRESENT_REPORT_ONLY"
    )
    assert result.latest_reviewer_quality_limitation_health_status == "PASS"
    assert result.latest_reviewer_quality_limitation_reviewer_authority_level == (
        REVIEWER_METADATA_PRESENT_ONLY
    )
    assert result.latest_reviewer_quality_limitation_quality_status_level == (
        QUALITY_METADATA_PRESENT_ONLY
    )
    assert result.latest_reviewer_quality_limitation_limitation_review_level == (
        LIMITATION_METADATA_PRESENT_ONLY
    )
    assert result.latest_reviewer_quality_limitation_permission_review_level == (
        PERMISSION_CLASS_METADATA_PRESENT_ONLY
    )
    assert result.latest_reviewer_quality_limitation_package_promotion_level == PACKAGE_PROMOTION_NONE
    assert result.latest_reviewer_quality_limitation_reviewer_metadata_present is True
    assert result.latest_reviewer_quality_limitation_reviewer_id_recorded is True
    assert result.latest_reviewer_quality_limitation_reviewer_id_preview == full_reviewer_id[:12]
    assert result.latest_reviewer_quality_limitation_reviewer_role == "reviewer"
    assert result.latest_reviewer_quality_limitation_reviewer_role_supported is True
    assert result.latest_reviewer_quality_limitation_reviewer_type == "human_declared_only"
    assert result.latest_reviewer_quality_limitation_reviewer_attestation_present is True
    assert result.latest_reviewer_quality_limitation_reviewer_authority_scope_declared is True
    assert result.latest_reviewer_quality_limitation_quality_status_present is True
    assert result.latest_reviewer_quality_limitation_quality_status_declared is True
    assert result.latest_reviewer_quality_limitation_quality_issue_count == 0
    assert result.latest_reviewer_quality_limitation_quality_warning_count == 0
    assert result.latest_reviewer_quality_limitation_quality_blocker_count == 0
    assert result.latest_reviewer_quality_limitation_limitations_present is True
    assert result.latest_reviewer_quality_limitation_limitation_count == 1
    assert result.latest_reviewer_quality_limitation_limitation_severity_max == "INFO"
    assert result.latest_reviewer_quality_limitation_limitation_categories == "schema_assumption"
    assert result.latest_reviewer_quality_limitation_unresolved_limitation_count == 0
    assert result.latest_reviewer_quality_limitation_blocking_limitation_count == 0
    assert result.latest_reviewer_quality_limitation_permission_class_present is True
    assert result.latest_reviewer_quality_limitation_permission_class == "public"
    assert result.latest_reviewer_quality_limitation_legality_flag == "public_confirmed"
    assert result.latest_reviewer_quality_limitation_issue_count == 0
    assert result.latest_reviewer_quality_limitation_warning_count == 0
    assert result.reviewer_quality_limitation_reviewer_authority_validated is False
    assert result.reviewer_quality_limitation_quality_status_validated is False
    assert result.reviewer_quality_limitation_permission_class_validated is False
    assert result.reviewer_quality_limitation_limitations_overridden_by_reviewer is False
    assert result.reviewer_quality_limitation_limitations_overridden_by_quality is False
    assert result.reviewer_quality_limitation_source_reliability_scored is False
    assert result.reviewer_quality_limitation_source_hash_validated is False
    assert result.reviewer_quality_limitation_revision_id_validated is False
    assert result.reviewer_quality_limitation_available_time_validated is False
    assert result.reviewer_quality_limitation_pit_admissibility_validated is False
    assert result.reviewer_quality_limitation_real_package_candidate_created is False
    assert result.reviewer_quality_limitation_active_replay_input is False
    assert result.reviewer_quality_limitation_buy_review_allowed is False
    assert result.reviewer_quality_limitation_trading_allowed is False
    assert row["status"] == "REVIEWER_QUALITY_LIMITATION_METADATA_PRESENT_REPORT_ONLY"
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_reviewer_quality_limitation_reviewer_id_preview"] == (
        full_reviewer_id[:12]
    )
    assert summary.loc[0, "reviewer_quality_limitation_reviewer_authority_validated"] == "False"
    assert metadata["reviewer_quality_limitation_context_visible"] is True
    assert metadata["latest_reviewer_quality_limitation_reviewer_id_preview"] == full_reviewer_id[:12]
    assert metadata["reviewer_quality_limitation_active_replay_input"] is False
    assert full_reviewer_id not in metadata_text
    assert full_reviewer_id not in report_text
    assert str(input_root) not in metadata_text
    assert str(input_root) not in report_text
    for stale in STALE_REVIEWER_QUALITY_LIMITATION_NEXT_ACTION_PHRASES:
        assert stale not in result.latest_reviewer_quality_limitation_recommended_next_task
    for unsafe in UNSAFE_REVIEWER_QUALITY_LIMITATION_WORDING:
        assert unsafe not in metadata_text
        assert unsafe not in result.latest_reviewer_quality_limitation_recommended_next_task
        assert unsafe not in row["notes"]


def test_research_status_surfaces_reviewer_quality_limitation_warn_and_fail_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    warn_manifest, warn_metadata = _reviewer_quality_limitation_inputs(
        tmp_path / "reviewer_quality_warn_inputs",
        quality_warning_count=1,
        limitation_severity_max="WARN",
        limitation_categories=["manual_review_needed"],
        unresolved_limitation_count=1,
    )
    run_reviewer_authority_quality_limitation(
        output_root=_reviewer_quality_limitation_output_root(root),
        run_id="007_warn",
        reviewer_quality_manifest_path=warn_manifest,
        reviewer_quality_metadata_path=warn_metadata,
        allowed_manifest_roots=[warn_manifest.parent],
        reviewer_authority_level=REVIEWER_METADATA_PRESENT_ONLY,
        quality_status_level=QUALITY_METADATA_PRESENT_ONLY,
        limitation_review_level=LIMITATION_METADATA_PRESENT_ONLY,
        permission_review_level=PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        package_promotion_level=PACKAGE_PROMOTION_NONE,
        allow_reviewer_quality_limitation_metadata=True,
    )

    warn_result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard_warn")
    warn_row = warn_result.dashboard_frame[
        warn_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_REVIEWER_AUTHORITY_QUALITY_LIMITATION_STATUS"
    ].iloc[0]

    assert warn_result.latest_reviewer_quality_limitation_runtime_status == (
        "REVIEWER_QUALITY_LIMITATION_WARN_LIMITATIONS_PRESENT"
    )
    assert warn_result.latest_reviewer_quality_limitation_health_status == "WARN"
    assert warn_result.latest_reviewer_quality_limitation_limitation_severity_max == "WARN"
    assert warn_result.latest_reviewer_quality_limitation_warning_count == 1
    assert warn_row["warning_count"] >= 1
    assert warn_row["blocking_error_count"] == 0
    assert warn_result.reviewer_quality_limitation_real_package_candidate_created is False
    assert warn_result.reviewer_quality_limitation_active_replay_input is False
    assert warn_result.reviewer_quality_limitation_buy_review_allowed is False
    assert warn_result.reviewer_quality_limitation_trading_allowed is False

    fail_manifest, fail_metadata = _reviewer_quality_limitation_inputs(
        tmp_path / "reviewer_quality_fail_inputs",
        permission_class="private",
        legality_flag="private_source",
        limitation_severity_max="BLOCKER",
        limitation_categories=["private_or_sensitive_source"],
        quality_blocker_count=1,
        blocking_limitation_count=1,
    )
    run_reviewer_authority_quality_limitation(
        output_root=_reviewer_quality_limitation_output_root(root),
        run_id="008_fail",
        reviewer_quality_manifest_path=fail_manifest,
        reviewer_quality_metadata_path=fail_metadata,
        allowed_manifest_roots=[fail_manifest.parent],
        reviewer_authority_level=REVIEWER_METADATA_PRESENT_ONLY,
        quality_status_level=QUALITY_METADATA_PRESENT_ONLY,
        limitation_review_level=LIMITATION_METADATA_PRESENT_ONLY,
        permission_review_level=PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        package_promotion_level=PACKAGE_PROMOTION_NONE,
        allow_reviewer_quality_limitation_metadata=True,
    )

    fail_result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard_fail")
    fail_row = fail_result.dashboard_frame[
        fail_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_REVIEWER_AUTHORITY_QUALITY_LIMITATION_STATUS"
    ].iloc[0]

    assert fail_result.latest_reviewer_quality_limitation_runtime_status == "FAIL"
    assert fail_result.latest_reviewer_quality_limitation_health_status == "FAIL"
    assert fail_result.latest_reviewer_quality_limitation_issue_count == 1
    assert fail_result.latest_reviewer_quality_limitation_permission_class == ""
    assert fail_result.latest_reviewer_quality_limitation_private_source_blocked is False
    assert fail_result.latest_reviewer_quality_limitation_blocking_limitation_count == 0
    assert fail_row["blocking_error_count"] == 1
    assert fail_result.reviewer_quality_limitation_permission_class_validated is False
    assert fail_result.reviewer_quality_limitation_active_replay_input is False
    assert fail_result.reviewer_quality_limitation_buy_review_allowed is False
    assert fail_result.reviewer_quality_limitation_trading_allowed is False


def test_research_status_preserves_paper_priority_over_reviewer_quality_limitation_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    manifest_path, metadata_path = _reviewer_quality_limitation_inputs(
        tmp_path / "reviewer_quality_priority_inputs"
    )
    run_reviewer_authority_quality_limitation(
        output_root=_reviewer_quality_limitation_output_root(root),
        run_id="007_priority",
        reviewer_quality_manifest_path=manifest_path,
        reviewer_quality_metadata_path=metadata_path,
        allowed_manifest_roots=[manifest_path.parent],
        reviewer_authority_level=REVIEWER_METADATA_PRESENT_ONLY,
        quality_status_level=QUALITY_METADATA_PRESENT_ONLY,
        limitation_review_level=LIMITATION_METADATA_PRESENT_ONLY,
        permission_review_level=PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        package_promotion_level=PACKAGE_PROMOTION_NONE,
        allow_reviewer_quality_limitation_metadata=True,
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.reviewer_quality_limitation_context_visible is True
    assert result.latest_reviewer_quality_limitation_health_status == "PASS"
    assert result.reviewer_quality_limitation_reviewer_authority_validated is False
    assert result.reviewer_quality_limitation_quality_status_validated is False
    assert result.reviewer_quality_limitation_real_package_candidate_created is False
    assert result.reviewer_quality_limitation_active_replay_input_ready_emitted is False
    assert result.reviewer_quality_limitation_replay_execution_allowed is False
    assert result.reviewer_quality_limitation_buy_review_allowed is False
    assert result.reviewer_quality_limitation_trading_allowed is False


def test_cli_research_status_prints_reviewer_quality_limitation_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    manifest_path, metadata_path = _reviewer_quality_limitation_inputs(
        tmp_path / "reviewer_quality_cli_inputs"
    )
    run_reviewer_authority_quality_limitation(
        output_root=_reviewer_quality_limitation_output_root(root),
        run_id="007_cli",
        reviewer_quality_manifest_path=manifest_path,
        reviewer_quality_metadata_path=metadata_path,
        allowed_manifest_roots=[manifest_path.parent],
        reviewer_authority_level=REVIEWER_METADATA_PRESENT_ONLY,
        quality_status_level=QUALITY_METADATA_PRESENT_ONLY,
        limitation_review_level=LIMITATION_METADATA_PRESENT_ONLY,
        permission_review_level=PERMISSION_CLASS_METADATA_PRESENT_ONLY,
        package_promotion_level=PACKAGE_PROMOTION_NONE,
        allow_reviewer_quality_limitation_metadata=True,
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "reviewer_quality_limitation_context_visible: True" in output.out
    assert "latest_reviewer_quality_limitation_run_id: 007_cli" in output.out
    assert "latest_reviewer_quality_limitation_reviewer_id_preview: private-revi" in output.out
    assert "reviewer_quality_limitation_reviewer_authority_validated: False" in output.out
    assert "reviewer_quality_limitation_active_replay_input: False" in output.out
    assert f"latest_reviewer_quality_limitation_recommended_next_task: {EXPECTED_REVIEWER_QUALITY_LIMITATION_NEXT_TASK}" in output.out
    assert "Research-Status Planning Report-Only v0.1" not in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_surfaces_preflight_no_input_context(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    run_real_reviewed_local_csv_package_candidate_preflight(
        output_root=_preflight_output_root(root),
        run_id="008_no_input",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")

    assert result.real_reviewed_local_csv_package_candidate_preflight_context_visible is True
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_run_id == "008_no_input"
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_NO_INPUT
    )
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "PASS"
    assert result.real_reviewed_local_csv_package_candidate_preflight_preflight_level == "PREFLIGHT_NONE"
    assert result.real_reviewed_local_csv_package_candidate_preflight_package_creation_level == (
        PREFLIGHT_PACKAGE_CREATION_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_csv_read_level == (
        PREFLIGHT_CSV_READ_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_report_only is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_diagnostic_only is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_target_csv_opened is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_artifact_opened is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_content_read is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_hash_recomputed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_expected_hash_reverified is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_available_time_compared_to_decision_time is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_real_package_candidate_created is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_active_replay_input is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_recommended_next_task == (
        EXPECTED_PREFLIGHT_NEXT_TASK
    )
    assert row["status"] == PREFLIGHT_STATUS_NO_INPUT
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "real_reviewed_local_csv_package_candidate_preflight_context_visible"] == "True"
    assert summary.loc[0, "real_reviewed_local_csv_package_candidate_preflight_trading_allowed"] == "False"


def test_research_status_surfaces_preflight_metadata_context_without_rereading_references(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    artifact, metadata_root, full_source_hash, full_reviewer_id = _run_preflight_metadata_context(
        tmp_path,
        root,
        run_id="008_metadata",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))
    metadata_text = json.dumps(metadata, sort_keys=True)
    report_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")

    assert result.real_reviewed_local_csv_package_candidate_preflight_context_visible is True
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_run_id == "008_metadata"
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_METADATA_CONTEXT
    )
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "PASS"
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_preflight_id == (
        "preflight-dashboard-0001"
    )
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_declared_package_id == (
        "declared-package-dashboard-0001"
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_preflight_level == (
        PREFLIGHT_METADATA_REFERENCES_ONLY
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_package_creation_level == (
        PREFLIGHT_PACKAGE_CREATION_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_csv_read_level == (
        PREFLIGHT_CSV_READ_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_hash_validation_level == (
        PREFLIGHT_SOURCE_HASH_VALIDATION_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_available_time_validation_level == (
        PREFLIGHT_AVAILABLE_TIME_VALIDATION_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_reviewer_authority_level == (
        PREFLIGHT_REVIEWER_AUTHORITY_NONE
    )
    assert result.real_reviewed_local_csv_package_candidate_preflight_evidence_reference_count == 8
    assert result.real_reviewed_local_csv_package_candidate_preflight_required_reference_count == 6
    assert result.real_reviewed_local_csv_package_candidate_preflight_required_reference_present_count == 6
    assert result.real_reviewed_local_csv_package_candidate_preflight_missing_required_reference_count == 0
    assert result.real_reviewed_local_csv_package_candidate_preflight_optional_reference_count == 2
    assert result.real_reviewed_local_csv_package_candidate_preflight_missing_optional_reference_count == 0
    assert result.real_reviewed_local_csv_package_candidate_preflight_csv_structural_header_metadata_present is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_revision_time_metadata_present is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_reviewer_quality_limitation_metadata_present is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_manifest_only_preflight_metadata_present is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_source_hash_recompute_not_performed is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_available_time_pit_gate_not_performed is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_reviewer_authority_validation_not_performed is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_package_creation_not_performed is True
    assert result.real_reviewed_local_csv_package_candidate_preflight_real_package_candidate_created is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_active_reviewed_input_candidate_created is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_active_replay_input is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_replay_execution_allowed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False
    assert row["status"] == PREFLIGHT_STATUS_METADATA_CONTEXT
    assert row["blocking_error_count"] == 0
    assert summary.loc[0, "latest_real_reviewed_local_csv_package_candidate_preflight_preflight_id"] == (
        "preflight-dashboard-0001"
    )
    assert metadata["real_reviewed_local_csv_package_candidate_preflight_target_csv_opened"] is False
    assert full_source_hash not in metadata_text
    assert full_source_hash not in report_text
    assert full_reviewer_id not in metadata_text
    assert full_reviewer_id not in report_text
    assert str(metadata_root) not in metadata_text
    assert str(metadata_root) not in report_text
    assert (
        metadata["latest_real_reviewed_local_csv_package_candidate_preflight_metadata_path"]
        == str(artifact["artifact_paths"]["metadata"])
    )
    for stale in STALE_PREFLIGHT_NEXT_ACTION_PHRASES:
        assert stale not in result.latest_real_reviewed_local_csv_package_candidate_preflight_recommended_next_task
    for unsafe in UNSAFE_PREFLIGHT_WORDING:
        assert unsafe not in row["stage"]
        assert unsafe not in result.latest_real_reviewed_local_csv_package_candidate_preflight_recommended_next_task


def test_research_status_surfaces_preflight_warn_and_fail_contexts(tmp_path: Path) -> None:
    root = _reports_root(tmp_path)
    _run_preflight_metadata_context(
        tmp_path,
        root,
        run_id="008_warn_optional",
        missing_optional="metadata_reference_following_metadata",
    )
    warn_result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard_warn")
    warn_row = warn_result.dashboard_frame[
        warn_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]

    assert warn_result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_WARN_MISSING_OPTIONAL
    )
    assert warn_result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "WARN"
    assert warn_result.real_reviewed_local_csv_package_candidate_preflight_missing_optional_reference_count == 1
    assert warn_result.real_reviewed_local_csv_package_candidate_preflight_missing_required_reference_count == 0
    assert warn_row["warning_count"] >= 1
    assert warn_row["blocking_error_count"] == 0
    assert warn_result.real_reviewed_local_csv_package_candidate_preflight_real_package_candidate_created is False
    assert warn_result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert warn_result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False

    _run_preflight_metadata_context(
        tmp_path,
        root,
        run_id="009_fail_required",
        missing_required="source_revision_time_metadata",
    )
    fail_result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard_fail")
    fail_row = fail_result.dashboard_frame[
        fail_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]

    assert fail_result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_BLOCKED_MISSING_REQUIRED
    )
    assert fail_result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "FAIL"
    assert fail_result.real_reviewed_local_csv_package_candidate_preflight_missing_required_reference_count == 1
    assert fail_result.real_reviewed_local_csv_package_candidate_preflight_issue_count >= 1
    assert fail_row["blocking_error_count"] == 1
    assert fail_result.real_reviewed_local_csv_package_candidate_preflight_active_replay_input is False
    assert fail_result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert fail_result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False


def test_research_status_surfaces_preflight_unsafe_and_forbidden_without_leakage(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _run_preflight_metadata_context(
        tmp_path,
        root,
        run_id="008_unsafe_validation",
        metadata_overrides={"source_revision_time_metadata": {"source_hash_validated": True}},
    )
    unsafe_result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard_unsafe")
    unsafe_row = unsafe_result.dashboard_frame[
        unsafe_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]

    assert unsafe_result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_BLOCKED_UNSUPPORTED_VALIDATION
    )
    assert unsafe_result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "FAIL"
    assert unsafe_row["blocking_error_count"] == 1
    assert unsafe_result.real_reviewed_local_csv_package_candidate_preflight_source_hash_validated is False
    assert unsafe_result.real_reviewed_local_csv_package_candidate_preflight_active_replay_input is False

    _run_preflight_metadata_context(
        tmp_path,
        root,
        run_id="009_forbidden_downstream",
        metadata_overrides={
            "reviewer_quality_limitation_metadata": {
                "forbidden_downstream_flags": {"buy_review_allowed": True}
            }
        },
    )
    forbidden_result = run_local_research_dashboard(
        root=root,
        output_dir=tmp_path / "dashboard_forbidden",
    )
    forbidden_row = forbidden_result.dashboard_frame[
        forbidden_result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_STATUS"
    ].iloc[0]
    metadata_text = forbidden_result.artifact_paths["metadata"].read_text(encoding="utf-8")

    assert forbidden_result.latest_real_reviewed_local_csv_package_candidate_preflight_runtime_status == (
        PREFLIGHT_STATUS_BLOCKED_FORBIDDEN_DOWNSTREAM
    )
    assert forbidden_result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "FAIL"
    assert forbidden_row["blocking_error_count"] == 1
    assert forbidden_result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert forbidden_result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False
    assert "ACTIVE_REPLAY_INPUT_READY" not in metadata_text
    assert "PACKAGE_ADMISSIBLE" not in metadata_text


def test_research_status_preserves_paper_priority_over_preflight_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _run_preflight_metadata_context(tmp_path, root, run_id="008_priority")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.real_reviewed_local_csv_package_candidate_preflight_context_visible is True
    assert result.latest_real_reviewed_local_csv_package_candidate_preflight_health_status == "PASS"
    assert result.real_reviewed_local_csv_package_candidate_preflight_real_package_candidate_created is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_active_replay_input is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_replay_execution_allowed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed is False
    assert result.real_reviewed_local_csv_package_candidate_preflight_trading_allowed is False


def test_cli_research_status_prints_preflight_fields(tmp_path: Path, capsys) -> None:
    root = _reports_root(tmp_path)
    _run_preflight_metadata_context(tmp_path, root, run_id="008_cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "real_reviewed_local_csv_package_candidate_preflight_context_visible: True" in output.out
    assert "latest_real_reviewed_local_csv_package_candidate_preflight_run_id: 008_cli" in output.out
    assert "real_reviewed_local_csv_package_candidate_preflight_package_creation_level: PACKAGE_CREATION_NONE" in output.out
    assert "real_reviewed_local_csv_package_candidate_preflight_csv_read_level: CSV_READ_NONE" in output.out
    assert "real_reviewed_local_csv_package_candidate_preflight_real_package_candidate_created: False" in output.out
    assert "real_reviewed_local_csv_package_candidate_preflight_buy_review_allowed: False" in output.out
    assert (
        f"latest_real_reviewed_local_csv_package_candidate_preflight_recommended_next_task: {EXPECTED_PREFLIGHT_NEXT_TASK}"
        in output.out
    )
    assert "Research-Status Planning Report-Only v0.1" not in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_source_artifact_byte_hash_no_input_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    output_root = (
        root
        / "manual_diagnostics"
        / "tiny_pit_real_reviewed_local_csv_package_candidate_source_artifact_byte_hash_v0_1"
    )
    run_source_artifact_byte_hash(output_root=output_root, run_id="009_no_input")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.source_artifact_byte_hash_context_visible is True
    assert result.latest_source_artifact_byte_hash_run_id == "009_no_input"
    assert result.latest_source_artifact_byte_hash_runtime_status == "NO_SOURCE_ARTIFACT_BYTE_HASH_INPUT"
    assert result.latest_source_artifact_byte_hash_health_status == "PASS"
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_read_level == SOURCE_ARTIFACT_BYTE_READ_NONE
    assert result.latest_source_artifact_byte_hash_source_hash_recompute_level == SOURCE_HASH_RECOMPUTE_NONE
    assert result.latest_source_artifact_byte_hash_source_content_read_level == SOURCE_ARTIFACT_BYTE_HASH_CONTENT_READ_NONE
    assert result.latest_source_artifact_byte_hash_csv_read_level == SOURCE_ARTIFACT_BYTE_HASH_CSV_READ_NONE
    assert result.latest_source_artifact_byte_hash_source_hash_validated is False
    assert result.latest_source_artifact_byte_hash_revision_id_validated is False
    assert result.latest_source_artifact_byte_hash_available_time_validated is False
    assert result.latest_source_artifact_byte_hash_pit_admissibility_validated is False
    assert result.latest_source_artifact_byte_hash_source_reliability_scored is False
    assert result.latest_source_artifact_byte_hash_reviewer_authority_validated is False
    assert result.latest_source_artifact_byte_hash_real_package_candidate_created is False
    assert result.latest_source_artifact_byte_hash_active_replay_input is False
    assert result.latest_source_artifact_byte_hash_replay_execution_allowed is False
    assert result.latest_source_artifact_byte_hash_buy_review_allowed is False
    assert result.latest_source_artifact_byte_hash_trading_allowed is False
    assert result.latest_source_artifact_byte_hash_data_raw_written is False
    assert result.latest_source_artifact_byte_hash_data_processed_written is False
    assert result.latest_source_artifact_byte_hash_data_cache_written is False
    assert result.latest_source_artifact_byte_hash_recommended_next_task == (
        EXPECTED_SOURCE_ARTIFACT_BYTE_HASH_NEXT_TASK
    )
    assert summary.loc[0, "source_artifact_byte_hash_context_visible"] == "True"
    assert metadata["source_artifact_byte_hash_context_visible"] is True


def test_research_status_includes_source_artifact_byte_hash_preview_only_fields(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = _source_artifact_byte_hash_fixture(tmp_path)
    expected_hash = fixture["full_hash"]
    preview = expected_hash[:SOURCE_ARTIFACT_BYTE_HASH_PREVIEW_CHARS]
    _run_source_artifact_byte_hash_context(
        root,
        fixture,
        run_id="009_matched",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata_text = result.artifact_paths["metadata"].read_text(encoding="utf-8")
    metadata = json.loads(metadata_text)
    dashboard_text = result.artifact_paths["local_research_dashboard"].read_text(encoding="utf-8")
    summary_text = result.artifact_paths["local_research_summary"].read_text(encoding="utf-8")

    assert result.source_artifact_byte_hash_context_visible is True
    assert result.latest_source_artifact_byte_hash_run_id == "009_matched"
    assert result.latest_source_artifact_byte_hash_runtime_status == (
        "SOURCE_ARTIFACT_BYTE_HASH_MATCHED_REPORT_ONLY"
    )
    assert result.latest_source_artifact_byte_hash_health_status == "PASS"
    assert result.latest_source_artifact_byte_hash_source_hash_algorithm == "SHA-256"
    assert result.latest_source_artifact_byte_hash_computed_source_hash_preview == preview
    assert result.latest_source_artifact_byte_hash_declared_source_hash_preview == preview
    assert result.latest_source_artifact_byte_hash_computed_source_hash_full_recorded_in_metadata is True
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_matched is True
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_mismatch is False
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_actionable_mismatch is False
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_read_level == (
        SOURCE_ARTIFACT_BYTE_READ_STREAMING_HASH_ONLY
    )
    assert result.latest_source_artifact_byte_hash_source_hash_recompute_level == SOURCE_HASH_RECOMPUTE_SHA256_ONLY
    assert result.latest_source_artifact_byte_hash_source_content_read_level == SOURCE_ARTIFACT_BYTE_HASH_CONTENT_READ_NONE
    assert result.latest_source_artifact_byte_hash_csv_read_level == SOURCE_ARTIFACT_BYTE_HASH_CSV_READ_NONE
    assert result.latest_source_artifact_byte_hash_source_artifact_opened_for_hash is True
    assert result.latest_source_artifact_byte_hash_source_artifact_bytes_streamed_for_hash is True
    assert result.latest_source_artifact_byte_hash_source_content_read is False
    assert result.latest_source_artifact_byte_hash_target_csv_opened is False
    assert result.latest_source_artifact_byte_hash_csv_header_read is False
    assert result.latest_source_artifact_byte_hash_csv_values_read is False
    assert result.latest_source_artifact_byte_hash_csv_full_content_read is False
    assert result.latest_source_artifact_byte_hash_source_hash_validated is False
    assert result.latest_source_artifact_byte_hash_revision_id_validated is False
    assert result.latest_source_artifact_byte_hash_available_time_validated is False
    assert result.latest_source_artifact_byte_hash_pit_admissibility_validated is False
    assert result.latest_source_artifact_byte_hash_source_reliability_scored is False
    assert result.latest_source_artifact_byte_hash_reviewer_authority_validated is False
    assert result.latest_source_artifact_byte_hash_real_package_candidate_created is False
    assert result.latest_source_artifact_byte_hash_active_replay_input is False
    assert result.latest_source_artifact_byte_hash_replay_execution_allowed is False
    assert result.latest_source_artifact_byte_hash_buy_review_allowed is False
    assert result.latest_source_artifact_byte_hash_trading_allowed is False
    assert summary.loc[0, "latest_source_artifact_byte_hash_computed_source_hash_preview"] == preview
    assert metadata["latest_source_artifact_byte_hash_computed_source_hash_preview"] == preview
    for public_text in [metadata_text, dashboard_text, summary_text]:
        assert expected_hash not in public_text
        assert str(fixture["source_artifact"]) not in public_text
        assert "synthetic source artifact bytes" not in public_text
        assert "symbol,available_time" not in public_text


def test_research_status_source_artifact_byte_hash_mismatch_is_context_only(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = _source_artifact_byte_hash_fixture(tmp_path, declared_hash="b" * 64)
    _run_source_artifact_byte_hash_context(root, fixture, run_id="009_mismatch")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.source_artifact_byte_hash_context_visible is True
    assert result.latest_source_artifact_byte_hash_runtime_status == (
        "SOURCE_ARTIFACT_BYTE_HASH_MISMATCHED_REPORT_ONLY"
    )
    assert result.latest_source_artifact_byte_hash_health_status == "WARN"
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_matched is False
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_mismatch is True
    assert result.latest_source_artifact_byte_hash_source_artifact_byte_identity_actionable_mismatch is True
    assert result.latest_source_artifact_byte_hash_source_hash_validated is False
    assert result.latest_source_artifact_byte_hash_pit_admissibility_validated is False
    assert result.latest_source_artifact_byte_hash_real_package_candidate_created is False
    assert result.latest_source_artifact_byte_hash_active_replay_input is False
    assert result.latest_source_artifact_byte_hash_buy_review_allowed is False
    assert result.latest_source_artifact_byte_hash_trading_allowed is False


def test_research_status_preserves_paper_priority_over_source_artifact_byte_hash(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = _source_artifact_byte_hash_fixture(tmp_path)
    _run_source_artifact_byte_hash_context(root, fixture, run_id="009_priority")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.source_artifact_byte_hash_context_visible is True
    assert result.latest_source_artifact_byte_hash_source_hash_validated is False
    assert result.latest_source_artifact_byte_hash_real_package_candidate_created is False
    assert result.latest_source_artifact_byte_hash_active_replay_input is False
    assert result.latest_source_artifact_byte_hash_replay_execution_allowed is False
    assert result.latest_source_artifact_byte_hash_buy_review_allowed is False
    assert result.latest_source_artifact_byte_hash_trading_allowed is False


def test_cli_research_status_prints_source_artifact_byte_hash_preview_only(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = _source_artifact_byte_hash_fixture(tmp_path)
    expected_hash = fixture["full_hash"]
    preview = expected_hash[:SOURCE_ARTIFACT_BYTE_HASH_PREVIEW_CHARS]
    _run_source_artifact_byte_hash_context(root, fixture, run_id="009_cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr().out

    assert code == 0
    assert "source_artifact_byte_hash_context_visible: True" in output
    assert "latest_source_artifact_byte_hash_run_id: 009_cli" in output
    assert f"latest_source_artifact_byte_hash_computed_source_hash_preview: {preview}" in output
    assert "latest_source_artifact_byte_hash_source_hash_validated: False" in output
    assert "latest_source_artifact_byte_hash_pit_admissibility_validated: False" in output
    assert "latest_source_artifact_byte_hash_real_package_candidate_created: False" in output
    assert "latest_source_artifact_byte_hash_active_replay_input: False" in output
    assert "latest_source_artifact_byte_hash_buy_review_allowed: False" in output
    assert "latest_source_artifact_byte_hash_trading_allowed: False" in output
    assert f"latest_source_artifact_byte_hash_recommended_next_task: {EXPECTED_SOURCE_ARTIFACT_BYTE_HASH_NEXT_TASK}" in output
    assert expected_hash not in output
    assert str(fixture["source_artifact"]) not in output
    assert "synthetic source artifact bytes" not in output
    assert "ACTIVE_REPLAY_INPUT_READY" not in output


def test_research_status_includes_historical_replay_pit_evidence_closure_worklist_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    worklist = _run_pit_evidence_closure_worklist_context(root, run_id="010_worklist")

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.historical_replay_pit_evidence_closure_worklist_context_visible is True
    assert result.latest_historical_replay_pit_evidence_closure_worklist_run_id == (
        worklist.worklist_run_id
    )
    assert result.latest_historical_replay_pit_evidence_closure_worklist_signal_date == "2024-04-02"
    assert result.latest_historical_replay_pit_evidence_closure_worklist_universe_name == "etf_core"
    assert result.latest_historical_replay_pit_evidence_closure_worklist_status == (
        "PIT_EVIDENCE_CLOSURE_WORKLIST_WARN_NO_CONTEXT"
    )
    assert result.latest_historical_replay_pit_evidence_closure_worklist_health_status == (
        "PIT_EVIDENCE_CLOSURE_WORKLIST_HEALTH_WARN_REVIEW_REQUIRED"
    )
    assert result.latest_historical_replay_pit_evidence_closure_worklist_workflow_stage == (
        "HISTORICAL_REPLAY_PIT_EVIDENCE_CLOSURE_WORKLIST_CREATED_REPORT_ONLY"
    )
    assert result.latest_historical_replay_pit_evidence_closure_worklist_row_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_blocked_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_missing_evidence_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_context_only_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_needs_manual_review_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_no_hit_review_needed_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_no_hit_accepted_context_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_closure_ready_not_pit_approved_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_profile_conflict_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_survivorship_warning_count == 0
    assert result.latest_historical_replay_pit_evidence_closure_worklist_recommended_next_task == (
        EXPECTED_PIT_EVIDENCE_CLOSURE_WORKLIST_NEXT_TASK
    )
    for field in PIT_EVIDENCE_CLOSURE_WORKLIST_SAFETY_FALSE_FIELDS:
        assert getattr(result, f"latest_historical_replay_pit_evidence_closure_worklist_{field}") is False
        assert metadata[f"latest_historical_replay_pit_evidence_closure_worklist_{field}"] is False
    assert summary.loc[0, "historical_replay_pit_evidence_closure_worklist_context_visible"] == "True"
    assert summary.loc[0, "latest_historical_replay_pit_evidence_closure_worklist_row_count"] == "0"
    assert metadata["historical_replay_pit_evidence_closure_worklist_context_visible"] is True


def test_research_status_preserves_paper_priority_over_historical_replay_pit_evidence_closure_worklist(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _run_pit_evidence_closure_worklist_context(root, run_id="010_priority")
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.historical_replay_pit_evidence_closure_worklist_context_visible is True
    assert result.latest_historical_replay_pit_evidence_closure_worklist_pit_evidence_closed is False
    assert result.latest_historical_replay_pit_evidence_closure_worklist_pit_admissibility_approved is False
    assert result.latest_historical_replay_pit_evidence_closure_worklist_active_replay_input is False
    assert result.latest_historical_replay_pit_evidence_closure_worklist_replay_execution_allowed is False
    assert result.latest_historical_replay_pit_evidence_closure_worklist_buy_review_allowed is False
    assert result.latest_historical_replay_pit_evidence_closure_worklist_trading_allowed is False


def test_cli_research_status_prints_historical_replay_pit_evidence_closure_worklist_context(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    _run_pit_evidence_closure_worklist_context(root, run_id="010_cli")

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr().out

    assert code == 0
    assert "historical_replay_pit_evidence_closure_worklist_context_visible: True" in output
    assert "latest_historical_replay_pit_evidence_closure_worklist_run_id: 010_cli" in output
    assert (
        "latest_historical_replay_pit_evidence_closure_worklist_signal_date: 2024-04-02"
        in output
    )
    assert "latest_historical_replay_pit_evidence_closure_worklist_universe_name: etf_core" in output
    assert "latest_historical_replay_pit_evidence_closure_worklist_pit_evidence_closed: False" in output
    assert (
        "latest_historical_replay_pit_evidence_closure_worklist_pit_admissibility_approved: False"
        in output
    )
    assert "latest_historical_replay_pit_evidence_closure_worklist_active_replay_input: False" in output
    assert (
        "latest_historical_replay_pit_evidence_closure_worklist_replay_execution_allowed: False"
        in output
    )
    assert "latest_historical_replay_pit_evidence_closure_worklist_buy_review_allowed: False" in output
    assert "latest_historical_replay_pit_evidence_closure_worklist_trading_allowed: False" in output
    assert (
        f"latest_historical_replay_pit_evidence_closure_worklist_recommended_next_task: "
        f"{EXPECTED_PIT_EVIDENCE_CLOSURE_WORKLIST_NEXT_TASK}"
        in output
    )
    assert "PIT_ADMISSIBLE" not in output
    assert "READY_FOR_REPLAY" not in output
    assert "ACTIVE_REPLAY_INPUT_READY" not in output
    assert "BUY_REVIEW_READY" not in output
    assert "TRADING_READY" not in output


def test_research_status_includes_personal_mvp_daily_advisory_review_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    daily = _run_personal_mvp_daily_advisory_review_context(root)

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.personal_mvp_daily_advisory_review_context_visible is True
    assert result.latest_personal_mvp_daily_advisory_review_run_id == daily.daily_review_run_id
    assert result.latest_personal_mvp_daily_advisory_review_status == (
        "DAILY_ADVISORY_REVIEW_READY_FOR_MANUAL_REVIEW"
    )
    assert result.latest_personal_mvp_daily_advisory_review_health_status == "PASS"
    assert result.latest_personal_mvp_daily_advisory_review_workflow_stage == (
        "DAILY_ADVISORY_REVIEW_READY_FOR_MANUAL_REVIEW"
    )
    assert result.latest_personal_mvp_daily_advisory_review_report_path.endswith(
        "daily_advisory_review_report.md"
    )
    assert result.latest_personal_mvp_daily_advisory_review_metadata_path.endswith("metadata.json")
    assert result.latest_personal_mvp_daily_advisory_review_row_count == 1
    assert result.latest_personal_mvp_daily_advisory_review_watch_count == 1
    assert result.latest_personal_mvp_daily_advisory_review_review_buy_candidate_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_review_sell_candidate_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_hold_review_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_no_action_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_blocked_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_demo_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_not_found_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_stale_artifact_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_missing_artifact_count == 0
    assert result.latest_personal_mvp_daily_advisory_review_manual_confirmation_required is True
    assert result.latest_personal_mvp_daily_advisory_review_recommended_next_task == (
        EXPECTED_PERSONAL_MVP_DAILY_ADVISORY_REVIEW_NEXT_TASK
    )
    assert result.latest_personal_mvp_daily_advisory_review_report_only is True
    assert result.latest_personal_mvp_daily_advisory_review_diagnostic_only is True
    assert result.latest_personal_mvp_daily_advisory_review_local_only is True
    for field in PERSONAL_MVP_DAILY_ADVISORY_REVIEW_FALSE_FIELDS:
        assert getattr(result, f"latest_personal_mvp_daily_advisory_review_{field}") is False
        assert metadata[f"latest_personal_mvp_daily_advisory_review_{field}"] is False
    assert summary.loc[0, "personal_mvp_daily_advisory_review_context_visible"] == "True"
    assert summary.loc[0, "latest_personal_mvp_daily_advisory_review_watch_count"] == "1"
    assert metadata["latest_personal_mvp_daily_advisory_review_watch_count"] == 1


def test_research_status_preserves_paper_priority_over_personal_mvp_daily_advisory_review(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _run_personal_mvp_daily_advisory_review_context(root)
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.personal_mvp_daily_advisory_review_context_visible is True
    assert result.latest_personal_mvp_daily_advisory_review_buy_review_allowed is False
    assert result.latest_personal_mvp_daily_advisory_review_trading_allowed is False
    assert result.latest_personal_mvp_daily_advisory_review_current_candidates_run is False
    assert result.latest_personal_mvp_daily_advisory_review_snapshot_built is False
    assert result.latest_personal_mvp_daily_advisory_review_signal_semantics_mutated is False


def test_cli_research_status_prints_personal_mvp_daily_advisory_review_context(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    daily = _run_personal_mvp_daily_advisory_review_context(root)

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr().out.lower()

    assert code == 0
    assert "personal_mvp_daily_advisory_review_context_visible: true" in output
    assert f"latest_personal_mvp_daily_advisory_review_run_id: {daily.daily_review_run_id}" in output
    assert "latest_personal_mvp_daily_advisory_review_status: daily_advisory_review_ready_for_manual_review" in output
    assert "latest_personal_mvp_daily_advisory_review_buy_review_allowed: false" in output
    assert "latest_personal_mvp_daily_advisory_review_trading_allowed: false" in output
    assert "latest_personal_mvp_daily_advisory_review_broker_api_called: false" in output
    assert "latest_personal_mvp_daily_advisory_review_order_placed: false" in output
    assert "latest_personal_mvp_daily_advisory_review_message_sent: false" in output
    assert "buy now" not in output
    assert "sell now" not in output
    assert "place order" not in output
    assert "submit order" not in output
    assert "trading_allowed: true" not in output
    assert "buy_review_allowed: true" not in output


def test_personal_mvp_daily_advisory_review_research_status_does_not_create_docs_project_sources(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    _run_personal_mvp_daily_advisory_review_context(root)
    run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert not (tmp_path / "docs" / "project_sources").exists()


def test_research_status_includes_tiny_pit_real_reviewed_package_candidate_contract_fixture_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_real_reviewed_package_candidate_contract_fixture_artifacts(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_package_candidate_contract_fixture_v0_1"
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_PACKAGE_CANDIDATE_CONTRACT_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert result.tiny_pit_real_reviewed_package_candidate_contract_fixture_context_visible is True
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_id == fixture.fixture_id
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_status == (
        "REAL_REVIEWED_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_health_status == "PASS"
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_workflow_stage == (
        "TINY_PIT_REAL_REVIEWED_PACKAGE_CANDIDATE_CONTRACT_FIXTURE_CREATED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_case_count == 63
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_pass_candidate_count == 5
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_warn_count == 10
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_fail_count == 48
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_blocker_count == 63
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_warning_count == 11
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_report_only is True
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_diagnostic_only is True
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_synthetic_only is True
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_active_replay_input is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_active_replay_ready is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_replay_execution_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_forward_labels_created is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_training_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_stock_profile_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_paper_validation_created is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_real_buy_review_eligible is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_strategy_performance_validated is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_trading_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_data_raw_written is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_data_processed_written is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_data_cache_written is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_recommended_next_task == (
        "Tiny PIT Real Reviewed Package Candidate Contract Fixture Post-Checkpoint "
        "Governance Audit Report-Only v0.1"
    )
    assert row["status"] == "REAL_REVIEWED_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    assert row["workflow_area"] == "TINY_PIT_REAL_REVIEWED_PACKAGE_CANDIDATE_CONTRACT_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert (
        summary.loc[0, "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_id"]
        == fixture.fixture_id
    )
    assert (
        summary.loc[0, "tiny_pit_real_reviewed_package_candidate_contract_fixture_context_visible"]
        == "True"
    )
    assert metadata["tiny_pit_real_reviewed_package_candidate_contract_fixture_context_visible"] is True
    assert metadata["latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_report_only"] is True
    assert metadata["latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_trading_allowed"] is False


def test_research_status_preserves_paper_priority_over_tiny_pit_real_reviewed_package_candidate_contract_fixture(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    build_real_reviewed_package_candidate_contract_fixture_artifacts(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_package_candidate_contract_fixture_v0_1"
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action=(
            "Paper workflow remains later priority; Tiny PIT real reviewed package candidate "
            "contract fixture is report-only."
        ),
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert result.tiny_pit_real_reviewed_package_candidate_contract_fixture_context_visible is True
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_active_replay_input is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_replay_execution_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_forward_labels_created is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_training_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_stock_profile_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_real_reviewed_package_candidate_contract_fixture_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = build_real_reviewed_package_candidate_contract_fixture_artifacts(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_package_candidate_contract_fixture_v0_1"
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "tiny_pit_real_reviewed_package_candidate_contract_fixture_context_visible: True" in output.out
    assert (
        f"latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_id: {fixture.fixture_id}"
        in output.out
    )
    assert (
        "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_status: "
        "REAL_REVIEWED_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    ) in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_health_status: PASS" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_case_count: 63" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_report_only: True" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_active_replay_input: False" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_training_allowed: False" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_buy_review_allowed: False" in output.out
    assert "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_trading_allowed: False" in output.out
    assert (
        "latest_tiny_pit_real_reviewed_package_candidate_contract_fixture_recommended_next_task: "
        "Tiny PIT Real Reviewed Package Candidate Contract Fixture Post-Checkpoint "
        "Governance Audit Report-Only v0.1"
    ) in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_context(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    fixture = write_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
        build_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
            output_root=(
                root
                / "manual_diagnostics"
                / "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_v0_1"
            )
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_CONTRACT_FIXTURE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert (
        result.tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_context_visible
        is True
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_id
        == fixture.fixture_id
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_status == (
        "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_health_status
        == "PASS"
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_workflow_stage == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_CONTRACT_FIXTURE_CREATED_REPORT_ONLY"
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_case_count == 56
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_pass_candidate_count
        == 1
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_warn_count == 3
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_fail_count == 49
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_blocker_count
        == 69
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_warning_count
        == 3
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_report_only is True
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_diagnostic_only is True
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_synthetic_only is True
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_csv_required is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_csv_consumed is False
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_package_candidate_created
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_reviewed_input_candidate_created
        is False
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_replay_input is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_replay_ready is False
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_replay_input_ready_emitted
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_replay_execution_allowed
        is False
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_trading_allowed is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_data_raw_written is False
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_data_processed_written
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_data_cache_written
        is False
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_recommended_next_task == (
        "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Preflight Contract Fixture "
        "Post-Checkpoint Governance Audit Report-Only v0.1"
    )
    assert row["status"] == "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    assert row["workflow_area"] == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_CONTRACT_FIXTURE"
    assert row["blocking_error_count"] == 0
    assert (
        summary.loc[
            0,
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_id",
        ]
        == fixture.fixture_id
    )
    assert (
        summary.loc[
            0,
            "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_context_visible",
        ]
        == "True"
    )
    assert (
        metadata[
            "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_context_visible"
        ]
        is True
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_report_only"
        ]
        is True
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_csv_consumed"
        ]
        is False
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_trading_allowed"
        ]
        is False
    )


def test_research_status_preserves_paper_priority_over_tiny_pit_real_reviewed_local_csv_package_candidate_preflight(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    write_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
        build_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
            output_root=(
                root
                / "manual_diagnostics"
                / "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_v0_1"
            )
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert (
        result.tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_context_visible
        is True
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_csv_consumed is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_package_candidate_created is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_replay_input is False
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_replay_execution_allowed
        is False
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_buy_review_allowed is False
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_trading_allowed is False


def test_cli_research_status_prints_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    fixture = write_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
        build_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_artifacts(
            output_root=(
                root
                / "manual_diagnostics"
                / "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_v0_1"
            )
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert "tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_context_visible: True" in output.out
    assert (
        f"latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_id: {fixture.fixture_id}"
        in output.out
    )
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_status: "
        "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_SCHEMA_DESIGNED_REPORT_ONLY"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_health_status: PASS"
        in output.out
    )
    assert "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_case_count: 56" in output.out
    assert "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_report_only: True" in output.out
    assert "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_csv_consumed: False" in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_real_package_candidate_created: False"
        in output.out
    )
    assert "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_active_replay_input: False" in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_buy_review_allowed: False"
        in output.out
    )
    assert "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_trading_allowed: False" in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_preflight_contract_fixture_recommended_next_task: "
        "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Preflight Contract Fixture "
        "Post-Checkpoint Governance Audit Report-Only v0.1"
    ) in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def test_research_status_includes_tiny_pit_real_reviewed_local_csv_manifest_only_preflight_prototype_fields(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    run = run_manifest_only_preflight_prototype(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_real_preflight_prototype_v0_1"
        )
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")
    row = result.dashboard_frame[
        result.dashboard_frame["component"]
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_MANIFEST_ONLY_PREFLIGHT_PROTOTYPE_STATUS"
    ].iloc[0]
    summary = pd.read_csv(result.artifact_paths["local_research_summary"], dtype=str).fillna("")
    metadata = json.loads(result.artifact_paths["metadata"].read_text(encoding="utf-8"))

    assert (
        result.tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_context_visible
        is True
    )
    assert result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_id == (
        run["run_id"]
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_status
        == "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_PROTOTYPE_NO_INPUT"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_health_status
        == "PASS"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_workflow_stage
        == "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_MANIFEST_ONLY_PREFLIGHT_PROTOTYPE_CORE_CREATED_REPORT_ONLY"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_csv_read_level
        == "CSV_READ_NONE"
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_manifest_read
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_references_followed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_local_file_hash_computed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_external_source_validated
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_pit_admissibility_validated
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_report_only
        is True
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_diagnostic_only
        is True
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_synthetic_only
        is True
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_csv_consumed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_package_candidate_created
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_reviewed_input_candidate_created
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_replay_input
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_replay_ready
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_replay_input_ready_emitted
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_replay_execution_allowed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_buy_review_allowed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_trading_allowed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_data_raw_written
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_data_processed_written
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_data_cache_written
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_recommended_next_task
        == "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Manifest-Only Preflight Prototype "
        "Post-Checkpoint Governance Audit Report-Only v0.1"
    )
    assert row["status"] == "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_PROTOTYPE_NO_INPUT"
    assert row["workflow_area"] == (
        "TINY_PIT_REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_MANIFEST_ONLY_PREFLIGHT_PROTOTYPE"
    )
    assert row["blocking_error_count"] == 0
    assert (
        summary.loc[
            0,
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_id",
        ]
        == run["run_id"]
    )
    assert (
        summary.loc[
            0,
            "tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_context_visible",
        ]
        == "True"
    )
    assert (
        metadata[
            "tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_context_visible"
        ]
        is True
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_csv_read_level"
        ]
        == "CSV_READ_NONE"
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_csv_consumed"
        ]
        is False
    )
    assert (
        metadata[
            "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_trading_allowed"
        ]
        is False
    )


def test_research_status_preserves_paper_priority_over_tiny_pit_real_reviewed_local_csv_manifest_only_preflight(
    tmp_path: Path,
) -> None:
    root = _reports_root(tmp_path)
    run_manifest_only_preflight_prototype(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_real_preflight_prototype_v0_1"
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    result = run_local_research_dashboard(root=root, output_dir=tmp_path / "dashboard")

    assert result.workflow_stage == "PAPER_WORKFLOW_READY"
    assert (
        result.tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_context_visible
        is True
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_csv_consumed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_package_candidate_created
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_replay_input
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_replay_execution_allowed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_buy_review_allowed
        is False
    )
    assert (
        result.latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_trading_allowed
        is False
    )


def test_cli_research_status_prints_tiny_pit_real_reviewed_local_csv_manifest_only_preflight_fields(
    tmp_path: Path,
    capsys,
) -> None:
    root = _reports_root(tmp_path)
    run = run_manifest_only_preflight_prototype(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_real_preflight_prototype_v0_1"
        )
    )
    _paper_workflow_status(
        root,
        status="WARN",
        workflow_stage="PAPER_WORKFLOW_READY",
        expected_demo_warning_count=1,
        next_manual_action="Paper workflow remains later priority.",
    )

    code = cli.main(["research-status", "--root", str(root), "--output-dir", str(tmp_path / "dashboard")])
    output = capsys.readouterr()

    assert code == 0
    assert (
        "tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_context_visible: True"
        in output.out
    )
    assert (
        f"latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_id: {run['run_id']}"
        in output.out
    )
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_status: "
        "REAL_REVIEWED_LOCAL_CSV_PACKAGE_CANDIDATE_PREFLIGHT_PROTOTYPE_NO_INPUT"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_health_status: PASS"
        in output.out
    )
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_csv_read_level: "
        "CSV_READ_NONE"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_manifest_read: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_references_followed: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_local_file_hash_computed: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_pit_admissibility_validated: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_csv_consumed: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_real_package_candidate_created: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_active_replay_input: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_buy_review_allowed: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_trading_allowed: "
        "False"
    ) in output.out
    assert (
        "latest_tiny_pit_real_reviewed_local_csv_package_candidate_manifest_only_preflight_prototype_recommended_next_task: "
        "Tiny PIT Real Reviewed LOCAL_CSV Package Candidate Manifest-Only Preflight Prototype "
        "Post-Checkpoint Governance Audit Report-Only v0.1"
    ) in output.out
    assert "workflow_stage: PAPER_WORKFLOW_READY" in output.out
    assert "ACTIVE_REPLAY_INPUT_READY" not in output.out


def _source_artifact_byte_hash_fixture(
    tmp_path: Path,
    *,
    declared_hash: str | None = None,
) -> dict[str, Path | str]:
    source_root = tmp_path / "source_artifact_byte_hash_source"
    manifest_root = tmp_path / "source_artifact_byte_hash_manifest"
    source_root.mkdir(parents=True, exist_ok=True)
    manifest_root.mkdir(parents=True, exist_ok=True)
    source_artifact = source_root / "synthetic-source-artifact.bin"
    source_bytes = b"synthetic source artifact bytes\n"
    source_artifact.write_bytes(source_bytes)
    full_hash = hashlib.sha256(source_bytes).hexdigest()
    declared = declared_hash if declared_hash is not None else full_hash
    metadata_path = manifest_root / "source_lineage_metadata.json"
    manifest_path = manifest_root / "source_artifact_hash_manifest.json"
    metadata = {
        "source_id": "SYNTH_SOURCE",
        "source_hash_algorithm": "SHA-256",
        "source_hash_value": declared,
        "revision_id": "rev-001",
        "available_time": "2024-01-01T00:00:00Z",
        "report_only": True,
        "diagnostic_only": True,
        "source_hash_validated": False,
        "pit_admissibility_validated": False,
        "reviewer_authority_validated": False,
        "forbidden_downstream_flags": {
            field: False for field in SOURCE_ARTIFACT_BYTE_HASH_REQUIRED_FALSE_FLAGS
        },
    }
    manifest = {
        "source_artifact_hash_request_id": "source_artifact_hash_request_001",
        "source_id": "SYNTH_SOURCE",
        "source_artifact_id": "synthetic_source_artifact",
        "source_artifact_declared_name": "Synthetic source artifact",
        "source_artifact_path_ref": str(source_artifact),
        "report_only": True,
        "diagnostic_only": True,
        "requested_source_artifact_byte_read_level": SOURCE_ARTIFACT_BYTE_READ_STREAMING_HASH_ONLY,
        "requested_source_hash_recompute_level": SOURCE_HASH_RECOMPUTE_SHA256_ONLY,
        "requested_source_content_read_level": SOURCE_ARTIFACT_BYTE_HASH_CONTENT_READ_NONE,
        "requested_csv_read_level": SOURCE_ARTIFACT_BYTE_HASH_CSV_READ_NONE,
        "requested_local_file_hash_level": SOURCE_ARTIFACT_BYTE_HASH_LOCAL_FILE_HASH_NONE,
        "requested_expected_hash_verification_level": SOURCE_ARTIFACT_BYTE_HASH_EXPECTED_HASH_NONE,
        "requested_source_hash_validation_level": SOURCE_ARTIFACT_BYTE_HASH_VALIDATION_NONE,
        "requested_revision_id_validation_level": SOURCE_ARTIFACT_BYTE_HASH_REVISION_ID_NONE,
        "requested_available_time_validation_level": SOURCE_ARTIFACT_BYTE_HASH_AVAILABLE_TIME_NONE,
        "requested_pit_admissibility_level": SOURCE_ARTIFACT_BYTE_HASH_PIT_NONE,
        "requested_source_reliability_level": SOURCE_ARTIFACT_BYTE_HASH_RELIABILITY_NONE,
        "requested_reviewer_authority_level": SOURCE_ARTIFACT_BYTE_HASH_REVIEWER_AUTHORITY_NONE,
        "requested_package_creation_level": SOURCE_ARTIFACT_BYTE_HASH_PACKAGE_CREATION_NONE,
        "requested_active_input_level": SOURCE_ARTIFACT_BYTE_HASH_ACTIVE_INPUT_NONE,
        "requested_replay_readiness_level": SOURCE_ARTIFACT_BYTE_HASH_REPLAY_READINESS_NONE,
        "source_hash_algorithm": "SHA-256",
        "declared_source_hash": declared,
        "source_lineage_metadata_ref": str(metadata_path),
        "revision_id_metadata_ref": "metadata-only-revision-ref",
        "available_time_metadata_ref": "metadata-only-available-time-ref",
        "compare_to_declared_source_hash": True,
        "full_hash_recording_policy": FULL_HASH_RECORDING_LOCAL_METADATA_ONLY,
        "disclosure_policy": DISCLOSURE_PREVIEW_ONLY_PUBLIC_SURFACES,
        "forbidden_downstream_flags": {
            field: False for field in SOURCE_ARTIFACT_BYTE_HASH_REQUIRED_FALSE_FLAGS
        },
        "limitations": ["Synthetic byte artifact for report-only source artifact hash tests."],
    }
    metadata_path.write_text(json.dumps(metadata, indent=2), encoding="utf-8")
    manifest_path.write_text(json.dumps(manifest, indent=2), encoding="utf-8")
    return {
        "manifest_path": manifest_path,
        "metadata_path": metadata_path,
        "source_artifact": source_artifact,
        "full_hash": full_hash,
    }


def _run_source_artifact_byte_hash_context(
    root: Path,
    fixture: dict[str, Path | str],
    *,
    run_id: str,
) -> dict:
    return run_source_artifact_byte_hash(
        output_root=(
            root
            / "manual_diagnostics"
            / "tiny_pit_real_reviewed_local_csv_package_candidate_source_artifact_byte_hash_v0_1"
        ),
        run_id=run_id,
        source_artifact_hash_manifest_path=fixture["manifest_path"],
        source_lineage_metadata_path=fixture["metadata_path"],
        source_artifact_path=fixture["source_artifact"],
        allowed_manifest_roots=[Path(fixture["manifest_path"]).parent],
        allowed_source_artifact_roots=[Path(fixture["source_artifact"]).parent],
        allow_source_artifact_byte_hash=True,
        source_artifact_byte_read_level=SOURCE_ARTIFACT_BYTE_READ_STREAMING_HASH_ONLY,
        source_hash_recompute_level=SOURCE_HASH_RECOMPUTE_SHA256_ONLY,
    )


def _run_pit_evidence_closure_worklist_context(root: Path, *, run_id: str):
    return run_historical_replay_pit_evidence_closure_worklist(
        root=root,
        output_dir=(
            root
            / "manual_diagnostics"
            / "historical_replay_pit_evidence_closure_worklist_v0_1"
        ),
        run_id=run_id,
        signal_date="2024-04-02",
        universe_name="etf_core",
    )
